# D-2026-07-05-2 propagation — codex review adjudication

**Commit reviewed**: `943b39c` (D-2026-07-05-2: no version
compatibility, ever — same-build peers only)
**Raw review**: `.review/results/d-2026-07-05-2-compat.codex.md`
**Reviewer**: gpt-5.5 (codex exec, read-only sandbox)
**Verdict line**: NEEDS FIXES (3 findings)
(fix sha recorded at end of file)

## Findings

1. **High — REV4 deeper sections still required compat — ACCEPTED.**
   Verified: beyond the §Constraints clause struck in `943b39c`, REV4's
   Acceptance Criteria (wire contract "names the mixed-version
   behavior"; Pull-deletion "compat tests cover old/new peer pairs"),
   Design ("capability bits so resize is never sent to an old peer"),
   Risks ("wire-compat break with mixed peers"), and three Revised
   Slices entries still stated old/new-peer requirements. Fixed: each
   struck + annotated with D-2026-07-05-2. Since REV4 is code-complete,
   the slice entries are annotated as executed-as-written history whose
   compat clauses are superseded — the shipped capability
   bits/compat tests are deleted with the unified session
   (ONE_TRANSFER_PATH otp-10), not hand-edited now.
2. **High — proto comments define mixed-version fallback semantics —
   ACCEPTED, mechanics deferred to otp-1/otp-10 (recorded, not
   edited).** The flagged `proto/blit.proto` comment blocks accurately
   describe the LIVE pre-cutover code (old-peer fallback behavior that
   is still compiled and running); rewriting comments to contradict
   running code would create reverse doc-vs-code drift, and proto
   edits are code-adjacent (rebuild) without a plan slice. Fixed at
   the governing layer instead: ONE_TRANSFER_PATH otp-1 now requires
   the NEW proto text to carry no version-tolerance semantics and
   pins the capacity-profile carve-out ("absent/0 = unknown hardware
   value, never old-peer detection"); the old comment blocks die with
   their messages at otp-10.
3. **Medium — WHITEPAPER §7.5 reviewer guidance — ACCEPTED.** The
   "watch for old daemon × new client" paragraph struck + annotated
   (mixed-build peers cannot exist).

Not adjudicated as findings: codex's grep list also hit REV1/REV2/
REV3, MULTISTREAM_PULL, PIPELINE_UNIFICATION (all Superseded/
Historical — non-authoritative by status header, left as history) and
BENCH_VERB_PLAN's "old daemons fail closed" (fail-closed is consistent
with D-2026-07-05-2).

reviewer: gpt-5.5
