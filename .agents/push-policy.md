<!-- push-policy: ask -->
Always ask before pushing.

This is the narrow push-only summary. The repo's full git-safety contract
(no agent-created branches, no history rewrite, branch deletion by explicit
name only, the `--merged` trap) lives in `.agents/repo-guidance.md` Earned
Practices and is more restrictive than a generic "ask" policy — read it in
full before any git operation beyond a local commit.
