Implemented Phase 2 of docs/plan/REMOTE_REMOTE_DELEGATION_PLAN.md. Remote-to-remote copy/mirror/move now dispatches to destination-side DelegatedPull by default via crates/blit-cli/src/transfers/remote_remote_direct.rs. The explicit --relay-via-cli flag on TransferArgs preserves the legacy CLI relay path; no silent fallback is performed for Unimplemented, gate rejection, connect, or transfer errors. Added env-gated BLIT_TEST_COUNTER_FILE instrumentation in blit-core::remote::instrumentation: DataPlaneSession records cli_data_plane_outbound_bytes for outbound payload bytes, and RemoteTransferSource::new records remote_transfer_source_constructed. Reworked crates/blit-cli/tests/remote_remote.rs to cover direct byte-path isolation (zero CLI data-plane bytes and zero RemoteTransferSource construction), gate-reject no-fallback, stale destination Unimplemented no-fallback, and explicit relay proving the counter observes payload bytes. Docs updated in README.md, docs/cli/blit.1.md, TODO.md, DEVLOG.md, and the remote-remote plan. Verification included cargo fmt, cargo fmt -- --check, cargo check -p blit-cli, cargo test -p blit-cli remote_remote_direct, cargo test -p blit-cli --test remote_remote, cargo test -p blit-core pull_sync_with_spec, cargo test -p blit-daemon delegated_pull, and cargo test --workspace. Existing warnings: F14 macOS FSEvents deprecation and an existing macOS test unused-variable warning during lib tests.