Phase 2.5 complete with remote/Windows benchmarks archived. CLI supports remote copy/mirror/move via canonical server:/module paths; blit list and blit-utils list/ls call daemon List/ListModules RPCs with optional JSON. blit-utils seeded (list-modules, ls, profile; other verbs pending). Roadmaps updated for predictor regression tests, perf-history schema migrations, journal-based incremental fast path, detailed blit-utils plan, and Phase 4 security/lint hardening.