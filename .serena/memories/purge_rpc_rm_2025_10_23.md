Implemented daemon-side Purge RPC (sanitises paths, respects read-only modules, removes files/dirs recursively) and added tokio tests covering successful purge + read-only rejection. `blit-utils rm` now calls Purge with confirmation prompts/`--yes` override. TODOs updated to highlight remaining admin verbs and wiring remote mirror deletions to Purge. Tests run: cargo check, cargo test -p blit-daemon, cargo test -p blit-utils (2025-10-23).