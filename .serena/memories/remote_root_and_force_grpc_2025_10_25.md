2025-10-25: Root exports now fully supported. CLI accepts host://path to target daemon default root. Daemon resolves empty module via default_root; ensure daemon started with --root or config root. New --force-grpc flag on blit copy/mirror/move lets clients bypass TCP data plane; RemotePushClient/RemotePullClient and daemon respect per-request fallback. Remote fallbacks validated on skippy/mycroft; results logged under logs/remote/.