# DEVLOG

Entries are latest-first. Each line starts with an ISO 8601 timestamp.
Per R5-F5 of `docs/reviews/followup_review_2026-05-02.md`: new entries
go at the top of the file, immediately below this header, so reviewers
scanning chronologically don't miss appended-at-the-bottom changes.
**2026-07-21 21:03:28Z** - **ADDITIVE STAGING (`ldt-4-live-f7`; exact `55fc5d5` ready on q):** Created new complete-history bundle `/Users/michael/blit-ldt4-stage-55fc5d5.bundle`, verified it locally and from q, and matched SHA-256 `5d48858f0cdec38a8797221abf9f382e83c4e1c01d787da1162af58748924a0b`. New retained q checkout `~/Dev/blit_v2_harness_55fc5d5` is clean, detached at exact reviewed code head `55fc5d5ff4561b0b126265f61e2962414db1de3e`, and has the expected 1,945-commit target history. q's native Bash 3.2 syntax/96-arm no-SSH self-test and all 77 analyzer tests pass. The bundle also carries the later review-record commit but the executable checkout is pinned to the reviewed code head. No existing bundle, checkout, artifact, fixture, session, evidence, daemon, runtime, transfer, push, or history rewrite changed.
**2026-07-21 21:01:24Z** - **TACTICAL REVIEW (`ldt-4-live-f7`; Grok clean at exact `55fc5d5`):** Grok 4.5/high reviewed exact one-commit range `9bf1fe7..55fc5d5` in retained detached worktree `/private/tmp/blit-grok-ldt4-f7-55fc5d5`. It confirmed exact normalized system `conhost.exe` name/path plus exact daemon name/path/command classification, raw count two, full mismatch identity reporting, retained sidecar PID/path, and unchanged recovery/teardown/listener checks. Embedded PowerShell array/count/path/command semantics and the two-second sample match the retained 250/500/1000/2000 ms diagnostic. Bash syntax, the 96-arm no-SSH self-test, and all 77 analyzer tests passed. Its independent production-only raw-count mutation two→one made the topology guard red; exact restoration returned focused green and a clean byte-identical worktree. Terminal structured output was `clean` with no findings. This is tactical advisory review, not formal openreview acceptance. Record: `.review/results/ldt-4-live-f7-r1.grok-verdict.md`. Additive staging/live retry remain. No rig endpoint, artifact, fixture, evidence, daemon, transfer, push, or history rewrite changed.
**2026-07-21 20:52:58Z** - **CANDIDATE (`ldt-4-live-f7`; exact Windows console-host/daemon partition):** Windows daemon startup now partitions the launcher's direct children by identity and requires exactly two raw children: one exact system-root `conhost.exe` and one exact registered daemon name/path/command. Missing, duplicate, or unrecognized children fail closed with a full identity summary. The retained per-arm daemon identity now includes console-host PID/path; existing exact daemon recovery, teardown, and listener checks are unchanged. The Bash 3.2 structural self-test pins the partition, counts, order, and evidence. Changing the production raw count from two back to one made that guard red; restoration returned syntax, the 96-arm no-SSH self-test, and all 77 analyzer tests green. Rustfmt, strict workspace clippy, and the full workspace suite pass. Tactical review, additive exact staging, and live retry remain. No rig endpoint, artifact, fixture, evidence, daemon, transfer, push, or history rewrite changed during the code slice.
**2026-07-21 20:47:05Z** - **LIVE FINDING (`ldt-4-live-f7`; exact Windows console-host topology):** Exact reviewed/staged `21fe468` live session `ldt4-20260721T204038Z-21fe468af129` passed all fixtures, stable q `LocalHostName/ComputerName=Q/Q`, complete start environment/fabric/quietness, Windows runtime preparation, and the first pre-arm runtime gate. `ldt4-001` launched the exact Windows daemon, which logged listening on 9031, then failed closed before client launch because startup required one raw launcher child but observed two. `runs.csv` has only its header. A new additive diagnostic on unused port 19032 retained at `D:/blit-test/ldt4-diagnostics/f7-20260721T204302Z` sampled the launcher at 250/500/1000/2000 ms and proved the stable exact pair: system `C:/Windows/System32/conhost.exe` plus the registered daemon command. It stopped only the exact diagnostic daemon/launcher; zero children and no listener remained. Two earlier retained diagnostic namespaces are non-authoritative: `...204300Z` contains only generated config/start files after the stdin transport stopped before launch; `...204301Z` launched the exact diagnostic binary but its deliberately separate module path was absent, so the daemon exited before sampling; its port is closed. Live teardown closed q/Windows 9031, left no Blit process, and restored the prior active daemon SHA-256 `1510d8d0…0512`. `f7` will require exactly one system console host and one exact daemon, rejecting every other child shape. No transfer, evidence deletion, fixture mutation, push, or history rewrite occurred.
**2026-07-21 20:39:22Z** - **ADDITIVE STAGING (`ldt-4-live-f6`; exact `21fe468` ready on q):** Created new complete-history bundle `/Users/michael/blit-ldt4-stage-21fe468.bundle`, verified it locally and from q, and matched SHA-256 `ce1f02fb18e981dd6eb27e2b8eafa2057687890847d04a76142d8f0726b42d8d`. New retained q checkout `~/Dev/blit_v2_harness_21fe468` is clean, detached at exact reviewed code head `21fe468af1290d5da4d0c60c9bff430a5b1ea61c`, and has the expected 1,941-commit target history. q's native Bash 3.2 syntax/96-arm no-SSH self-test and all 77 analyzer tests pass. The bundle also carries the later review-record commit but the executable checkout is pinned to the reviewed code head. No existing bundle, checkout, artifact, fixture, session, evidence, daemon, runtime, transfer, push, or history rewrite changed.
**2026-07-21 20:36:46Z** - **TACTICAL REVIEW (`ldt-4-live-f6`; Grok clean at exact `21fe468`):** Grok 4.5/high reviewed exact one-commit range `16fb0cd..21fe468` in retained detached worktree `/private/tmp/blit-grok-ldt4-f6-21fe468`. It confirmed the change replaces only resolver-derived q hostname with stable exact macOS LocalHostName/ComputerName `Q/Q`, preserves every independent IP/MAC/NIC/MTU/media/route/ARP/MSS/Windows-topology gate, records both fields at start/end, and analyzer-binds them. Bash 3.2, `set -e`, global `IFS`, and `scutil` failure semantics were audited. Syntax, the 96-arm no-SSH self-test, and all 77 analyzer tests passed. Its independent production mutation `Q`→`Q.local` made the self-test red; exact restoration returned focused green and a clean byte-identical worktree. Terminal structured output was `clean` with no findings. This is tactical advisory review, not formal openreview acceptance. Record: `.review/results/ldt-4-live-f6-r1.grok-verdict.md`. Additive staging/live retry remain. No rig endpoint, artifact, fixture, evidence, daemon, transfer, push, or history rewrite changed.
**2026-07-21 20:31:25Z** - **CANDIDATE (`ldt-4-live-f6`; stable macOS identity gate):** Replaced q's resolver-derived literal `hostname == q.lan` check with exact `scutil` LocalHostName/ComputerName `Q/Q`, while retaining the independent registered IP/MAC/NIC/MTU/media/topology gates. Both stable identities are recorded at start/end and required by the analyzer. The 96-arm no-SSH Bash self-test exercises the production helper; the analyzer suite grows 76→77 with an invalid-identity evidence test. Mutating the producer to expect `Q.local` made the self-test red; loosening the analyzer to accept `Q.local` made the new test red; both restored green. Bash syntax, focused tests, rustfmt, strict workspace clippy, and the full workspace suite pass. Tactical review, additive exact staging, and live retry remain. No rig endpoint, artifact, fixture, evidence, daemon, transfer, push, or history rewrite changed.
**2026-07-21 20:23:44Z** - **LIVE FINDING (`ldt-4-live-f6`; stable q identity replaces resolver hostname):** Exact reviewed/staged `322a161` strictly authenticated Windows `.173` and retained session `ldt4-20260721T202216Z-322a1611230e` verified large `1/1073741824`, small `10000/40960000`, and mixed `5001/547110912` manifests across endpoints. Its start environment gate then failed closed on literal `hostname == q.lan`: after the 10 GbE service returned, the same pinned Mac reports resolver-derived `Q.local` while stable macOS `LocalHostName=Q` and `ComputerName=Q`. `runs.csv` has zero rows; q port 9031 is closed; Windows runtime preparation, daemon swap, and transfer never began. The retained evidence/session namespaces remain. `ldt-4-live-f6` will gate and record stable identities while preserving IP/MAC/NIC/MTU/media checks. No endpoint file was deleted or overwritten, and no push/history rewrite occurred.
**2026-07-21 20:21:54Z** - **ADDITIVE STAGING (`ldt-4-live-f5`; exact `322a161` and verified `.173` trust ready):** Complete-history bundle `/Users/michael/blit-ldt4-stage-322a161.bundle` matched SHA-256 `429e4bea…79da` on nagatha and q. New retained q checkout `~/Dev/blit_v2_harness_322a161` is clean/detached at exact `322a161` with target history count 1,938; q Bash 3.2 self-test and all 76 analyzer tests pass. Nagatha's trusted `.173` keyset exactly matched q's trusted `.177` keyset (ED25519/RSA/ECDSA). q's known-hosts file was backed up additively at `~/.ssh/known_hosts.ldt4-pre-173-20260721T202154Z`, those same three keys were aliased to `.173`, and strict SSH returned `NETWATCH-01` / `10.1.10.173`. No keyscan trust, bypass, existing checkout, artifact, fixture, session, evidence, daemon, transfer, push, or history rewrite changed.
**2026-07-21 20:19:27Z** - **TACTICAL REVIEW (`ldt-4-live-f5`; Grok clean at exact `322a161`):** Grok 4.5/high reviewed the actual one-commit range `31c12c9..322a161` in retained detached worktree `/private/tmp/blit-grok-ldt4-f5-322a161`. It verified clean identity, audited all SSH/scp, route/ARP/MSS, daemon-readiness, transfer-argument, topology, and analyzer uses, and confirmed unchanged COMPUTERNAME/MAC/NIC/MTU/10GbE gates with no host-key bypass. Bash syntax, the 96-arm no-SSH self-test, and all 76 analyzer tests passed. Independent old `.177`, split SSH/IP, synchronized wrong-IP, and analyzer `.177` mutations each failed closed. Final exact JSON was `clean` with no findings. The analyzer's store-only `WINDOWS_IP` is pre-existing and live authority remains in harness IP/MAC/NIC gates. This is tactical advisory review, not formal openreview acceptance. Record: `.review/results/ldt-4-live-f5-r1.grok-verdict.md`. Additive exact staging, q's verified `.173` host-key alias, and live retry are next. No rig endpoint, file, fixture, daemon, runtime, transfer, evidence, push, history rewrite, or prior artifact changed.
**2026-07-21 20:12:41Z** - **CANDIDATE (`ldt-4-live-f5`; follow verified Windows `.173` identity):** D-2026-07-21-1 records the owner's direction to adapt to reality rather than force netwatch-01 back onto stale `.177`. The harness now moves SSH and data-plane pins together to strictly authenticated `.173`; the analyzer matches it. Bash self-test and analyzer tests independently pin the exact identity. Reverting only production identities to `.177` makes both guards red; restoring `.173` returns the 96-arm no-SSH self-test and all 76 analyzer tests green. Formatting and strict workspace clippy pass. The first full-suite attempt was invalidated by concurrent docs edits changing dirty-build provenance between client/daemon binaries; after edits stopped, the exact failed test passed, targeted affected Cargo artifacts were cleaned, and an untouched full workspace rebuild passed without a code change. Tactical Grok review, additive staging, q's exact verified host-key alias, and the live retry remain. No rig file, fixture, daemon, runtime, transfer, evidence namespace, push, or history rewrite changed.
**2026-07-21 19:56:16Z** - **LIVE PREFLIGHT BLOCKED (exact `d53b5fd`; Windows DHCP `.177`→`.173`):** After the owner reconnected q's adapter, read-only gates verified registered `en8`/`10.1.10.54`, MTU 9000, active 10Gbase-T, and the route to Windows on `en8`. The exact clean staged harness launched through attached SSH, then failed closed at its first Windows path preflight because `10.1.10.177:22` timed out. This occurred before harness identity/artifact checks, evidence reservation, endpoint session creation, fixture staging, daemon swap, or transfer. q reaches the router normally but gets incomplete ARP for `.177`; nagatha also cannot reach it. Current DNS resolves `netwatch-01` to `.173`, and nagatha's existing strict host-key entry authenticated `.173` as `NETWATCH-01`; its key equals q's trusted `.177` key, proving DHCP address drift rather than host substitution. q intentionally lacks a `.173` trust entry, and no host-key bypass or machine mutation occurred. Restore Windows to registered `.177` to preserve reviewed `d53b5fd`, or separately plan/review a harness endpoint change. No evidence row, endpoint file, fixture, daemon, runtime, transfer, push, history rewrite, or retained artifact changed.
**2026-07-21 18:58:13Z** - **ADDITIVE STAGING (`ldt-4-live-f4`; exact `d53b5fd` ready on q):** Created new retained complete-history bundle `/Users/michael/blit-ldt4-stage-d53b5fd.bundle`, verified it locally and on q, and matched SHA-256 `08cb3393…0af7`. q's new retained checkout `~/Dev/blit_v2_harness_d53b5fd` is clean, detached at exact `d53b5fd`, and carries the expected 1,930-commit target history. q's native Bash 3.2 ran the exact harness no-SSH self-test and returned `PASS (96 arms, no SSH)`. No existing bundle, checkout, fixture, session, evidence, daemon, runtime, or artifact was replaced. The registered product remains accepted `406a7e5`; only the reviewed harness head changed. The live retry did not launch because q's registered 10 GbE adapter/IP is absent; using the reachable 1 GbE management path would invalidate the rig-W evidence. No endpoint process, transfer, push, history rewrite, or retained artifact changed.
**2026-07-21 18:53:43Z** - **TACTICAL REVIEW (`ldt-4-live-f4`; Grok clean at exact `d53b5fd`):** Grok 4.5/high reviewed exact range `a39f0c5..d53b5fd` in retained detached worktree `/private/tmp/blit-grok-ldt4-f4-d53b5fd`. It verified exact clean identity, inspected the full Windows start/teardown lifecycle, reproduced the old 20-element versus fixed 12-line PowerShell command in memory including spaced paths, passed Bash syntax and the complete Bash 3.2 no-SSH self-test, and independently made each production-parenthesization mutation fail closed. It returned `clean` with no findings; this is tactical advisory review, not formal openreview acceptance. Record: `.review/results/ldt-4-live-f4-r1.grok-verdict.md`. A read-only rig probe found q reachable only through management `en0`/`.221`; the registered `.54` 10 GbE address is unreachable and q enumerates no Aquantia interface, so additive staging can proceed but the live retry waits for physical link restoration. No endpoint file, fixture, daemon, transfer, retained evidence, push, history rewrite, or prior artifact changed.
**2026-07-17 06:38:21Z** - **LIVE FINDING + FIX (`ldt-4-live-f4`; keep generated start-command paths inline at `d53b5fd`):** Exact `a39f0c5` was bundled and staged only under new retained local/q paths; bundle hashes matched, q verified complete history, and the checkout was clean, detached, replacement-free, and exact. q's real Bash 3.2 self-test passed. Attached live session `ldt4-20260717T062334Z-a39f0c570191` verified the large `1/1073741824`, small `10000/40960000`, and mixed `5001/547110912` manifests across endpoints and passed the start environment gate. Its first arm then failed closed before daemon launch or timing because PowerShell parsed two unparenthesized `$startText` concatenations as array concatenation: retained `start.cmd` had 20 items instead of 12 lines, splitting the launch gate and config/stdout/stderr paths. `runs.csv` has only its header. Both ports are closed, no session process remains, Windows' active daemon returned exactly to prior SHA-256 `1510d8d0…0512`, and the tested daemon plus all bundle, checkout, and session evidence remain retained. Fix `d53b5fd` parenthesizes the complete two dynamic array elements and pins both production forms. Local and actual Windows PowerShell 7.6.3 reproduced old 20 versus fixed 12 lines; Windows returned in-memory `LDT4-STARTTEXT|PASS|12`. Removing only the parentheses makes the 96-arm no-SSH self-test red; exact restoration is green. All 75 analyzer tests, formatting, strict workspace clippy, the full workspace suite, documentation, and diff checks pass. Formal Fable openreviews remain on owner-directed capacity hold; tactical Grok or Claude Opus 4.8 review, additive staging, and fresh live retry are next. No deletion, push, or history rewrite occurred.
**2026-07-17 06:09:55Z** - **TACTICAL REVIEW (`ldt-4-live-f2`/`f3`; Grok clean at exact `a39f0c5`):** Grok 4.5/high reviewed exact range `5a2265e..a39f0c5` in retained detached read-only worktree `/private/tmp/blit-grok-ldt4-live-a39f0c5`. The first process stopped during semantic analysis without a verdict; the same session `019f6ea9-d29c-7322-a536-0a4dcc66bb45` resumed rather than duplicating the review and emitted model-written heartbeats through checks and synthesis. It ran Bash syntax and the complete Bash 3.2 no-SSH self-test (`PASS (96 arms, no SSH)`), reproduced old one-element versus fixed two-element PowerShell log arrays in memory, parsed the no-launch fragment, audited every startup ordering window, and confirmed the launched-state exact ownership path remains intact. Final exact JSON was `clean` with no findings, and the worktree remained clean at the reviewed SHA. This is tactical advisory review, not formal openreview acceptance; Fable remains on owner-directed capacity hold. Record: `.review/results/ldt-4-live-fixes-r1.grok-verdict.md`. Next: additively stage exact `a39f0c5`, re-establish live endpoint quietness, and retry through attached SSH. No endpoint, fixture, daemon, transfer, Time Machine setting, mount, deletion, push, history rewrite, or retained artifact changed during review.
**2026-07-17 05:55:58Z** - **FIX (`ldt-4-live-f3`; prove no-launch teardown state at `a39f0c5`):** Windows daemon startup now treats its fully flushed `start.cmd` as the durable boundary before the sole `Invoke-CimMethod` process creation. Teardown permits a missing start command only with zero PID inputs, no `launcher.pid`, `daemon.pid`, `launch.ok`, or `daemon-identity.txt`, and no port 9031 listener; any contradiction fails closed before process enumeration, and the existing exact PID/command/executable/parent teardown remains unchanged once the start command exists. The offline guard pins the boundary and ordered refusals and forbids process inspection/stopping in the no-launch branch. Disabling the branch and weakening the durable flush independently turn the 96-arm no-SSH self-test red; exact restoration is green. The exact current function was streamed in memory over SSH through q and returned `LDT4-NO-LAUNCH-TEARDOWN|PASS` against retained Windows arm `ldt4-001`; that branch contains no registered session or runtime file write. Bash syntax, all 75 analyzer tests, formatting, strict workspace clippy, documentation, and diff checks pass. The first full workspace run hit a transient temporary-daemon failure in `test_admin_list_modules`; that exact test passed alone and the complete workspace rerun passed unchanged. Per owner direction, further formal Fable openreviews are held while Fable is out of capacity; tactical code review may use Grok or Claude Opus 4.8 before additive restaging and the fresh live retry. No deletion, overwrite, push, or history rewrite occurred.
**2026-07-17 05:40:02Z** - **LIVE FINDING + FIX (`ldt-4-live-f2`; Windows daemon log paths split at `b9b8080`):** Exact reviewed harness `5a2265e` was staged additively on q and launched through attached SSH as retained session `ldt4-20260717T052509Z-5a2265e202a4`. Canonical large/mixed staging succeeded; q and Windows source manifests matched exactly at large `1/1073741824`, small `10000/40960000`, and mixed `5001/547110912`. The first registered arm then failed closed before daemon launch or timing: PowerShell parsed `@(\$dir + '/daemon.out',\$dir + '/daemon.err')` as one space-joined filename. Teardown also required the not-yet-created `start.cmd`, recorded separately as `ldt-4-live-f3`. `runs.csv` contains only its header; no transfer datum was accepted. Read-only SSH checks proved both port 9031 listeners closed, no session process remained, q Time Machine stayed manual/idle, and Windows' active daemon hash returned exactly to durable prior SHA `1510d8d0…0512`; the exact staged daemon remains under the session-specific retained-tested path. Nothing was deleted or overwritten. Fix `b9b8080` parenthesizes each concatenated log path as its own PowerShell array element and pins the exact generated loop. An in-memory probe reproduced old count 1 versus fixed count 2; restoring only the old line makes the 96-arm no-SSH self-test red, and exact restoration returns green. All 75 analyzer tests, formatting, strict workspace clippy, the full workspace suite, documentation, and diff checks pass. `ldt-4-live-f3` is next; final Fable review and live retry wait for it. No push or history rewrite occurred.
**2026-07-17 05:18:51Z** - **REVIEW (ldt-4 canonical staging accepted at exact `5a2265e`; fresh live gates next):** Claude CLI 2.1.212 with `--model claude-fable-5 --effort max` completed the formal neutral whole-change review over exact `4e0fdc3..5a2265e` in retained detached worktree `/private/tmp/blit-openreview-ldt4-final-5a2265e-r5`. The one-shot stream exited zero with terminal success, exact SHAs, `verdict=clean`, no findings, and literal `guard_confirmed=true`. Fable emitted model-written activity heartbeats while discovering the goal, tracing manifest/promotion/session-namespace machinery, checking records, and running verification. Its narrow `dontAsk` toolset, deterministic Bash/Edit safety hook, and empty strict MCP configuration blocked several conservative read-only command forms; it adapted, and no denied command executed. All 75 analyzer tests and the 96-arm no-SSH self-test passed at head. Fable independently replaced exclusive promotion with `mv -n`; the self-test turned red on `canonical fixture promotion is not an exclusive atomic rename`; exact Edit reversal returned green, and the worktree ended clean at the reviewed SHA. Raw stream SHA-256 `6ca88f5e…3694`; record: `.review/results/ldt-4-canonical-r2.claude-verdict.md`. Exact `5a2265e`, not this later record commit, is accepted for additive restaging and fresh attached-SSH environment/preflight/live gates. No endpoint, SSH, fixture, daemon, transfer, evidence tree, Time Machine setting, mount, deletion, push, history rewrite, or prior retained artifact changed.
**2026-07-17 05:04:10Z** - **CANDIDATE (ldt-4 canonical staging fixed through `fdf7b37`; final Fable review next):** Both canonical-fixture review findings now carry their exact one-finding commits: exclusive atomic promotion at `1302b90` and pre-copy canonical shape/capacity validation at `fdf7b37`. Each is independently mutation-proved, and exact-current-code formatting, strict workspace clippy, the full workspace suite, Bash syntax, all 96 no-SSH harness arms, all 75 analyzer tests, documentation, and diff checks pass. The formal neutral Fable 5/max review will pin `4e0fdc3` to the exact clean record head containing this candidate state; no endpoint restaging or live arm occurs first. No endpoint, SSH, fixture, daemon, transfer, evidence tree, Time Machine setting, mount, deletion, push, history rewrite, or retained review artifact changed.
**2026-07-17 05:01:32Z** - **FIX (`ldt-4-r3-f2`; reject wrong canonical shape before copy and size the real capacity gate from it):** The harness now parses the hash-verified Windows large/mixed manifest immediately after fetch, requires its file count and bytes to equal the registered fixture shape before `scp`, and derives `fixture_bytes` from that validated manifest. The q free-space probe, required-byte calculation, and fail-closed `awk` enforcement all precede the copy; afterward, q's shape and exact manifest still have to match before the exclusive atomic promotion. The offline guard pins that full order. Restoring registered-constant sizing plus post-copy source validation turns the 96-arm no-SSH self-test red; independently removing only the low-space refusal also turns it red; exact restoration passes Bash syntax and all 96 arms. Formatting, strict workspace clippy, the full workspace suite, all 75 analyzer tests, documentation, and diff checks pass. No endpoint, SSH, fixture, daemon, transfer, evidence tree, Time Machine setting, mount, deletion, push, history rewrite, or retained review artifact changed.
**2026-07-17 04:36:25Z** - **FIX (`ldt-4-r3-f1`; exclusive atomic canonical-fixture promotion):** Replaced `mv -n` staging promotion with the harness's existing `rename_q_directory_exclusive` helper, which calls macOS `renameatx_np(RENAME_EXCL)` on the exact stable destination. This keeps no-clobber inside the atomic rename and fails closed on a cross-device layout instead of allowing `mv` to degrade into copy-plus-source-removal; the obsolete `mv` runtime prerequisite is gone. The offline safety test now requires that exact helper call after manifest equality and rejects an `mv -n` fallback. Restoring only the old promotion turns the 96-arm no-SSH self-test red with `canonical fixture promotion is not an exclusive atomic rename`; exact restoration passes Bash syntax and all 96 offline arms. Formatting, strict workspace clippy, the full workspace suite, all 75 analyzer tests, documentation, and diff checks also pass. `ldt-4-r3-f2` remains separate before the final Fable review. No endpoint, SSH, fixture, daemon, transfer, evidence tree, Time Machine setting, mount, deletion, push, or history rewrite changed.
**2026-07-17 04:31:46Z** - **REVIEW + INTAKE (ldt-4 canonical-fixture Fable r1; two Low staging fixes admitted):** A first retained attempt was invalidated and interrupted before any verdict after it issued a prohibited `rm -rf /tmp/ldt4_guard`; because that path's prior state is unproven, no no-deletion claim is made, and every resulting artifact remains retained. The valid retry used Claude CLI 2.1.212 with `--model claude-fable-5 --effort max`, a narrow `dontAsk` allowlist, deterministic Bash/Edit safety hook, no MCP servers, ordinary model-emitted activity heartbeats, raw stream retention, and the exact neutral best-way question over `4e0fdc3..ef48920`. It exited zero with exact SHAs, two Low findings, and literal `guard_confirmed=true`. Fable ran the 96-arm no-SSH self-test and all 75 analyzer tests; its independent path-registry mutation made the self-test fail, exact Edit reversal made it pass, and the detached worktree ended clean. Intake admitted `ldt-4-r3-f1` (replace `mv -n` promotion with the existing `renameatx_np(RENAME_EXCL)` helper) and `ldt-4-r3-f2` (validate fetched canonical shape before the large copy and size the free-space gate from that validated manifest). They land one per commit before the final neutral Fable pass and any new endpoint launch. No r4 endpoint command, SSH, daemon, transfer, Time Machine change, mount, deletion, push, or history rewrite occurred. Record: `.review/results/ldt-4-canonical-r1.claude-verdict.md`.
**2026-07-17 03:45:02Z** - **LIVE FINDING/CANDIDATE (`ldt-4-live-f1`; canonical byte-identical fixtures before hardware):** Exact accepted harness `4e0fdc3` was staged additively on q with exact registered product `406a7e5`; clean Mac and Windows build identities and hashes were verified. Its first attached SSH launch reserved retained evidence/session namespaces and staged the 10k-small fixture, then voided before any arm or Windows runtime swap: the q and Windows large fixtures had identical path shape and 1 GiB size but different SHA-256 content. Nothing was deleted or overwritten; void tag `ldt4-20260717T032327Z-4e0fdc307ba2` remains on both endpoints, and the pre-existing Windows daemon stayed byte-identical. Fix `b0c6ce3` makes Windows large/mixed sources canonical and gives q stable per-fixture source paths: a missing q copy is read-only manifested on Windows, copied into that run's retained incoming namespace only after a capacity check, exact-manifest validated on q, then promoted no-clobber. Interrupted or invalid copies remain session-scoped evidence rather than poisoning the fixed path. Harness/analyzer source registries now agree. Reverting the q mapping, direct-to-final-copy guard, or analyzer registry turns the corresponding test red; exact restoration passes Bash syntax, the 96-arm no-SSH self-test, all 75 analyzer tests, formatting, strict workspace clippy, the full workspace suite, and diff checks. No adaptive hardware datum exists yet. Formal Fable review of the exact candidate is next; no push occurred.
**2026-07-17 03:12:00Z** - **PROCESS (formal Claude reviews now expose model-emitted heartbeats):** Owner clarified that review health is established by seeing the reviewer advance, not by calculating token use or translating CLI telemetry into subscription cost. Future formal Claude Fable 5/max dispatches use `--brief --output-format stream-json`; the mechanical prompt requires concise model-emitted progress heartbeats directly in the stream before the final JSON at major phase transitions, around long checks, and during prolonged tool-heavy work. Heartbeats may state current activity only—no provisional verdicts, candidate findings, or substantive framing. The raw event stream is retained and only the terminal schema-valid result can accept a review. A quiet live session is inspected, never duplicated merely for being quiet. Canonical rule: `.agents/repo-guidance.md` Review policy.
**2026-07-17 03:06:24Z** - **REVIEW (ldt-4 harness accepted at exact `4e0fdc3`; live preflight next):** Claude CLI 2.1.212 with `--model claude-fable-5 --effort max` completed the neutral whole-change re-review over exact `e41b871..4e0fdc3` in retained detached worktree `/tmp/blit-openreview-ldt4-harness-4e0fdc3-r2`. Its schema-valid envelope exited zero with exact SHAs, `verdict=clean`, no findings, literal `guard_confirmed=true`, no permission denial, and zero web use. Fable independently reversed the live step-up blocked-ratio comparison; the focused dial suite turned red at 17 passed/13 failed. It restored the exact file, all 30 dial tests passed, the file SHA returned to `48b2eaa…323fe`, HEAD remained exact, and the worktree ended clean/detached. Prompt SHA `66d8aa0…9922b`; schema SHA `02d943b…091ab`. The raw result's nominal USD field is retained as CLI telemetry and is not treated as Claude Max subscription billing. This accepts the additive harness/analyzer for live gates; it does not claim Windows CI, quiet endpoints, staging, a completed arm, or hardware behavior. No endpoint, SSH, Time Machine setting, mount, push, deletion, history rewrite, or retained artifact changed during the Fable pass. Record: `.review/results/ldt-4-harness-r2.claude-verdict.md`.
**2026-07-17 02:55:00Z** - **CANDIDATE (ldt-4 harness fixed head `92a5a89`; exact local gates green, Fable r2 next):** All seven round-one fixes plus f1's parser-discovered shell-generation correction are committed and recorded. Exact-current-head `cargo fmt --all -- --check`, strict workspace clippy, `cargo test --workspace` (1,532 passed, 2 ignored), 30 focused release dial tests, Bash syntax, the 96-arm no-SSH harness self-test, all 75 analyzer tests, documentation, diff, and clean-tree checks pass. Before this final run, two earlier full-suite attempts on the preceding head showed different isolated macOS CLI subprocess failures (`du --json`, then two admin reads); each failed test and its complete test binary passed immediately in isolation, and two subsequent intact workspace runs—including this exact current head—were green. No product or harness change was made to hide those intermittent failures. The exact generated Windows fetch/prepare/normal-restoration parser proof is also green. Hosted Windows CI, neutral Fable 5/max re-review, artifact staging, live endpoint preflight, and the 96-arm run remain. No endpoint file/process/daemon/staging path, Time Machine setting, mount, push, deletion, history, or retained artifact changed.
**2026-07-17 02:42:40Z** - **FIX (ldt-4-r1-f1 follow-up; generated Windows fetch now survives Bash quoting):** The required fresh read-only SSH parser pass caught a defect before formal re-review: f1's new `[Console]::Out.Write` fragment carried raw PowerShell quotes/backticks inside a Bash double-quoted command, so Bash consumed the quotes and attempted local backtick command substitution before SSH. The producer now escapes both shell layers, and the offline static safety guard requires the exact generated source form. Removing only the quote escapes turns the 96-arm no-SSH self-test red with `Windows fetch framing is not escaped through Bash`; exact restoration passes. A fresh SSH probe then compiled the exact generated guard+prepare, guard+fetch, and guard+normal-restoration scripts with PowerShell `[ScriptBlock]::Create` and returned `LDT4-PARSER-PROBE|PASS`; none of the compiled blocks was invoked. The local tagged decoder accepted a synthetic parser-only payload and matched its independently computed SHA. Parser/mutation artifacts remain retained under `/private/tmp`; nothing was deleted. No endpoint file, process, daemon, staging path, transfer, Time Machine setting, mount, push, history, or prior review artifact changed.
**2026-07-17 02:19:19Z** - **FIX (ldt-4-r1-f7b; exact blocked-ratio/counter equality):** Replaced the analyzer's `1e-12` absolute tolerance with exact equality between the serialized `blocked_ratio` and the value recomputed from raw integer blocked/elapsed/stream counters. The producer and analyzer use the same division and JSON round-trips the producer `f64`; the tolerance was therefore unnecessary and wide enough to cross the strict 0.05/0.30 policy thresholds. The new full-analyzer guard sets counters to exactly 0.05 and emits the next float below it. Restoring the tolerance makes that test fail because no error is raised; exact restoration passes the focused guard and all 75 analyzer tests. This closes the seventh and final admitted round-one fix, each landed separately. No endpoint, SSH, staging, daemon, transfer, evidence tree, Time Machine setting, mount, push, deletion, history rewrite, or retained review artifact changed.
**2026-07-17 02:17:10Z** - **FIX (ldt-4-r1-f7a; duplicate trace JSON keys now fail closed):** Added one central `object_pairs_hook` that rejects a repeated key anywhere in a session-phase JSON object before the line becomes an ordinary dictionary, with the exact trace line and key in the refusal. The full analyzer guard injects conflicting `reason` keys into raw evidence; ordinary Python parsing previously collapsed them last-wins and accepted the matrix. Reverting the loader to plain `json.loads` makes the new test fail because no exception is raised. Exact restoration passes the focused guard and all 74 analyzer tests. No endpoint, SSH, staging, daemon, transfer, evidence tree, Time Machine setting, mount, push, deletion, history rewrite, or retained review artifact changed.
**2026-07-17 02:14:40Z** - **FIX (ldt-4-r1-f6; guard independent Python cooldown/sustain/bound replay):** Added one fast table-driven test for `hysteresis`, `cooldown`, `sustain`, and ceiling `bound` decisions in `DialPolicyReplay`, plus a closure assertion that those cases and the fixture-guarded reasons exactly equal the ten registered sample reasons. Rust already guards the full production taxonomy; this closes only the independent Python transcription gap and does not manufacture a real-emitter-fixture prerequisite before hardware. Mutating `cooldown` to `sustain` makes the focused test fail on the exact reason mismatch. Exact restoration passes the focused guard and all 73 analyzer tests. No endpoint, SSH, staging, daemon, transfer, evidence tree, Time Machine setting, mount, push, deletion, history rewrite, or retained review artifact changed.
**2026-07-17 02:12:16Z** - **FIX (ldt-4-r1-f5a; accepted resize is target-or-fault, not partial settlement):** Corrected the stale generic `TransferDial::resize_settled` comment that said a post-ACK local dial failure could settle at the unchanged count. The active contract and production are stricter: accepted membership failure faults the session; source handling checks actual logical count equals target and only then publishes `accepted=true`. The analyzer therefore remains unchanged and correctly refuses partial accepted evidence. Focused guards for ACK effective-count validation and accepted terminal ADD/REMOVE under both layouts pass. A manual comment-truth guard turns red when the old sentence is restored and green after exact restoration. No runtime behavior, wire, endpoint, SSH, staging, daemon, transfer, evidence tree, Time Machine setting, mount, push, deletion, history rewrite, or retained review artifact changed.
**2026-07-17 02:08:45Z** - **FIX (ldt-4-r1-f3; padded low PIDs reach exact q-client recovery):** Added one `awk` normalizer for the PID-only `ps` column and routed the orphan-client fallback through it. macOS right-justifies low PIDs, and global space-free `IFS` preserved that padding, so the old numeric regex silently skipped common PIDs below 10000. The loop now receives plain digits but retains the same exact command line, run-id environment, trace-enable environment, and ambiguity checks; kill authority is not broadened. The offline self-test requires padded PID 1 and 265 plus 12345 to survive normalization and rejects noise. A mutation that restores the effective 10000 floor turns it red; exact restoration passes Bash syntax and the 96-arm no-SSH self-test. No endpoint, SSH, staging, daemon, transfer, evidence tree, Time Machine setting, mount, push, deletion, history rewrite, or retained review artifact changed.
**2026-07-17 02:07:02Z** - **FIX (ldt-4-r1-f2; refuse an ambiguous Windows runtime baseline after hard crash):** Before creating a new swap intent or mutating the active daemon, the generated PowerShell now validates the staged hash, refuses any exact `retained-before-*-blit-daemon.exe` entry (also rejecting directory/reparse matches), and refuses an active daemon whose hash already equals the staged test build. Those are the two signatures an untrapped prior run can leave; the harness neither deletes nor auto-recovers them, so it cannot preserve test bytes as the supposed machine baseline. Normal `retained-tested-*` evidence remains allowed. The offline safety test pins both checks before intent creation; mutating the active/staged equality to inequality turns it red, and exact restoration passes. A read-only SSH PowerShell probe on netwatch-01 used only in-memory strings and returned the registered refusal for equal hashes; it did not read/write a file, stage an artifact, touch a daemon, or run a transfer. No endpoint state, evidence tree, Time Machine setting, mount, push, deletion, history rewrite, or retained review artifact changed.
**2026-07-17 02:03:26Z** - **FIX (ldt-4-r1-f1; exact tagged Windows evidence fetch):** Replaced the fragile whole-SSH-stdout strict base64 decode with one explicit `LDT4-FILE-B64|` payload line. The remote PowerShell producer emits a leading newline plus the tag, so even unterminated profile output cannot share the payload line; the local decoder normalizes CRLF/CR, requires exactly one tagged line, strictly decodes it, and retains the independent remote/local SHA match. Missing, duplicate, or malformed tags fail closed, while unrelated banner output is ignored. The offline harness self-test covers all four shapes and uses no SSH. Mutating the exactly-one check to accept duplicates turns it red with exit 1; exact restoration passes Bash syntax, the 96-arm self-test, and diff checks. No endpoint, SSH, file staging, daemon, transfer, evidence tree, Time Machine setting, mount, push, deletion, history rewrite, or retained review artifact changed.
**2026-07-17 01:58:26Z** - **REVIEW (ldt-4 harness Fable r1; seven bounded fixes admitted before hardware):** Claude CLI `2.1.212` with `--model claude-fable-5 --effort max` reviewed exact immutable range `e41b871..0e48721` in retained detached worktree `/tmp/blit-openreview-ldt4-harness-0e48721-r1` under only the neutral best-way question plus mechanical coordinates, isolation, safety, schema, and independent-guard requirements. The authoritative call exited zero with exact SHAs, literal `guard_confirmed=true`, seven candidates, zero web use, no permission denial, and a Claude-reported `$24.275937` cost; its usage record names the requested Fable driver plus an internal Opus delegation. Two earlier schema forms were rejected before any reviewer turn or model token and are caller configuration errors, not verdicts. Intake admitted three Medium harness-safety defects (tagged strict Windows fetch framing, hard-crash stale daemon-baseline refusal, and normalized padded-PID recovery) plus four Low fixes (correct one stale post-ACK comment, guard Python cooldown/sustain/bound replay, reject duplicate trace JSON keys, and require exact blocked-ratio recomputation). The claim that `REVIEW_REQUIRED` is indistinguishable from acceptance was declined because status is machine-readable and the marker means measurements complete; the claim that successful accepted settlement may miss its target was declined because the active contract and production fault the session, though its stale generic comment became the separate Low. Fixes land one per commit, then a fresh neutral whole-change Fable pass; no endpoint staging or live arm occurs first. Raw result/adjudication: `.review/results/ldt-4-harness-r1.claude.json` and `.review/results/ldt-4-harness-r1.claude-verdict.md`. No endpoint artifact, process, daemon, transfer, evidence tree, Time Machine setting, mount, push, deletion, history rewrite, or retained review artifact changed.
**2026-07-17 00:10:42Z** - **DECISION (D-2026-07-16-4; Fable owns formal openreview, Grok remains advisory):** The owner corrected the cost policy boundary before the ldt-4 review launched: **"no don't use grok for openreview. that should be fable."** Every formal `openreview` now uses Claude CLI with `--model claude-fable-5 --effort max`; Grok may still provide lower-cost advisory second eyes or tactical slice checks, but cannot issue the acceptance verdict. D-2026-07-16-3's risk-based frequency still controls spend: fewer changes receive formal review, while the acceptance reviewer is not substituted. The interrupted turn had only read the openreview skill pointer; no Grok process, worktree, prompt, result, endpoint command, staging, daemon, transfer, deletion, Time Machine change, mount change, push, or history rewrite occurred. The ldt-4 harness candidate now goes to Fable.
**2026-07-17 00:01:00Z** - **CODER + DRIFT (ldt-4 harness/analyzer candidate through `7491b4f`; Grok next, no endpoint data):** Added the registered non-destructive rig-W `q`↔`netwatch-01` evidence instrument in nine one-purpose commits over `e41b871..7491b4f`: a fail-closed analyzer plus 72 synthetic tests, then the 96-arm six-cell/eight-pair `ABBAABBA` harness and isolated fixes for evidence-path/socket binding, durable Windows runtime recovery, exact small-fixture roots, endpoint-owned daemon teardown, environment shape, and real self-test failure propagation. Both initiator layouts invoke `blit copy` over the same exact source/active/archive paths; role changes only which endpoint calls the responder. The runner is additive, exclusively creates evidence, retains failed/partial sessions, binds exact PID+command ownership, refuses symlink/reparse/overwrite, and contains no deletion or name-wide kill. Final independent read-only audit found all prior findings closed; Bash 3.2 syntax/self-test, analyzer compile/Python-3.9 grammar/72 tests, formatting, strict clippy, 1,532 workspace tests with 2 ignored, docs, and diff checks pass. Generated Windows prepare/restoration PowerShell parsed cleanly through a read-only SSH parser call; it was not executed. Current records now say candidate, not accepted: hosted Windows CI, fixed-SHA Grok review, exact-artifact staging, live preflight, 96 arms, and evidence adjudication remain. No endpoint write, staging, daemon, transfer, deletion, Time Machine change, mount change, push, or history rewrite occurred.
**2026-07-16 21:30:29Z** - **DECISION (D-2026-07-16-3; risk-select reviews, reserve Fable):** The owner ended blanket Fable dispatch after reviewing the recorded ~$90.62 live-dial spend and uneven marginal value. Formal review is now risk-selected: Grok supplies ordinary independent second eyes and slice reviews when needed; Claude Fable 5/max is reserved for final acceptance and tactical high-risk/contested questions. Fixed-SHA neutral `openreview`, independent guard proof, fail-closed structured verdicts, intake/adjudication, one-finding-per-commit, no-agent-branch, and no-push safety remain whenever review is invoked. The in-progress ldt-4 harness/analyzer will receive a Grok slice review; the final ldt-4 package receives Fable absent an earlier tactical need. This docs-only policy correction changes no runtime code, endpoint, SSH state, daemon, benchmark, artifact staging, push, deletion, Time Machine setting, or mount.
**2026-07-16 21:15:56Z** - **REVIEW (ldt-4 endpoint correction accepted; harness next):** After the owner explicitly authorized a fresh round, direct Claude CLI `2.1.211` with `--model claude-fable-5 --effort max` reviewed exact `daa439e..9926bf7` in new retained detached worktree `/tmp/blit-openreview-ldt4-target-9926bf7-r2`. The prompt again contained only the neutral best-way question plus mechanical repository/SHA/isolation/schema/independent-guard boundaries. Attempt one returned server-side 529 before any Fable tokens; the single retry exited zero after 18 turns with exact SHAs, literal `guard_confirmed=true`, zero web use, `clean`, and no findings. Direct checks confirm the retained tree clean/detached at exact `9926bf7`; it remains retained. The correction from stale Mac↔Mac wording to owner-selected rig-W `q`↔`netwatch-01` is accepted. Raw result/adjudication: `.review/results/ldt-4-target-r2.claude.json` and `.review/results/ldt-4-target-r2.claude-verdict.md`; the failed attempt is preserved separately. No endpoint command, SSH, daemon, benchmark, artifact staging, push, deletion, Time Machine setting, or mount changed. Next: implement and independently review the additive adaptive harness/analyzer before endpoint staging.
**2026-07-16 20:12:02Z** - **REVIEW BLOCKED (ldt-4 endpoint correction; no Claude verdict):** Direct Claude CLI `2.1.211` with `--model claude-fable-5 --effort max` was dispatched over exact `daa439e..9926bf7` in retained detached worktree `/tmp/blit-openreview-ldt4-target-9926bf7-r1`, using only the neutral best-way question and mechanical coordinates/isolation/schema/independent-guard boundaries. Attempt one returned server-side 529 before any Fable tokens. The one fail-closed retry completed 12 reviewer turns with zero web use, then returned another server-side 529 before emitting the required structured verdict. Both raw envelopes are preserved; neither is an acceptance or finding. Per openreview, no third attempt was silently dispatched: the endpoint correction remains unaccepted and ldt-4 staging/measurement is paused for owner direction. Direct checks confirm the retained tree clean/detached at exact `9926bf7`; it remains retained. Record: `.review/results/ldt-4-target-r1.claude-verdict.md`. No endpoint command, SSH, daemon, benchmark, artifact staging, push, deletion, Time Machine setting, or mount changed.
**2026-07-16 19:55:17Z** - **PLAN CORRECTION CANDIDATE (ldt-4 endpoint pair):** Reconciled the active live-dial plan and current-state/index wording with the owner's later explicit endpoint selection: ldt-4 evidence runs on quiet rig-W `q`↔`netwatch-01`, not the earlier Mac↔Mac wording. The evidence requirements are unchanged: exact accepted `406a7e5` binaries, identical large/10k-small/mixed fixtures, both initiator layouts, raw adaptive traces, integrity, throughput, and role-invariance grading. This docs-only correction does not claim endpoint evidence or change runtime behavior. Neutral fixed-SHA Claude Fable 5/max openreview follows before this corrected contract governs the run. No endpoint command, daemon, benchmark, artifact staging, push, deletion, Time Machine setting, or mount changed.
**2026-07-16 19:44:29Z** - **REVIEW (ldt-3 accepted; ldt-4 quiet Mac↔Mac evidence next):** Direct Claude CLI `2.1.211` with `--model claude-fable-5 --effort max` re-reviewed exact whole-change range `e863ef0..406a7e5` in fresh retained detached worktree `/tmp/blit-openreview-ldt3-406a7e5-r2`. It received only the neutral best-way question plus mechanical repository/SHA/isolation/schema/independent-guard boundaries—no prior-finding summary or preferred conclusion. The first r2 call reached no reviewer turn and returned server-side `529 Overloaded`; the single fail-closed retry kept the substantive question unchanged, exited zero, named exact SHAs, returned literal `guard_confirmed=true`, zero web use, `clean`, and no findings. Direct post-review checks confirm both primary and retained worktrees clean/detached at exact `406a7e5`; neither retained review tree was deleted. Round one's admitted Low is therefore closed by the mutation-proved wake-after-observe fix, and ldt-3 is accepted at `406a7e5`. Raw result/adjudication: `.review/results/ldt-3-r2.claude.json` and `.review/results/ldt-3-r2.claude-verdict.md`; the failed attempt is preserved separately. No endpoint, SSH, rig, benchmark, push, deletion, Time Machine setting, or mount changed. ldt-1..3 are now independently accepted; ldt-4 exact-build endpoint evidence is next.
**2026-07-16 19:22:53Z** - **CODER (ldt-3 r1 Low fixed; mutation-proved, full gates green, re-review next):** Fixed the sole admitted Claude finding without changing policy, membership, the wire, or transfer output. `resize_settled` now emits the optional settlement observation before waking the tuner; the wake was removed from the locked state helper, so the next sample cannot publish epoch N+1 before epoch N's settlement callback returns. New deterministic guard `settlement_observer_precedes_waiter_notification` registers a waiter, parks the settlement observer on a barrier, and requires the waiter to remain blocked until the callback is released. Exact production mutation back to the old wake-before-observe order fails immediately with “waiter notification must not overtake settlement observation”; restoration passes all 23 focused dial tests. Exact-final formatting, strict workspace clippy, `cargo test --workspace` (**1,532 passed, 2 ignored**, including 381 core library, 42 role, 160 daemon, and 649 TUI tests), 30 release dial tests, docs, and diff checks pass. Hosted Windows CI remains unobserved without a push/PR. No endpoint, SSH, Time Machine setting, mount, daemon, benchmark, push, deletion, or network action occurred. One fix commit and neutral Claude Fable 5/max fixed-SHA re-review follow.
**2026-07-16 19:12:16Z** - **REVIEW (ldt-3 round 1 reopened; one Low observer-ordering fix next):** Direct Claude CLI `2.1.211` with `--model claude-fable-5 --effort max` reviewed exact immutable range `e863ef0..436e1bb` in retained detached worktree `/tmp/blit-openreview-ldt3-436e1bb-r1`. The prompt used only the neutral best-way question plus mechanical repository/SHA/isolation/schema/independent-guard boundaries. The first call reached no reviewer turn and returned server-side `529 Overloaded`; the single fail-closed retry kept the substantive question unchanged, exited zero, returned exact SHAs, literal `guard_confirmed=true`, zero web use, and one Low candidate. **ADMITTED `ldt-3-r1-f1`:** `resize_settled_locked` wakes settlement waiters under the epoch mutex, but `resize_settled` emits the settlement event only after dropping that mutex; a woken tuner can theoretically emit epoch N+1 pending before epoch N settlement appears in file-order JSONL. Policy, membership, wire, and transfer results remain correct, but trace chronology can be false and the ordered role-parity assertion can flake, so this predicts an observable failure and passes intake. The bounded fix is notification after observer emission plus a deterministic waiter-order guard. Direct checks confirm the primary and retained worktrees clean/detached at exact `436e1bb`; the retained tree remains. Raw result/adjudication: `.review/results/ldt-3-r1.claude.json` and `.review/results/ldt-3-r1.claude-verdict.md`; the failed attempt is preserved separately. No endpoint, SSH, rig, benchmark, push, deletion, Time Machine setting, or mount changed.
**2026-07-16 18:42:50Z** - **CODER (ldt-3 lifecycle/observer candidate; all local gates green, neutral review next):** Closed the live controller's terminal and cleanup races without changing its policy or the wire. A ready-but-unsent ADD/REMOVE at `NeedComplete` now settles unchanged, while an accepted ADD completes real admission/authentication plus normal END and an accepted REMOVE reaches exact retirement/already-ended settlement; terminal cleanup cannot relabel acceptance as refusal. SOURCE and DESTINATION now own and join all tuner, elastic-pipeline, nested-worker, scan/count/filter/checksum, accepted-socket, and receive-worker tasks on normal, fault, and cancellation exits. The scan chain reaps in reverse creation order and closes its bounded receiver before joining a non-abortable blocking producer; pipeline Finish/Abort acknowledges only after nested cleanup, and cancellation retains a cleanup owner. The default-off session-phase observer is wire-neutral and policy-inert, records coherent raw bytes/blocked/elapsed/stream/valid/ratio samples with the exact ten-reason sample taxonomy, keeps pending/add/remove/refused lifecycle reasons separate, and reports peak separately from final logical membership. Mutations turned guards red for the terminal event-loop race, every observer omission/mislabel tested, incoherent raw samples, lost peak accounting, early cleanup ACK, dropped retained ownership, forward-order scan deadlock, manifest receiver deadlock, missing SOURCE/DESTINATION/receive cleanup, false cancellation settlement, and abort-before-first-poll tracing; exact restoration is green. Historical `8e019ef` evidence is now correctly labeled pre-ldt-2 static-target orientation parity at eight, not adaptive up/down proof. Exact-final `cargo fmt --all -- --check`, strict workspace clippy, `cargo test --workspace` (**1,531 passed, 2 ignored**, including 380 core library, 42 role, 160 daemon, and 649 TUI tests), 29 focused release dial tests, docs, and diff checks pass. Hosted Windows CI remains unobserved without a push/PR, and ldt-4 still owns the first endpoint evidence. No endpoint, SSH, Time Machine setting, mount, daemon, benchmark, push, deletion, or network action occurred. Candidate record: `.review/findings/ldt-3.md`; neutral Claude Fable 5/max openreview follows the fixed commit.
**2026-07-16 16:45:07Z** - **REVIEW (ldt-2 accepted; ldt-3 next):** Direct Claude CLI `2.1.211` with `--model claude-fable-5 --effort max` reviewed exact immutable range `602941f..65a0f9f` in retained detached worktree `/tmp/blit-openreview-ldt2-65a0f9f-r1`. The prompt contained only the neutral best-way question plus mechanical repository/SHA/isolation/schema/guard boundaries—no plan summary, suspected issue, preferred mutation, or steering. Claude returned exit zero, exact SHAs, `guard_confirmed=true`, zero web use, and one Low candidate: `settle_inflight_resize` retains an unused `FrameTx` argument discarded with `let _ = tx;`. The evidence is true, but Claude explicitly predicted “No runtime failure” and only a possible future-reader misunderstanding, so the candidate is DECLINED at codereview intake as style/maintainability without an observable failure; no code churn is admitted. Two read-only `rtk git` wrapper attempts were denied, but Claude recovered and completed the proof; direct post-review checks confirm the retained tree is clean/detached at exact `65a0f9f` and the primary tree was untouched. Raw result/adjudication: `.review/results/ldt-2-r1.claude.json` and `.review/results/ldt-2-r1.claude-verdict.md`. ldt-2 has no admitted material finding and is accepted; ldt-3 lifecycle/observer closure is next. No endpoint, SSH, rig, benchmark, push, deletion, Time Machine setting, or mount changed.
**2026-07-16 16:27:35Z** - **CODER (ldt-2 live controller cutover candidate; all local gates green, neutral review next):** Replaced the retired workload-shape worker target with one SOURCE-owned live telemetry controller shared by both socket layouts. SOURCE-initiator and SOURCE-responder now enter the same constructor and differ only in dial-versus-accept socket acquisition; epoch zero is the receiver-bounded floor, zero capacity remains unknown/default, and 32 is a safety ceiling rather than a target. Every epoch-zero and ADD worker carries the exact registered `LiveProbe`; the production sampler folds byte and blocked-write deltas, proposes one-step ADD or REMOVE with cooldown/sustain/hysteresis, and settles against actual keyed pipeline membership. DESTINATION validation is shared and exact for epoch/operation/target/token/floor/ceiling/duplicates/refusal; final reports use settled logical membership rather than cumulative socket opens. Deleted the static proposal/shape-ramp authority and need accumulators, added abort-and-join teardown and admission barriers, and closed a discovered resume-payload telemetry hole by timing socket writes there too. Deterministic paired-session guards prove identical growth `4→17`, shrink `4→1`, idle/hysteresis holds, receiver ceilings, and zero workload-shape resize authority under both layouts. More than twenty focused mutations turned the corresponding guards red and restored green, including hidden 8/16 caps, missing/distinct probes, no-op REMOVE, wrong settlement/ACK/epoch/token/refusal behavior, missing blocked telemetry, and incomplete task abort. Exact-final formatting, strict workspace clippy, all workspace tests, docs, shell syntax, and diff checks pass (including 366 core library tests, 42 role tests, 160 daemon tests, and 649 TUI tests). The first workspace attempt had one daemon transport startup failure in `test_utils_df`; its exact filtered rerun and two complete workspace reruns passed. Cheap chunk/prefetch/TCP values remain construction-time snapshots for existing sockets/queues and are recorded as separate performance-policy fidelity debt, not worker-control or role-parity authority. ldt-3 still owns lifecycle/observer race closure; ldt-4 is the first endpoint evidence. No endpoint, SSH, Time Machine, mount, daemon, benchmark, push, deletion, or network action occurred. Candidate record: `.review/findings/ldt-2.md`; neutral Claude Fable 5/max openreview follows the fixed commit.
**2026-07-16 14:47:50Z** - **REVIEW (ldt-1 accepted; ldt-2 cleared):** Claude Fable 5 via Claude Code 2.1.211 with `--effort max` reviewed exact immutable range `2ed3ead..f8f3c51` in retained detached worktree `/tmp/blit-review-ldt1-f8f3c51-r1` under the neutral D-2026-07-16-1 best-way prompt. It returned exit zero, exact SHAs, `guard_confirmed=true`, and schema-valid `accepted` with no material issue. It accepted the acknowledged non-cloneable control, exact-ID LIFO ledger, two-phase worker admission under the sampler mutex, ordered Seal, terminal outcomes, delivery/ack errors, rejected-sink cleanup, and first-error preservation. Its independent gates included fmt, strict workspace clippy, and the full workspace suite. Mutation one leaked the exact retire authority and turned all three retirement/END guards red; mutation two selected FIFO instead of LIFO and returned member 10 rather than 99. Exact restoration returned green and left the worktree clean at `f8f3c51`. Four non-blocking notes are recorded without inflating them into ldt-1 defects: add a private-terminal-queue discriminator and audit an unreachable missing-control ordering in ldt-3; correct one stale ADD cleanup comment in ldt-2; ledger extraction is optional style. No endpoint, SSH, rig, benchmark, push, deletion, Time Machine setting, or mount changed. Raw result/adjudication: `.review/results/ldt-1-r1.claude.json` and `.review/results/ldt-1-r1.claude-verdict.md`. ldt-1 is closed; ldt-2 production controller cutover is next.
**2026-07-16 14:28:28Z** - **CODER (ldt-1 acknowledged elastic membership candidate; guard-proved and full gates green):** Replaced fire-and-forget elastic ADD/REMOVE with one non-cloneable acknowledged control that returns exact `StreamId`, logical count, and live-versus-terminal outcomes. The pipeline now owns a keyed duplicate-safe probe registry, an exact LIFO terminal ledger, and worker readiness/admission gates: ADD registers the probe and releases the blocked worker under the sampler mutex before acknowledgement; REMOVE marks/signals the exact worker and unregisters its exact probe under that same barrier. Sealed running retirement returns `RetireMarked`; only an exact worker whose normal END already completed returns `AlreadyEnded`; terminal ADD admits a real member onto a private disconnected queue and returns `JoinedThenEnded`. Control delivery/ack loss is errorful, rejected sinks finish once, pending replies fail with the pipeline, and first error wins. Both `SourceDataPlane` constructors now use identical identified members, monotonic ADD IDs, ordered Seal, exact target settlement, and a final logical-count cross-check. Seven coder mutations each turned its new guard red and restored green: first-vs-last LIFO ID, wrong probe ID, fabricated ADD success, overwritten first error, mislabeled sealed retirement, duplicate probe replacement, and wrong non-tail registry removal. Focused elastic/telemetry tests, all 42 role tests, full fmt, workspace strict clippy, and `cargo test --workspace` pass. Scope is honest: production still creates unprobed members and does not start the tuner; shared wire REMOVE and deletion of static shape authority are ldt-2, lifecycle closure is ldt-3, and no endpoint/SSH/hardware action occurred. Candidate record: `.review/findings/ldt-1.md`; exact committed SHA goes to neutral Claude Fable 5/max next.
**2026-07-16 13:39:33Z** - **REVIEW (live-dial activation round 2 accepted; ldt-1 cleared):** Claude Fable 5 via Claude Code 2.1.211 with `--effort max` reviewed exact immutable range `acd368f..63d94ac` in retained detached worktree `/tmp/blit-review-live-dial-activation-63d94ac-r2` under the neutral D-2026-07-16-1 best-way prompt. It returned exit zero, exact SHAs, `guard_confirmed=true`, and schema-valid `accepted`. Every admitted stale reference now names `LIVE_DIAL_TUNING.md` Active with D-2026-07-16-2, no live document says Draft or owner approval pending, and ldt-1 is unambiguously the first open item and stated next action. Claude accepted the record-before-fix commit shape. Its independent docs plus 15-assertion guard was green, turned exactly 14 range assertions red under full base restoration and five correction assertions red under fix-only restoration, then returned green/clean at exact `63d94ac`. Two denied optional `rtk git` wrapper reads did not affect the completed proof; exact HEAD and cleanliness were independently confirmed. No code/runtime behavior, endpoint, SSH, rig, benchmark, push, deletion, Time Machine setting, or mount changed. Raw result/adjudication: `.review/results/live-dial-activation-r2.claude.json` and `.review/results/live-dial-activation-r2.claude-verdict.md`. Activation review is closed; next is ldt-1 implementation.
**2026-07-16 13:27:34Z** - **CODER (live-dial activation consistency corrected; re-review next):** Corrected the three admitted activation-round findings. The live plan finding now records D-2026-07-16-2 activation and ldt-1 as next; active `docs/plan/ONE_TRANSFER_PATH.md` and `docs/TRANSFER_SESSION.md` now identify `LIVE_DIAL_TUNING.md` as Active rather than Draft while preserving their truthful current-runtime drift warning. This is documentation consistency only; no Rust/proto/runtime behavior, endpoint, SSH, rig, benchmark, push, deletion, Time Machine setting, or mount changed. Docs/diff gates precede the fixed-SHA neutral Claude Fable 5/max re-review; ldt-1 code remains untouched until acceptance.
**2026-07-16 13:23:12Z** - **REVIEW (live-dial activation round 1 reopened; three stale references):** Claude Fable 5 via Claude Code 2.1.211 with `--effort max` reviewed exact immutable range `8f08546..acd368f` in retained detached worktree `/tmp/blit-review-live-dial-activation-acd368f-r1` under the neutral D-2026-07-16-1 best-way prompt. It returned exit zero, exact SHAs, `guard_confirmed=true`, and schema-valid `reopened`. The activation itself and every primary state/decision/plan/index record are correct, but the live plan finding still says owner approval is pending and `docs/plan/ONE_TRANSFER_PATH.md` plus `docs/TRANSFER_SESSION.md` still call the now-Active correction a Draft. All three are admitted because they conflict with D-2026-07-16-2 and can stop a cold agent at the conflict gate. The reported `.review/check-state.sh` historical-pair warning predates this diff and is not admitted. Claude's independent 26-assertion guard was green, turned 20 activation assertions red under base restoration and six plan assertions red under plan-only restoration, then returned green/clean at exact `acd368f`. No code/runtime behavior, endpoint, SSH, rig, benchmark, push, deletion, Time Machine setting, or mount changed. Raw result/adjudication: `.review/results/live-dial-activation-r1.claude.json` and `.review/results/live-dial-activation-r1.claude-verdict.md`. Next: commit this verdict, correct the three stale references, and re-review before ldt-1.
**2026-07-16 13:07:48Z** - **DECISION (D-2026-07-16-2; LIVE_DIAL_TUNING Active):** The owner replied “go” at the reviewed Draft→Active checkpoint. `docs/plan/LIVE_DIAL_TUNING.md` is now Active. Implementation proceeds ldt-1→ldt-4 in order through the synchronous Claude Fable 5/max reviewloop using D-2026-07-16-1 neutral best-way prompts. The first slice is acknowledged elastic ADD/REMOVE membership with exact member identity, explicit live versus terminal outcomes, normal END retirement, and busy/idle/failure/first-error/exactly-once guards. This activation changes no Rust/proto/runtime behavior, endpoint, SSH, rig, push, Time Machine setting, mount, or retained artifact. Next: review the exact activation record, then implement ldt-1.
**2026-07-16 06:40:21Z** - **REVIEW (live dial tuning Draft accepted under neutral D-2026-07-16-1 prompt):** Claude Fable 5 via Claude Code 2.1.211 with `--effort max` reviewed exact range `554d080..b99637f` in fresh retained detached worktree `/tmp/blit-review-live-dial-b99637f-r2-neutral`. The substantive prompt contained only the one-sentence goal and “Is this plan the best way to achieve this goal?”—no prior findings, expected fixes, checklist, alternatives, or preferred verdict. Claude returned exit zero, exact SHAs, `guard_confirmed=true`, and schema-valid `accepted` with no new material issue. It independently concluded that the terminal accepted-resize rule is coherent on the existing elastic pipeline; the target-17 plus 8/16-clamp mutations prove the static caps absent; all needed tuner/probe/REMOVE/two-layout substrate already exists; and the ldt-1→4 order avoids a dual-policy window while keeping the source-bound signal observable and hardware tuning separate. Its self-chosen guard ran docs/diff plus 35 semantic assertions green, restored only the plan to the round-one blob and got exactly 23 correction failures with 12 architecture invariants still green, then restored exact blob `b3add75` and returned all green/clean at `b99637f`. The earlier steered round-2 attempt remains retained and excluded. No code/runtime change, endpoint, SSH, rig, benchmark, push, deletion, Time Machine setting, or mount changed. Raw result/adjudication: `.review/results/live-dial-tuning-plan-r2.claude.json` and `.review/results/live-dial-tuning-plan-r2.claude-verdict.md`. The Draft is review-complete; next and only next step is owner Draft→Active approval.
**2026-07-16 06:28:13Z** - **DECISION + REVIEW PROCESS (D-2026-07-16-1; neutral Claude best-way prompts):** The owner replaced the existing elaborate end-goal framing with a stricter Fable rule: give one neutral goal and ask whether the code as implemented is the best way to achieve it — no plan-conformance request, author diagnosis, prior findings, expected fix/outcome, suggested alternative, checklist, framing, or steering. The same rule applies on re-review; only immutable identity, safety limits, structured schema, and an independently chosen guard proof may accompany the question. Recorded D-2026-07-16-1, annotated the superseded portions of D-2026-07-14-5/-15-1, and updated the active reviewloop, repo guidance, and protocol. The in-flight live-dial round-2 prompt violated this rule by naming both expected fixes and prescribed mutations, so it was interrupted after 19 turns with no verdict; its exact error envelope, prompt, and detached worktree are retained and excluded rather than deleted or graded. No code/runtime behavior, endpoint, SSH, rig, push, deletion, Time Machine setting, or mount changed. Next: docs gate/commit this policy, then retry the same immutable plan candidate with the neutral question.
**2026-07-16 06:15:38Z** - **CODER (live dial tuning round-1 findings corrected; round 2 next):** Corrected the two admitted Claude findings one per commit. `a78d553` resolves the need-completion race: peer refusal/pre-accept failure may settle unchanged, but once ACKed an ADD/REMOVE is no longer optional; a live pipeline applies or faults, while a closing pipeline retains exact member identity long enough to satisfy the accepted transition through normal authentication/member admission/END retirement. The Draft now requires both layouts to prove accepted/unaccepted ADD and REMOVE at need completion without healthy faults, false settlement, duplicate retirement, or leaked socket/probe/member/receive task. `41dcb4d` strengthens cap removal: a receiver ceiling ≥17 must yield identical role traces through target 17, and mutations clamping production at 8 or 16 must turn the guard red. Shape still has no worker-count authority; one semantic SOURCE controller still owns both layouts. Docs/diff gates are green after each finding. No Rust/proto/runtime behavior, endpoint, SSH, rig, push, deletion, Time Machine setting, or mount changed. Next: fixed-SHA Claude Fable 5/max round-2 re-review, then stop at the owner Draft→Active checkpoint if accepted.
**2026-07-16 06:09:18Z** - **REVIEW (live dial tuning plan round 1 reopened; two bounded corrections next):** Claude Fable 5 via Claude Code 2.1.211 with `--effort max` reviewed exact immutable range `35d7d13..554d080` in retained detached worktree `/tmp/blit-review-live-dial-554d080-r1`. The direct, explicit Anthropic invocation returned exit zero, exact SHAs, `guard_confirmed=true`, and schema-valid `reopened`. One MEDIUM is admitted: the Draft's accepted-resize fault rule contradicts its required need-completion race and could fault a healthy completed transfer or settle membership falsely; an already accepted ADD/REMOVE must instead be satisfied by draining/completion plus normal END retirement, with faults reserved for a live pipeline that refuses/errors. One LOW is admitted: a growth guard that merely crosses 8 cannot prove a hidden 16 cap is gone, so it must cross 16 or reach a lower receiver ceiling. Claude also observed that the frozen blocked-ratio policy can boundedly grow toward the ceiling on source-bound work; the observer/evidence should report that signature, not grade it as a tuner failure. All other architecture questions passed. Independent docs/diff plus 33-assertion proof went green→17 expected red failures with the two old contract blobs→green after exact restoration; worktree clean at `554d080`. The earlier proxy-routed attempt returned no verdict and remains a non-authoritative error record. No endpoint, SSH, rig, push, deletion, Time Machine setting, or mount changed. Raw result and adjudication: `.review/results/live-dial-tuning-plan-r1.claude.json` and `.review/results/live-dial-tuning-plan-r1.claude-verdict.md`. Next: commit this verdict before editing the Draft, correct the two admitted details, and re-review.
**2026-07-16 05:35:39Z** - **PLAN CANDIDATE (live dial tuning; Claude review next):** Drafted `docs/plan/LIVE_DIAL_TUNING.md` to restore D-2026-06-20-1/-2: one SOURCE-owned controller samples live per-stream telemetry and adjusts TCP workers up and down within the receiver's safety ceiling, identically under both initiator layouts. The static file/byte shape table is no longer a startup or terminal worker authority; exact 8 is narrowed to static-orientation parity, not adaptive completion. The plan specifies acknowledged pipeline membership, tokenless REMOVE with normal END retirement, exact epoch/target/probe settlement, deterministic >8 growth and below-start shrink guards, lifecycle/default-off observation, and quiet Mac↔Mac evidence. Corrected the contradictory active parent-plan and session-contract wording and queued the Draft in STATE. No Rust/proto/runtime behavior, endpoint, SSH, rig, push, deletion, Time Machine setting, or mount changed. Candidate docs gate and fixed-SHA Claude Fable 5/max review follow.
**2026-07-16 03:58:44Z** - **REVIEW (otp12 pf-1 P2 aggregate observer accepted; pinned historical control next):** Claude Fable 5 via Claude Code 2.1.211 with `--effort max` accepted exact immutable range `f01d662..713526e` with exact SHAs, `guard_confirmed=true`, and no material finding. It audited every hunk for default-off behavior, role/carrier parity, observer bias, bounded output, timing/correlation honesty, sink fidelity, historical-backport semantics, and guard completeness. Independently, it passed the three probe units and the complete 256-file OFF/ON × role × carrier guard; keeping tests unchanged, it replaced the production keyed shard ID with one constant, observed the predicted cross-run key-isolation failure, restored exact blob `181975b`, and reran both commands green. The retained detached worktree remains clean at `713526e`; no endpoint, SSH, rig, push, deletion, or performance run occurred. The first Claude invocation was interrupted before any verdict or completed proof and is retained as a non-authoritative error artifact; the authoritative retry excluded the unresponsive ptk integration and used direct Cargo. Raw result, failed attempt, and adjudication: `.review/results/otp12-pf1-p2-observer-r1.claude.json`, `.review/results/otp12-pf1-p2-observer-r1.claude-attempt1-error.json`, and `.review/results/otp12-pf1-p2-observer-r1.claude-verdict.md`. The observer remains descriptive and cannot grade H6/H7; next is the pinned `0f922de` adaptation and local old/new plus observer-OFF/ON TCP/gRPC control. In-stream correlation still requires one identical, unique run ID per observed session, so the existing block-level rig harness is invalid unchanged.
**2026-07-16 00:09:37Z** - **REVIEW (otp12 pf-1 rig-W probe record accepted; P2 is next):** The owner-requested Grok 4.5/high second eye and the required Claude Fable 5/max review both independently accepted exact immutable range `f120f4c..7ecc2f9` with exact SHAs and `guard_confirmed=true`; Grok had no comments and Claude found no overclaim or underclaim. They recomputed the 290-file/20,517,586-byte inventory and digest, all 128 arms and 768 clocks, raw/exported trace identity, all generated reports and summary arithmetic, `N_resolution=329 ms`, live 8/8 parity for both initiator layouts, and every two-layout phase-table number. Both retained fresh additive copies, passed exact green, failed at 127/128 after removing one `runs.csv` row, restored exact bytes, and returned green with byte-identical reports. Claude's first restore invoked `cp -i`, blocked on its overwrite prompt, and was interrupted; that pass was not counted, and Claude independently retried with a separate explicit noninteractive restore before accepting. Three earlier Claude attempts remain recorded but non-authoritative: proxy/plugin-updater traffic, the stale loopback route, and a direct pre-reset 429. The authoritative retry used direct Anthropic, updater disabled, zero web calls, no endpoint activity, 23/23 analyzer tests, docs and authored-whitespace green, and a clean detached worktree at `7ecc2f9`. Records: `.review/results/otp12-pf1-rigw-probe-record-r1.{grok,claude}.json`. Licensed result is unchanged: current-build P1 non-reproduction + live parity, failed resolution/control, no causal grade or P1 closure. Next approved slice is the separate P2 high-volume small-fixture observer, then the pinned `0f922de` historical control; more P1 rig time requires an owner-approved amendment.
**2026-07-15 21:55:57Z** - **LIVE RESULT + RECORD CANDIDATE (otp12 pf-1 rig-W exact `8e019ef`; 128/128 accepted, live parity proved, no causal grade):** Fresh additive local/q/Windows staging matched exact clean reviewed candidate `8e019ef5e948b94a7aca7cb3a8d0be41204742af`; q's Bash 3.2 self-test, SSH-driven launcher smoke `20260715T211635Z.29953`, and preflight `20260715T211714Z.30253` passed provenance, 10 GbE/MTU/MSS, firewall, quietness, fixture, clock, and lifecycle gates. Registered session `20260715T211759Z.30531` completed all four OFF–ON–ON–OFF blocks and the analyzer accepted: 128 unique exit-zero/drained/valid arms, 768 clocks, 128 client logs, 128 matching landed manifests, 48 trace-on TCP sessions, 11,392 phase events, and 14,964 endpoint-local intervals. The sole completion marker contains exact `8e019ef`; no VOID/temp marker exists. Finalization removed the exact endpoint session trees, both 9031 ports are closed, and no benchmark process remains; all staging and prior evidence remain retained. Every traced `wm_tcp_mixed` arm reached accepted target/live eight streams under both initiator layouts on both endpoint traces, proving the former 3-vs-2/1 cap is gone live. Historical target P1 did not reproduce: SOURCE/DESTINATION durable medians were 1469.5/1368.5 ms off and 1494.5/1367 ms on, both point ratios ≤1.10 and reversed. That is not a causal or formal P1 pass: `N_resolution=329 ms` exceeds the historical gap, the gRPC control fails, and the current plan cannot grade a counterfactual where the baseline gap is absent. The exact 290-file/20,517,586-byte payload, hashes, two-layout descriptive phase report, and bounded interpretation are imported at `docs/bench/otp12-pf1-rigw-2026-07-15/`; inventory digest `1e8d815c…6e51f`. Claude Fable 5/max probe-record review is next. P2 small-fixture instrumentation and the `0f922de` historical control remain plan-compatible; more P1 rig time requires an owner-approved amendment for adequate resolution and the absent/reversed baseline.
**2026-07-15 21:09:49Z** - **REVIEW (otp12 pf-1 rig-W G15 accepted; exact `8e019ef` cleared for live gates):** Claude Fable 5 via Claude Code 2.1.210 with `--effort max` reviewed exact immutable range `af2ffb5..8e019ef` in retained detached worktree `/tmp/blit-review-g15-8e019ef` and returned exit zero plus schema-valid `accepted`, exact SHAs, and `guard_confirmed=true`. It confirmed the controller schedule-stdin diagnosis and analyzer's correct 32/128 fail-closed rejection; `-n` applies only to ordinary noninteractive `wssh`, no caller supplies remote stdin, `wscp` is unchanged, and the separately piped batched clock sampler retains its intentional protocol. A whole-loop audit found no second schedule-stdin consumer. Syntax, complete Bash 3.2 self-test, all 23 analyzer tests, docs, and diff checks passed. Keeping every G15 test, Claude removed only production `-n`; the complete self-test failed exactly on `noninteractive Windows SSH consumed the registered block schedule`. Exact restoration returned syntax/self-test green, script blob `ae60920` / SHA-256 `85cda14f…295a05d`, and a clean worktree at `8e019ef`. Roles, paths, workers, schedule contents, measured timing, trace/analyzer rules, thresholds, Time Machine, and mounts are unchanged. No endpoint was contacted or retained artifact deleted. Exact `8e019ef`, not this later review-record commit, is cleared for fresh additive build/staging and SSH-driven launcher/preflight/registered gates. Record: `.review/results/otp12-pf1-rigw-harness-r15.claude.json`.
**2026-07-15 20:53:45Z** - **IDENTITY (otp12 pf-1 rig-W G15):** The noninteractive Windows SSH stdin-isolation fix and its executed four-block schedule guard are committed at `7bdaf8bda5919f2ed03a17709baf6d4aefabe8e0`; this code-fix identity is the base for the immutable Claude Fable 5/max review candidate. Final script blob `ae60920`, SHA-256 `85cda14fbeecb9446b1ad2462f938e5cf397f2f042a0d106690b6332c295a05d`. No endpoint, retained evidence, Time Machine setting, or mount state changed by the fix.
**2026-07-15 20:52:30Z** - **LIVE GATE + CODER (otp12 pf-1 rig-W G15 schedule-stdin consumption fixed/guard-proved; no accepted datum):** Exact round-14 Claude-accepted `1f62ce5` was bundled, built, and staged only under fresh additive local/q/Windows paths with exact clean identities and hashes; q's real Bash 3.2 self-test, SSH-driven launcher smoke, and standalone preflight passed exact provenance, 10 GbE/MTU/MSS, firewall, quietness, lifecycle, fixtures, and clock gates. Registered session `20260715T203809Z.25897` completed all 32 block-1 arms with exit zero, drained/valid status, matching landed manifests, 192/192 clocks, and no trace-off phase events. G14 worked live: 44.1% Spotlight recovered after one five-second poll, then load1 4.61 plus 51.5% Spotlight recovered with exact five-second polling until both were quiet at 2.95/0.0%; pair 4 and end gates then completed. The analyzer correctly rejected the incomplete session at 32/128 rows. Root cause: the four-record block loop read from stdin and ordinary `wssh` inherited it, so the first Windows quiet probe drained blocks 2–4; every earlier candidate carried the bug but stopped before this boundary. The session is retained `SESSION-VOID` with no completion marker or analyzer report; nothing was accepted, analyzed, or graded. Both endpoint evidence trees and every prior stage remain, both ports are closed, and no benchmark process survives. G15 adds `ssh -n` only to the ordinary Windows command wrapper; the separately piped clock sampler and `scp` are unchanged. An executed Bash 3.2 shim drains stdin unless it sees `-n` and requires blocks 1–4; removing only `-n` fails on `noninteractive Windows SSH consumed the registered block schedule`, and exact restoration returns syntax/self-test green. Format, strict clippy, the full workspace suite, all 23 analyzer tests, docs, and diff checks pass. No transfer role, role-invariant path, worker policy, schedule, measured arm, trace schema, analyzer rule, threshold, retained artifact, Time Machine setting, or mount state changed. Claude Fable 5/max review is next.
**2026-07-15 20:24:12Z** - **REVIEW (otp12 pf-1 rig-W G14 accepted; exact `1f62ce5` cleared for live gates):** The first Claude Fable 5/max invocation through the request proxy returned no verdict. After the owner removed that proxy and explicitly instructed a retry, the exact stale Claude PID was terminated with SIGTERM; its worktree, prompt, and debug log remain retained as non-authoritative evidence. A fresh retry in retained detached worktree `/tmp/blit-review-g14-1f62ce5-r2`, with no matching proxy environment variables, returned exit zero and schema-valid `accepted` with exact range `35f160f..1f62ce5` and `guard_confirmed=true`. Claude confirmed the fixed 10% Spotlight ceiling, immediate preflight, five-second/120-second runtime budget for either load or Spotlight, simultaneous-quiet success, every-sample contaminant rechecks, and fail-closed persistent/malformed states. Syntax, complete Bash 3.2 self-test, all 23 analyzer tests, docs, and diff checks passed. Keeping every G14 test, Claude independently made exact G13 Spotlight behavior, ten-second offender polling, and over-advanced elapsed accounting fail their targeted guards; exact restoration returned syntax/self-test green and script blob `f3a6195` / SHA-256 `21f4b686…75852d8`. The authoritative worktree ended clean; no endpoint, retained evidence, Time Machine setting, or mount changed. Exact `1f62ce5`, not this later review-record commit, is cleared for fresh additive build/staging and SSH-driven launcher/preflight/registered gates. Record: `.review/results/otp12-pf1-rigw-harness-r14.claude.json`.
**2026-07-15 19:49:11Z** - **IDENTITY (otp12 pf-1 rig-W G14):** The bounded post-transfer Spotlight recovery fix, exact-poll guards, and complete coder record are committed at `942c88e601ea2d27f0a1da52aa5408b763ee61f4`; this code-fix identity is the base for the immutable Claude Fable 5/max review candidate. No endpoint, retained evidence, Time Machine setting, or mount state changed.
**2026-07-15 19:27:55Z** - **LIVE GATE + CODER (otp12 pf-1 rig-W G14 post-transfer Spotlight recovery gap fixed/guard-proved; no accepted datum):** Exact Claude-accepted `d7345f1` was bundled, built cleanly for arm64 and Windows, and staged only under fresh SHA-named paths. Bundle SHA-256 `6600f2fe…2509` matched local/q/Windows; arm binaries matched local/q; native Windows build/stage hashes matched; every clone was clean at exact HEAD without replacement refs and every binary embedded clean `+d7345f19299e`. q Bash 3.2 self-test passed; launcher and standalone preflight passed at `...T191455Z.20128` and `...T191532Z.20431` with correct 10 GbE/MTU/MSS and 397/401 ms clock batches. Registered session `20260715T191605Z.20751` completed 16 arms with Spotlight 0.0% before pair 1 and 1.0% before pair 2, then voided at pair 3 on 58.7%. Six post-teardown samples stayed 0.0–0.1% with load1 1.62–1.76; fresh session `20260715T192044Z.22145` again completed exactly 16 arms, with Spotlight 0.0% then 1.2%, and voided at the same boundary on 87.3%. Each retained void has 16/128 rows and 96/768 clocks; no analyzer/finalizer ran and nothing was accepted, analyzed, or graded. Both teardowns closed both ports and left no benchmark process; all evidence and staging remain. Repeated schedule-boundary correlation and the q destination-tree mechanism support harness-triggered indexing with high confidence, but the retained logs do not prove exclusive causality. G14 preserves the fixed 10% ceiling and existing 5 s × 24 budget: preflight still rejects immediately, but runtime checks outside timed arms may age either owned load history or delayed Spotlight work, rechecking conflicts, Time Machine, load parsing, and Spotlight every sample; persistent/malformed contamination still fails closed. New Bash 3.2 guards cover transient/staggered/immediate/persistent/malformed Spotlight, exact poll cadence and bound, plus recovery-poll Time Machine and offender checks. Exact G13 Spotlight behavior, delayed offender rechecks, and over-advanced recovery accounting each turn their targeted guard red; exact restoration returns syntax/self-test green at script blob `f3a6195` / SHA-256 `21f4b686…75852d8`. Formatting, clippy, the full workspace suite, analyzer tests, docs, and diff checks are green. No role, path, worker, schedule, measured timing, trace schema, analyzer math, threshold, Time Machine setting, mount, retained evidence, or prior staging changed. Exact identity and Claude Fable 5/max review are next.
**2026-07-15 19:07:48Z** - **REVIEW (otp12 pf-1 rig-W G13 accepted; exact `d7345f1` cleared for live gates):** Claude Fable 5 via Claude Code 2.1.210 with `--effort max` reviewed exact immutable range `2c8e8d9..d7345f1` in retained detached worktree `/tmp/blit-review-g13-d7345f1` and returned schema-valid `accepted` with exact SHAs and `guard_confirmed=true`. It confirmed preflight remains immediate at load1 3.0; only runtime checks outside timed arms may poll 5 seconds for at most 120 seconds; conflicts, Time Machine, load parsing, and Spotlight are rechecked every sample; persistent or malformed load fails closed; and roles, endpoint-local paths, workers, schedule, measured timing, trace schema, and analyzer math are unchanged. Syntax, complete Bash 3.2 self-test, all 23 analyzer tests, docs, and diff checks passed. Keeping every new test, Claude replaced only `q_quiet_gate` with its byte-exact base body and got the targeted runtime-load recovery failure; exact restoration returned syntax/self-test green and script SHA-256 `febc195282feafd9e6bd25fe8a00aec153e8a515d3f8aa809590d4f8d1ab3a9c`. The worktree ended clean; no benchmark endpoint or retained artifact changed. Exact `d7345f1`, not this later record commit, is cleared for fresh additive build/staging and SSH-driven launcher/preflight/registered gates. Record: `.review/results/otp12-pf1-rigw-harness-r13.claude.json`.
**2026-07-15 18:46:38Z** - **REVIEW (D-2026-07-15-1 reviewer policy accepted):** Claude Fable 5 via Claude Code 2.1.210 with `--effort max` reviewed exact immutable range `50fcf31..2c8e8d9` in retained detached worktree `/tmp/blit-review-policy-2c8e8d9` and returned schema-valid `accepted` with exact SHAs and `guard_confirmed=true`. It confirmed the sole grandfathered G12 Grok boundary, exact future Claude flags, non-substitution rule, harness-neutral reviewloop pointers, historical/do-not-execute GPT workflow, and untouched historical outcomes. Docs and diff gates passed. Its 23-assertion semantic guard was green on reviewed bytes, red in five independent ways with only `docs/agent/PROTOCOL.md` restored to its exact base blob, and green after exact restoration. The worktree ended clean; no endpoint was contacted and no retained artifact was deleted. Record: `.review/results/reviewer-policy-claude-fable5-r1.claude.json`.
**2026-07-15 18:31:20Z** - **IDENTITY (otp12 pf-1 rig-W G13):** The bounded runtime load-recovery fix and its complete coder record are committed at `0cbb16a295dd7a2104355af1799cb35b3c325701`; this code-fix identity is the base for the immutable Claude Fable 5/max review candidate. No endpoint, retained evidence, Time Machine setting, or mount state changed.
**2026-07-15 18:15:09Z** - **LIVE GATE + CODER (otp12 pf-1 rig-W G13 runtime load self-contamination fixed/guard-proved; no accepted datum):** Exact independently accepted `d5e9dda` was bundled, built cleanly for arm64 and Windows, and staged only under new SHA-named paths; all bundle/source/stage hashes, clean embedded IDs, exact HEADs, and absent replacement refs were verified. q's real Bash 3.2 self-test passed. Launcher smoke (`/Users/michael/Dev/blit_v2_d5e9dda/logs/otp12pf-rigw-20260715T180301Z.17538`) and standalone preflight (`...T180338Z-preflight`) passed exact provenance, 10 GbE/MTU/MSS, firewall, quietness, timers, streamed Windows result, fixtures, and 414/404 ms clock batches. The registered run (`...T180425Z`, session `20260715T180421Z.18113`) cleared G12 live: all eight pair-1 arms, including the formerly failing q-client destination-init layout, completed exit zero with verified drain, durability, and landed manifests. q load1 was 1.79 with Spotlight 0.0%, Time Machine disabled/stopped, and no known conflicting process immediately before those arms. After 74 seconds of owned transfers, the pair-2 entry gate sampled the one-minute load history at 3.19 and voided against 3.0. The retained session has 8/128 rows and 48/768 clocks; analyzer/finalizer never ran, no `SESSION-COMPLETE` exists, and nothing was accepted, analyzed, or graded. Both ports closed, exact daemon PIDs are absent, and q plus both Windows trees remain retained. G13 preserves the 3.0 bar and every per-round check: preflight still rejects immediately; runtime samples now wait 5 seconds up to 120 seconds outside timed arms, rechecking conflicts, Time Machine, and Spotlight each time, and persistent load still fails closed. Executed Bash 3.2 guards cover transient recovery, immediate rejection, bounded exhaustion, every run-block quiet call, and the end gate. Restoring only the old one-shot gate makes the transient-recovery guard fail; exact restoration returns syntax and the complete self-test green. Format, strict clippy, all workspace tests, all 23 analyzer tests, docs, and diff checks pass. No role, source/destination path, worker, schedule, measured-arm timing, trace schema, analyzer math, retained evidence, Time Machine setting, or mount state changed. Claude Fable 5/max review is next.
**2026-07-15 17:54:03Z** - **DECISION + DOCS (D-2026-07-15-1; future reviews use Claude Fable 5/max):** Recorded the owner's exact reviewer boundary: the already-dispatched G12 Grok review was allowed to finish and remains valid; every review dispatched afterward uses Claude CLI with `--model claude-fable-5 --effort max`, with neither Codex nor Grok substituting absent a later explicit owner instruction. The repo-wide synchronous review requirement, fixed-SHA contract, validation and guard proof, structured fail-closed verdict, adjudication, master commits, and no-push rule remain unchanged. Current repo guidance, protocol, STATE, and the retired async review header now point to `.agents/playbooks/reviewloop.md`; the old GPT/Codex procedure is prominently Historical/do-not-execute but retained as prior rationale. Historical review outcomes and old plan prose were not rewritten; the new specific decision supersedes only their reviewer-selection nouns. Docs gate and diff check are required before commit; the committed governance slice will be the first future review sent to the requested Claude model/effort.
**2026-07-15 17:44:34Z** - **REVIEW (otp12 pf-1 rig-W harness round 12 accepted; exact `d5e9dda` cleared for live gates):** Independent Grok (`grok-4.5`, grok 0.2.101, reasoning high) reviewed exact immutable `aa0785c..d5e9dda` in the retained detached worktree. The orchestrator mistakenly interrupted the first invocation after Grok had completed green gates, the exact old-body red mutation, exact-byte restoration, and final green/clean checks but before it returned a verdict. At the owner's direction the same in-flight session resumed and returned an `EndTurn`, schema-valid `ACCEPTED` with exact SHAs and `guard_confirmed=true`; only final `structuredOutput` was adjudicated. Grok confirmed G12 changes only the q client wrapper and its offline guard, that the invoked array is permanently nonempty under Bash 3.2, stale trace variables are removed off and replaced exactly on, and no second empty user-array expansion exists. Syntax, complete Bash 3.2 self-test, and all 23 analyzer tests were green. Restoring only the exact live-failing empty array reproduced `trace_env[@]: unbound variable` and the trace-off wrapper fatal; restoring reviewed bytes returned all focused gates green and the worktree clean. It confirmed no role, measured-path, worker, schedule, timing, trace-schema, or analyzer-math change. Script blob `d3f2fb3`, SHA-256 `5e3f3aa802b9b9bd92f9673b0b31ce7166046fa00e2e5d8cd9aef6a0f2559c95`. Exact candidate `d5e9dda`, not this verdict-record commit, is next for additive staging and SSH-driven launcher/preflight/registered-run gates. No endpoint, retained evidence, Time Machine setting, or mount state was changed.
**2026-07-15 17:24:48Z** - **LIVE GATE + CODER (otp12 pf-1 rig-W G12 Bash 3.2 trace-off wrapper fixed/guard-proved at `cd78ab9`; no accepted datum):** Exact independently reviewed `aa0785c` was bundled, built from clean detached clones, and staged only into new q/Windows paths with source/stage hashes and clean embedded identities verified. q's actual Bash 3.2 self-test passed. Launcher smoke (`/Users/michael/Dev/blit_v2_aa0785c/logs/otp12pf-rigw-20260715T171555Z.15522`) and standalone preflight (`...T171647Z-preflight`) passed exact provenance, q en8/Windows 10 GbE MTU 9000, directional MSS 8948/8960, firewall, quietness, timers, streamed Windows result, canonical manifests, and the complete batched clock path at 396/403 ms. The registered session (`...T171727Z`, tag `20260715T171727Z.16108`) completed `b1_wm_tcp_mixed_p1_source_init` at transfer/settle/flush/total `1027/447/86/1310` ms and appended its provisional row, confirming G11's settle path live. The paired trace-off q-client arm completed drain and its three before-clock samples, then failed before Python or blit launched: macOS Bash 3.2 with `set -u` rejects expansion of the empty `trace_env[@]`. The EXIT trap wrote `SESSION-VOID`; `runs.csv` has one of 128 rows and `clock-samples.csv` nine of 768 samples. The analyzer/finalizer never ran, no `SESSION-COMPLETE` exists, and zero session data was accepted, analyzed, or graded. Identity-scoped teardown closed both 9031 ports with no benchmark process; local, q-session, and Windows-session evidence remains retained. G12 puts `env -u` plus both trace names in a permanently nonempty command array and appends exact assignments only when trace is on. An executed Bash 3.2 guard poisons both variables and proves off removes them, on replaces them exactly, and both real-wrapper sentinels succeed. Restoring the exact live-failing empty array turns the guard red with `trace_env[@]: unbound variable`; restoration returns syntax, the complete harness self-test, and all 23 analyzer tests green. A whole-script audit found no second empty user array. No retained evidence, Time Machine state, mount state, firewall, route, or worker policy was changed. External Grok or Claude review is next.
**2026-07-15 17:07:59Z** - **REVIEW (otp12 pf-1 rig-W harness round 11 accepted; exact `aa0785c` cleared for live gates):** Independent Grok (`grok-4.5`, grok 0.2.101, reasoning high) reviewed exact immutable `5a7e7ec..aa0785c` in the retained detached worktree. The first response claimed acceptance and guard confirmation in about six seconds without reading files, invoking a tool, or running the mandatory mutation, so it was discarded fail-closed and recorded rather than hidden. The authoritative retry ran for 14 model turns, returned `EndTurn` plus schema-valid `ACCEPTED` with exact SHAs and `guard_confirmed=true`, and supplied the missing proof: syntax/self-test green; exact base sequential `record_clock_samples` restored alone; self-test red on `three clock samples did not share one exact reaped SSH/PowerShell channel`; exact reviewed bytes restored; syntax/self-test green; all 23 analyzer tests green. Grok audited binary partial-line deadlines, bounded concurrent stderr, exact indexed grammar, EOF/nonzero/extra rejection, child cleanup/reaping, Bash 3.2 row validation, the complete-path 750 ms preflight gate before daemons, and unchanged settle placement/accounting. Final worktree status was clean at `aa0785c`; script SHA-256 was `af13f4d5dace4ad1933d85acee1950e6030302b154881f6f22c55643dab39562`. It found no reintroduced push/pull byte path, role-specific measured path, or worker cap. No endpoint was contacted. Exact candidate `aa0785c`, not this verdict-record commit, is next for additive staging and SSH-driven launcher/preflight/registered-run gates; no retained evidence, Time Machine state, or mount state was changed.
**2026-07-15 16:54:31Z** - **LIVE GATE + CODER (otp12 pf-1 rig-W G11 batched clock channel fixed at `d1686a0`; no accepted/graded datum):** Exact independently reviewed candidate `5a7e7ec` passed launcher smoke (`...T162815Z-launcher`) and full preflight (`...T162853Z-preflight`): exact identity, q en8/Windows 10 GbE MTU 9000, directional MSS 8948/8960, firewall, both quiet gates, timer, streamed Windows result, fixtures, and helper. The registered run at `/Users/michael/Dev/blit_v2_5a7e7ec/logs/otp12pf-rigw-20260715T162938Z` (session `20260715T162944Z.13808`) started exact block-1 daemons and executed `b1_wm_tcp_mixed_p1_source_init`, then correctly voided before durability or CSV append because post-client settle was 1247 ms, outside `[250,1000)`. `runs.csv` is header-only; nothing was accepted, analyzed, or graded. Identity-scoped teardown left no q/Windows blit, cargo, or rustc process and no 9031 listener. All failure evidence remains at that q log path, `/Users/michael/blit-bench-work/rigw-sessions/20260715T162944Z.13808`, and `D:/blit-test/rigw-pf1/20260715T162944Z.13808`. Diagnosis from `clock-samples.csv`: the three sequential after-clock SSH/PowerShell probes each cost roughly 360–400 ms RTT and their ~1.1 s instrumentation delay alone made the registered settle infeasible. G11 preserves three independent q-before/Windows-clock/q-after samples and their exact placement/accounting, but serves indexed `P|1..3`→`C|same|<ns>` handshakes over one bounded SSH/PowerShell process per phase. Binary nonblocking reads, concurrent bounded stderr drain, exact EOF/exit, kill/reap cleanup, and raw four-field grammar fail closed. Preflight now times the complete record/parse/CSV path and requires it below the 750 ms settle headroom before daemon launch. Bash 3.2 tests cover one launch/three rows, arithmetic, ordered handshakes, wrong index, malformed/extra output, nonzero, partial-line timeout, early EOF, and no surviving process; restoring the exact three-SSH implementation turns the one-launch guard red, restoration green. Format, strict clippy, all workspace tests, Bash syntax/self-test, all 23 analyzer tests, docs, and diff checks pass. External Grok or Claude review is next. No retained evidence was deleted, and no Time Machine setting, mount state, or endpoint policy was changed.
**2026-07-15 16:21:09Z** - **REVIEW (otp12 pf-1 rig-W harness round 10 accepted; still NO DATA):** Independent Grok (`grok-4.5`, grok 0.2.101, reasoning high) reviewed exact immutable `d57a86e..5a7e7ec` in a detached disposable worktree and returned an `EndTurn`, schema-valid `ACCEPTED` with exact SHAs and `guard_confirmed=true`. On the actual Bash 3.2.57 it ran syntax/self-test green, then independently rejoined each of the four dependent local declarations one at a time. The fetch guard failed on unset `local_path`; block-log, q-daemon, and run-block guards each failed on unset `block`. Every exact restoration returned the full self-test green before the next mutation. Its final lexical audit found no remaining same-command local dependency; syntax/self-test passed and the worktree ended clean. Repeated payloads in Grok's text were ignored; only authoritative `structuredOutput` was adjudicated. No endpoint was contacted. Review is closed; exact candidate `5a7e7ec`, not this later record commit, is the only build allowed into live gates.
**2026-07-15 16:11:47Z** - **LIVE GATE + CODER (otp12 pf-1 rig-W G10 Bash 3.2 local derivation fixed at `b1cfde7`; still NO DATA):** Exact reviewed candidate `d57a86e` was built/staged without replacing the retained `6fb369e` artifacts; both endpoint fixtures matched `5001/547110912` and `1/1073741824`. Its retained launcher smoke (`...T155415Z-launcher`) passed exact identity, 10 GbE/MTU/MSS, firewall, quietness, timer, and streamed Windows-result gates, then failed in preflight before helper staging, daemon launch, registration, or transfer. Cause: macOS Bash 3.2 expands one compound `local` command's right-hand sides before its assignments, so `fetch_win_file` read `local_path` while unset. Bash handed its EXIT trap status zero; the harness correctly converted the impossible success-without-strict-cleanup to exit one and retained both endpoint evidence trees. G10 splits that derivation. A complete audit of production declarations found exactly three more same-command dependencies—block-log path, q-daemon block path, and run-block identity—whose current callers masked the defect through Bash dynamic scope; all four are split. The self-test directly invokes each real function with inherited names unset and pins its bytes/path/identity. Rejoining each declaration individually produces the expected targeted red; restoring it returns the full Bash 3.2 self-test green. Format, strict clippy, all workspace tests, shell syntax/self-test, all 23 analyzer tests, docs, and diff checks passed. No Time Machine, mount, endpoint policy, or retained evidence was changed. Fresh independent Grok or Claude review is next, followed by a new exact candidate and SSH-driven launcher/preflight/run.
**2026-07-15 15:40:28Z** - **REVIEW (otp12 pf-1 rig-W harness round 9 accepted; still NO DATA):** Independent Grok (`grok-4.5`, grok 0.2.101, reasoning high) reviewed exact immutable `6fb369e..d57a86e` in a detached disposable worktree and returned an `EndTurn`, schema-valid `ACCEPTED` with exact SHAs and `guard_confirmed=true`. It first ran the Bash 3.2 self-test green, restored the exact live-failing PowerShell backtick-newline expression, reproduced `n) + : command not found` plus the literal-LF guard failure, restored G9's `[string][char]10` bytes, and returned the complete self-test green. It confirmed LF-only canonical manifest semantics were unchanged; the worktree ended clean and was removed. No endpoint was contacted. Review is closed. The exact reviewed candidate `d57a86e`, not this later record commit, is the only build allowed into launcher smoke, preflight, and the registered run.
**2026-07-15 15:33:57Z** - **LIVE GATE + CODER (otp12 pf-1 rig-W G9 Windows manifest payload fixed at `f7ef1d7`; still NO DATA):** After the owner set q Time Machine manual (`AutoBackup=0`, `Running=0`), exact live gates resumed. q's prior staging and both registered fixture roots had disappeared; q was recreated from the retained immutable `6fb369e` bundle/binaries without overwriting a path, and Windows reused the exact `5001/547110912` + `1/1073741824` fixtures still under `bench-module/pull_src_*`. The first retained retry (`...T152231Z-launcher`) refused on the absent Windows source. The second (`...T152457Z-launcher`) passed provenance, fabric, direction-specific MSS, firewall, quiet-host, timer, and streamed-result gates, then exposed a real pre-daemon G9: PowerShell `` `n`` escapes inside Bash's double-quoted `wssh` payload executed as command substitution (`n) + : command not found`) and corrupted the Windows manifest program. G9 now emits LF as `[string][char]10`; an executed Bash 3.2 `wssh` capture requires the exact rendered expression and forbids grave accents. Restoring the exact live-failing expression reproduces the shell error and turns the guard red; restoration green. Format, strict clippy, complete quiet workspace rerun, Bash syntax/self-test, all 23 analyzer tests, docs, and diff checks passed; the first workspace run hit the recorded `test_utils_find` daemon-start race once, then its isolated test and the full rerun passed with two expected ignores. No helper or daemon launched and no transfer was timed. Fresh independent Grok or Claude review is required before rebuild/launcher retry.
**2026-07-15 14:06:01Z** - **STAGING + LIVE GATE (otp12 pf-1 rig-W exact `6fb369e`; safely blocked, still NO DATA):** Built the exact independently-reviewed candidate from a fresh detached Mac clone and a fresh native Windows clone, staged new `6fb369e` paths, and verified clean embedded `+6fb369e3d70f`, bundle identity, replacement-free clean checkouts, and source/stage SHA-256 equality; q did not build. The reviewed helper blob remained `3e4e9ccf...` / SHA-256 `31285a4a...`. q's actual Bash 3.2 self-test passed. The live launcher smoke then refused before helper staging or daemon launch: q still reports Time Machine `AutoBackup=1`, a policy the harness is forbidden to mutate. Evidence is retained as `SESSION-VOID` under the isolated q clone's `logs/otp12pf-rigw-20260715T140346Z-launcher`; q and Windows port 9031 were closed and the active Windows benchmark daemon was absent afterward. Read-only follow-up found every other current quiet gate ready: Time Machine not running, q load1 1.35, Spotlight 0.0%, Windows CPU 2.3%, no conflicting processes. The sole blocker is disabling q Automatic Backups; launcher smoke, preflight, and all 128 arms remain untimed.
**2026-07-15 13:54:36Z** - **REVIEW (otp12 pf-1 rig-W harness round 8 accepted; still NO DATA):** Independent Grok (`grok-4.5`, grok 0.2.101) reviewed complete immutable `4c7c754..6fb369e` in a detached disposable worktree and returned schema-valid `ACCEPTED`, exact SHAs, and `guard_confirmed=true`. It independently drove removal of the replacement-disable setting, commit/path-only bypass, and blob-content-only bypass red at the exact provenance guard, restoring the Bash 3.2 self-test green after each; it reconfirmed G8 cleanup, one `Transfer`, SOURCE-send/DESTINATION-receive roles, role-invariant endpoint-local paths, and the shared 1→8 worker target with no push/pull cap. The worktree ended clean at the reviewed SHA and was removed. An attempted same-model Codex review was stopped and its partial output discarded after the owner correctly flagged that Codex reviewing Codex is not independent; it was not counted. No endpoint was contacted. The exact reviewed SHA `6fb369e`, not this later record, is the only candidate for isolated builds and live gates.
**2026-07-15 13:43:15Z** - **CODER (otp12 pf-1 rig-W G8 replacement-object provenance fixed; still NO DATA):** Accepted round-7 Codex F1. Every reviewed-helper object operation now command-scopes `GIT_NO_REPLACE_OBJECTS=1`: provenance HEAD/short/status, `HEAD_FULL:path`, object type, and blob content all read the literal reviewed object graph before the existing worktree/pre-SCP/remote/per-arm comparisons. The Bash 3.2 fixture installs both commit and blob replacements while visible HEAD stays unchanged and ordinary status stays clean, proves ordinary Git resolves substituted bytes, and requires binding to refuse. Removing the wrapper setting, bypassing it only for commit/path resolution, or bypassing it only for blob content each turns the exact replacement-provenance guard red; restoration green. An independent audit reproduced the wrapper red→green, found the fixture cleanup relied on suppressed `errexit`, then passed after explicit replacement-deletion, empty-list, checkout, and final-status checks. Format, strict clippy, all workspace tests, Bash syntax/self-test, all 23 analyzer tests, docs, and diff checks passed. No endpoint was contacted; fresh complete Codex + Grok review remains required before rebuild or launcher retry.
**2026-07-15 13:23:47Z** - **REVIEW (otp12 pf-1 rig-W harness round 7 reopened; still NO DATA):** Mandatory Codex (`gpt-5.6-sol`, xhigh, codex-cli 0.144.4) and additive Grok (`grok-4.5`, grok 0.2.101) reviewed complete immutable `4c7c754..a539715`. Grok returned schema-valid `ACCEPTED`, exact SHAs, `guard_confirmed=true`, independently mutation-proved both G7 worktree comparisons red→green under Bash 3.2, restored all 23 analyzer tests and a clean detached worktree, and reconfirmed one `Transfer`, SOURCE-send/DESTINATION-receive roles, role-invariant physical paths, and the same eight-worker target under both initiator layouts. Codex returned NEEDS FIXES with one accepted High: ordinary Git object lookup honors `refs/replace`, so an unchanged visible HEAD and clean status can resolve substituted helper bytes and bless a no-op helper as reviewed. G8 will disable replacement-object interpretation for every reviewed-helper object lookup and pin the exact unchanged-HEAD/clean-status spoof red→green. No endpoint was contacted; fresh complete Codex + Grok review remains required before rebuild or launcher retry.
**2026-07-15 12:40:55Z** - **CODER (otp12 pf-1 rig-W G7 reviewed-blob binding fixed; still NO DATA):** Accepted round-6 Codex F1. The purge-helper SHA-256 now comes from `HEAD_FULL`'s exact `scripts/windows/purge-standby.ps1` Git blob, with the worktree required to match at binding and rechecked immediately before SCP; remote post-move and per-arm checks retain that Git-derived expected value. The Bash 3.2 self-test commits one helper, changes its working file, and refuses it; removing the blob/worktree comparison turns that guard red, restoration green. An independent audit caught that the pre-SCP guard initially preceded remote reservation, so it was moved adjacent to SCP and a second fixture now mutates during reservation and proves the copy mock is never reached; removing that comparison turns the copy-reached guard red, restoration green. Format, strict clippy, all workspace tests, Bash syntax/self-test, all 23 analyzer tests, docs, and diff checks passed. The first final workspace attempt hit the recorded macOS `blit_utils` daemon-start race in `test_utils_find_dirs_only`; the isolated test and complete quiet workspace rerun passed with two expected ignores. No endpoint was contacted; fresh complete Codex + Grok review remains required before rebuild or launcher retry.
**2026-07-15 12:28:24Z** - **REVIEW (otp12 pf-1 rig-W harness round 6 reopened; still NO DATA):** Mandatory Codex (`gpt-5.6-sol`, xhigh) and additive Grok (`grok-4.5`) reviewed complete immutable `4c7c754..75a9a33`. Grok returned schema-valid `ACCEPTED`, exact SHAs, `guard_confirmed=true`, and independently mutation-proved G6's post-stage and per-arm hash checks red→green under Bash 3.2; restored worktree clean. Codex returned NEEDS FIXES with one accepted High upstream gap: G6 adopted the expected helper hash from working-tree bytes after lengthy gates, so a concurrent replacement after the clean-tree check could become the new manifested expected helper and pass its own hash/sentinel checks. G7 will derive the expected value from the helper blob at the exact reviewed commit and refuse a working file that differs immediately before SCP, with a committed-blob/changed-worktree guard. No endpoint was contacted.
**2026-07-15 12:09:40Z** - **CODER (otp12 pf-1 rig-W G6 purge-helper provenance fixed; still NO DATA):** Accepted round-5 Codex F1. The harness no longer trusts `D:/blit-test/purge-standby.ps1` as endpoint state: after every read-only endpoint/fabric/quiet gate, it stages `scripts/windows/purge-standby.ps1` from the exact clean checkout into the fresh per-session Windows tree, rejects reparse points, SHA-256 verifies before/after the atomic move, records the fifth provenance row, then re-hashes and requires the exact `standby-purged` sentinel before every arm. Functional Bash 3.2 mocks pin stage/copy/hash/move and per-arm hash/invoke/sentinel contracts; removing either final staged hash verification or the per-arm hash comparison turns self-test red, restoration green. The first endpoint write is source-order pinned after the read-only gates. No endpoint was contacted; fresh complete Codex + Grok review remains required before rebuild or launcher retry.
**2026-07-15 12:01:21Z** - **REVIEW (otp12 pf-1 rig-W harness round 5 reopened; still NO DATA):** Mandatory Codex (`gpt-5.6-sol`, xhigh, codex-cli 0.144.4) and additive Grok (`grok-4.5`, grok 0.2.101) reviewed the complete immutable `4c7c754..06b3322` range. Both independently confirmed G5: q's real en0/en1/en8 ARP fixture selects exactly registered en8; removing that predicate turns Bash 3.2 self-test red and restoration green. Codex also confirmed the exact 128-arm schedule and role-invariant endpoint-local paths, then returned NEEDS FIXES with one accepted High: the harness trusts an endpoint-resident `purge-standby.ps1` by existence/exit only, so a stale/no-op helper can leave Windows warm while the run records cold. Grok returned schema-valid `ACCEPTED`, `guard_confirmed=true`, exact SHAs; its detached worktree ended clean and was removed. G6 will stage/hash the reviewed helper per session, re-hash and require its success sentinel per arm, and mutation-prove that contract before fresh complete review. No endpoint was contacted.
**2026-07-15 11:28:55Z** - **REVIEW (otp12 pf-1 rig-W harness round 4 accepted; still NO DATA):** Mandatory Codex (`gpt-5.6-sol`, xhigh, codex-cli 0.144.4) reviewed the complete immutable `4c7c754..6f517ea` range and returned PASS with no material finding after checking role/session/path/timing/analyzer/launcher/recovery/cleanup semantics and the repaired Bash contexts. Additive Grok (`grok-4.5`, grok 0.2.101) returned a schema-valid `ACCEPTED`, `guard_confirmed=true`, exact base/head, and independently mutation-proved G3 role-path parity plus G4 finalization, cleanup-ownership, and signal-marker removal red→green under Bash 3.2; restored self-test, 23 analyzer tests, and 41 role tests passed. Its detached worktree ended clean at `6f517ea` and was removed. No endpoint was contacted. The immutable reviewed SHA, not this later verdict-record commit, is the only build allowed into launcher smoke, endpoint preflight, and the registered run.
**2026-07-15 11:13:07Z** - **REVIEW + CODER (otp12 pf-1 rig-W harness rounds 3–4; still NO DATA):** Mandatory Codex (`gpt-5.6-sol`, xhigh) reviewed the complete immutable range through `53bb5e5` and returned PASS with no findings. The owner-requested additive Grok (`grok-4.5`) independently confirmed one `Transfer`, SOURCE-send/DESTINATION-receive semantics, worker parity, timing anchor, launcher gate, and cleanup, but returned schema-valid REOPENED / `guard_confirmed=false`: under macOS Bash 3.2, the bare path-parity `[[ ... ]]` assertions survived a real role-in-path mutation and the self-test exited zero. G3 fixed every path assertion with explicit failure handling (`27c94b0`); the exact mutation now fails and restoration passes. A systematic follow-up audit admitted separate G4: material destination/finalization/cleanup/marker/signal assertions had the same Bash 3.2 risk, and the signal marker check was unseeded. G4 fixed them and seeded the signal case (`7e9d2d5`); missing finalization, retained may-exist state, and skipped signal-marker removal each mutate red, restoration green. Both slices passed fmt, strict clippy, the full workspace suite, 23 analyzer tests, Bash syntax/self-test, docs, and diff checks. No endpoint contacted; fresh complete Codex + Grok review is required before launcher smoke or preflight.
**2026-07-15 05:51:14Z** - **REVIEW (otp12 pf-1 TCP phase trace):** The implementation at `5b8cc29` completed the mandatory Codex loop with a clean `gpt-5.6-sol` PASS, independent focused/full tests, and the previously recorded red→green mutations. The owner-requested additive Grok second eye was run with `grok 0.2.101` / `grok-4.5` in a clean detached worktree at the exact `4dba35a..5b8cc29` range. Both the initial response and the playbook's one schema retry failed closed: `structuredOutput` was null, output contained four then five concatenated payloads, `stopReason` was `Cancelled`, and the free-form thought contradicted the payload or admitted the guard had not run; the retry's payloads explicitly had `guard_confirmed=false`. Recorded as a CONTESTED reviewer-protocol failure, not a Grok PASS and not an admitted code finding. The worktree stayed clean; no code fix was required. Records: `.review/results/otp12-pf1-session-phase-trace.{codex.md,gpt-verdict.md,grok-second-eye.json,grok-second-eye-retry.json,grok-verdict.md}`.
**2026-07-15 03:55:46Z** - **REVIEW (otp-12 worker parity, Grok second eye):** Ran the repo reviewloop with `grok 0.2.101` / `grok-4.5` against the exact code range `6b0f01c..42b9b38` in a detached disposable worktree. The structured verdict was **ACCEPTED**, `guard_confirmed=true`, with exact base/head SHAs and no material finding. Grok independently reran both exact-8 role tests, the both-layout gated-resize-ACK progress test, and refusal/arbitration/unknown-capacity guards. Its own production mutation changed zero receiver capacity back into a one-stream cap: the destination-initiator role test failed at 1 versus 8, then passed after restoring the reviewed head. The review worktree was clean and removed. Raw JSON + adjudication: `.review/results/otp-12-worker-parity.grok-second-eye.json`, `.review/results/otp-12-worker-parity.grok-verdict.md`.
**2026-07-15 03:19:30Z** - **CODER (otp-12 worker parity closed; no hardware data):** Re-grounded after the prior session's incorrect push/pull conclusion and proved the current architecture from source: there is one byte-moving `Transfer` RPC; a destination-side caller connects to the SOURCE daemon, declares DESTINATION, and that daemon sends through the same SOURCE pipeline; only initiator→responder socket acquisition differs for NAT/firewall reachability. The worker concern itself was real: identical 10,000-file fixtures settled at 3 vs 2 workers, and destination initiation interpreted legal `max_streams=0` as a one-worker cap. Fixed shared receiver-ceiling semantics, nonblocking payload/resize convergence, terminal refusal, and atomic tuner/shape/settlement arbitration. Final role pins are exactly 8/8; with resize ACK #2 held, both layouts transfer all 2,000 tiny files before convergence and land identical trees. Codex review converged through five FAIL rounds (2+1+2+1+1 findings, all accepted): pre-payload serialized RTTs, refusal reproposal, split-atomic races/ABA, stale tuner decisions, and scheduler-dependent guards. The final guard observes a real settlement `try_lock` and carries one guard-owned acquisition identity from eligibility through claim; drop/reacquire, omitted refusal, pre-dispatch settlement, and release-only `debug_assert!` mutations all go red. Final Codex re-review PASS with no findings; fmt, strict clippy, docs, release compilation, repeated debug/release guards, and the full workspace suite are green (1,490 passed, 2 ignored). Commits `a76b785`, `cfd9dd7`, `8e993aa`, `641916e`, `f7f12ec`, `42b9b38`. **No Mac↔Mac run was performed:** worker parity is no longer a blocker, but the live round-12 instrument can call a 1.092/acceptance-PASS cell `REPRODUCES` and its end gate can grade after a 10GbE→1GbE renegotiation because it omits link speed. **No next hardware experiment was selected:** the owner still chooses between repairing/re-reviewing that instrument before the missing Mac↔Mac cell and instrumenting P1's dial/accept path on rig W as the round-12 reviewers recommended.
**2026-07-15 02:30:00Z** - **CODER + ADJUDICATION (round 11 fixed and re-reviewed round 12 — and the round-12 reframe caught that the whole experiment was aimed at a question the existing data already answers; still NO DATA, claude)**: **Fixed all of round 11 across 7 commits, each with a guard proof. ENGINE (`4270e42`, `584845f`, `352ed5c`, `7b685f9`, `4bfbb32`):** `B` applied once inside `classify` and only ever hardening; **`B ≥ T/2` on any measurand ⇒ `CONTROLS-NOT-CLEAN`** (owner D-2026-07-14-4, verbatim "Refuse to grade" — on a slow measurand `T` is capped at Δ_ref while the permitted control bias is a *fraction* of the arm, so `T−B` can go negative and license an "effect" that is mostly rig); **MIXED decided on the UNHARDENED cell states** so a noisier rig can't upgrade an inconclusive session to `REPRODUCES`; the **arm medians use the conventional even-n median** not the low median (the low median shrank `T` on a bimodal arm — and rig W's fast arm is known bimodal); and the **arm-count check finally has a case AND a selective mutation** (both reviewers flagged it unguarded — deleting it left the suite green while a skewed CSV graded `REPRODUCES`; the test helper gained `extra_rows` so an unpaired-valid-row CSV is expressible at all). Guard: **40 cases, 19 mutations, all killed.** **HARNESS (`1562cde`):** the round-11 **BLOCKER closed** — the registered topology is now pinned in code and refused from the environment; `topology_gate` proves the NIC (MTU 9000, negotiated 10Gbase-T, active) and `mss_gate` proves the PATH (negotiated MSS 8948 via `getsockopt(TCP_MAXSEG)`, the gate pf-0 used, plus the egress IP), re-checked at session end and voiding on change; plus the four HIGHs (drain producer statuses, `resolve_disk`'s `df`/`grep` statuses, both Time Machine gates no longer coercing `"0%"` to "disabled", `ps` no longer mapping every error to GONE) and the MEDIUMs. **Two of my OWN fixes were caught by RUNNING the self-test, invisible to `bash -n`:** the pinned topology literals were first placed ABOVE the override check so the harness refused EVERY run (a protection that cannot PASS is as dead as one that cannot FAIL), and a new sentinel was `:`-delimited around a MAC address (all colons) so a gate went BLIND on a good link. **Fabric mutation proof:** pointing the rig at nagatha's 1GbE `en0` is caught by THREE independent gates (link, NIC, path); each fabric claim fires on its own when falsified. **PREREGISTRATION → rev 11** (`bfae311`): the summary, the state table, the two new rule clauses and the two unregistered gates (fabric + per-run RTT) all now match the code. **THE REFRAME — D-2026-07-14-5 (owner): the FIRST review asks "is this the best way to do it", not "is this correct per the plan".** The owner's diagnosis, verbatim: *"You keep finding problems in the plan you wrote, so it's likely codex will have a more coherent idea."* Round 12 was re-dispatched under this rule — codex and grok each given the END GOAL and told a well-built-instrument-pointed-at-the-wrong-question was the most valuable answer available. **BOTH said DO NOT RUN IT**, and codex's design review raised a would-be BLOCKER: the harness that measured P1 (`bench_otp12_win.sh:505`) flushes with no settle, so a free-writeback artifact could have MANUFACTURED P1, and the numbers should be re-measured before proceeding. **I relayed that to the owner as a pivot — WRONGLY, without checking it against the recorded data.** The owner refused the pivot and demanded it be put to consensus. **ADJUDICATION (`p1-adjudication-r1.{codex,grok}.md`): both reviewers, independently, from the actual CSVs, reached `P1 REAL` (high confidence), reversing the artifact claim.** The artifact hypothesis fails every one of its own predictions in the recorded data: on `wm_tcp_mixed` the **flush is symmetric** (72 vs 73 ms) against a **~300 ms** effect; the effect is entirely in **transfer time** — removing flush makes P1 *worse* (1.385 → **1.417**), and every slow-arm run (1012–1104 ms) exceeds every fast-arm run (647–820 ms) with **zero overlap**; the **same-fixture gRPC control passes at 1.020** (a writeback artifact would hit it identically — same files, same dirty pages); and Linux uses the same immediate-flush method with **no P1** (flush 780 vs 780). Grok confirmed across all three P1 sessions (98.6% / ~100% / 99.0% of the effect in transfer time). Both independently cited the project's own precedent: a *real* durability-accounting artifact was caught here once (`2c0af86`) precisely because it **polluted the gRPC control** — P1 is carrier-specific, so it passes that test the way a genuine code effect must. **P1 is a real property of the macOS↔Windows TCP-mixed transfer path; the release blocker is genuine, not measurement error.** Both point next at instrumenting the dial/accept transfer path on the rig where P1 lives, not at the Mac↔Mac run or a settle re-measure. **NO CODE (crates/proto) TOUCHED, NO RIG TIME, NO DATA — suite still 1488 as of `bb28ddd`.** Also this session: recreated nagatha's missing `f35702a` worktree + release build (STATE's "both Macs ready" had covered daemons and Time Machine, not binaries). **NEXT: owner's call on direction — instrument the TCP dial/accept path on rig W (both reviewers' recommendation), vs the parked Mac↔Mac run.** D-2026-07-14-4 and D-2026-07-14-5 recorded.

**2026-07-14 22:45:00Z** - **CODER (rounds 7–11 of the Mac↔Mac instrument — the owner said "simplify", and the RULE turned out to be where the bugs lived; still NO DATA, claude)**: *Backfills the gap the 21:10Z drift pass flagged: rounds 7–11 had landed with no DEVLOG entry.* **ROUND 7 (`f7f6e17`) — both reviewers NOT READY again, and the diagnosis changed the plan.** Every new finding was mine: **the drain failed open AGAIN** — my own round-5 rc fix was itself fail-open, because if `hrun` prints `drained_*` and *then* exits non-zero, `|| echo DRAIN-ERROR` appends a second line and the value **still starts with `drained`**; prereg rev 7 **contradicted itself** (older sections still described the bar-based materiality the rewrite had removed); and `CONTROLS-UNCERTIFIED` told the operator to escalate to `RUNS=16` while the harness refused anything but 8. The pattern was now unmistakable: **four of the last five BLOCKERs were in the DECISION RULE, not in the measurement.** **THE OWNER'S CALL — D-2026-07-14-3, verbatim: "simplify"** (`30d4374`). Not "harden it again". **ROUND 8 (`79c1f2d` rewrite → `08570b5` review): the rule was cut from 647 engine lines to 321** — one statistic, one threshold, four mutually exclusive states — **and the simplification promptly introduced a REAL hole of its own.** codex found it **in the rewrite's own reasoning, not in a branch**: I had deleted the `UNSTABLE` state arguing "a bimodal arm widens the CI, so it lands in UNCLEAR". **That is TRUE at n=8 — where the ≥95% order-statistic CI *is* the full range and therefore cannot trim — and FALSE at n=16**, where the interval `[d₍₄₎,d₍₁₃₎]` **trims three outliers per side**, so a bimodal arm yields a *narrow* CI and a **false null**. It drove `CI=[1,1]`. **ROUND 9 (`8830fda` rework → three findings, 8/8 accepted):** grok reproduced a **BLOCKER** I had created by half-deleting the escalation — I removed 16 from the registered pair counts but left the completeness check reading `len(d) >= PAIRS`, so **a 16-pair CSV was graded, the CI picked k=4, and it TRIMMED the three pairs at −500 while keeping the thirteen at +200 → `REPRODUCES`.** The entire rule leans on the n=8 identity (the interval cannot trim); **that only holds if n is EXACTLY 8**, and it now is. grok also killed **`B` (the residual arm bias the controls could not rule out)**: I had defined it as the max |CI bound| over clean controls, but **the CI is the wrong quantity — it is an interval for the MEDIAN and it trims**; the honest bound is the control's **full RANGE**. grok drove a control with range `[5,40]` yielding `B=5` instead of `40`, and a measurand at `+105` then read `REPRODUCES`. codex (reworded prompt) added that **`B` carried RAW MILLISECONDS between controls of different arm speeds** — the same 4.9% bias is 122 ms on a 2500 ms large-file control and a different number on a fast one — so **`B` is now RELATIVE to the arm**. Also: the drain accepted **`"."`** as a number. **ROUND 9's OTHER LESSON, recorded because it cost a whole review: codex's first round-9 run was KILLED BY A CONTENT FILTER** after reading 85k tokens and **produced no review at all** — its file contains only stale round-1 material quoted out of the prereg's own history (`eb864ac`). **A killed run's file must never be mistaken for a review.** The trigger is the framing: *"find the fail-open protection, assume the defect class is present"* reads as vulnerability scanning when it sits on ssh/sudo/pgrep code. **Split the review (engine / harness) and word it as plain measurement-correctness, and it goes through.** **ROUND 10 — two BLOCKERs, and both were self-inflicted by deletions.** Engine (`0caca92`): **the registered cell IDENTITIES AND ROLES came from mutable environment variables** — omitting a dirty control from `CONTROL_CELLS`, or dropping it from `REGISTERED_CELLS`, let the session grade anyway. **Which cells are controls is part of the pre-registration, exactly like `DELTA_REF`, and for exactly the same reason: otherwise the rule can be retuned from the command line, after the data exists, toward the answer you want.** Pinned in code; the engine now refuses an env set that disagrees. (Also: all-zero rows reported an `EFFECT`.) Harness (`8997f92`): **THE BUILD PIN WAS GONE, AND I DELETED IT MYSELF.** `EXPECT_SHA` was never compared to `REGISTERED_BUILD`, so **any sha was accepted — including `f35702a.dirty`**. It was collateral damage from cutting the escalation block out: adjacent lines, and the slice took both. **A regression introduced by a deletion, in the very commit that was removing machinery to make the thing safer.** Plus: five RTT samples at *preflight* are not a bound on the ssh dispatch of a run taken minutes later. **ROUND 11 (`e65863c`) — THE ENGINE HAS NO BLOCKERS FOR THE FIRST TIME IN ELEVEN ROUNDS.** Two HIGHs remain in it, both real: **`B` can EXCEED `T`** on a slow measurand (`T` is capped at Δ_ref=230 ms while the controls' permitted bias is a *fraction* of the arm, so above ~4600 ms `T−B` goes negative — a null becomes impossible while `T+B` licenses an "effect" that is mostly rig); and **`B` hardens each cell but can make the SESSION verdict EASIER via `MIXED` precedence** (if `B` pushes one cell out of `EFFECT`, `MIXED` stops firing and the session reports `REPRODUCES` instead of the inconclusive `MIXED`). **THE HARNESS STILL HAS ONE BLOCKER: the registered topology is not enforced — NIC/IP/MAC are env-overridable and THE MTU IS NEVER CHECKED**, so the run could silently go over the **1GbE** NIC or at **MTU 1500**. pf-0 spent 256 runs establishing that MTU moves wall time; **a rig must prove it is on the fabric it claims.** Four HIGHs with it: `resolve_disk` discards the `df` pipeline status; **both Time Machine gates use `tr -cd '0-9'`, so a malformed `"0%"` reads as "disabled"**; the per-pair RTT gate exists in code but **not in the pre-registration**; the drain still discards its producers' statuses. **THE ARC, stated plainly: eleven rounds, ~110 findings, 100% accepted, none rejected — and the instrument has still never taken a single datum.** Three project claims were already retracted to harness bugs, which is why this discipline exists. **Two defect classes recur in EVERY round and the next review must assume both are present: (1) "fixed the branch I was shown, not the class"** (the same materiality bug escaped four rounds; a fail-open `pgrep` was fixed in one gate and left in its duplicate; the drain was fixed by VALUE and left failing by STATUS) **and (2) "a protection that never executes, or cannot fail"** (`SETTLE_MS` had never run in any revision — a quoting bug killed the `sleep` and its status was discarded — while the prereg asserted it for three revisions). Earned rules: **verify the instrument before believing the measurement; `bash -n` is not an execution; a protection that cannot be observed is not a protection; a mutation that cannot be killed is not a proof.** Suite untouched (**1488 as of `bb28ddd`**; zero `crates/`/`proto/` changes in any of these commits). **NEXT: fix round 11 (1 harness BLOCKER + 4 HIGH; 2 engine HIGH), re-review, THEN run.**

**2026-07-14 21:10:00Z** - **DRIFT (STATE hygiene: the handoff log was four review rounds stale — the one place a cold session actually reads, claude)**: Owner ran `drift`, scoped to the state-hygiene pass. **No code touched, no rig time, no data taken.** **THE FINDING**: `docs/STATE.md`'s newest handoff entry (49th) said *"In-flight: the round-7 review … READ THEM FIRST. NEXT: adjudicate round 7"* — while **rounds 7, 8, 9, 9b, 10 and 11 had all landed since** (`1e03063..e65863c`, results on disk in `.review/results/macmac-harness-r{7,8,9,9b,10,11}.*`). The top of STATE was current (it correctly described round 11's open findings); only the **entry point** was lying, and the entry point is what a fresh session reads to decide what to do. **A cold session trusting it would have re-adjudicated closed work on an instrument that had moved five times underneath the entry.** Corrected in place with the git evidence rather than rewritten into a new narrative. **The rest of STATE verified TRUE** — every doc path, every `docs/bench/` directory, the pre-registration really is **rev 10**, the harness really is still at round 10's `8997f92` (no round-11 fixes started), and STATE's summary of round 11 matches the three result files line for line. Also fixed, all evidence-backed: **`docs/history/state-archive.md` created** — the rotation target AGENTS.md's `drift` definition names, which had **never existed** — and the landed otp-1..11 closed-slice record rotated into it verbatim, leaving a pointer; **`Suite 1488` was a volatile count with no anchor** → now **`1488 as of bb28ddd`**, the last commit to touch `crates/`+`proto/` (everything since is docs/scripts, so it stands without a re-run); **the rig's IPs and MTU were restated in STATE** → replaced with a pointer to `.agents/machines.md`, which owns host facts; **`"windows-latest CI pending a push"` was a recorded push-state line** (the rules say delete on sight — git owns push state) **and it was also false**: local, `origin` and `github` are all at `7fc48d3`; and the Mac↔Mac run added to `## Blocked` as a **pointer, not a restatement**. **⚠ THE GAP I DID NOT FILL: DEVLOG has NO ENTRY FOR ROUNDS 7–11.** Its newest entry before this one is 18:45Z (rounds 5+6) and it ends by naming round 7 as next. Five rounds — including the decision-rule rewrite under **D-2026-07-14-3** and the **build-pin regression** caught in round 10 — have no history entry; their only record is the review result files and the commit messages. **Drift audits history; it does not author history it did not witness**, so this is flagged rather than reconstructed. Docs only: `check-docs.sh` OK, STATE **199/200** lines. Commit `f933097`. **NEXT: fix round 11's findings (2 HIGH in the engine; 1 BLOCKER + 4 HIGH in the harness — the registered 10GbE/MTU-9000 topology is not enforced), then re-review, then the run.**

**2026-07-14 18:45:00Z** - **CODER (rounds 5 AND 6: THE SETTLE HAD NEVER RUN — not once, in any revision — and the same materiality bug escaped a THIRD and then a FOURTH time; 25 more findings, 25 accepted, still no datum taken, claude)**: **ROUND 5 — codex (3 BLOCKER, 6 HIGH, 2 MEDIUM) + grok, converging independently; 12/12 accepted (`aebd50b`).** **THE HEADLINE IS A DEFECT THE REVIEW DID NOT FIND, WHICH ITS FINDING EXPOSED.** Codex filed a HIGH — *"failure of the required settle `sleep` is ignored, because the succeeding python fsync walk supplies the command status"* — and **executing it showed the status was ALWAYS failure**: the `awk` computing the settle duration sat inside a **command substitution**, which bash parses **fresh**, so the `\"` escapes (correct in `hrun`'s two-level strings, and correct everywhere else in the file) were **literal backslashes** to awk. Measured: `awk: syntax error`, `usage: sleep number[unit]`. **The awk errored on every call, `sleep` got an empty argument and failed, and the walk ran immediately, every time. SETTLE_MS HAS NEVER BEEN APPLIED — and it was introduced in `24660ae`, THE COMMIT THAT ADDED IT TO FIX the free-writeback asymmetry that reverses sign with direction — the artifact judged capable of MANUFACTURING a one-directional P1 out of nothing. The fix for that BLOCKER never executed, and the pre-registration asserted it through revisions 3, 4 and 5.** Nothing is retracted only because **no data was ever taken** — the fourth time this project has been saved by not having run yet, and the sharpest possible argument for the rule that found it: **`bash -n` is not an execution.** Round 5's other BLOCKERs: **`bar == FAIL` was DIRECTION-BLIND** (the bar is computed on the MARGINAL medians, the CI on the PAIRED differences, and they can point OPPOSITE ways — at n=16, thirteen `+1 ms` pairs plus three that fall below the whole distribution make the medians fail the bar *inversely* while every surviving pair is +1 ms → **`REPRODUCES` off ONE MILLISECOND**); an **UNDERPOWERED control escaped the void** — the same materiality bug's **third** branch, found independently by **both** reviewers (one zero pair drags `ci_lo` to 0, demoting a control carrying the *full* rig-W effect from `PARTIAL`, which round 4 made void, to `UNDERPOWERED`, which it did not) → a clean **`VANISHES` with every control at `D=+230`**; and **the registered constants were ENV-OVERRIDABLE** — `DELTA_REF_MS=240` turned a `RIG-VOID` into a `VANISHES`, i.e. **the pre-registered rule could be retuned from the command line, after the data existed, in the direction of the answer you want.** Also: the **same fail-open `pgrep`** I had fixed in the quiescence gate, still sitting in the **stale-daemon probe**; the drain **falling back to the APFS *synthesized* disk** (whose counters read **idle while the physical store saturates** — a false quiet, not a harmless default); a **p-hackable escalation** (a flag was sufficient); and **`SELFTEST` itself dishonest** — it labelled *every* nonzero result `[FIRED]`, **including a probe that could not answer**, exited zero, and claimed "every gate executes" while never touching drain, purge, daemon, fsync/settle or end-load. **ROUND 6 — codex (3 BLOCKER) + grok (2 BLOCKER); 13/13 accepted (`1e03063`).** Grok's sentence is the one that matters: ***"The rework again fixed the branch that was shown, not the class."*** The materiality bug escaped a **FOURTH** time: round 5 made the bar failure *direction-aware*, so codex simply **moved the outliers** so the bar failed in the **matching** direction — three outliers shift the marginal median (1000→1201, ratio 1.201) while every pair in the CI is `+1 ms` → **`REPRODUCES` off a one-millisecond paired effect** (verified before accepting). And certification used the **same threshold as materiality**, so a control carrying **`D = +229`** — *one millisecond* under the reference effect — **certified as "clean"** and the session printed `VANISHES` with the prose *"every control is CERTIFIED clean"*. **Certifying a control with the very threshold that DEFINES the effect is incoherent**: it would let the gRPC control carry all but 1 ms of P1 while we claim P1 is TCP-only. Codex added: **uncertified controls blocked only the NULL** — with every control at `D=+230` the engine still confidently declared **P1 REPRODUCED** (*"uncertainty about a rig-wide confound is not evidence that the confound is absent"*). And the settle repair was **still not provable** (Class 2, second instance): `sleep` is PATH-resolved, the walk's timer starts *after* it, and the self-test only counted files — so a **no-op sleep would have passed** while the log narrated "settle included". **A log line is a sentence, not an assertion — which is precisely how the settle stayed dead for three revisions.** **THE REV-7 ANSWERS ARE STRUCTURAL, aimed at the classes**: (1) **the 1.10 bar takes NO PART IN INFERENCE AT ALL** — it is the project's *acceptance* criterion, computed and reported; **direction** is the sign test, **magnitude** is the paired CI, **equivalence** is the CI against the margin, and no marginal statistic may decide anything; (2) **the controls are a PRECONDITION** — unless every control certifies below **HALF** the material effect, **no measurand verdict is read, neither a null nor a reproduction** (new registered outcome `CONTROLS-UNCERTIFIED`); (3) **the settle is performed and MEASURED inside the same python process as the fsync walk** (`settled_ms`, now a CSV column; the pair **VOIDS** if it did not elapse; SELFTEST measured **260 ms** on both hosts); (4) **blindness is marked EXPLICITLY** (`die_blind` → `FATAL[PROBE-BLIND]`, 10 sites) rather than inferred by **grepping a gate's prose** — grok found `timer_gate`'s wording didn't match the regex, so **a blind measurand clock scored `[FIRED]` and the self-test PASSED**; (5) there is exactly **ONE** process probe in the file, so there is no second site to forget. Also landed: the ssh dispatch bound is now **ENFORCED** (a measured bound that is not enforced is a note, not a protection), the escalation is bound to the prior session's **runs.csv hash** (a copy cannot buy a second re-roll), and the engine **refuses to grade with no controls**. Guard: **27 cases, 18/18 mutations KILLED**, 300-input fuzz over measurand **and** controls; five mutations went **STALE** when the engine was restructured and the stale-detector caught every one. **Codex ran out of credits mid-session (the owner redeemed a reset).** **Six rounds, 69 findings, 69 accepted, 0 rejected — and not one datum taken, which is the only reason none of it is a retraction.** Suite untouched (**1488**; zero crates/proto changes; docs gate OK). **NEXT: adjudicate the round-7 review (launched, results in `.review/results/macmac-harness-r7.*`), then the run.**

**2026-07-14 16:30:00Z** - **CODER (rounds 3 AND 4 of the Mac↔Mac instrument: my timer measured a 1000 ms transfer as −1 ms, and then grok drove a clean "VANISHES" with every control dirty — 24 more findings, 24 accepted, STILL NOT CLEARED TO RUN, claude)**: **NO DATA TAKEN, and the harness now refuses a timed run outright (`exit 2` without `CLEARED_BY_REVIEW=1`).** Two more rounds on the instrument. **ROUND 3 — codex (12 findings) + grok (3 more), 15/15 accepted (`cae2e0f`).** **THE KILLER WAS MINE, INTRODUCED BY THE REWORK THAT FIXED ROUND 2**: the transfer timer captured `time.monotonic()` in **two separate `python3 -c` processes** and subtracted them. On macOS that clock is **process-relative** — I measured a **1000 ms sleep reading as −1 ms on nagatha and 2 ms on `q`**. *Negative.* Every `ms` row would have been ≈ the fsync time alone, and the invariance ratio — **the entire measurand** — would have been computed on **fsync noise**, which can manufacture or mask a one-directional effect at will. The rig would have produced a clean session, 0 voided pairs, and a **confident, meaningless verdict**. Grok caught it independently (a 500 ms sleep reading ~3 ms) before seeing codex's findings. **The repo had ALREADY WRITTEN THIS LESSON DOWN** — `bench_otp12_zoey.sh:116` uses `time.time()` and says *why* — and I reintroduced the bug anyway. **The lesson is not "add a reviewer"; it is READ THE EXISTING HARNESSES BEFORE WRITING A NEW ONE.** Fixed structurally: ONE process times itself and spawns the client, and **preflight now PROVES THE CLOCK on both hosts against a known 1000 ms sleep before any data is taken** (a non-positive transfer time VOIDs the run instead of entering the data as a "fast" row) — the bug class cannot ship again without the instrument catching it on the rig. Round 3 also found the **preflight could not succeed at all** (`grep -c` exits 1 on no match, so a **clean** binary tripped the dirty-marker probe and died; `norm_mac` used gawk's `strtonum()`, absent from macOS awk) — proof that **round 1's "fixes" were never executed**: I had run `bash -n`, not the gates. `SELFTEST=1` now runs every gate for real and takes no data; **it immediately earned itself twice** — `link_gate` was **refusing a perfectly good link** (`arp -n <ip>` prints one line **per interface**, and `q` holds entries for nagatha on en0, en1 *and* en8, so the MAC was a three-line string that could never match; it now checks the ARP entry on the NIC the traffic will **egress**, which is the more correct question anyway), and my ssh-RTT bound was **wrong by 7×** (35 ms vs ~15 ms) because `now_ms()` spawned a python per call — **I was timing interpreter startup and calling it network latency: the same cross-process trap as the timer, in a different dress.** The decision rule was broken three ways: the equivalence margin was tied to the **bar**, which on a slow arm is **wider than the effect it must exclude** (all eight `d_i = 230` at `src=2500` → **"VANISHES"**, a rig-W-sized effect in *every pair*); the negative bound was `−0.10·src` when the bar is symmetric in **ratio**, so `−src/11`; and the bootstrap CI was **not 95% at n=8** (it resolves to ≈`[d₂,d₇]`, true coverage 92.97%) while the sign test was computed and **never read**. **ROUND 4 — grok alone (9 findings, 9/9 accepted, `a9460ce`), and its headline is the one that stings**: it **drove the engine to a clean `VANISHES` while EVERY control carried the full rig-W effect** (`d_i = 230` ×8, ratio 1.092, bar PASS → `PARTIAL` → escaped RIG-VOID). **This is the SAME STRUCTURAL ERROR as round 3's**: I fixed the bar-tied margin for the **measurand** and left it bar-tied for the **controls**. **Fixing a bug in one place is not fixing its class — and that is now twice in a row.** Grok also reproduced: the engine **trusted `meta.complete`** and never counted pairs, so a **one-pair CSV emitted `VANISHES` at 0% CI coverage** (it is separately executable and hashed into the manifest precisely so it can regrade CSVs — so it must not trust the harness); and session precedence **hid a clean one-direction `REPRODUCES`** (8/8 in `nq`, reported as `BAR-FAIL-INCONSISTENT` because `qn` was noisy — a **false NON-reproduction** against the prereg's own "either direction" rule). And **my own comment lied**: it said the end-load was captured before the verdict "so a session can void on it" — **the code only logged it**. *A doc claim the code did not honour, in the commit that boasts about killing that exact defect class.* Also accepted: preflight ran the guard cases but **not the mutations**; at n=8 the ≥95% order-statistic interval **is** `[min,max]`, so one noisy pair blocks a null **forever** and the rig could only ever say `UNDERPOWERED` (**a null-incapable instrument is also broken**) → a **pre-registered escalation**: an `INCONCLUSIVE-UNDERPOWERED` session may re-run **once** at `RUNS=16` (coverage 97.9%), triggered by a **power failure and nothing else**, and the harness refuses it without `UNDERPOWERED_ESCALATION=1`; "the CI and sign test are duals" is **false once a zero exists** (`d=[0,300…360]` → 7/7, `p=.0156` significant, while `CI_lo = 0`), and the verdict text was **claiming a sign disagreement that did not exist**. One finding accepted **with a modification**: grok wanted a non-numeric `ms` soft-mapped to `INCOMPLETE`; softening a corrupt row **hides** the corruption, so it now fails **loudly and closed** (named error, exit 2). Guard test: **17 cases** (every one a defect a reviewer actually found), **11/11 mutations KILLED**, 300-input fuzz now perturbing **controls as well as the measurand** (grok: the old fuzz pinned controls clean, so **every dirty-control path — the one hiding the BLOCKER — went unexercised**). **Two of my own new mutations failed honestly and I fixed the mutations, not the report**: one went **STALE** (its target text drifted in a refactor; the stale-detector caught it) and one **SURVIVED** because the new coverage guard killed the mutant first, so it no longer proved what it named. Prereg → **rev 5**, amended before any data (the only honest time). **⛔ CODEX COULD NOT REVIEW ROUND 4 — its credits are exhausted until 2026-07-19**, so `.review/results/macmac-harness-r3.codex.md` holds a **usage-limit error, not a review**. **D-2026-07-14-2 makes codex MANDATORY and says grok "never runs alone", so the slice is NOT CLEARED and no agent may self-authorize it** — owner call: wait for codex, buy credits, or amend the rule. **Four rounds, 44 findings, 44 accepted, 0 rejected — and every rework has introduced a defect of its own.** Suite untouched (**1488**; zero crates/proto changes; docs gate OK). **NEXT: the owner's call on the codex blocker; then the round-5 review; then, only then, the run.**

**2026-07-14 08:05:00Z** - **CODER (the Mac↔Mac rig FAILED REVIEW TWICE ON SUBSTANCE — and two instrument bugs were caught before any rig time, claude)**: **NO DATA WAS TAKEN. That is the headline, and it is a good outcome.** The Mac↔Mac experiment (queue 1(ii), the missing 2×2 cell) was pre-registered before any run, and the codex loop then dismantled it **twice**. **ROUND 1 (NOT READY — 1 BLOCKER, 7 HIGH, 1 LOW; 9/9 accepted).** The BLOCKER killed the experiment's whole reason for existing: I claimed *"P1 reproduces macOS↔macOS ⇒ **H1 DIES**, because H1 accuses the Windows accept branch."* **H1 does not accuse Windows.** Verbatim in the parent it accuses **blit's own code paths** (`SourceSockets` Dial/Accept, `add_dialed_stream`, the dial-before-ACK at `transfer_session/mod.rs:3113`) — **the word "Windows" appears nowhere in it**, and that code runs on macOS too, so a Mac↔Mac reproduction is **consistent with** H1, not fatal to it. (The plan itself warns: *"'consistent with H1' is not confirmation."*) **I took the framing from `docs/STATE.md` and never opened H1** — the SECOND time in one session I propagated a wrong claim about the hypotheses instead of reading them (the first: "H1/H5/H6/H7", where H5/H6/H7 are **P2**). Both fixed in STATE. Round 1 also killed: "endpoint asymmetry cancels" (it does not — switching the initiator also reassigns which Mac runs the CLI vs the daemon, and `q` is faster); "both directions must fail" (P1's rig-W signature is **one-directional** — demanding both would have let a real reproduction be waved away); a noise band that was **not a noise floor** (max of four point-estimate ratios across different carriers/fixtures/destinations); and gates that only **warned** on the Time Machine hole pf-0 had just exposed. **ROUND 2 of the rewrite (NOT READY AGAIN — 3 BLOCKER, 6 HIGH, 2 LOW; 11/11 accepted).** (1) **Rev 2 swapped one false dichotomy for another**: a reproduction on *these two Macs* could be **macOS/APFS or host×role residue**, not a "platform-general layout cost", and a null licenses only *"did not reproduce on this pair"*, **not** "Windows required" — and the "platform-residue escape" I claimed it would close **does not exist** (the parent states P1 has **no escape hatch**; D-2026-07-12-1 does not cover it). (2) **My power gate was BROKEN, with a damning counterexample**: `S = max−min` is a *range*, not a minimum-detectable-effect, and codex constructed `d = [0,180,180,190,190,200,200,200]` — **7 of 8 pairs positive, effect 83% of the 230 ms reference** — on which my rule returns **"VANISHES, powered=yes"**. It would have declared P1 *absent* on data showing a large consistent effect: **pf-0's exact error (a null from an instrument that cannot see the effect), reproduced inside the document written to prevent it.** (3) **The harness implements NONE of the registered rule** — `compute_verdicts` emits only per-cell PASS/FAIL, with no control gate, no clustering, no six outcomes; so a human applies the rule *after seeing the numbers*, which is what pre-registration exists to forbid. Rev 3 must make the **harness** the authority (it computes the verdict; the prose only describes the code) and use a real paired **equivalence** test (distribution-free CI on median(dᵢ); at n=8 the order stats [d₍₂₎,d₍₇₎]). **TWO INSTRUMENT BUGS CAUGHT BY LIVE VALIDATION, before either review and before any timed run** — the "verify the instrument before believing the measurement" rule paying for itself: (i) **the timed bracket was INVALID** — it read `t0` in one `python3 -c` and `t1` in another and subtracted them, but **`time.monotonic()`'s reference point is undefined across processes** (only same-process differences are valid); measured on this rig, consecutive cross-process reads returned **−1 ms and −4 ms**. It would have emitted plausible-looking garbage. And interpreter startup sat **inside** the window — and since the two arms of a cell are initiated by **different Macs**, any startup difference is charged to one arm: **the otp-2w failure mode (a cost billed to one arm and not the other) in a new disguise.** Fixed with a single python process bracketing the transfer. (ii) **Landed paths were assumed, so I measured them**: push lands at `<mod>/<tag>/src_<W>`, pull lands **contents directly** under `<mod>/<tag>` — both match what the harness computes. (A 10k-file push initially landed *nothing*; the cause was a **stale daemon with a different config**, which is exactly what the harness's stale-daemon refusal exists to catch.) Bracket validated live in all four shapes (local/ssh × push/pull × with/without `--force-grpc`); fixtures verified identical on both ends (1 / 5001 / 10000); rig left clean. **The quiescence gate also fired on its FIRST invocation**, refusing to start while the codex review of rev 1 was running on nagatha — nagatha is a bench END now, and the gate proves the review loop and a Mac↔Mac session genuinely cannot overlap. Suite untouched (**1488**; zero crates/proto changes; docs gate OK). **NEXT: rev 3 (prereg + harness), then run it.**

**2026-07-14 09:40:00Z** - **CODER (the Mac↔Mac rig: pre-registration and instrument BOTH rejected by review, before a single timed run — and one of my own statistics would have declared a real P1 "absent", claude)**: Built the third harness (`scripts/bench_otp12pf_mac.sh`) for the missing 2×2 cell — macOS↔macOS on nagatha `.92` ↔ `q` `.54`. **Nothing has been measured yet, and that is the whole story: two codex rounds caught 20 defects across the design and the instrument, several of which would have produced a confidently wrong result from a clean-looking run.** **ROUND 1 — THE DESIGN (9 findings, 1 BLOCKER, 9/9 accepted).** The BLOCKER killed my central inference. I wrote "P1 reproduces macOS↔macOS ⇒ **H1 DIES**, because H1 accuses the Windows accept branch." **H1 does not accuse Windows.** Verbatim in the parent it accuses **blit's own code paths** (`SourceSockets` Dial/Accept, `add_dialed_stream`, the dial-before-ACK at `transfer_session/mod.rs:3113`) — the word "Windows" appears **nowhere** in it. Windows merely *happens to be the accepting source* in P1's slow arm on rig W, and that code runs on macOS too, so a reproduction is **consistent with** H1, not fatal to it. **I took the framing from `docs/STATE.md` and never opened H1** — the *second* time this session I propagated a wrong claim about the hypotheses instead of reading them (the first: "H1/H5/H6/H7", when H5/H6/H7 are **P2**). H1 now carries a **canonical note** in the parent so the shorthand cannot mislead a third time. Also killed: "both directions must fail" (which would have **rewritten** P1, whose rig-W signature is one-directional — `wm` FAILS, `mw` PASSES — and let a real reproduction be waved away as "machine asymmetry"); "endpoint asymmetry cancels" (it does not — switching the initiator also reassigns which Mac runs the CLI vs the daemon, and `q` is faster); and a "VANISHES" branch with **no power gate** — pf-0's exact error, about to repeat. **ROUND 2 — THE INSTRUMENT (11 findings, 3 BLOCKER, 11/11 accepted).** (1) **The harness never computed its own registered rule** — it emitted per-cell PASS/FAIL only, so the six-outcome decision would have been applied **by hand, after seeing the numbers**, which is precisely what pre-registration exists to prevent. Now mechanized (`scripts/otp12pf_mac_verdict.py` → `session_verdict.txt`). (2) **My noise statistic would have declared a REAL effect absent.** I graded "vanished" against `S = max(d) − min(d)` — a **range**, which grows with n and is dominated by outliers. Codex's counterexample, which my code accepted: `srcinit=2000, d=[0,180,180,190,190,200,200,200]` → `D=190, S=200` → **"VANISHES"** — on **7/8 positive pairs**, an effect **83% the size of rig W's Δ_P1**. It would have reported "P1 requires the Windows peer" off an effect nearly as big as P1 itself. Replaced with a **bootstrap 95% CI on the median** (seeded → deterministic) + an **exact sign test**, and a null is now an **equivalence** result: VANISHES requires the CI to **exclude a bar-breaching effect**, not merely to look small; otherwise **INCONCLUSIVE-UNDERPOWERED**. Pinned by `otp12pf_mac_verdict_test.py` and **mutation-proven** (restore the range rule → the counterexample flips back to VANISHES and the test fails). (3) The inference **still** overreached ("platform-general cost" — two machines cannot license that); now scoped to **this pair**. **THE TWO INSTRUMENT BUGS THAT COULD HAVE MANUFACTURED THE RESULT**, both fixed: **(a) the durability check was FAIL-OPEN** — `os.walk()` of a missing/empty path returns **0 files in 0 ms and reads as a fast, successful flush**. The arms need *different* landed paths (blit's rsync slash semantics: a push to `/bench/RUN/` lands `RUN/src_<W>`; a pull into `RUN` lands files **directly in** `RUN` — verified empirically), so a wrong path would charge one arm **zero** durability — **the otp-2w bug that once manufactured P1**. The walk now returns its **file count and byte sum** and the pair VOIDs unless both match the fixture. I found this one myself, minutes before the review returned it. **(b) The free-writeback gap REVERSED SIGN WITH DIRECTION** — dirty pages flush "for free" between the client exiting and the fsync starting, and that gap is longer for whichever arm ran over ssh, which is `destinit` in `nq` and `srcinit` in `qn`. **P1's signature is one-directional, so this artifact could have produced the finding out of nothing.** Rather than argue, I **measured** it: a 10/20/200 ms pre-fsync delay produced **no measurable change** in fsync time (72–94 ms, no trend — APFS fsync here is per-file-metadata bound, not writeback bound), and fixed it structurally anyway with a fixed equal settle on both arms. Also: **every environmental gate failed OPEN** (a `pgrep` error read as "quiet", a `tmutil` read error read as **"disabled"**, `top` failures read as 0%) — the same class as pf-0's `ps` decaying average reporting a *finished* backup as 255%. All now fail **closed**. And the ARP gate accepted **any** MAC, so the documented **own-MAC black hole** would have passed; it now compares measured MACs both ways and asserts the route egresses the 10GbE NIC. **THE QUIESCENCE GATE PROVED ITSELF ON ITS FIRST INVOCATION** — it refused to start because codex was running on nagatha, which is now a bench **END**, not just the driver. Suite untouched (**1488**; zero crates/proto changes). **NEXT: round-2 review of the instrument, then the run — which needs BOTH Macs quiet (the owner's codex sessions must be closed).**

**2026-07-14 07:10:00Z** - **CODER (D-2026-07-14-1: re-record the baselines at jumbo — and my first draft of the owner's own decision was NOT EXECUTABLE, claude)**: Owner chose, from three options, **"Re-record the baseline at 9000"** — because pf-0 showed the committed anti-drift ceilings were recorded at **MTU 1500** while the fabric now runs 9000, and a reference must share the MTU of the sessions graded against it. I recorded it (`d71c0ed`), propagated it, and sent it through the loop. **codex: NOT READY — 3 HIGH, 2 MEDIUM, 1 LOW, 6/6 accepted (`c9933d8`).** The owner's *decision* was never in question; **my rendering of it could not be performed.** (1) **The guard I never wrote.** `OTP12_ACCEPTANCE_RUN.md` D2 exists, in its own words, so that *"the fixed pre-cutover bar must not be loosened by a slower old rerun"* (its design finding F2) — and a re-record re-rolls **hardware and day state as well as MTU** (rig W's Mac end is now `q`, not the nagatha that recorded 2026-07-10). I proposed re-recording with **no non-loosening guard at all**: old-file immutability would have survived while the *functional* freeze quietly died. Applying F2 rather than inventing a rule, the reference is now the **per-cell MINIMUM of {2026-07-10 median, re-recorded 9000 median}** — it can only tighten, and a cell whose re-record is SLOWER is **flagged, never silently adopted** (the old build getting slower on faster hardware would mean the rig drifted). (2) **I amended D5 and MISSED D2**, which still mandated the 2026-07-10 median — the acceptance contract contradicting itself with one `BASELINE_SUMMARY` to satisfy both. Both baseline READMEs are now re-labelled **SUPERSEDED AS THE ACCEPTANCE REFERENCE / retained as historical MTU-1500 records** (data untouched). (3) **Rig Z has NO clean "original old build"** — its otp-2 baseline ran a **dirty `731023b`** daemon (the clean-pair discipline forbids reusing it) while its client was a clean `e757dcc`, so my "same old build" rule was **impossible** there. Resolved on evidence: `git diff 731023b e757dcc -- crates proto` is **EMPTY** (identical committed daemon code) and **otp-12a already staged a clean `e757dcc` rebuild** — so rig Z re-records on a clean `e757dcc` pair. Precedent, not novelty. (4) **The "3–4% faster at jumbo" rationale was OVER-APPLIED** — it is ONE cell (`wm_tcp_large`), ONE rig, and **both arms are the NEW build**; pf-0 measured no small cells, no rig-Z cells, no OLD-build MTU response. **Exactly the same failure mode the pf-0 review caught hours earlier: a real result stretched past its domain.** The justification is now **methodological** (reference and graded sessions must share the fabric's MTU), which is firmer than the number ever was. (5) **STATE contradicted itself**: I set NEXT ACTION to pf-1 while STATE's own queue still requires the **Mac↔Mac** experiment before any pf code, and the newest handoff still pointed at the decision I had just made. **Corrected: NEXT is the Mac↔Mac rig** — the missing 2×2 cell, and it **discriminates H1 outright** (reproduces ⇒ P1 is macOS-side and H1, which accuses the *Windows* accept branch, DIES; vanishes ⇒ H1 strongly supported) — then pf-1. The re-record is a **pf-final** prerequisite, not a pf-1 blocker. **Verified rather than assumed** (before the review landed): zoey really was at 1500 when otp-2 was recorded — its pre-jumbo `systemd-networkd` backups (`*.premtu`, dated 2026-04-30) carry **no `MTUBytes` stanza**; the 9000 configs were written 2026-07-13. **And a gate I broke**: I committed `bb912f4` while `check-docs.sh` was FAILING (STATE 202 lines vs its 200 cap) — the loop's gate is never-skip, never-commit-on-failure. Fixed in `9957d44`, recorded rather than amended away; every commit since ran the gate BEFORE committing. Suite untouched (**1488**; zero crates/proto changes).

**2026-07-14 06:20:00Z** - **CODER (pf-0: MTU is KILLED as a cause of P1 — the A-B-B-A ran clean, and then codex proved my CLAIMS outran my DATA, claude)**: **THE RESULT**: the pre-registered A-B-B-A MTU experiment ran on rig `q` — four sessions (9000, 1500, 1500, 9000), RUNS=8, **256 timed runs, 0 voided**, MSS gate held at start AND end of every session (8948 jumbo / 1448 at 1500). `Δ_9000 = 236 ms`, `Δ_1500 = 229 ms`, measured noise floor `N_Δ = 78 ms`, **`r = −3.1%` → KILLED (r < 20%)**. Raising the MTU recovers **none** of P1's gap; P1 FAILS in all four sessions (1.237–1.362) regardless of MTU while every control passes in all four. **The null is not vacuous**: `wm_tcp_large` ran **3–4% faster at jumbo on BOTH arms**, so the manipulation demonstrably reached the wire — the benefit is **symmetric**, which is exactly why it cannot explain an **asymmetry**. Evidence: `docs/bench/otp12-jumbo-win-2026-07-13/` (`363fa6f`); plan amendment `63f400e`. **THEN CODEX (gpt-5.6-sol, ultra) RETURNED NOT READY — 7 findings, 7/7 ACCEPTED (`11f0c2a`)**, and the pattern is worth naming: it **independently recomputed every number and CONFIRMED them** (Δ 275/241/217/197, N_Δ=78, r=−3.0568%, N_arm=72). The arithmetic and the rule-application were faithful. **Every single finding was a CLAIM that outran the DATA.** (1) I wrote "**EXCLUDED**", "the environmental escape is closed", "P1 is a property of the code, not the wire" — from ONE environmental variable, with **segment fill unmeasured**. The registered outcome is "KILLED as a material cause"; the prereg's own round-2 F5 exists to prevent exactly that overreach and I ran it again. (2) **The sharpest one: the experiment is NOT POWERED for its own CONTRIBUTING boundary.** 20% of Δ_1500 = **46 ms**, which is **BELOW the 78 ms noise floor I myself measured**. It can exclude a DOMINANT effect (≥114 ms); it **cannot** exclude a contributing-size one. I reported a KILL without noticing my instrument could not have seen the thing it was killing. (3) I declared pf-final's committed rows **VOID** and enumerated "only two ways forward" — but that baseline is a deliberately **FROZEN anti-drift ceiling** (D2/D5) and rewriting the acceptance contract **is not an agent's call**. The substantive point survives and is sharper: jumbo made both arms 3–4% faster, so a jumbo NEW arm graded against a **1500-recorded** ceiling is **LENIENT, not conservative** — the MTU gain flatters the ratio and could let a real regression pass. (4) I claimed the same-session interleave "is the only design with enough resolution": 78 ms is **between**-session noise; it rules OUT cross-session grading but says **nothing** about the paired within-session variance, which pf-0 never measured. **pf-1 must now measure its own paired floor and register a resolution check before grading any hypothesis.** (5) **H5/H6/H7 are P2 hypotheses, not P1** — only H1 (+H2's residual) bears on P1. This error was **PRE-EXISTING and propagated**: it already stood in the q-baseline README and `docs/STATE.md`; I copied it without checking the plan's own hypothesis list. All three fixed. (6) "committed before any datum existed" is literally false (rev 4 post-dates the `q` baseline) — the *rule* was fixed in rev 3 before any S1–S4 datum, which is the claim that actually matters. (7) The masking guard silently collapsed replicate medians by **mean**; the prereg never specified it. Outcome invariant, but "exactly as pre-registered" overstated the spec. **MY OWN FINDING, which explains the noise floor the two BLOCKERs turn on**: recomputing medians from the RAW `runs.csv` instead of trusting `summary.csv` shows **the fast (`win_init`) arm is BIMODAL** (~730 ms and ~840 ms clusters). S1 drew 6 low/2 high; S4 drew 2 low/6 high **AT THE SAME MTU** — that **mode mixture, not MTU**, produced the 72 ms replicate spread that sets N_Δ. `mac_init` is stable to **5–6 ms**. **Trap for pf-1: a counterfactual that merely shifts the mixture would masquerade as a recovery** — grade the distribution, not the median. The MTU verdict is robust to it (pooling all 16 runs/condition: r = −4.7%, same grade). **RIG FAILURES, for the record**: Time Machine had autobackup ON on `q` and had fired 1 minute before the run (hourly cadence, one destination on `skippy` — the same 10 GbE fabric); the harness's quiet-gate only refuses on codex/cargo/rustc and **would have sailed straight through it**. Owner disabled it. Then three harness starts died at the old-pair smoke with a gRPC `transport error` — I blamed the MTU flip, then a daemon startup race, then the working directory, and **falsified all three myself** (the daemon binds in 169–665 ms; a hand-run smoke succeeded every time; 30/30 connects survived an MTU set). The real cause was a **physically flapping `en8`**, which the OWNER spotted and reseated; after that, 5×1 GiB at 891–897 ms and all four sessions ran clean. I should have said "intermittent" an hour earlier instead of hunting a deterministic culprit. A `bash -x` diagnostic session that DID pass was **discarded, not banked** — it differed from its own replicate, and the design requires the four sessions be identical. Suite untouched (**1488**; zero crates/proto changes — docs gate `check-docs.sh` OK). **NEXT: the owner's call on the MTU-mismatched frozen baseline (it blocks pf-final's assembly), then pf-1.**

**2026-07-14 00:15:00Z** - **CODER (P1 REPRODUCES ON A SECOND MAC; a new dedicated bench Mac; and I contaminated an hour-long experiment with my own review loop, claude)**: **THE RESULT**: P1 had only ever been measured on ONE Mac (nagatha), yet every live hypothesis (H1/H5/H6/H7) assumes it is a property of the macOS<->Windows PAIRING. Nobody had ever tested that. On the new Mac `q` (M4 mini): `wm_tcp_mixed` = **1.385 FAIL** at MTU 9000, while ALL THREE controls PASS at **1.002-1.043 in the same session** — so the rig's asymmetry noise is ~2-4% and P1 is an order of magnitude outside it. **P1 follows the platform pairing, not the machine**, and the signature is unchanged and sharp: TCP only (gRPC 1.020), `mixed` only (`large` 1.002), destination-initiator only (reverse direction 1.043). **And it FAILS AT JUMBO** — so MTU 9000 does not dissolve P1, which was Queue 1a's whole premise. What is still unmeasured is how much MTU *contributes* (a FAIL proves only that jumbo is INSUFFICIENT); that needs the matched 1500 arm. Evidence: `docs/bench/otp12-q-baseline-2026-07-13/`. **THE OWNER'S QUESTION THAT REFRAMES THE HUNT**: *"have we tested mac<->mac?"* — **No, and it is now possible** (nagatha .92 + q .54, both 10GbE/9000). The 2x2: Linux<->Linux = NO P1 (8/8 PASS); macOS<->Windows = P1 (1.237/1.300/1.385); **macOS<->macOS = UNTESTED**. It discriminates the hypotheses outright — reproduces => P1 needs no Windows peer, it is macOS-side and **H1 DIES** (H1 accuses the WINDOWS accept branch); vanishes => P1 REQUIRES the Windows peer and H1 is strongly supported. The plan is currently hunting a mechanism without knowing WHICH HALF of the pair is at fault. Needs a 3rd harness variant (rig-W's is Windows-specific, the Linux one Linux-specific); scheduled for nagatha idle time. **THE FAILURE OF THE DAY — I contaminated my own experiment.** The first A-B-B-A MTU run (4 sessions, 53 min) ran with codex jobs chewing the Mac's CPU — **and the Mac is ONE END of rig W**. I had written that exact warning to the owner hours earlier and then did it anyway. The contamination is **asymmetric** (mac_init runs the client ON the Mac; win_init runs it on Windows), so CPU starvation **inflates Delta and MANUFACTURES P1** — the very finding under test. Same shape as the durability retraction: a cost billed to one arm and not the other. **The noise model caught it**: `wm_tcp_large` read 911ms in S1 and 1847ms in S4 AT THE SAME MTU, and the noise floor came out at 473ms — larger than the 325ms gap under test. Under my own rev-2 design (one session per condition) it would have looked clean and been REPORTED. The replicates existed only because codex round 2 forced them. Run discarded; a quiescence GATE now refuses to start a session while codex/cargo/rustc runs, and records load1 per session. Then I made it worse: `pkill -f codex` killed the OWNER'S OWN codex sessions. Never blanket-kill; ask. **MTU PREREG rev 1->4, codex 15/15 accepted across two rounds.** Round 1 (7 findings) killed a FACTUAL ERROR at the premise: "`mixed` is the most packet-heavy fixture" — the stated reason for the whole experiment, repeated in STATE — is FALSE (at MSS 1448, `large` ~741k segments vs `mixed` ~378k; **`large` is packet-heaviest by ~2x**). Round 2 (8 findings, 5 blocking) found the root defect: **every threshold I wrote was INVENTED, because the design had no noise model** — RUNS=8 measures variance WITHIN a session while the entire MTU comparison is BETWEEN sessions, and MTU was perfectly aliased with session order. Codex's counterexample passed every guard I had written (a shared 985ms floor => ratio 1.000, r=100%, fast arm regressing only 4.9%, inside my invented 5% tolerance). Fixed by counterbalanced **A-B-B-A** with same-MTU replicates supplying a MEASURED noise floor. Also withdrew a falsifier that would have KILLED a true result. **NEW BENCH RIG `q`** (M4 mini, 10GbE .54, MTU 9000, 1.18 GB/s — faster than nagatha): dedicated and quiet, which **permanently decouples the codex loop from rig-W benchmarking**. Validating it took four attempts because I kept accepting instruments that reported success while measuring nothing: (1) a host route that created a BLACK HOLE (next hop = the NIC's own MAC; 100% loss while `route -n get` still said `interface: en8`); (2) my own throughput probe reporting "**6787 MB/s — 10GbE CONFIRMED**" from a transfer that had FAILED on publickey and moved ZERO bytes; (3) its replacement flagging a FALSE failure (78 MB/s) until a control showed nagatha's known-good 10GbE scores the same 79 — the test was bound by Windows' ssh, not the wire. **The fix that worked every time was a CONTROL — an identical measurement against a known-good reference — not more care.** Rig facts + every trap: `.agents/machines.md`. Suite untouched (**1488**; zero crates/proto changes). **NEXT: the A-B-B-A MTU run on `q` (~55 min, staged and validated), and the Mac<->Mac rig.**

**2026-07-13 21:30:00Z** - **CODER (blit silently drops Windows attributes + ADS — found while benchmarking against robocopy; and the local path does not scale, claude)**: Owner asked for a robocopy baseline and then, seeing the numbers, asked the question that broke my finding open: *"robocopy with /mt:N beats our tar streaming? or was that single-threaded robocopy?"* It was **8-thread robocopy against 1-worker blit** — I had passed `/MT:8` (matching `bench_baseline_tools.sh`) while blit's local apply ships **one** worker (`transfer_session/local.rs:602`; `--workers` is a hidden debug flag that sets `debug_mode`). My headline "blit is 2x slower" was an artifact of my own setup. **At EQUAL concurrency the result inverts**: blit BEATS robocopy at one thread (small 0.91, mixed 0.88) and LOSES at eight (1.92 / 1.61). The real defect is that **blit does not scale**: 8x the workers buys it 1.05x (small) / 1.19x (mixed) where robocopy gets ~2.2x from 8 threads (4-arm interleaved session, `docs/bench/win-local-ab-2026-07-13/`). blit's per-file path is FINE; its parallelism is not, and users get one worker by default. Plan drafted (`docs/plan/LOCAL_SMALL_FILE_PATH.md`, Draft) with L3 (no scaling) as prime suspect and L1 (tar framing) DEMOTED — framing cannot be the dominant cost of a tool that wins at one thread. **A rig instability found and NOT papered over**: blit's absolute time is bi-stable across sessions (1388ms vs 2225ms, identical binary and flags, flat within each session) while robocopy /MT:8 reads 697ms in every session. Cause: an 8-thread neighbour leaves the CPU boosted and blit's single-worker, syscall-bound run inherits it — so **absolute times on this rig are only meaningful WITHIN a session**. The 4-arm interleaved design (every arm sharing identical neighbours) is the control; ratios held across both regimes (0.879 -> 0.911), which is what interleaving is for. **THE FINDING THAT OUTRANKS THE PERF WORK** (D-2026-07-13-3, `docs/bugs/windows-attrs-and-ads-lost-on-tar-path.md`): codex's review of the harness asked whether blit was simply doing MORE work than robocopy — and the answer is that it does **LESS**. **blit silently discards Windows file attributes (ReadOnly/Hidden/System) and alternate data streams, exit code 0, on BOTH the local and the remote route** (both MEASURED — a loopback daemon on netwatch-01 proved the wire path loses them identically). And the loss is **conditional on FILE COUNT**: `transfer_plan.rs:103-109` tars when there are >=2 small files AND (>=32 of them OR average <=128 KiB); otherwise files go through `CopyFileExW`, which carries attributes and ADS for free. Proven with identical 200 KiB files where only the COUNT varied — **40 files: LOST. 3 files: PRESERVED.** So the same file keeps its metadata copied alone and loses it copied beside 39 siblings, which means essentially every real directory (source tree, photo folder, documents) takes the lossy path. mtime survives; Unix permissions survive (`#[cfg(unix)]`) — the gap is Windows-only. Empty directories are absent too, but that is a **documented non-goal** (`blit check`'s help, `cli.rs:20-35`), not this bug — and note `test_push_empty_directory` only asserts the command SUCCEEDS, never that the directory arrived: a crash smoke test wearing a fidelity test's name. Owner's framing and the correct one: **unlanded Windows support, not a regression** — blit began as a Linux alternative to robocopy and the metadata half never shipped. Queued behind otp-12, to be planned TOGETHER with the local-scaling plan (they pull in opposite directions: a fidelity fix ADDS per-file work to a path already losing to robocopy). **Fixing it is a WIRE CONTRACT change** — the tar shard IS the wire format — so `docs/TRANSFER_SESSION.md` gets amended through the loop BEFORE any code. **Also**: the MTU experiment was pre-registered BEFORE any data (`35b9620`) and codex returned NOT READY with 4 BLOCKER + 3 HIGH — **all 7 accepted** (`7921adc`). The one that stings: "`mixed` is the most packet-heavy fixture we test" — the stated rationale for the whole experiment, repeated in STATE — is **FALSE**. At MSS 1448 `large` is ~741k segments vs `mixed` ~378k; **`large` is packet-heaviest by ~2x**. Also killed: my masking guards admitted the exact artifact they existed to catch (a shared 1000ms floor passed all three), and my "r>=1.20 means MTU is not the cause" band contradicted the parent plan's own 20-50% CONTRIBUTING grade. Design now measures BOTH MTU conditions (9000 AND 1500, identical CELLS, RUNS=8, same NIC and sha) — a lone jumbo run attributes nothing. Instrument validated first this time: negotiated MSS on the rig-W path is **8948 both directions** (`getsockopt(TCP_MAXSEG)` + Linux `ss -ti`), vs 1448 at 1500 — a MEASURED 6.18x segment reduction. A candidate instrument was tested and DISCARDED: the Windows NIC counter reports 10680 bytes per received "packet", larger than a 9014-byte frame, so it coalesces and cannot discriminate 1500 from 9000. **And a false attribution corrected**: `GPT_REVIEW_LOOP.md` still named `gpt-5.5` as the reviewer, so I signed a verdict file with it — `~/.codex/config.toml` is ground truth and says `gpt-5.6-sol` at effort `ultra`. Both fixed; the loop doc now says READ the config rather than name a model that goes stale. Suite untouched (**1488**; zero `crates/`/`proto/` changes). Next: the MTU experiment (Queue 1a).

**2026-07-13 21:00:00Z** - **HOUSEKEEPING (STATE cap prune, claude)**: `docs/STATE.md` Queue item 6 ("Post-REV4 residue", unowned) pruned out of STATE to hold the 200-line cap; the list is unchanged and lives here so STATE can point rather than restate: epoch-0/early-ADD hardening; remote perf-history lanes (the 1e gap); receive-side dial tuning residue (w3-1 scoped it out); the source send half's bounded `dp.queue()` is not raced against control-lane events (codex otp-7b-1 F3; residual: the narrow CANCELLED->INTERNAL decay); the CLI progress monitor lives through the in-session mirror purge (display-only; fix = the M-C `AppProgressEvent` phase reshape — codex otp-10b-2 F5).

**2026-07-13 20:00:00Z** - **CODER (otp-12c closed; the same-OS rig built; the fleet moved to jumbo; and THREE of my claims retracted, claude)**: **Shipped**: otp-12c through the codex loop (rig D delegated parity **7/7 PASS** — delegation costs nothing; review 7/7 accepted incl. F2, where I had misread D2's escalation amendment as converge-only and thereby ducked a verdict). A real bug found en route: a peer `Frame::Error` arriving mid-record was reported as a ProtocolViolation about frame position instead of surfacing the peer's own fault — a CANCELLED must stay CANCELLED (`920c6a7`, plan D4; both file and tar-shard receivers now match the block-record handling). Plus the CLI `./NAME` foot-gun hint (`ace91de`) and the CI fmt fix (`bb28ddd` — I had skipped `cargo fmt --all -- --check`, the FIRST line of the three-command gate, and reported slices green on partial checks; full gate now clean, suite **1488**). Perf plan flipped **ACTIVE** (D-2026-07-13-1, owner: "just write the code and reviewloop slice by slice — that converges faster than plans with no ground truth to test"), after a 5th codex round whose 3 blockers were all real and all fixed. **Built a new rig**: magneto↔skippy, Linux BOTH ends — the only pair in the fleet that can measure blit's layout with zero platform terms (owner promoted magneto from "never a bench end" after confirming it saturates 10GbE where zoey cannot). Result under full methodology (cold caches both ends, drains, ABBA, pair-void, 64 runs, zero voided): **8/8 invariance cells PASS**, P1's own cell at **1.092/1.003**. So **P1 does not reproduce on a same-OS pair** — it is platform-INTERACTING, not a pure layout property. It is NOT exonerated (a code path that only bites on one platform looks identical), and per codex r5 F1 it has **no escape hatch**: D-2026-07-12-1 waives only a cross-direction miss for a cell that ALREADY passes invariance, and P1 *is* the invariance failure. Fix it, or the owner amends criterion 1. **THE LESSON OF THE DAY — three retractions, one root cause.** (1) I reported "P1 reproduces at 1.78 on Linux↔Linux, therefore it is CODE" — WRONG: my harness ran the durability `sync` inside the *initiating* host's bracket, so the push arm (initiator = source, which only read) never paid the destination's writeback while the pull arm (initiator = destination) paid all of it. One arm was billed for durability the other got free — multi-second on skippy's ZFS — manufacturing "failures" on every carrier and fixture *including the gRPC control that is supposed to be clean*. **The carrier-independence is what exposed it**: a real code effect is carrier-specific, an accounting artifact is not. Fixed `2c0af86` (durability keyed by DESTINATION, never verb — the otp-2w rule this repo already knew and I broke). (2) I told the owner twice that P1 was "probably acceptable platform residue" — codex proved no decision on the books permits it. (3) I diagnosed "macOS cannot send jumbo frames, the switch is broken," had the owner swap a network adapter, and was **wrong again**: `net.inet.raw.maxdgram` caps *raw sockets* at 8192 and `ping` uses one, so DF pings above ~8164 fail from a Mac regardless of the real path MTU — while TCP, which does not use raw sockets, was fine the whole time (verified: 231/225/157 MB/s at 9000). My "TCP is blackholed" test was itself broken (`more > NUL` is a pager; I never baselined it). **Every one of the three was the same failure: I trusted a measuring instrument without first proving it measures.** Each was caught from outside me — a suspicious gRPC control, codex, and an owner who refused to accept the answer. **The fleet is now uniformly MTU 9000** (zoey converted — UniFi UNAS, systemd-networkd, `[Link] MTUBytes=9000`, proven by a live `networkctl reconfigure` with the static IP intact; overlayfs means a firmware update can silently revert it). **Which unlocks the next experiment and it is a big one**: Windows sat at **1500 for EVERY benchmark ever recorded**, so jumbo has never once been exercised in a blit measurement. P1's failing cell is TCP × *mixed* — the most packet-heavy fixture we test — precisely where ~6× fewer packets could move the number. **Next: re-run rig-W invariance at jumbo BEFORE touching any code** (Queue 1a); it may dissolve P1 outright. Confound to control: the Mac's NIC also changed (Aquantia @ .54, was the TB5 dock @ .91 — so 1.237→1.300 is NOT evidence of a code regression), so if the asymmetry vanishes, re-run at 1500 on the same adapter to separate MTU from hardware. Also recorded: `.agents/repo-guidance.md` had the remotes **inverted** (origin = gitea, NOT GitHub) — every agent push this session went to the mirror while GitHub sat dozens of commits stale until the owner pushed by hand.


**2026-07-13 04:45:00Z** - **CODER (otp-12c CLOSED through the codex loop — 7/7 accepted, incl. the one finding that was against me, claude)**: Owner: "1. reviewloop codex / 2. push after 1." Note on invocation: `/playbook reviewloop codex` names `.agents/playbooks/reviewloop codex.md`, which does not exist; `.agents/playbooks/reviewloop.md` does, but it is a PORTABLE TEMPLATE assuming `.agents/state.md` + `.agents/review/index.md` + **per-finding branches** — all of which collide with this repo's actual layout and its "no agent-created branches" earned practice. Its own text and `.agents/repo-guidance.md` both defer to the repo: the canonical procedure for review-loop work here is `docs/agent/GPT_REVIEW_LOOP.md`, which is what I ran (flagged rather than silently substituted). **Codex (gpt-5.6-sol, 202k, range `dcbd6ea..9350b24`): FAIL → 7 findings, 7/7 ACCEPTED, 0 rejected.** It independently CONFIRMED the parts that mattered most: the otp-12b F5 arm asymmetry does **not** recur (both arms pass contents-form sources, land the identical tree, pay the same in-window destination mkdir — the exact confound I'd flagged for it to attack), every committed CSV recomputes exactly, both READMEs otherwise reserve acceptance to otp-13, and the range touches no `crates/`/`proto/`/Cargo files (so 1484 stands by construction, verified not re-run). **F2 (HIGH) is the finding that matters and it was against me**: my evidence README, findings doc and STATE all said the RUNS=4 FAILs and the RUNS=8 PASSes "neither supersedes the other — owner call", on the theory that D2's escalation-supersession amendment covered only the converge-up rows. **Wrong.** The escalation rule and its amendment sit in D2 *after all four bar definitions* and speak of "**a comparison**" generically — delegated parity included — and both FAIL cells met the trigger exactly as pre-registered (straddle 1.119/1.129 vs 1.10, arm spread 86.0%/55.4% > 25%). So the RUNS=8 re-run I ran *was* the pre-registered mechanism firing, and its medians **govern**: **rig D is 7/7 PASS** (5 at RUNS=4 + 2 escalated). Re-reading a pre-registered rule *after seeing the numbers*, in a direction that conveniently let the slice duck a verdict, is precisely what pre-registration exists to prevent — corrected in all three places. **F1 (HIGH) cold-cache fails open**: the sudo grant only WARNED and every per-run purge ended `|| true` — a silently-failed purge produced a WARM run that still counted valid (the same class as the `a2dea3f` no-op). Now a hard preflight gate (`COLD_REQUIRED=0` opts out) + per-run cold outcome recorded and a failure VOIDS the pair. **Checked rather than assumed whether this corrupts the recorded session: it does not** — the grant was probed live (`sudo -n -l` shows the NOPASSWD tee entry; a direct drop returned DROP_OK), the session ran at `a2dea3f` which uses exactly that grant, and the recorded drain outcomes vary run-to-run (6s/8s/4x2s/9x2s), which a no-op purge could not produce. Latent fail-open, not corrupted data. **F3** provenance: `proto/` was missing from the dirty-tree gate (blit-core compiles `proto/blit.proto` INTO the binaries) and `grep "+<sha>"` substring-matches `+<sha>.dirty.<hash>` — the exact shape that fooled otp-12a on zoey; both closed (`embeds_clean_{local,skippy,win}`). **F4** the manifest and every `runs.csv` row recorded harness HEAD (`a2dea3f`) as the build identity while the gated+hashed binaries embed `f35702a` — machine-readable fields now carry the gated identity, HEAD recorded separately. **F5** a failed skippy `sync` was masked by the succeeding `echo` (plausible flush on unflushed bytes → valid run) and a disk regex matching NO device reported `drained` — both fail closed now (NA→void; DRAIN-NODEV). **F6** both teardowns suppressed every failure, cleared their flags and logged "stopped" — the harness could exit 0 with a daemon still holding 9031; now verified, loud, and non-zero on a leak. **F7** the README listed `sw_tcp_small` (93.6% spread) among the FAILs though it PASSED at 1.034. Fixes `0fb4a64` (+ cap trim `4cc9b6e`); verdict `.review/results/otp-12c.gpt-verdict.md`; REVIEW row `[x]`. Gate: `bash -n` OK, `check-docs` OK, suite untouched at **1484**. The harness fixes bind the NEXT session — the recorded evidence stands on the harness as it ran. Next: **otp-12d assembly** → otp-13 owner walk.

**2026-07-13 03:30:00Z** - **CODER (otp-12c RECORDED: the delegated rig-D session + a direct-path re-baseline at the cutover sha; NOT yet through the codex loop, claude)**: Owner "proceed with all of the testing without waiting on my go". **Rig prep** (netwatch-01↔skippy): skippy's July binaries were REV4-era (D-2026-07-05-2 refuses them at session open) → fresh `x86_64-unknown-linux-musl` zigbuild staged to the pool (`/mnt/generic-pool/video/blit-bin/`, old ones kept as `*.rev4-jul04`); the three fixture trees pulled onto skippy's `cmp/` module over the smoked TCP path (shape-verified: 1/1073741824, 10000/40960000, 5001/547110912 — byte-identical, large-file sha256 matches netwatch's); `[delegation] allow_delegated_pull` + IP allowlists written both ends (the gate is IP/CIDR only — hostnames fail the SSRF rule). **Rig foot-gun recorded**: Win32-OpenSSH REAPS `Start-Process` children when the spawning ssh session closes — the netwatch daemon died silently between turns and 9031 stealth-dropped, mimicking a firewall block (rules were never the problem; the smoke *is* the firewall test because the data plane binds ephemeral ports). WMI `Invoke-CimMethod Win32_Process Create` is the only durable launch. **Direct-path re-baseline** (`d12534d`, `docs/bench/otp12c-win-2026-07-13/`): the whole rig-W matrix re-run with the new arm at the CUTOVER sha `f35702a` (12b measured `e21cf84`), old arm `0f922de` — 198 timed runs, 24/24 cells, 3 DRAIN-TIMEOUT pairs voided+re-run, 0 CR residue (the `856af64` strip-at-source held); **93 PASS / 12 FAIL / 3 FAIL-SAME-SESSION / 12 RECORDED**; `wm_tcp_mixed` invariance **1.300** (12b: 1.237) — the TCP×mixed×dest-initiator cell did NOT wash out at the cutover sha; losses stay in TCP×{small,mixed}×push + `pull_tcp_mixed` while the new arm WINS the small-pull side (1301 vs 1480, 1479 vs 1663, 2264 vs 2656). **The delegated session** (`docs/bench/otp12c-delegated-2026-07-13/`, harness `scripts/bench_otp12_delegated.sh` @ `a2dea3f`, binaries `EXPECT_SHA=f35702a`, 5-hash manifest identical across both sessions): plan D4 parity = delegated (Mac CLI triggers DelegatedPull; no payload through the Mac) vs direct (the destination host's own CLI pulls), same bytes/data-plane/destination-disk/flush; 7 cells (`sw_*`/`ws_*` × {large,small,mixed} TCP + `sw_grpc_large` secondary). **Primary RUNS=4: 56 runs, 7/7 cells, 0 voided → 5 PASS / 2 FAIL** (`sw_tcp_mixed` 1.119, `ws_tcp_large` 1.129). **Confirmation RUNS=8 on exactly those two: 32 runs, 0 voided → both PASS** (1.035, 1.068) — and at n=8 the big spread appears on the DIRECT arm too (31.5%/64.0% vs the primary's delegated 86.0%/55.4%), so the 4-pair FAILs read as low-n noise rather than delegation cost; `ws_tcp_large`'s primary delegated BEST (3000 ms) already beat its direct best (3870 ms). Both records committed; **neither supersedes the other — the 4-vs-8 reading is the owner's call at otp-13** (the D2 escalation amendment says RUNS=8 governs, but this harness's rows are not the same shape, so the README self-adjudicates nothing). **Three harness bugs found live, each caught by the script's own gates**: `:?` default messages containing apostrophes silently swallowed the next 20 assignments (the exact otp-12b `772cfe6` bug, re-made) → `b49413d`; macOS `$TMPDIR` blows ssh's 104-byte ControlPath socket limit → `/tmp` mux dir, same commit; skippy's NOPASSWD grant is exactly `tee /proc/sys/vm/drop_caches`, so `sudo -n sh -c '…'` silently no-op'd the cache drop (runs would have read WARM) → probe+invoke via the exact grant, `a2dea3f`. **Owed: the codex loop** (D-2026-07-04-1) on the harness + both evidence commits — this slice is recorded, not closed. Next: **otp-12d assembly**, then the otp-13 owner walk. Suite untouched at **1484** (zero crates/proto changes).

**2026-07-12 19:20:00Z** - **CODER (otp-12b CLOSED both halves: the Mac↔Windows acceptance session — THE INVARIANCE CRITERION MEASURED, claude)**: Rig facts: the box is `michael@netwatch-01` at **10.1.10.177** (recorded .173 stale — DHCP; machines.md + harness defaults corrected). Staging: old `0f922de` exes aside-copied BEFORE the checkout move; bundle → detached `e21cf84`; native build 15–21 s; sha-named bins; 7-hash manifest; `OLD_CLIENT_PROVENANCE_BY_BUILD=1`. **Two found-live harness bugs, both caught by the session's own guards**: (1) pwsh parses bare `$rc:R` as a SCOPE-qualified variable → the win-initiated sentinel never printed → the win→mac smoke failed fast rc=99 exactly as designed; manual repro proved the path itself fine (no macOS-firewall issue); `${rc}` fix `e21cf84`. (2) pwsh CRLF in the drain outcome split every runs.csv row under python universal newlines → 192 valid runs verdicted INCOMPLETE; CR strip at source `856af64`, recorded session CR-sanitized post-hoc with the raw file committed (no timing value altered). **The session** (`44c2046` + review fixes `49dee5c`, `docs/bench/otp12-win-2026-07-12/`, 192 runs, zero voided, zero drain anomalies): **Block 2 invariance — the owner's sentence measured: 11/12 PASS at 1.003–1.057**; the exception `wm_tcp_mixed` **1.237** (mac_init 1127 vs win_init 911, tight spreads): Win→Mac mixed over the TCP data plane is ~25% slower when the MAC initiates (pull-verb/destination role) — corroborated independently by block-1 (`pull_tcp_mixed` 1.313 vs today's old arm), absent on grpc (1.013) and other fixtures — a CODE-SHAPED finding for the walk (TCP×mixed×destination-initiator signature). Block 1 converge: 10/12; `push_tcp_small` **1.149 FAIL-BOTH** at 3–4% spreads — the zoey 1.105 gap CONFIRMED ON A SECOND RIG (real small-push session cost); `pull_tcp_mixed` FAIL-SAME-SESSION (same root as the invariance fail). Cross: **Win→Mac 6/6 beat even the better committed old direction (0.760–0.990)**; Mac→Win 6/6 miss it with gap shapes RECORDED per D-2026-07-12-1 (large unchanged 1.98→1.95 = the pre-ruled platform shape; mixed 1.946→1.408 + grpc_small 1.929→1.644 NARROWED = code improvement with residue for the owner; tcp_small WIDENED 1.332→1.527 = the small-push code component). Rig drift note: today's old arms run far faster than the 07-10 committed medians (old push_tcp_large 1908 vs 3054) — committed bars easy, same-session bars binding. No pre-registered escalation trigger fires (tight spreads, no straddles) — results stand. **Codex run-round (gpt-5.6-sol, 131k): FAIL → 3/3 accepted+fixed (`49dee5c`)** — F1 (High) the README self-adjudicated the D-2026-07-12-1 attribution (narrowed-gap cells are the owner's call; prose now records shapes only); F2 cross-row range 0.760–0.990; F3 comment count. Earlier same slice: harness round 12/12 accepted (`d3eae58`). Verdicts `.review/results/otp-12b{,-run}.gpt-verdict.md`. REVIEW row `[x]`; STATE current → **otp-12c (delegated, netwatch-01↔skippy — needs fresh skippy staging: x86_64-musl zigbuild to the pool, noexec /tmp)**, then 12d assembly, otp-13 owner walk. Suite untouched at **1484**.

**2026-07-12 18:15:00Z** - **CODER (otp-12b harness half through the codex loop; magneto/VM recorded build-only, claude)**: Owner host facts recorded (`d5fd17f`+`1fd50d7`: magneto = Arch x86_64 10GbE but BUSY BitTorrent box — build-only fallback, never a bench end; Ubuntu ARM VM likewise; zigbuild covers every Linux target anyway → **otp-12c stays Windows↔skippy as designed**). **otp-12b harness** `d30b1e3` (+ quote-parity fix `772cfe6` — an apostrophe inside the `MAC_HOST :?` message broke bash parsing 323 lines downstream; the gate chain misreported under shell nesting, so `bash -n` is now its own verified step): `scripts/bench_otp12_win.sh` — block 1 = the otp-2w matrix as interleaved old(`0f922de`)/new matched pairs (both-reference verdicts); block 2 = **the plan's headline initiator/verb-invariance cells** (`mw_*`/`wm_*`, mac_init vs win_init ABBA, new pair only) + per-arm converge rows (F3) + F4 cross rows + D-2026-07-12-1 discriminator gap rows (`RECORDED`, never self-adjudicated); Windows plumbing verbatim from the frozen otp-2w script (WMI launch, stale-refusal, TOML literal paths, Get-Counter drain, standby purge); arm swap via a fixed active exe path (ONE program-scoped firewall rule); Mac daemon serves `$MAC_WORK` itself (design F6); win-initiated windows self-timed ON Windows (Stopwatch-in-one-ssh printing sentinel-framed `ms,exit`); win→mac smoke gates the macOS-firewall unknown. **Codex (gpt-5.6-sol, 109k): FAIL → 12/12 accepted+fixed (`d3eae58`), zero rejected**: F1 manifest-hash fail-closed regression (the 12a lesson re-applied); F2 arm swap now hash-verifies the active exe before launch (old arm has no handshake); F3 the arm was missing from destination paths — cross-arm no-op collision possible (the sharpest catch); F4 derived verdicts gated on complete(); F5 invariance arms made byte-identical (precreated containers outside the window, uniform nesting); F6 MAC_MODULE_ROOT hardcoded; F7 sentinel-framed fail-closed Windows timing (flush NA voids; parse failure = rc 99); F8 mandatory references + registered `NO-SAME-SESSION-REF`; F9 WMI cmd-pid recorded immediately + daemon resolved by ParentProcessId (no untracked window); F10 CELLS header false-match; F11 firewall rule verified not name-trusted; F12 vocabulary closed + gap-row labels exact. Verdict `.review/results/otp-12b.gpt-verdict.md`. Suite untouched at **1484**. Next: **the otp-12b RIG SESSION** — fresh bundle → Windows box, copy the `0f922de` detached-build exes aside FIRST, checkout the run commit + native build (daemon + blit.exe), stage sha-named bins, MAC_HOST export, PREFLIGHT_ONLY, then both blocks (~2-3.5 h).

**2026-07-12 16:30:00Z** - **CODER (otp-12a CLOSED both halves: the zoey rig run recorded through the codex loop, claude)**: Owner rig go ("any of the previously used hosts") → the full zoey session, three acts. **(1) Provenance find**: the new preflight caught that zoey's staged "e757dcc" daemon actually embeds `731023bfc8a1.dirty.…` — the otp-2 README's "binaries both ends" claim was wrong on the daemon end (committed daemon code identical between the commits; dirt unknowable) → correction committed (`b2b6901`), both arms rebuilt CLEAN (old `e757dcc` worktree client + fresh musl daemon staged as `blit-daemon-e757dcc`; original artifact untouched); also live-fixed: BSD grep needs `-a` for binary matches (`b3729da`). **(2) The storm**: first matrix attempt drove zoey to load 444 with 10× run times BOTH arms — aborted; accumulated per-run push destinations implicated (probes with per-run deletion held at baseline 2466/2525/3714 vs committed 2702) → harness sweeps each dest after its measured flush (`042c06f`); CELLS escalation filter (`6bc9cb6`). **(3) The evidence** (`b0ebf73`, `docs/bench/otp12-zoey-2026-07-12/`, 3 sessions incl. the aborted storm): main RUNS=4 matrix 48 pairs none voided → 9 PASS + 3 escalated per D2; RUNS=8 escalation settles them — push_tcp_large **PASS** (0.959/0.912 — the RUNS=4 FAIL-BOTH was noise), pull_tcp_large **FAIL-REFERENCE-DRIFT** persisting (new-vs-old same-session **0.995**; the old arm itself runs 1.248× its own committed baseline — rig-side by strongest evidence, confound named), push_tcp_small **FAIL-SAME-SESSION 1.105** (stable across sessions, tight spreads — the one real marginal gap; still 0.935 vs the committed baseline) — both non-PASS cells carried to the otp-13 walk; the README declares nothing. **Codex run-round (gpt-5.6-sol, 144k): FAIL → 6/6 accepted+fixed (`fa18787`)** — numerics explicitly confirmed ("recomputed table numerically exact"); F1 provenance grep false-positived on a cargo build-dir path (now `+<sha>` form + explicit by-build acknowledgment for pre-cutover clients that embed no id); F2 the D2 escalation supersession rule was never pre-registered (now a dated amendment: RUNS=8 governs, RUNS=4 rows stay visible) + a wrong best-run claim corrected; F3 "provably rig-side" reframed correlation+parity with the dirty-reference confound named; F4 marginal-gap wording restated per CSVs; F5 mistyped CELLS dies; F6 stale header. Verdicts `.review/results/otp-12a-run.gpt-verdict.md` (+ the harness round earlier). REVIEW.md: otp-12a row `[x]`, stale otp-2 row closed (owner go). STATE: otp-12a recorded; **next otp-12b (Mac↔Windows: converge-up + cross-direction + initiator/verb invariance)**, then 12c delegated, 12d assembly, otp-13 owner walk. Suite untouched at **1484** (zero crates/proto changes across all of otp-12).

**2026-07-12 05:30:00Z** - **CODER (otp-12a harness half: zoey converge-up A/B through the codex loop; design flipped Active, claude)**: Owner "yes to both" → `docs/plan/OTP12_ACCEPTANCE_RUN.md` **Draft → Active** (`260fb26`; approval recorded in the Status line — the doc's only open question was already ruled D-2026-07-12-1) and the stale REVIEW.md otp-2 row closed (`ae498d3` — it had sat `[ ]` "PENDING RIG ACCESS" since before the 2026-07-10 closes). **otp-12a harness** `8f4fbf9`: `scripts/bench_otp12_zoey.sh` — the otp-2 matrix as matched-pair interleaved old(`e757dcc`)/new A/B, ABBA counterbalance, pair-void valid-run rule (2×RUNS cap, INCOMPLETE surfaced), exit codes captured, stale-daemon refusal, sha256 staging manifest, PREFLIGHT_ONLY mode, python3 summary+verdicts against BOTH references (same-session old AND the committed otp-2 medians; D2 vocabulary); runs.csv schema +`valid` (doc amended). Frozen `bench_otp2_baseline.sh` untouched (D5). Gate: `bash -n` (shellcheck not installed — recorded); no crates/proto changes anywhere in otp-12, suite stands at the recorded **1484** (`git diff ce36da3..HEAD -- crates proto` empty; fresh fmt+clippy run green). **Codex (gpt-5.6-sol, 107k): REQUEST CHANGES → 9 findings, 9/9 accepted+fixed (`50dc135`), zero false positives**: F1 zero-valid cells vanished from verdicts + short medians (verdicts now iterate every attempted comparison; summary only for complete cells); F2 fixtures/pull-sources were existence-trusted (now count+byte-sum verified, converging re-stage); F3 provenance unenforced — stale-but-matching pairs pass the handshake (embedded-sha grep on all four binaries; dirty tree fatal; hash capture fails closed; reference hashed); F4 EXIT trap could kill an unowned recycled PID (session-gated + comm-verified); F5 reference silently overrideable + unregistered vocabulary (hardcoded; fail-closed; vocabulary registered in D5); F6 timed window wrote per-run logs (stdout→/dev/null, stderr-only); F7 reused pull dest (never-seen per-run paths, swept); F8 RUNS unrestricted (4|8 only); F9 doc cell grammar order. Verdict `.review/results/otp-12a.gpt-verdict.md`. Next: **the otp-12a RIG RUN** (blocked on: owner's fresh go for zoey daemon runs + zoey out of maintenance + staging: old client worktree rebuild, new musl daemon zigbuild) — then otp-12b (Windows harness).

**2026-07-12 03:57:00Z** - **CODER (otp-12 design: acceptance-run matrix + harness plan through the codex loop, claude)**: `docs/plan/OTP12_ACCEPTANCE_RUN.md` (Draft) at `045da4a`; owner ruled Q1 in-session → **D-2026-07-12-1** (`bfb9670`: a rig-W cell that beats its own old direction and is initiator-invariant but misses `min(old_push, old_pull)×1.10` only by a discriminator-attributed destination write-path residue COUNTS as satisfying criterion 2's cross-direction half; both parent criteria annotated in place). Design: rig Z (Mac↔zoey) = per-direction converge-up via interleaved matched-pair old(`e757dcc`)/new A/B; rig W (Mac↔Windows, owner-designated closest-spec) = converge-up + cross-direction + the initiator/verb invariance matrix (first-of-kind reverse-initiator arms: Mac daemon, Windows client, self-timed remote windows, flush keyed by destination OS never verb); rig D (Windows↔skippy) = delegated-vs-direct parity, new-build only (July skippy binaries are pre-`Transfer`, unusable — fresh x86_64-musl staging on the pool); sub-slices 12a–12d; verdict arithmetic pre-registered; otp-12 declares nothing (otp-13 owner walk). **Codex (gpt-5.6-sol, 125k): CHANGES REQUIRED → 7 findings, 6 accepted+fixed (`92e1d51`) + F1 overtaken by the owner decision** (the exact concern was surfaced in chat before the review returned; residual criterion-1 instantiation note applied): F2 converge-up needs BOTH references (same-session old AND the committed 2026-07-10 medians; `FAIL-REFERENCE-DRIFT` + one pre-registered re-run); F3 every unified arm meets the bars independently (tolerance compounding to 1.21× killed); F4 acceptance-checkbox flips moved out of 12d to otp-13; F5 ABBA counterbalanced interleave (arm never confounds with within-pair order); F6 Mac module root = `$MAC_WORK` itself (both initiators read the same physical inodes); F7 nonzero-exit/undrained runs void their PAIR and re-run to RUNS valid pairs (capped, `INCOMPLETE` surfaced). Verdict `.review/results/otp-12-design.gpt-verdict.md`. Next: **otp-12a** (zoey harness + run — needs the standing fresh owner go for zoey daemon runs + zoey out of maintenance). Noted in-session: REVIEW.md's otp-2 row still reads `[ ]` PENDING RIG ACCESS though otp-2 closed 2026-07-10 (stale, unfixed — offered to the owner).

**2026-07-12 08:00:00Z** - **CODER (otp-11b: THE LOCAL ORCHESTRATION DELETION — otp-11 CLOSED, every transfer on the one session, claude)**: slice `805e48c` + docs `b1650c4` + review fixes `9e810ee` (−6,197/+1,532). The last old path is out of the tree: `orchestrator/` (16 tests), `engine/` (strategy/streaming_plan/tuning/history/journal/single_file/mirror/options/summary; **`dial.rs` RELOCATED VERBATIM → `src/dial.rs`** — codex verified blob-identical, 17 tests carry), `local_worker.rs`, `auto_tune/` (absorbs the STATE residue "derive_local_plan_tuning fold-or-retire": retired), `change_journal/` (the UNSOUND journal skip — the addendum's data-loss repro; its `objc2-core-services` dep died with it), `copy/parallel.rs`+`stats.rs`, `lib.rs::CopyConfig`; the **otp-10c-2 F2 deferred `compare_manifests` sweep** (aggregate + `ManifestDiff`/`FileComparison`/`include_deletions` die; `header_transfer_status` survives as the one compare owner — 13 of its 16 tests converted to direct pins); stranded `plan_local_mirror`/`LocalDiffInputs`/`filter_unchanged` (14 tests → 12 converted: 9 direct `file_needs_copy_with_mode` pins — the sink defense layer — + 3 `plan_transfer_payloads` pins); `LocalMirrorOptions`/`Summary`/`TransferOutcome` re-homed → `transfer_session/local.rs` (unreachable engine-era axes dropped; `JournalSkip` + `PredictorEstimate` retired; CLI arms deleted); frontends re-import via `blit_core::transfer_session`; `docs/TRANSFER_SESSION.md` §Transport gains the precise LOCAL-carrier contract note (the design-round F1 commitment). **Test floor: suite 1513 → 1484 — the otp-13 ≥1483 criterion MET AT THE DELETION SLICE, margin +1, by real pins** (died-in-modules 41 + deleted integration files 10 + retired-with-surface 5; 25 conversions in place; +27 new: 15 session-route e2es incl. empty-source mirror full-delete + nested split counts + checksum/ignore-times/ignore-existing/size-only cells + resume fresh-dest + unreadable-subdir + planner-mix/scanned-bytes accounting, 7 unit pins incl. the `build_local_record` contract trio (R44-F1 split carried forward) + the `DestSubtreeExcludedSource` stream pin + the **streaming-overlap port** (`first_apply_lands_before_enumeration_completes`, the gated-source port of the engine's overlap pin — the 11a-F2 commitment), 2 `mirror_delete_pass` unit pins (plan-only; split), 2 manifest arm pins, 1 sink file-root pin). Guard proof: SizeOnly arm mutated → converted pin FAILS → restored. **Codex (gpt-5.6-sol, 203k): CHANGES REQUESTED → the core CONFIRMED** ("deletion, re-homes, converted coverage, remote-session behavior, one-transfer-path structure, and the 1484-pass suite check out") **+ 6 findings, 6/6 accepted+fixed** (`9e810ee`): B1 STATE staleness at the pinned commit (records-commit cadence, now landed); B2 live-doc sweep completed — WHITEPAPER local→local row now maps to `run_local_session`, §5 tuning marked HISTORICAL, bug-surface auto_tune/change_journal items struck dated, ARCHITECTURE diagram cells replaced, `.agents/repo-guidance.md` Style exemplar + Project Map updated, `diff_planner.rs` module doc rewritten; B3 predictor promises retyped (TUI F4 "populate the history", `blit profile` states the training retirement, `--null` help describes the history lane); B4 the throughput line prints the EFFECTIVE apply worker count (1 unless the debug limiter); B5 the accounting equation corrected (1513−41−10−5+27=1484; conversions don't appear in the delta); B6 dial = 17 tests. Verdict `.review/results/otp-11b.gpt-verdict.md`. **otp-11 (a + addendum + b) is CLOSED: local transfers ride the session, the separate local orchestration does not exist, the perf gate passed against sound baselines, and the deletion-proof acceptance line completes.** Next: **otp-12** (symmetric-rig acceptance run — rig availability in Blocked) then **otp-13** (owner checklist walk).

**2026-07-12 05:00:00Z** - **CODER (otp-11a addendum review closed — codex CONFIRMED the unsoundness verdict, claude)**: `d74c1ac` reviewed (after one quota-blocked dispatch), fixes `5f1188c`. **Codex (gpt-5.6-sol): CHANGES REQUESTED → the core claims all CONFIRMED independently** — "the data-loss claim is confirmed, no later validation catches it, Windows's changed-USN mtime fallback is also unsound, and the regression pin would fail under reintroduction" — with 4 record-quality findings, 4/4 accepted+fixed: **A1 (Med)** the 610 ms sound baseline was a single observation — re-certified by 5-run interleaved medians with the old binary's `journal_cache.json` removed before each run (probe → Unknown → its sound full pass): old **507 ms** vs session **226 ms** = **2.2× — gate PASS on real medians**; **A2 (Med)** STATE's summary line still said 11b-blocked/1512 — fixed; **A3 (Low)** floor arithmetic redone from 1513 (−71 retirements = 1442 → ≈+41 pins to the ≥1483 floor); **A4 (Low)** Linux mechanism precision — no global event counter there: `compare_linux`'s FIRST arm is the root dir's CTIME, which a deep write equally never touches (macOS: event-id arm always differs → root-mtime fallback decides; Windows: strict-USN arm needs a write-quiet volume, decays to the same fallback) — README + pin comment retyped, conclusion unchanged. Verdict `.review/results/otp-11a-addendum.gpt-verdict.md`. **otp-11a is now fully closed including the addendum round. Next: otp-11b** — dial re-home, option/summary type re-home, the deletion (orchestrator/, engine/ remainder, local_worker, auto_tune, change_journal, copy/parallel+stats, CopyConfig), the compare_manifests sweep, stranded dead code, deletion proof + retirement accounting (≈+41 pins), Windows parity.

**2026-07-12 03:30:00Z** - **CODER (otp-11a addendum: the change-journal question RESOLVED — the old fast path was unsound, claude)**: Owner rejected both framings of the no-op regression ("one violates FAST, the other violates SIMPLE — figure out a real fix"). The investigation found the premise false: the old engine's journal skip trusted `ChangeState::NoChanges`, whose macOS/Linux verdict (`change_journal/snapshot.rs compare_macos/compare_linux`) decays to ROOT-directory mtime equality whenever the global FSEvents event id / root ctime moved — which is every run on a live system — and a write to `src/sub/deep.txt` never touches the root dir's mtime. **Reproduced against the pre-otp-11 binary** (`d2bd843` release build): warm the journal with 3 mirror runs, modify a deep file, mirror again → "Filesystem journal fast-path: source/destination unchanged... Up to date" with the destination still holding the OLD content — silent data loss, transcript recorded in `docs/bench/otp11-local-2026-07-11/README.md`. Verdict: the 21 ms steady-state was fraudulent FAST (violates RELIABLE); the honest sound-vs-sound baseline is the old path's full pass (610 ms on 10k files) vs the session's 219 ms — **the session route is 2.8× faster than every sound no-op in the tree; the noop gate cell PASSES against the honest baseline; otp-11b is UNBLOCKED and its `change_journal/` deletion now removes a data-loss bug**. New regression pin `deep_modification_after_warm_runs_syncs` (warm repeated mirrors + deep modification MUST land — the exact lost shape). The principled O(changes) tier is filed as designed future work in the slice doc D3: journal-assisted no-op done SOUNDLY (Windows USN range replay; macOS FSEvents historical replay honoring must-rescan; fail-open) as a negotiated SESSION phase — both roles, both carriers, remote no-ops skip manifest streaming too; new wire surface, own reviewed slice set. Suite 1512 → **1513**.

**2026-07-12 01:30:00Z** - **CODER (otp-11a: local transfers ride the session — the local route, claude)**: design `0da65d6`+`c7b463b`, slice `dfdddd6` + review fixes `e445e8d`, bench `631255b`. otp-11 staged 11a (route) / 11b (deletion) via a slice-design doc (`docs/plan/OTP11_LOCAL_SESSION.md`, D1–D3 — itself codex-reviewed as a plan change). The route: `run_local_session` joins `run_source`+`run_destination` over `in_process_pair()` (the otp-3 wiring); the LOCAL byte-carrier is a process-local `LocalApply` extension on `DestinationSessionConfig` — crate-private, NO wire representation, so no peer can ever select it — under which the destination plans needed headers (`plan_transfer_payloads`) and applies them in-process through `FsTransferSink` (clonefile/block-clone/copy_file_range preserved; `execute_sink_pipeline_streaming` stays alive as the apply pipeline); no NeedBatch is sent, nothing enters `outstanding`, a payload record still violates. `blit_app transfers/local.rs` (the one chokepoint, CLI+TUI) re-pointed; `LocalMirrorOptions` in / `LocalMirrorSummary` out (synthesized destination-side; outcome replicates the old strategy-gate reachability); mirror = the in-session delete rule with the user's FileFilter threaded process-locally, plan-only under dry-run, split (files,dirs) counts (the old `apply_mirror_deletions` tuple restored); move's IgnoreTimes mapping + caller-side unreadable gate untouched and now load-bearing (all 3 otp-10b-2 F3 regression pins green). Slice also fixed a latent sink bug the ported single-file pin exposed: file-root File payloads (empty rel path) hit trailing-slash ENOTDIR joins — `copy_root_file_payload` routes the identity case. **Design codex (gpt-5.6-sol, 241k): CHANGES REQUIRED → 10 findings adjudicated** (`.review/results/otp-11-design.gpt-verdict.md`): 3 already independently fixed in the slice (empty-rel sink, dest-subtree exclusion wrapper, mirror split/plan-only); doc amended (D1's "unchanged choreography" overclaim → the carrier delta stated precisely; symlink parity scoped to reachable options; local resume framed as the carrier's sink-level block phase + new pin; floor arithmetic redone honestly — 11b retires 71, needs ≈+44 real pins by otp-13). **Slice codex (gpt-5.6-sol, 267k): FAIL → 9 findings: 7 accepted+fixed** (`e445e8d`) **, 1 doc defect, 1 rejected**: ONE diff core extracted (`diff_chunk_verdicts` — both carriers, dispatch-only difference); apply pipeline aborts on drop (no writes behind a returned operation); **apply-time unreadable mirror guard** — verified the old engine refused mirror deletions on ANY unreadable entry incl. apply-time (engine/mod.rs R46-F2), the session's local carrier now carries that exact posture, pinned deterministically with a vanishing-source stub (mode-000 fixtures are caught at scan time) and guard-proven; `--workers` maps to apply-pipeline width under debug_mode (default 1 = the old shape); `--null` keeps its `null_sink` RunKind tag; bench harness hardened against swallowed failures; finding-doc evidence citation fixed. Rejected: "immediate-start regression" — diff batching is session-uniform (`DEST_DIFF_CHUNK`, both carriers since otp-4); making local eager-er would recreate per-topology drift; overlap pin ports at 11b. **Perf gate: huge/tree/small PASS both runs (1 GiB single file = 22 ms BOTH binaries — the clone survives; the cell byte-relay would have lost by orders of magnitude); the focused noop10k cell surfaced the real cost of retiring the change journal (D3): warm-journal old ~21 ms vs session full-diff ~219 ms on 10k files (the session BEATS the old non-journal pass, 610 ms). OWNER QUESTION — blocks 11b per the slice doc's gate rule** (options in STATE). 4 mutation guard proofs this slice (dest-subtree bypass, dry-run execute, split swap, apply-time guard). Suite 1488 → 1510 → **1512**/0 (2 ignored), fmt/clippy clean; two mid-session full-suite "failures" were dirty-tree BUILD_MISMATCH sampling artifacts (D-2026-07-05-2 refusing correctly across mid-edit builds), converged by a consistent clean rebuild. Verdict `.review/results/otp-11a.gpt-verdict.md`. Next: owner call on the journal question, then **otp-11b** — dial re-home, the deletion, compare_manifests sweep, deletion proof + retirement accounting (≈+44 pins).

**2026-07-11 21:30:00Z** - **CODER (otp-10c-2: THE CUTOVER DELETION — otp-10c CLOSED, one transfer path by construction, claude)**: `7aac28b` + review fixes `995e1cc`. The plan's invariant is now structural: the four per-direction drivers (`remote/pull.rs`, `remote/push/`, daemon `service/push/`, `service/pull_sync.rs`), `rpc Push` + `rpc PullSync` + 13 exclusively-theirs messages (incl. `DataTransferNegotiation`, `PushSummary`/`PullSummary`, `PullSyncAck`, `metadata_only` — the relay scan flag whose caller died at 10c-1), the two wire-specific gRPC fallback sinks + `grpc_fallback.rs` (the session's in-stream carrier pins the same 1 MiB frame unit via `IN_STREAM_CHUNK`), and the dead dispatch/purge/util helpers are OUT of tree and proto — no bridge (D-2026-07-05-2); −13,771 lines. Relocated verbatim: the delegated spec builder (`DelegatedSpecOptions`/`delegated_spec_from_options` → operation_spec.rs; delegated trigger = its only consumer) and `FsTransferSource`'s fs-scan helpers (→ source.rs). The four A/B parity pins became ABSOLUTE tree+exact-count pins (the perf half lives in the committed otp-2/otp-2w baselines + otp-12's interleaved runs on zoey's pinned `e757dcc` binaries); dispatcher/updater pins re-pointed to the session variants; the DelegatedPull no-payload-bytes proof recorded (trigger-only request + progress-only oneof + the `cli_data_plane_outbound_bytes == 0` pin with its 10c-1 positive control). File-by-file deletion proof + all 106 test retirements enumerated in `.review/findings/otp-10c-2-driver-deletion.md`. Two full-run hiccups, neither this slice's code: the otp-3-reviewed dirty-tree BUILD_MISMATCH sampling window (converged by resample) and one w9-3-class daemon-spawn flake (3/3 green isolated). **Codex (gpt-5.6-sol, 282k tokens): NEEDS FIXES → 6/6 accepted, F6 owner-gated** (`995e1cc`) — and it independently confirmed the relocations verbatim, the no-payload proof, the A/B conversions, and the session invariants: **F1 (Med)** `TransferOperationSpec.client_capabilities`/`.receiver_capacity` + `PeerCapabilities` were semantically orphaned (nothing read them since otp-9b stopped forwarding the spec; the proto comments claimed a forwarding boundary that no longer exists) — deleted, fields reserved; **F2 (Med)** five more pub helpers lost their only callers with the drivers (`is_complete`, `files_needing_transfer`, `allows_relative`, the `FsTransferSink` path-tracker plumbing, `return_vec`) — deleted (`compare_manifests` also caller-less; deferred to otp-11's sweep as local-path-adjacent); **F3 (Med)** the relocated builder was live code with no direct pins — 7 new `delegated_spec_tests` (endpoint mapping, the old driver's full precedence table, mirror scopes, carriage + normalization round-trip), precedence mutation-proven; **F4 (Med)** my finding doc claimed containment coverage otp-6b never had — `mirror_delete_pass`'s per-target `verify_contained` wiring is now directly pinned (foreign-root refusal + real-root control), mutation-proven (the unpinned mutation genuinely survived the suite pre-fix), claim corrected in place; **F5 (Med)** `docs/API.md` had never been swept (whole Push/Pull/negotiation sections + `RemotePushClient` example + a version-negotiation claim contradicting D-2026-07-05-2) — rewritten; ARCHITECTURE/WHITEPAPER/transfer.rs residue fixed; REVIEW `w6-2b` re-scoped from the deleted handlers to the served-session dispatcher where the byte-counter gap really persists; **F6 (Low)** the tracked `.claude/worktrees/vigilant-mayer` snapshot still contains the old tree — true; its `git rm -r` is the standing `725aa07` owner question, so the deletion proof is scoped to the workspace until the owner's go. Suite 1586 → 1480 (the priced-in deletion) → **1488** (+8 codex-round pins; the otp-13 floor of 1483 is met again, margin +5 — watch it at otp-11). fmt/clippy clean. Verdict `.review/results/otp-10c-2.gpt-verdict.md`. Next: **otp-11** — local transfers ride the in-process transport; the separate local orchestration is deleted; local perf pins hold.

**2026-07-11 18:30:00Z** - **CODER (otp-10c-1: `--relay-via-cli` removed — the relay read half is out of the tree, claude)**: `f53f5a4` + review fixes `27bef56`. The 10b-2 deferral ("relay's PullSync-read half decided at 10c") went to the owner as a live choice this session — remove the flag, or keep it as a stage-to-temp-dir composition of two unified sessions (the streaming relay is unrebuildable once PullSync dies: its on-demand per-file remote read is a capability the session deliberately lacks). **Owner picked removal — D-2026-07-11-1**: remote→remote is delegated-only, the CLI never in the byte path; the unreachable-source topology gets the manual two-hop (pull local, push), which the delegated CONNECT_SOURCE hint now states. Deleted: the flag, `TransferRoute::RemoteToRemoteRelay` (+ the `select_transfer_route` relay param), all four relay-combination gates (detach×/mirror×/move×/resume×relay — their data-loss reasoning is moot with no relay to combine), `RemoteTransferSource` + its two bounded-read helpers (F7/R11-F1 bounds guarded the relay's remote tar assembly) + the constructed-instance counter, and the `relay_fallback_suggestable` hint machinery. **`PushExecution.source` narrowed `Endpoint` → `PathBuf`** — a remote push source is now unrepresentable; CLI/TUI call sites follow. Suite 1605 → 1585: 20 relay-only tests retired with per-test accounting (finding doc), zero live behavior left unguarded. **Codex (gpt-5.6-sol): FAIL → 3/3 accepted + fixed** (`27bef56`): **F1 (Med, the catch of the round)** the deleted relay e2e was the diagnostics counter's ONLY positive control — every survivor asserts `== 0` and `read_counters` maps a missing file to 0, so silently broken instrumentation would leave the load-bearing delegated isolation pins green vacuously; new `local_to_remote_push_is_the_positive_counter_control` (real daemon, ≥ payload bytes through the same flag/file/parser), guard-proven by no-op'ing the recorder to its FAILING vacuity shape and restoring (its own first run also caught a wrong landing-path assumption — the rsync no-trailing-slash nesting — fixed before the proof); **F2 (Med)** live guidance still advertised the flag (`--detach` help, README, blit.1.md, DAEMON_CONFIG, perf page) — root cause: the slice's docs grep was head-truncated; the unbounded re-sweep also caught ARCHITECTURE.md + WHITEPAPER.md beyond codex's list; **F3 (Low)** comment retype (the chokepoint fake models "an impl that ignores scan(filter)", not the deleted type), LOCAL_ERROR_TELEMETRY route list annotated, REVIEW `relay-1-subpath-double-join` closed as moot. Suite → **1586**, fmt/clippy clean throughout. Verdict `.review/results/otp-10c-1.gpt-verdict.md`. Push-state note: origin/master moved to `6d37a22` since the 42nd-session count (a partial push landed); unpushed is now `6d37a22..HEAD`. Next: **otp-10c-2** — the four drivers (`remote/push/`, `remote/pull.rs`, daemon `service/push/`, `service/pull_sync.rs`) + `Push`/`PullSync` out of tree AND proto (exclusive messages only — `TransferOperationSpec`/`ManifestBatch`/`BytesProgress`/job-kind enum stay), the delegated spec builder relocated out of `pull.rs`, ported-test accounting incl. the A/B reference pins, the DelegatedPull no-payload-bytes assertion, file-by-file deletion proof.

**2026-07-11 16:00:00Z** - **CODER (otp-10b-2: the pull-shaped verb rides the unified session — VERB CUTOVER COMPLETE, claude)**: `2014782` + review fixes `3534ffa`. Every pull-shaped verb (CLI copy/mirror/move from a remote source, TUI F3) now initiates a DESTINATION-role `Transfer` session through one chokepoint (`blit_app run_remote_pull`); with otp-10a no verb can reach either old driver — otp-10c is pure deletion. Landed: **ONE args→compare mapping for BOTH verbs** (`blit_app transfers/compare.rs`, the old pull's precedence verbatim; push's `--checksum` gate lifted and push now honors `--checksum`/`--size-only`/`--ignore-times`/`--force`/`--ignore-existing` exactly like pull); **move mapping = IgnoreTimes, or Checksum when asked** (the one skip that is content-proven safe before a source-delete) + a NEW `--size-only` move gate (the old move-pull honored it — a live skip-then-delete hole); dest-side **w6-1 progress** via new `DestinationInstruments` (need batches = the denominator, mirroring push; per-file `Payload`/`FileComplete` on both carriers — `execute_receive_pipeline` already spoke the contract, the in-stream arms report inline) + pull `--trace-data-plane` (receive dials, epoch-0 + resize); **mirror = the in-session one delete rule** (`apply_pull_mirror_purge` and the client-manifest upload leave the verb path); printers retype to the session `TransferSummary` (pinned `Pull complete:`/`[gRPC fallback]` kept; `bytes_zero_copy` dies with the driver); the old pull's pre-created dest parent proved redundant by mutation (the sink creates parent chains) and was not ported; `test_pull_multistream_many_files` deliberately ported to trace-based fan-out (the old driver's unconditional `[pull-data-plane]` line died with it). New pins: `pull_session_cutover.rs` (11 — A/B parity vs old pull on twin daemons, move-shaped + checksum cells with SizeMtime controls, resume both carriers, daemon `--force-grpc-data` on pull, single-file layout, `--ignore-existing`), pull-move + push-checksum-pair + `--size-only` binary e2es, TUI builder pin, compare-mapping unit pins. **Codex (gpt-5.6-sol): NEEDS FIXES → 6 findings: 5 accepted (1 in part) + fixed, 1 deferred** (`3534ffa`): **F1 (High)** the mirror delete pass awaited its blocking task without reading the control lane — a peer CANCELLED sat unread while deletions ran behind a cancelled session; the purge now races one biased lane read, flips the abort flag, and the peer's fault owns the outcome (scripted-peer pin, 2000-file tree); **F2 (High)** delegated remote→remote MOVE still rode SizeMtime-then-delete (the otp-10a F1 loss on the missed route) — `delegated_pull_options` forces ignore_times unless `--checksum`, wire-pinned, TUI options defended; **F3 (High, in part)** the rewritten gate texts made route-dependent claims — local move (CLI+TUI) now maps through the move rule explicitly and the texts state the uniform truth; the claimed LIVE local data loss did NOT reproduce (probed the binary: the non-mirror local path copies unconditionally regardless of compare mode) — the two new pins are otp-11 regression pins, documented as such; **F4 (Med)** every served session was counted/exposed as Push with an empty endpoint — an `on_open` hook now fires at open-resolve (new `ActiveJobUpdater`): row kind + endpoint, `inc_pull` vs `inc_push` by role, `TransferStarted` with real values (pull sessions record **PullSync** rows again — CancelJob-capable; pre-open deaths emit the placeholder started to keep event pairing); **F5 (Med) DEFERRED** — the progress monitor lives through the in-session purge (display-only ticks/avg dilution; the fix is the M-C `AppProgressEvent` phase reshape; STATE residue); **F6 (Low)** TUI F1 builder routes through the one mapping. Guard proofs: **12 temporary mutations across both rounds** (slice: mirror/filter/ignore_existing/resume/force_grpc wiring, need-batch denominator, data-plane per-file lane, CLI move mapping — reproduced the exact skip-then-delete loss at the binary level — and pull trace; fix round: the purge race gated off, the delegated forced-ignore_times dropped, the source-resolver kind swapped), each run to a FAILING pin and restored; mutation U (the ported parent-creation step) left its pin GREEN and the redundant code was deleted instead. Suite 1581 → **1605** (full workspace 1605/0), fmt/clippy clean throughout. Verdict `.review/results/otp-10b-2.gpt-verdict.md`. Next: **otp-10c** — the four drivers + `Push`/`PullSync` out of the tree AND the proto, ported-test accounting, file-by-file deletion proof (incl. the DelegatedPull no-payload-bytes assertion; relay's PullSync-read half decided there).

**2026-07-11 10:00:00Z** - **CODER (otp-10b-1: checksum compare on the session — contract v3, claude)**: `e82859e` + review fixes `7d3a1f2`. Staged sub-slice before the pull-verb cutover (10b-2): old pull honors `--checksum` content-skip but the session's `COMPARISON_MODE_CHECKSUM` degraded to transfer-everything (neither end hashed). Now a real content compare, role-agnostic: the SOURCE fills each manifest `FileHeader.checksum` via the new `ChecksummingSource` decorator (hashes through the inner source's own `open_file` — source-impl-agnostic, composed OUTSIDE the filter so only in-scope files pay), the DESTINATION hashes its SAME-SIZE diff candidates inside the existing blocking-pool chunk (size mismatch is already Modified — no hash), and a daemon with `--no-server-checksums` refuses a Checksum open at OPEN with the new `CHECKSUM_DISABLED` code (proto + `docs/TRANSFER_SESSION.md`; CONTRACT_VERSION 2→3; the old F11 ack-refusal contract reborn) via `ResponderPolicy`, which absorbs otp-10a's `force_in_stream` bool. Pins: role suite BOTH initiator layouts with SizeMtime controls (content-equal-diff-mtime SKIPS; same-size+mtime content change TRANSFERS — the cell `--checksum` exists for), daemon e2e served-skip + both-role refusal; 3 guard proofs by mutation (source wrap, dest hash, refusal). **Codex (gpt-5.6-sol): NEEDS FIXES → 5/5 accepted+fixed** (`7d3a1f2`): **F1 (High)** an unhashable file was dropped from the manifest — only the SOURCE sees its own unreadable list, so a pull could report success with the file silently absent (byte-identity hole; violated the slice's own conservative rule) — it now EMITS with an empty checksum and transfers unconditionally, unit-pinned with a failing-open stub; **F2/F3 (Med)** teardown bounding — the hashing task stops within one 64 KiB chunk of its consumer dying (`tx.is_closed` probe), and the destination diff chunk got the otp-9b `AbortFlagOnDrop` (hoisted to module scope) + chunked `hash_file_abortable`, abort never decaying into conservative-transfer; **F4 (Med)** `CHECKSUM_DISABLED` → NEGOTIATE in the delegated phase map; **F5 (Low)** stale STATE residue line. Suite 1576 → **1581**, fmt/clippy clean. Also this session: `blit_utils` e2e flake (du_json/ls_remote) REPRODUCED at pre-otp-10a `6d37a22` (2/8 binary-level runs) — pre-existing w9-3 daemon-spawn class, green in isolation 5/5, not introduced by otp-10 work. Owner note: zoey under maintenance today — no rig activity this session (all local). Next: **otp-10b-2** — pull-shaped verb rides `run_pull_session` (ONE args→compare mapping for BOTH verbs incl. lifting push's `--checksum` gate, dest-side w6-1 progress, printers retype, mirror retires `apply_pull_mirror_purge` from the verb path, move-pull adopts IgnoreTimes).

**2026-07-11 07:00:00Z** - **CODER (otp-10a: the push-shaped verb rides the unified session — first otp-10 cutover slice, claude)**: `0fbc966` + review fixes. The one verb chokepoint (`blit_app run_remote_push` — CLI copy/mirror/move-push, `--relay-via-cli`, TUI F1) now initiates `run_push_session` as SOURCE; no verb can reach `RemotePushClient::push` (deletion = 10c). Deferred wiring landed: `PushSessionOptions` mirror/filter (open mapping, otp-9a's pull twin), `--force-grpc`→in-stream, **w6-1 progress via new `SourceInstruments`** (recv half reports NeedBatch as the push denominator; both carriers per-file `Payload`+`FileComplete` — chosen over the STATE sketch's ByteProgressSink because the plan pins the w6-1 event contract and a bare byte counter can't feed the files/manifest denominators), `--trace-data-plane` (the remote_parity pin's `[data-plane-client]` mechanism), resume flags, verb-level `end_of_operation_summary` print (D-2026-07-09-1 Q2), and the **old-push unreadable-scan error** (send-what's-readable then fail) that `blit move`'s source-delete gate relies on. `PushExecutionOutcome` retyped `RemotePushReport`→session `TransferSummary` (JSON drops files_requested/bytes_zero_copy/first_payload_ms, gains files_resumed; human drops the post-hoc negotiation/data-port lines; `[gRPC fallback]`/"already up to date"/purge/Destination survive) so 10c is pure deletion; TUI touch = 2 field reads. **Codex (gpt-5.6-sol): NEEDS FIXES → 8 findings, 7 accepted+fixed, F1 in part**: **F1 (High)** the session's same-size-dest-newer skip + move's source-delete = silent data loss (guard-proof reproduced it: exit 0, wrong bytes at dest) — move now pushes `ComparisonMode::IgnoreTimes` (transfer unconditionally; also closes the OLD documented R54 matching-size+mtime move hole; copy-verb skip stays the standing owner question, not self-adjudicated); **F2 (High)** `endpoint_module_path` put native `\` on the wire after the CLI's rsync join — POSIX-normalized via `path_posix` (fixes pull's client too); **F3 (High)** served sessions ignored daemon `--force-grpc-data` — threaded through `run_responder`/`responder_finish` (grant-less accept); **F4 (Med)** relay+resume faults on the data plane (`RemoteTransferSource` can't prepare composite ResumeFile) but works in-stream — refused up front; **F5 (Med)** fault stringification stripped the `io::ErrorKind` the `--retry` classifier downcasts for — `SessionFault.io_kind` captured at `fault_from_report` + the 4 dial sites, classifier extended (contract-tested); **F6 (Med)** resumed files emitted no progress — both carriers now report stale-bytes + one FileComplete; **F7 (Med)** fault-summary print untested — extraction split out + 4 unit pins; **F8 (Low)** `build_spec` validated no globs — pre-connection validation restored. Suite 1555 → **1576** (17 new pins incl. A/B parity vs old push on twin daemons, byte-identical + equal counts); **10 guard proofs by temporary mutation across both rounds**, all restored green; fmt/clippy clean. Verdict `.review/results/otp-10a.gpt-verdict.md`. Next: **otp-10b** (pull-shaped verb; the one args→open compare-flag mapping for both verbs lands there), then 10c deletion.

**2026-07-11 01:00:00Z** - **BENCH + REVIEW (otp-2w codex round: 7/7 accepted — a measurement bug found on BOTH rigs; both matrices re-run, claude)**: Codex on `0c43d2a`+`ceea6ed`: NEEDS FIXES (4 Med, 3 Low), all accepted. The big one, **F3**: push windows wrapped `ssh host <flush>` and thereby carried ~1.2 s of connection/shell overhead (measured: ~0.5 s ssh + pwsh spawn to Windows; ~1.2 s slow-core key exchange to zoey) that pull windows never paid — inflating every published push median and ratio on BOTH rigs. Fix is structural: **self-timed durability steps** (Stopwatch around `Write-VolumeCache`; `/proc/uptime` around zoey's `sync`; the fsync walk reports its own elapsed) so only the flush itself joins the window; ControlMaster multiplexing added for wall-time. Both matrices re-run; biased sessions kept as labeled probes. **Corrected medians** — Windows: push_tcp 3054/1868/2288, pull_tcp 1294/1280/1284 (large/small/mixed; ratios ×1.46–×2.38; carrier-insensitive large in BOTH directions; spreads 0.2–14.5%); zoey: push_tcp 2702/4263/2070, pull_tcp 1744/2784/1401 (ratios ×1.25–×1.75; pool noise up to 48.6% on one cell — median carries it). Also fixed: fail-open drain probe ($null counted as quiet), stale-daemon masking (launch refuses; PID-exact teardown), unchecked P/Invoke steps + leaked token handle in purge-standby.ps1, my 8/12-vs-7/12 spread miscount (recounted from CSVs), "NEAR-SYMMETRIC" wording (owner said closer-spec), drain.log references (the `*.log` gitignore had silently dropped the drain evidence — renamed drain-outcomes.txt). The F3 fix's own two bugs (cross-process monotonic → 0/negative windows; PowerShell CRLF vs bash arithmetic) caught by running and documented. Verdict `.review/results/otp-2w.gpt-verdict.md`. Owner also opened **skippy** (TrueNAS, admin@skippy, existing blit-bin folder) for remote↔remote/Mac↔Linux — recorded in STATE. Next: **otp-10 (cutover + deletion)** — nothing holds it.

**2026-07-10 23:00:00Z** - **BENCH (otp-2w: Windows cross-direction baseline — otp-2 CLOSED both halves, claude)**: The owner adjudicated the asymmetric-rig question by designating Mac↔Windows ("mac to windows would be closer spec. windows is faster, both have 10gbe") and standing up OpenSSH on the host (Win11 26200, Ryzen 9950X3D/32t, 96 GiB, Gen5 NVMe `D:`, PS7 default shell, admin token). Same-commit builds both ends: `0f922de` shipped as a **git bundle** (unpushed commits; pushes owner-gated; bundle = file copy between the owner's machines), host's prior checkout stashed (`bench-cargo-lock`), native build 31 s. New harness `scripts/bench_otp2w_baseline.sh` + `scripts/windows/purge-standby.ps1` — the zoey methodology with the daemon-host half in PowerShell: standby-list purge (NtSetSystemInformation + SeProfileSingleProcessPrivilege) for cold caches, `Write-VolumeCache D` durable push windows, Get-Counter disk-write drain, and two Windows traps found live: TOML double-quoted strings corrupt `\b` in paths (config uses TOML literal strings) and **Windows OpenSSH kills session children on disconnect** — a Start-Process daemon died with its ssh session (reproduced), so the daemon launches via WMI `Win32_Process.Create` (survives; cmd /c supplies log redirection). One program-scoped inbound firewall rule (`blit-bench-daemon`, removal documented). Byte-identical smoke, then the 12-cell matrix: **zero drain timeouts, 8/12 cells ≤2% spread (worst 11.9%)** — verdict-grade. Medians (`docs/bench/otp2w-baseline-2026-07-10/`): push_tcp large/small/mixed 3549/2503/2844 ms; pull_tcp 1309/1381/1316 ms; grpc ≥ tcp everywhere. **Recorded reading (not adjudicated): old push trails old pull ×1.8–×2.7 even on this close-spec pair, and on large pushes the carrier makes NO difference (3549 vs 3562) — the ceiling is the receive/write side (NTFS/Defender and/or the old push-receive code); otp-12's interleaved old-vs-new per cell discriminates which.** STATE: the open question is RESOLVED (zoey = per-direction; Windows pair = cross-direction; otp-12 runs both, interleaved A/B for pushes); nothing holds otp-10 anymore. Next: **otp-10 (cutover + deletion)**.

**2026-07-10 21:00:00Z** - **BENCH + REVIEW (otp-2: codex round — 8 findings, re-run under the fixed harness, claude)**: Codex on `e757dcc`: **NEEDS FIXES, 8/8 accepted (one in part)** — the strongest review round of the plan so far. **F1 (High, upheld)**: "symmetric baseline" was mislabeled — D-2026-07-05-1's own rule (cross-direction comparisons valid ONLY on symmetric endpoints; "tmp on one side, spinning rust on the other is not a valid test") excludes Mac↔zoey (SSD vs pool), so the dataset anchors PER-DIRECTION converge-up only; re-framed everywhere, and the otp-12 cross-direction half is now an explicit owner question (options: per-direction verdicts suffice, and/or designate a symmetric pair). **F3 (High)**: STATE had pre-adjudicated that question ("gate satisfied", "Current: otp-10") — retracted; otp-10 HOLDS on the owner call. **F2 (High)**: macOS `sync(2)` schedules rather than waits, so pull windows weren't durable like Linux-sync push windows — pulls now fsync every landed file (`fsync_tree`; the honest +~150 ms on 10k-file pulls is visible vs the pre-review session). **F4/F5 (Med)**: drain now syncs-first and records per-run timeouts (never silent); push destinations carry a per-invocation tag + EXIT sweep. **F6 (Med)**: README claims re-stated to exact CSV numbers (probe-1 spread per cell up to 8.0×; pull stability ±6% typical/+21% worst; push/pull ratios ×1.23–×2.19; the manual drained probe committed as `probe3-drained-pushes.csv`); "unreachable regardless of code" replaced by the decision citation. **F7/F8 (Low)**: rounding policy stated; python3 preflight-checked; the monotonic-clock half of F8 was tried and REVERTED with evidence — cross-process `time.monotonic()` has undefined reference points, and the attempt produced 0/negative windows while the daemon logs showed real multi-second transfers (aborted run; wall clock restored with the why in a comment). **Full matrix re-run under the fixed harness** (`e757dcc` binaries both ends): medians push_tcp large/small/mixed 3025/3929/2666 ms, pull_tcp 1664/2699/1503 ms, grpc uniformly slower; exactly ONE drain timeout (the expected post-staging first run, recorded in the committed `drain.log`); cross-session agreement with the pre-review run ~10% (kept as `probe4-prereview-session-runs.csv`). Evidence dir refreshed; verdict `.review/results/otp-2.gpt-verdict.md`. **NEXT: the owner's (a)/(b) adjudication in STATE unblocks otp-10.**

**2026-07-10 19:00:00Z** - **BENCH (otp-2: symmetric disk-to-disk baseline RECORDED, claude)**: The rig gate lifted mid-session — owner opened `root@zoey` (work confined to the standing `blit-temp` rule), designated the Mac's Thunderbolt 10GbE as the client end, and noted zoey's CPU won't saturate the link (confirmed; cells are CPU/storage-bound, which the per-cell reference tolerates). Also captured as a standing instruction: the Windows 10GbE box + TrueNAS serve **remote↔remote (delegated)** testing when that stage arrives. Both ends built from `731023b` — macOS arm64 client + static aarch64-musl daemon via `cargo zigbuild` (the toolchain path the July session proved but never recorded: brew zig + rustup musl target + cargo-zigbuild; binaries staged in zoey `blit-temp`, byte-identical smoke round-trip before any timing). **Methodology corrections earned by probe runs** (both kept as committed evidence): naive transfer-return timing is a write-cache lottery on zoey's pool (push cells 4–8× spread — mixed 1.4/6.1/11.6 s); durable-at-destination windows (transfer + dest `sync`) then exposed the pool's STATEFUL tiered write path (NVMe mirror destaging to 8 spindles — identical pushes ascend 2.7→13.4 s as the tier fills); a per-run pool DRAIN (three consecutive quiet 2 s windows) restores agreement (drained probe 4.5/2.7/3.1 s), with MEDIAN-of-4 absorbing the residual one-in-four outlier. Final matrix (`docs/bench/otp2-baseline-2026-07-10/`, harness `scripts/bench_otp2_baseline.sh`): 12 verdict cells (3 fixtures × push/pull × tcp/grpc), pulls ±2–8%, push medians stable (residual ±10–20% spread → otp-12 prescription: interleaved same-session A/B for push verdicts; the `731023b` binary pair stays staged on zoey for that). Medians: push_tcp large/small/mixed 2886/4048/2648 ms; pull_tcp 1707/2552/1510 ms; grpc uniformly slower (sanity ✓); 1 GiB durable ≈ 3.0 Gbit/s push / 5.0 Gbit/s pull; small-file ≈ 405/255 µs per file — July's per-file-bound shape at zoey's slower constant. July tmpfs/warm data re-labeled wire-reference only (plan sub-deliverable). **NEW OPEN QUESTION routed to the owner** (STATE): the plan's cross-direction acceptance bar presupposes hardware-symmetric endpoints; this rig's write ends are asymmetric (SSD vs pool — old-pull beats old-push ~1.7× everywhere for physics reasons), so the proposal is per-direction converge-up on asymmetric rigs, cross-direction only on symmetric ones. Not self-adjudicated. Finding doc `.review/findings/otp-2-symmetric-baseline.md`; codex review of the slice follows this entry. Next: **otp-10 (cutover + deletion)** — the rig gate is satisfied.

**2026-07-10 17:30:00Z** - **CODER (otp-9b: delegated transfer rides the unified session — otp-9 done, claude)**: The otp-9 core (`b2fd876` + review fixes `1ce73b5`). `run_delegated_pull`'s validation front half (locator, spec boundary, delegation gate, module opt-outs, containment) is untouched; the bespoke old-driver body is replaced by a DESTINATION-initiator `Transfer` session against the validated source IP: `connect_transfer_client` (audit-1's 30 s bound preserved) → `Started` → `run_pull_session_with_client` (new split, so connect failures keep `CONNECT_SOURCE` structurally) with the spec mapped onto `PullSessionOptions` (`force_grpc` → in-stream carrier — the `--force-grpc` plumbing lands for delegation; mirror through `delete_list_authorized`; resume/filter/compare verbatim) and the row's `ByteProgressSink` (otp-9a). Session summary → wire summary (`in_stream_carrier_used` → `tcp_fallback_used`, its historical meaning). **Dead by construction**: the pre-enumerated local manifest (session diffs incrementally), the source-attested delete list (mirror runs locally via the one delete rule — R30-F1/R32-F1/R34-F1 structural), the R25-F2/ue-r2-1b capability/receiver-capacity overrides (the session advertises this end's own capacity). Retired with their 9 tests (called out; 1558 → 1552): `dst_capabilities`, `apply_dst_capabilities_override`, `apply_delete_list`, `build_summary`, `enumerate_local_manifest`. New two-daemon in-process e2es (`delegated_session_e2e.rs`): session landing + dst-authoritative counts, local mirror purge (plain copy never deletes), force_grpc in-stream pin; CLI `remote_remote` suite green over the reroute incl. the no-CLI-byte-path isolation pin; the `RejectingPullSyncBlit`/`StallingPullSyncBlit` fakes now model refusal/stall on the Transfer surface. **Codex (gpt-5.6-sol): NEEDS FIXES → 4/4 accepted + fixed** (`1ce73b5`): **F1 (High, session-wide)** `require_complete_scan` was forwarded but never enforced — a delegated MOVE could omit unreadable source files, report success, then the CLI deletes the source; the destination now refuses `ManifestComplete{scan_complete=false}` with `SCAN_INCOMPLETE` (R49-F2 on the session; scripted-peer pin with bounded wait). **F2 (High, pre-existing since otp-6b)** the mirror pass ran plan+delete in one `spawn_blocking` a dropped/cancelled session future cannot stop — deletions could continue behind a cancelled job; `AbortFlagOnDrop` flips an `AtomicBool` the pass checks before every fs op (unit-pinned). **F3 (Med)** open-time Transfer failures other than Unimplemented/PermissionDenied lost NEGOTIATE — `TransferOpenRefusal` marker + pure `session_error_phase` classifier (unit-pinned). **F4 (Med)** the fakes' retained legacy PullSync behavior made the reroute unguarded — their `pull_sync` is now Unimplemented, so a revert fails the refusal + cancel pins. All guard proofs by temporary mutation, run live. Suite 1552 → **1555**, fmt/clippy clean. **otp-9 CLOSED; otp-1..9 `[x]`** (one `test_utils_find_dirs_only` under-load flake seen once mid-slice — w9-3 daemon-spawn class, 3/3 green isolated). Next: **otp-10** (cutover + deletion) — but otp-2 (symmetric baseline, RIG-GATED) comes first per the plan.

**2026-07-10 15:30:00Z** - **CODER (otp-9a: pull session-client surface, claude)**: First otp-9 (delegated transfer) sub-slice, staged like 4b/5b/6/7: before the delegated-pull handler can initiate the unified session (otp-9b), the pull session client needed the surface the old driver had. `7bf8ef8`: `PullSessionOptions` gains `filter`/`mirror_enabled`/`mirror_kind` mapped onto `SessionOpen` (the session honors both since otp-6 — this is client wiring only) + `byte_progress`; `DestinationSessionConfig.byte_progress: Option<ByteProgressSink>` threads into the destination session's `FsTransferSink` via its existing `with_byte_progress` contract (the delegated dst daemon keeps its ActiveJobs row live with it; CLI progress reuses the seam at otp-10). Served responder path unchanged (`run_responder` passes `None` — the daemon row-counter wiring stays the core.rs follow-up). Pins (suite 1555 → **1558**): mirror-ALL purge via options (`entries_deleted` scored, trees identical), include-filter scoping the REMOTE scan via options, caller `AtomicU64` == `bytes_transferred`. Guard proofs by temporary revert: dropped open-mapping fails both option pins; disabled sink hook fails the counter pin at 0. **Codex (gpt-5.6-sol): NEEDS FIXES → 1/1 accepted + fixed** (`607a924`): F1 (Low) stale `PullSessionOptions` rustdoc contradicted the new fields — corrected (push doc left: still accurate until otp-10). Codex independently confirmed the byte model (applied payload bytes) and the deferral scoping. Verdict `.review/results/otp-9a.gpt-verdict.md`. fmt/clippy clean throughout. Next: **otp-9b** — reroute `run_delegated_pull` steps 9-12 onto a DESTINATION-initiator session against the validated source endpoint (connect timeout kept; Started/Summary progress relay from the session outcome; mirror deletions land via the session's one delete rule so `apply_delete_list`/`enumerate_local_manifest` retire from the handler), A/B parity pin old-vs-session delegated, and the no-payload-bytes-through-`DelegatedPull` assertion.

**2026-07-10 14:15:00Z** - **CODER (otp-8: fallback byte-carrier — closed by assessment + wire residue pins, claude)**: Executed assess-first per the 41st handoff: the slice's substance (the in-stream carrier, its negotiation-time selection via `SessionOpen.in_stream_bytes` or grant-less accept on bind failure, both wire directions, resume, clamps, `in_stream_carrier_used` reporting, the `--force-grpc`-shaped session-client seam) landed incrementally across otp-3..7b and was already pinned; the plan's "selected at negotiation — not a separate transfer path" wording deliberately retires the old drivers' mid-flight TCP→gRPC downgrade, so no new selection machinery was owed. The genuine residue: in-stream RESUME had no pin over the real gRPC transport — the 2 MiB in-stream block ceiling (D-2026-07-10-1) exists for tonic's 4 MiB frame decode limit, which the in-process role suite cannot enforce. Landed `5ffc9be` (tests only): `push_session_resumes_partial_over_in_stream_carrier` (the otp-7b data-plane fixture forced in-stream) and `pull_session_resume_clamps_oversized_blocks_to_in_stream_ceiling` (an 8 MiB block-size request over a 6 MiB file with ONE corrupt byte at 3 MiB must move exactly one 2 MiB block — the assertion is sensitive to the exact ceiling, and an unclamped implementation ships a 6 MiB frame tonic rejects). Guard proofs: ceiling 2→1 MiB fails the clamp pin at exactly 1 MiB; ignoring the in-stream request fails the carrier-flag asserts. Suite 1550 → 1552. **Codex (gpt-5.6-sol): FAIL → 2/2 accepted + fixed** (`643294a`). **F1 (High, the assessment's own Known-gaps deferral falsified)**: the in-stream record sends ran inline with NOTHING racing the receive half's framed peer faults — where the data plane routes `dp.queue()` errors through `prefer_peer_fault` and races its drain against `recv_peer_fault`, the in-stream branches were bare `?`; a mid-transfer cancel over the in-stream carrier provably HUNG the client (new e2e failed its 10 s no-hang timeout pre-fix, the framed CANCELLED sitting unread in the events queue). Fix: `SourceEventSender` mirrors every queued `Fault` onto a `watch` signal; both in-stream send sites `tokio::select!` it biased fault-first — a non-consuming side channel, so mid-send `Need`s stay queued. Pinned by `mid_transfer_cancel_surfaces_cancelled_over_in_stream_carrier`. **F2 (Medium)**: one `TarShardHeader` frame carried a shard's whole header list — the planner caps count (≤4096) and content bytes but not encoded header size, so legal long-path shards exceed the tonic limit (pre-existing in the OLD gRPC fallback lane too, but post-cutover the session is the only path). Fix: `MAX_IN_STREAM_TAR_HEADER_BYTES` (2 MiB) + `bound_in_stream_tar_headers`, an in-stream-only post-planner splitter (same grammar, same planner decisions, only record boundaries move; data plane untouched — its binary records have the 64 MiB cap). Pinned by a pure splitter test + a frame-capturing wiring test (4096 × ~590-byte paths, >2 MiB encoded, must emit multiple under-bound frames); guard proof by wiring-line revert. Verdict `.review/results/otp-8.gpt-verdict.md`. Suite 1552 → **1555**, fmt/clippy clean, full workspace run 1555/0 (one earlier unrelated `test_utils_rm_file` transport-error flake under suite load — w9-3's daemon-spawn class, 3/3 green isolated, not this slice's code). **otp-8 CLOSED; otp-1..8 `[x]`.** Next: **otp-9** (delegated transfer = daemon-initiated session; bespoke delegated-pull driver retired behind the gate; `DelegatedPull` reduced to trigger + progress relay).

**2026-07-10 07:30:00Z** - **CODER (otp-7b: resume over the TCP data plane + the D4 fault-summary rider, claude)**: otp-7b landed as two slices through the codex loop, closing otp-7. **otp-7b-1** (`ecac9b0`): the responder no longer suppresses the data-plane grant for resume sessions; each correlated (need, hash-list) pair queues as ONE composite `TransferPayload::ResumeFile` work item, so one pipeline worker emits the whole binary `BLOCK*`/`BLOCK_COMPLETE` record on one socket — strict per-file serialization with no cross-socket reorder against the finalization truncate. The block-diff is single-sourced (`remote/transfer/resume_diff.rs::ResumeBlockDiff`) for both carriers; the DEST's grant map + `files_resumed` are `Arc`-shared with `NeedListSink`, which replicates the in-stream claim strictness on the sockets (grant-only, in-bounds, size-verified, exactly-once, resume-flagged grants refuse file/tar delivery). Block-size ceiling is per carrier — **D-2026-07-10-2**: 2 MiB in-stream, 64 MiB data plane (= `MAX_WIRE_BLOCK_BYTES`), decided by grant presence. `Push/PullSessionOptions.resume{,_block_size}` wired to `SessionOpen.resume`. Pins: roles suite both initiator assignments over loopback data planes + daemon e2e push/pull; guard proofs by temporary revert (neutered diff, re-suppressed grant). **otp-7b-2** (`071799a`): the Q2 rider — `SessionFault.relative_path` (wire: `SessionError.relative_path=5`, CONTRACT_VERSION→2), a typed `FaultedPath` eyre-chain marker lifted by `fault_from_report`, and `end_of_operation_summary()` naming the affected file + suggesting a re-run (verb-level print lands at otp-10; pins at session-client/e2e level per the plan's staging). Plus the 7a-deferred cancel-during-resume daemon e2e (7a F4) and a **gate-discovered RELIABLE bug from 7a**: `write_file_block_payload` never flushed its tokio file handle — `write_all` returning ≠ bytes at the OS — which made the 7a mid-resume pin ~50% flaky under full-suite load (block 0 vanishing); one `flush().await` fixed it, 12/12 clean loops after. **Codex (gpt-5.6-sol; reviews delayed ~1 h by an account usage limit): 7b-1 FAIL → 3 accepted+fixed, 1 pre-fixed, 1 deferred, 1 rejected; 7b-2 NEEDS FIXES → 4/4 accepted+fixed** (`d48351d`): F1 (High) a mostly-matching hash scan is silent long enough to trip the receiver's 30 s StallGuard — `ResumeBlockDiff` now emits keepalive ticks (stall/3) the data-plane sink answers with zero-length BLOCK records (unit-pinned); F4 resume batches now drive the sf-2 shape resize; F6 new pin proves the 64 MiB data-plane ceiling (guard-proven by ceiling revert); F2 = the flush bug, already fixed in 7b-2; F3 (queue not raced vs control events) DEFERRED — pre-existing otp-4b shape, keepalive bounds the new window, both cancel e2es pin behavior, filed in STATE residue; F5 (diff buffer outside BufferPool) REJECTED as blocking — worst case needs an explicit 64 MiB request, documented in the finding doc. G1 `relative_path` became proto3 `optional` ("" is the valid single-file-root identity; renders non-blank); G2/G3 tagging completed on both carriers' record paths; G4 contract doc updated. Verdicts: `.review/results/otp-7b-{1,2}.gpt-verdict.md`. Suite **1540 → 1550** (fmt/clippy clean throughout). ONE_TRANSFER_PATH otp-1..7 now `[x]`; next: **otp-8** per the plan queue.

**2026-07-10 04:30:00Z** - **CODER (otp-7a resume over the in-stream carrier, claude)**: The unified session now RESUMES (`4e5ff58` + review fixes). Choreography per `docs/plan/OTP7_RESUME.md` (Active, D-2026-07-09-1): the DESTINATION flags resume-eligible needs (plan D2 — non-empty regular-file partial + compare says transfer), sends each flagged grant's `BlockHashList` right after its `NeedBatch`, and applies `BlockTransfer`/`BlockTransferComplete` records in place through the existing sink (`write_file_block_payload`/`_complete`, truncate+fsync+mtime/perms stamp from the retained manifest header); the SOURCE holds a resume-flagged need until its hash list arrives (the contract's strict ordering — enforced by construction, plus a fail-fast if NeedComplete leaves a need without its list), then Blake3-diffs the live file block-by-block and sends only stale blocks; a stale/garbage hash means SEND, never trust (D1 — graceful full-transfer fallback, pinned). `TransferSummary.files_resumed` is real; resume sessions get no data-plane grant (blocks are control-lane frames until otp-7b). All four plan guard-proof pins run under BOTH initiator roles (D6). **Codex: FAIL → 6 findings: 4 accepted+fixed, 1 partial, 1 deferred.** F1 (High, accepted): resume frames could exceed tonic's default (unraised) 4 MiB decode limit on the gRPC-served in-stream carrier — the D5 clamp allowed 64 MiB blocks (a 16×-oversized BlockTransfer frame) and a block_size=1 open amplified the hash list 32×; fixed as **D-2026-07-10-1** (plan D5 amended in place): DEST clamps block size into [64 KiB, 2 MiB], one BlockHashList capped at 65_536 hashes with over-cap partials degrading to the empty-list full-transfer fallback, SOURCE range-validates at arrival. F3 (Med, accepted): whole-file and tar records could claim resume-granted paths, silently bypassing the hash choreography — both arms now reject, and SourceDone verifies no resume grant is left uncompleted. F5 (Low, accepted): block-size validation moved to frame arrival (fail fast, not after pending plain files transmit). F6b (Low, accepted): the mid-fault pin now proves the fault was mid-record (block 0 landed in the partial, byte `bs` untouched); F6a rejected — zero `bytes_transferred` IS the zero-blocks observable, demonstrated by guard proof. F2 (Med, partial): per-list buffering now bounded (≤2 MiB) by the F1 cap; the O(resume-needs) aggregate is documented as a Known gap (same shape as the pending-needs vector). F4 (Med, deferred): hash/block phases inherit the session's existing payload-phase cancel latency — not a 7a regression; cancel-during-resume e2e added to otp-7b's scope in the plan. **Guard proofs (5, all run live)**: neutered block-diff ⇒ only-changed-blocks + zero-blocks pins FAIL; trusted stale hashes ⇒ byte-identity FAILS on corrupt output; removed in-record Error handling ⇒ dest-fault pin FAILS; removed the block-size clamp ⇒ both clamp pins FAIL; removed the bypass check ⇒ the bypass pin fails bounded (the destination otherwise absorbs the record and hangs — which is why the pin waits on a clock). Suite: true pre-slice baseline **1530** (the recorded 1529 was a miscount — re-counted on the stashed pre-slice tree) → **1540** (+9 role-suite pins incl. clamp/bypass, +1 lib cap-boundary test). fmt/clippy clean. REVIEW.md: otp-7a row added; otp-6a/6b rows backfilled (the 07-06 session logged those closes only in DEVLOG). Next: **otp-7b** — resume over the TCP data plane + the D-2026-07-09-1 CLI end-of-op fault summary rider + cancel-during-resume e2e.

**2026-07-09 03:30:00Z** - **PLANNER (otp-7 Active — owner Q1–Q3, claude)**: Owner answered `docs/plan/OTP7_RESUME.md`'s three open questions and flipped it **Active** (`70c9688`, D-2026-07-09-1). The deciding principle, quoted: "FAST, SIMPLE, RELIABLE file transfer. if we abort the whole thing when we could have fixed or surfaced a single error, we are violating all of those." **Q1**: contract wins — stale/mismatched partial degrades to full-file transfer, never an abort. **Q2**: in-place patch stays (parity), with an owner rider: a mid-resume fault must appear in the CLI's END-of-operation summary naming the file(s) and suggesting a re-run — not only a scrolling mid-stream line ("the current progress display is absolutely useless for this"). **Q3**: 7a-then-7b confirmed, no collapse, codex loop per slice. Same exchange: owner re-confirmed the CLI transfer-output redesign (persistent stats block + scrolling file frame, "probably a TUI") — TODO.md row updated with the owner wording; stays out of otp-7. **Codex on the flip commit: CHANGES REQUESTED → 3 accepted + 1 partially accepted, fixed** (`30c5b4e`): F1 (High, partial) D4's abort ambiguity — now states the abort is whole-session, the session's existing payload failure model (EOF-short file record aborts today), per-file continue-on-error explicitly out of otp-7 scope (the "conflicts with owner principle" half REJECTED: the owner's Q2 answer is the principle applied to this case); F2 the CLI fault summary rider now explicitly owned by otp-7b (7a's surface is the in-process roles suite, no CLI); F3 parent ONE_TRANSFER_PATH's "New features" non-goal now carries the narrow D-2026-07-09-1 exception; F4 stale 07-06 handoff "first action" annotated done. Verdict `.review/results/otp-7-plan-activate.gpt-verdict.md`. Next: implement **otp-7a** (resume over the in-stream carrier) per the plan's choreography + 4 guard-proof pins.

**2026-07-07 01:07:11Z** - **AUDIT (owner bug triage — `--exclude` no-op, claude)**: Owner reported `--exclude` "doesn't do anything": `blit mirror /home/michael/ /run/media/michael/USB_DRIVE -pvy --force --exclude /home/michael/.java` still descended into `.java/` and tried to write `.java/fonts/1.8.0_472/fcinfo-…-en.properties`, which then hit `audit-17`'s `os error 22` (`sink.rs:608`) on the FAT-family destination. Triaged and filed to `TODO.md` as **audit-19** — no code, no `docs/STATE.md` Queue change (backlog only; Queue stays pinned to ONE_TRANSFER_PATH per D-2026-07-05-4). Verified by reading the matcher end-to-end (not run): the filter *is* plumbed (local mirror enumerates via `FileFilter`, `transfers/local.rs:191` → `enumerate_directory_filtered`), so this is a matching-semantics bug, not a dropped filter. Two compounding root causes in `FileFilter::allows_entry` (`crates/blit-core/src/fs_enum.rs:194-240`): (1) excludes are matched against the source-root-*relative* path (`path_str`, `fs_enum.rs:211-213`) and the bare filename (`fs_enum.rs:207-210`), never the absolute path, so an absolute pattern `/home/michael/.java` is structurally unmatchable (globset needs a whole-string match; `glob_match` with no `*` is exact-equality, `fs_enum.rs:399-417`); `--exclude` → `exclude_files` only (`blit-app/src/transfers/filter.rs:42`), with no source-prefix stripping. (2) a directory pattern doesn't prune its subtree — even `--exclude .java` wouldn't drop `.java/fonts/…` (globset `*` doesn't cross `/`), and there is **no `--exclude-dir` flag** (the `exclude_dirs`/`dir_globs`/`should_include_dir` machinery at `fs_enum.rs:274-295` is never assigned anywhere in `crates/`); the only working incantation today is `--exclude '.java/**'`. Silent no-op — nothing warns that a pattern matched zero globs; `--help` (`cli.rs:292`) documents none of the source-relative / `/**` semantics, and rsync users expect leading-`/`-anchor and trailing-`/`-dir semantics blit doesn't implement. Fix needs an owner design call (`plan` before coding): accept absolute-under-source patterns / rsync-like dir semantics or a real `--exclude-dir` / at minimum warn on structurally-unmatchable patterns — applied uniformly across the one `FileFilter`/`FilterInputs` chokepoint (local-mirror/push/pull/remote-remote, `cli.rs:288-291`) with `--help`/manpage/README updates in the same change. The crash itself is `audit-17` recurring (now also reproduced under `blit mirror`, which audit-17 predicted since the same `write_tar_shard_payload` helper backs both paths); audit-19 is the *reported* bug — a working exclude would have skipped the tree and side-stepped that crash entirely. Next: otp-7 (unrelated; continues per `docs/plan/OTP7_RESUME.md`).

**2026-07-06 22:00:00Z** - **REVIEW + AUDIT (session-wide codex pass; CLI-output backlog item, claude)**: Owner invoked `/playbook reviewloop codex to review everything we've done this session`. The literal argument string didn't resolve to a real file, and `.agents/repo-guidance.md` already states the named generic template (`.agents/playbooks/reviewloop.md`) is not this repo's operative review loop anyway — its per-finding `fix/<id>-<slug>` branch model directly conflicts with the Earned Practice "no agent-created branches." Ran `docs/agent/GPT_REVIEW_LOOP.md`'s mechanism instead, scoped to the whole session diff (`9876687..44de868`, 14 commits, all docs). **Codex: CHANGES REQUESTED → 3 Medium + 2 Low, 5/5 accepted + fixed** (`419f5d1`). All were cross-file staleness/contradictions from mid-session edits not propagating everywhere they should have: (1) `LOCAL_ERROR_TELEMETRY.md`'s header still said Q5 was "pending final confirmation" after the Q5 section below had already recorded the resolution; (2) `STATE.md`'s Q5 timing note flatly said "not a D-2026-07-05-4 exception," contradicting the plan doc's own honest "in substance, an exception" framing (itself a fix from the prior review round) — reworded to match; (3) `STATE.md`'s handoff log was stale, still showing the audit-17/18 commit as newest and omitting the entire telemetry-plan saga since — added an entry, pruned a now-redundant one; (4) `DEVLOG.md`'s newest-first ordering looked broken (2026-07-06 entries above 2026-07-07-labeled ones) due to a pre-existing narrative-date-vs-wall-clock drift verified via `git log --format=%ad` (every "07-07" entry here was actually committed 2026-07-06) — added a one-line dated note at the boundary rather than rewriting any historical timestamp; (5) "every writer hardcodes error_count to 0" overstated the source — the one real production write path is `engine/history.rs:87-96`, the other three citations are test-only record constructors. Verdict: `.review/results/session-2026-07-06.gpt-verdict.md` (raw `.codex.md`). **Separately this session**: owner asked for a CLI transfer-output redesign — current `blit copy`/`mirror` output "doesn't convey any useful information," wants something closer to `rclone`/`cargo` (a persistent stat block at a static location plus a scrolling filename list). Confirmed by reading the code that no persistent/redraw rendering exists anywhere today, only scrolling `println!`/`eprintln!` lines: the local streaming-manifest heartbeat (`crates/blit-core/src/remote/push/client/helpers.rs:176`, same call site `audit-16` already flagged for a separate bug) and the remote-transfer progress path's once-a-second summary line (`crates/blit-cli/src/transfers/remote.rs:33-140`). Filed to `TODO.md` only (`598f102`) — not designed, needs its own `plan` when picked up, and is explicitly distinct from `docs/plan/TUI_REWORK.md` (the separate interactive `blit-tui` app). Next: otp-7 (unrelated to either of these — continues per `docs/plan/OTP7_RESUME.md`).

**2026-07-06 21:00:00Z** - **PLANNER (LOCAL_ERROR_TELEMETRY.md Q1-Q4 + v2 review, claude)**: Owner answered the plan's four open questions: **Q1** new dedicated `errors_local.jsonl` (matched prior rec); **Q2** message-chain only for v1, no `error_location` yet (matched prior rec); **Q3** BOTH local and remote-mediated routes, not local-only as previously scoped — a real reversal, so the `main.rs`-level chokepoint the prior review had moved OUT of scope moved back in, since it now matches the (widened) stated scope; **Q4** no size cap and unconditional (non-config-gated) recording for this development phase — owner clears manually via `--clear`, config-gated opt-in deferred to a follow-up. Folded into the doc (`906524e`) with a `route` field added to the schema. **Codex v2: CHANGES REQUESTED → 3/3 accepted + fixed** (`8ea2334`). F1 (Medium): the doc's "no fire-and-forget/detached path" claim was false — `RemoteToRemoteDelegated` honors `--detach` (`remote_remote_direct.rs:130-152`) and returns a synthesized `Ok` once the daemon confirms `Started`, so a post-Started failure never reaches `run_transfer`'s `Result`; scoped the doc to state precisely that this plan covers every route up to where the CLI stops observing it, which excludes a detached transfer past `Started` (that stays `recents.jsonl`'s job). F2 (Medium): the `route` field's derivation was hand-waved and had a real crate-layering bug — `TransferRoute`/`select_transfer_route` live in `blit-app` (`dispatch.rs:69,112`), which `blit-core` cannot depend on, and `run_transfer` can fail before route selection; fixed by making `route: Option<String>` a plain string label stringified in `blit-cli`/`blit-app` (which already see the enum), never derived inside `blit-core`'s `error_history`. F3 (Low): Q5's "without formally reopening D-2026-07-05-4" framing asserted a compatibility that doesn't actually hold given that decision's absolute wording — the owner's choice (option b, chosen live via `AskUserQuestion` with the tension stated) stands unchanged; the fix was to the doc's honesty about the tradeoff, not a reversal of the decision itself, since that isn't this review loop's call to make. Verdict: `.review/results/local-error-telemetry-plan-v2.gpt-verdict.md` (raw `.codex.md`). Also this session: recorded the Q5 resolution (pick up at ONE_TRANSFER_PATH's next natural pause, no formal exception) as a `docs/STATE.md` Open Questions timing note, not a Queue entry (`00245ed`). Next: still Draft — no code until the otp-7 pause point per Q5, or a further owner go.

**2026-07-06 20:15:00Z** - **PLANNER (LOCAL_ERROR_TELEMETRY.md draft, claude)**: Owner, hitting `audit-17` repeatedly (three times across two drives, each requiring a manual copy-paste of the crash into chat), asked for error reporting added to telemetry so failures can be "swept up" instead. Flagged the scope conflict first: `docs/STATE.md`'s Queue is pinned to ONE_TRANSFER_PATH exclusively (D-2026-07-05-4, "the only work item until it ships"); owner chose **Draft only, hold for later** — this plan is not in the Queue and builds no code until that gate lifts or the owner gives an explicit go. Research first (`perf_history.rs`/`perf_local.jsonl`): today's telemetry only records successful transfers; its `error_count` field is dead (hardcoded `0` everywhere); `record_performance_history` is only reached from the success path, so a hard failure like `audit-17`'s writes nothing. Drafted `docs/plan/LOCAL_ERROR_TELEMETRY.md` (`284f7f9`): a new sibling `errors_local.jsonl` (not a schema change to `perf_local.jsonl`), fed from a single chokepoint, plus a `blit diagnostics errors` read-back verb. **Codex: CHANGES REQUESTED → 3/3 accepted + fixed** (`ebb668f`). F1 (High): the proposed chokepoint (`main.rs`'s `Commands::Copy`/`Mirror` arms) actually sees every `TransferRoute` (local + remote + delegated) via `run_transfer`/`select_transfer_route`, not just local-to-local — contradicted the doc's local-only scope; relocated to the `TransferRoute::LocalToLocal` arm specifically (`transfers/mod.rs:235-241`), with a slice-2 test pinning that other routes don't record. F2 (Medium): recorder-failure handling said `log::warn!`, but `blit` installs a real stderr backend (`stderr_log.rs`) that would itself alter stderr on a recorder failure, contradicting the byte-identical-stderr acceptance criterion; switched to the existing `engine/history.rs` precedent (silent by default, `--verbose`-gated `eprintln!`). F3 (Low): dangling "(see D1)" references retargeted to the actual Q1 open question. Verdict: `.review/results/local-error-telemetry-plan.gpt-verdict.md` (raw `.codex.md`). Next: surfaced to the owner for Draft review — no implementation until Active.

**2026-07-06 19:45:00Z** - **AUDIT (owner bug triage, claude)**: Two owner-reported `blit copy` crashes triaged and filed to `TODO.md` as `audit-17` (`5628c03`) and `audit-18` (`deb3800`) — no code, no STATE.md Queue change (TODO.md is the exclusive backlog location per D-2026-07-05-4). Both are the same failure shape: one bad filename partway through a large local→removable-media copy aborts the entire transfer instead of being skipped/reported. `audit-17`: the destination (a FAT-family filesystem on removable media, inferred from the hex volume label) rejects a `:` character in a NuGet http-cache path; `write_tar_shard_payload`'s `create_dir_all` (`sink.rs:605`) surfaces the raw `Invalid argument (os error 22)` and kills the run — grepped the codebase for any existing invalid-filename handling and found none. `audit-18`: a non-UTF-8 source filename (valid on ext4) gets irreversibly mangled by `path_posix::relative_path_to_posix`'s per-component `to_string_lossy()` before any I/O is attempted; `build_tar_shard`'s `File::open` on the corrupted path 404s (`payload.rs:360`, `os error 2`) — traced to the single shared helper both local mirror and remote push use (`engine/mirror.rs`, `mirror_planner.rs`, `remote/transfer/{payload,tar_safety}.rs`, `remote/endpoint.rs`, `remote/push/client/helpers.rs`), so it isn't a remote/proto-only issue, though a full remote-side fix is complicated by proto3 `string` being UTF-8-only on the wire. Both findings flag a needed owner design call (skip/report/sanitize/fail-fast policy) before any code — filed, not fixed. Next: otp-7 (unrelated; continues per `docs/plan/OTP7_RESUME.md`).

*(Ordering note, added 2026-07-06: the entries below are labeled
2026-07-07 but, per `git log --format=%ad`, were committed on the real
date 2026-07-06 — a pre-existing narrative-date-vs-wall-clock drift that
predates this note, not a violation of "newest first." The three entries
above this line are genuinely newer (later commits, same real day); do not
"fix" the date strings below without separately confirming the actual
git timestamps, since a same-day relabel here could as easily introduce
an error as resolve one.)*

**2026-07-07 02:00:00Z** - **PLANNER (otp-7 resume slice design, claude)**: Drafted `docs/plan/OTP7_RESUME.md` (**Status: Draft**) for otp-7, the plan's explicit RELIABLE exception (block-hash resume). Owner was offered three scopes (7a-in-stream-then-7b / one-slice / plan-doc-first) and chose **plan-doc-first**, so NO CODE lands until the owner answers the doc's Q1–Q3 and flips it to Active. The doc records: the strict-ordering choreography (a `NeedEntry{resume=true}` → DEST sends `BlockHashList` → SOURCE holds that file until the list arrives, then sends only changed blocks `BlockTransfer`/`BlockTransferComplete` → DEST patches its partial in place); the reuse map — the wire frames, the sink apply path (`FsTransferSink::write_file_block_payload`/`_complete`), block hashing (`compute_block_hashes`), the block-diff reference (`resume_copy_file`), and the 1 MiB default all already exist, so the new work is only the exchange choreography + a home for the SOURCE block-diff; design decisions (**D1** graceful stale-partial full-file fallback per the contract, reconciling the old data-plane hard-error against the gRPC/contract behavior; **D3** the source block-diff as a session free helper, not a `TransferSource` trait method — same reasoning that made `FilteredSource` the one filter chokepoint; **D4** keep the in-place-patch mid-failure model, no temp+rename, matching the code being replaced); staging **7a in-stream / 7b data-plane** (matching otp-4b/5b); and the four guard-proof targets (partial-resume, identical-file, stale-partial fallback, mid-resume-failure). Parent `ONE_TRANSFER_PATH.md` otp-7 slice line gains a pointer. No validation gate (docs-only; `check-docs.sh` green). Per D-2026-07-04-1 the plan change itself still owes a codex-loop review — deferred until the owner finalizes the design (avoids reviewing a draft that may change). Next: owner review → Active → codex-review the plan → implement otp-7a.

**2026-07-07 00:45:00Z** - **CODER (otp-6b mirror on the session — the one delete rule, claude)**: The unified session now MIRRORS (`01d9c41` + review fixes `3c99557`). The DESTINATION — the end that holds the tree, whichever end initiated — accumulates the COMPLETE source manifest and, at SourceDone (after every payload is written, so the tree is final), computes the extraneous set (present at dest, absent from source) and deletes it locally, filling `TransferSummary.entries_deleted`. This is the ONE delete rule that replaces the three divergent per-direction purges (local engine `apply_mirror_deletions`, daemon push `purge_extraneous_entries`, client pull `delete_listed_paths`) — the old paths keep working until the otp-10 cutover deletes them. **blit-core** (`mirror_planner.rs`): new `plan_session_deletions(dest_root, source_files, filter)` reuses the tested `plan_from_sets`; the session manifest is files-only (dirs implicit — `spawn_manifest_task`), so a dir's survival is derived from the parent chains of the kept files (a dir is extraneous iff no kept file lives under it), the same derivation the daemon's `plan_extraneous_entries` does. **blit-core** (`transfer_session/mod.rs`): new `mirror_delete_pass` (blocking pool) deletes files then dirs deepest-first, each containment-checked against the canonical dest root; `remove_dir` (not `remove_dir_all`) so out-of-scope content is never removed — FilteredSubset tolerates ENOTEMPTY (dir holds filter-excluded files), All expects empty dirs. `destination_session` accumulates `source_files` when mirroring, refuses at `ManifestComplete{scan_complete=false}` (an incomplete scan would misjudge a still-unread source file extraneous — data loss), and runs the pass at SourceDone. Scope: FilteredSubset enumerates the dest through the user filter; All enumerates the whole tree — matching proto `MirrorMode`. `destination_open_validator` stops refusing mirror; now refuses enabled+OFF/UNSPECIFIED kind (protocol violation) and validates filter globs at OPEN. **Codex: NEEDS FIXES → 2 accepted + fixed** (`3c99557`). **F1 (High)**: the keep-set used `CasefoldKey`, which folded case only on Windows; the session is the first mirror path to diff a WIRE source set (source on-disk case) against the DEST filesystem, so on a case-insensitive dest (APFS/macOS) the write path treats `Foo.txt`/`foo.txt` as one file while the exact keep-set treats the dest entry as absent and DELETES the just-written file. Fixed by extending the folding `CasefoldKey` variant to macOS (`any(windows, target_os = "macos")` folds, else exact) — case-sensitivity is a platform-default-FS property, which is what the key models; this also improves the retired paths on macOS. **F2 (Med)**: `mirror_delete_pass` skipped `win_fs::clear_readonly_recursive`, so a read-only extraneous entry would fail the pass on Windows (the two old executors clear it first) — fixed under `cfg(windows)`. Verdict `.review/results/otp-6b.gpt-verdict.md` (raw `.codex.md`). **Guard proofs (run live on Linux)**: (1) neutralize the delete pass (`if false`) → `mirror_all_purges_extraneous_under_both_initiators` FAILS (stale file remains); (2) neutralize the scan-complete guard → `mirror_refused_when_source_scan_incomplete` no longer refuses. Both fixes are macOS/Windows-specific and CANNOT be guard-proven on the Linux dev box: a cfg-gated `CasefoldKey` fold test runs on macOS/Windows CI, a Linux exact-match test pins no-regression, and the F2 `win_fs` call matches the two existing executors verbatim (windows-latest CI verifies). Removed the obsolete `mirror_request_is_refused_until_its_slice_lands`. 5 new mirror tests (all-purge under both initiators, filtered-subset preserve, all-purge-out-of-scope, enabled-without-scope refused, scan-incomplete refused). Suite 1524 → **1529/0** (+5 mirror −1 obsolete +1 Linux casefold). fmt/clippy clean. **otp-6 fully closed** (6a filters + 6b mirror). Next: **otp-7** (resume block phase — ordering + stale-partial pins per the plan's RELIABLE exception). Known gaps: Unicode (non-ASCII) case folding still approximate; case-insensitive Linux mounts / case-sensitive macOS volumes are undetectable by a compile-time cfg (documented, rsync/robocopy share this). (Unrelated `Cargo.lock` drift still unstaged.)

**2026-07-06 22:30:00Z** - **CODER (otp-6a filters on the session, claude)**: The unified session now honors `SessionOpen.filter` (include/exclude/size/age globs) on the SOURCE scan instead of refusing any non-default filter (`c026692` + review fix `0bb27f5`). otp-6 ("mirror + filters, one delete rule") splits into 6a (filters, this) + 6b (mirror), mirroring the otp-4b/5b staging — filters land first because the mirror FilteredSubset scope is defined in terms of the filter. **blit-core** (`transfer_session/mod.rs`): `source_open_validator` stopped refusing a non-default filter and now VALIDATES the globs (`operation_spec::filter_from_spec`, faulting `protocol_violation` on a malformed pattern) — because the validator runs on the responder's *received* open, a hostile/buggy peer's bad glob is refused at OPEN and peer-notified before any bytes move. `source_send_half` applies the filter. `operation_spec::filter_from_spec` widened to `pub(crate)`. **Codex: FAIL → 1 Medium accepted + fixed.** F1 (Med, `0bb27f5`): the first cut threaded the filter through the per-impl `TransferSource::scan(filter)` argument, which ONLY `FsTransferSource` honors — `RemoteTransferSource::scan` ignores it and relies on the universal `FilteredSource` decorator being the single filter chokepoint. A session whose source is a `RemoteTransferSource` (the otp-9 delegated / remote→remote relay) with `open.filter` set would silently transfer everything; the daemon-as-source pull path happened to work only because `run_responder` builds an `FsTransferSource`. Fix: route filtering through `FilteredSource` — `source_send_half` wraps the source in the decorator when `open.filter` is present, then scans through the wrapper, so filtering is correct-by-construction for every inner source impl. Verdict `.review/results/otp-6a.gpt-verdict.md` (raw `.codex.md`). Guard proofs (run live): (1) feature — revert the scan-filter wiring → all 4 files transfer → `source_filter_limits_manifest_under_both_initiators` FAILS (expects 2); (2) fix mechanism — a new `FilterIgnoringSource` (delegates to an inner `FsTransferSource` but drops the `scan` arg, modelling `RemoteTransferSource`) drives `session_filters_via_chokepoint_not_scan_arg`; reverting the fix to the scan-arg approach → the ignoring source drops the filter → all files transfer → FAIL; restored → PASS. Suite 1522 → **1524/0** (+2 role-suite tests), fmt/clippy clean. Codex confirmed clean: peer-glob refusal at OPEN, no bytes on a refused handshake, count +1 (before the second guard test). Next: **otp-6b** (mirror on the session — the one delete rule: DESTINATION accumulates the complete source manifest, scan-complete-guarded, filter-scoped extraneous delete, `entries_deleted` in the summary). (Unrelated `Cargo.lock` drift still unstaged — STATE open item.)

**2026-07-06 20:15:00Z** - **CODER (otp-5b-2 pull data-plane resize, claude)**: The pull data plane now grows mid-transfer via the sf-2 shape correction, lifting otp-5b-1's single-stream cap (`d579365` + review fix `773a877`). The resize control-lane frames (`DataPlaneResize{ADD}`/`DataPlaneResizeAck`) are IDENTICAL to push; only socket **acquisition** flips per connection role — the SOURCE **responder** ACCEPTS each epoch-N socket off its bound listener, the DESTINATION **initiator** DIALS it. All resize machinery is reused. **blit-core** (`data_plane.rs`): `SourceDataPlane` gained a `SourceSockets` enum (`Dial{host,tcp_port}`=initiator/push, `Accept{listener}`=responder/pull) replacing the `host`/`tcp_port`/`resizable` fields; `accept_source_data_plane` retains its bound listener instead of dropping it; `propose_resize` lost the `!resizable` early-return (both directions propose); `add_stream` branches — the initiator dials the epoch-N socket (unchanged), the responder `accept_authenticated`s the socket the DEST dials after its ack (`session_token ‖ sub_token`), then hands it to the elastic pipeline via `SinkControl::Add`. The accept is bounded + unambiguous (≤1 resize in flight via the dial's `pending_epoch`; epoch-0 already accepted ⇒ next connection is exactly this resize — no arm set needed on the SOURCE). `InitiatorReceivePlaneRun` retained host/port/token/sink and gained `add_dialed_stream` (dial epoch-N, spawn receive worker, bump settled count). **blit-core** (`mod.rs`): `destination_session`'s initiator branch seeds `resize_live` from the epoch-0 streams and `resize_ceiling` from the capacity THIS end advertised (not a fresh local read — see F1), replacing the otp-5b-1 `(0,0)` that made every `Resize` a violation; the `Frame::Resize` handler branches arm (responder) vs dial (initiator). Ordering: the DEST initiator dials BEFORE it acks, so the SOURCE responder — which accepts on the ack — never commits to an accept the peer did not dial; a dial failure is fatal (symmetric with push's SOURCE dial), firing before any commit. **Codex: NEEDS FIXES → 1 Low accepted + fixed.** F1 (Low, `773a877`): the DEST initiator's resize ceiling read a fresh `local_receiver_capacity()` instead of the `receiver_capacity` it advertised in `SessionOpen` (`run_destination` keeps a caller-provided profile); the SOURCE responder's dial clamps to the advertised value, so the two ends could disagree on the ceiling. Now reads `negotiated.open.receiver_capacity.max_streams` — both ends use the identical bound. Same-build happy path was already protected (the SOURCE never proposes beyond what it clamped to), hence Low; the fix aligns the code with its own comment. Verdict `.review/results/otp-5b-2-pull-data-plane-resize.gpt-verdict.md` (raw `.codex.md`). Guard proof (run live, on the fixed code): force the DEST initiator ceiling to 0 → `pull_data_plane_shape_corrects_to_more_than_one_stream` FAILS "settled at 1" while `pull_data_plane_single_stream_lands_bytes` still passes (byte correctness independent of stream count); restored → both pass. Tests 1521 → **1522** (+1 roles `pull_data_plane_shape_corrects_to_more_than_one_stream`: 10k tiny files, DEST-initiator/SOURCE-responder over real loopback TCP, `data_plane_streams > 1`; the single-stream test's rationale comment corrected — small tree below the shape threshold, not a disabled resize). fmt/clippy clean. Next: **otp-6** (mirror + filters on the session, one delete rule) — otp-5b-3 (mid-transfer cancel over the pull data plane) is optional (the CANCELLED framing is already role-agnostic; a distinct guard may be unnecessary). (Unrelated `Cargo.lock` drift still unstaged — STATE open item.)

**2026-07-06 18:32:35Z** - **CODER (otp-5b-1 single-stream SOURCE-responder data plane, claude)**: The pull-equivalent now rides the TCP data plane by default (`e6a0b3b` + review fix `13485ee`). The data plane was keyed to ROLE (DESTINATION binds+accepts+receives; SOURCE dials+sends) — a coincidence that holds only for push. otp-5b splits the two axes: the **connection role** (RESPONDER binds+accepts, INITIATOR dials — NAT reality) and the **byte role** (SOURCE sends, DESTINATION receives). This slice (5b-1) adds the pull pair single-stream: a SOURCE **responder** that accepts+sends and a DESTINATION **initiator** that dials+receives; resize (the accept-based epoch-N socket) is otp-5b-2. **blit-core**: `responder_finish` binds a data plane for EITHER role when `!in_stream_bytes` (was DESTINATION-only); the bound listener travels in `Negotiated.responder_data_plane` and is consumed by whichever role's driver runs (`receiver_capacity` in the accept stays DESTINATION-only — the byte RECEIVER advertises, and a DESTINATION initiator advertises in its own open). New `data_plane::accept_source_data_plane` accepts the granted epoch-0 socket(s) off the bound listener, wraps each via `DataPlaneSession::from_stream`→`DataPlaneSink` (the same primitive old `pull_sync` daemon-send used), and drives the SAME elastic send pipeline `dial_source` builds — single-stream (`resizable=false`, so `propose_resize` returns `None`, no `DataPlaneResize` flows). New `data_plane::dial_destination_data_plane` + `InitiatorReceivePlaneRun` dials the epoch-0 socket(s) and drains each via `execute_receive_pipeline` (same `NeedListSink` strictness); the `DestRecvPlane` enum tags the DESTINATION receive by connection role (`Responder` accept / `Initiator` dial) and `finish()` reports the settled stream count either way. `source_send_half` selects accept-vs-dial by whether it holds a bound listener or a received grant; `destination_session` selects dial-vs-accept the mirror way; a `Resize` on the single-stream pull data plane is a protocol violation. `DestinationSessionConfig` gains `data_plane_host` (the initiator dials the responder's host), symmetric with `SourceSessionConfig`. **client**: `run_pull_session` defaults to the TCP data plane (`in_stream_bytes` from `PullSessionOptions`, default false) with `data_plane_host = endpoint.host`; `PullSessionOptions` gains the in-stream fallback knob. **Codex: FAIL → 1 Medium accepted + fixed.** F1 (Med, `13485ee`): a DESTINATION initiator that received a grant but had no `data_plane_host` silently took the in-stream branch — it cannot fall back (the SOURCE responder has already bound and blocks on accept), so it degraded to a 30s accept-timeout hang; now it fails fast with an INTERNAL fault mirroring the symmetric SOURCE guard in `source_send_half`. Push path, resize suppression, byte-accounting, StallGuard, AbortOnDrop, handshake, in-stream fallback all confirmed intact. Verdict `.review/results/otp-5b-source-responder-data-plane.gpt-verdict.md` (raw `.codex.md`). Guard proof (run live): revert the SOURCE-responder bind (grant no data plane) → both new data-plane tests fall back to in-stream and FAIL `!in_stream_carrier_used`; restored → all pass. Tests 1519 → **1521** (+1 roles `pull_data_plane_single_stream_lands_bytes` DEST-initiator/SOURCE-responder over real loopback TCP, `data_plane_streams==Some(1)`; the old in-stream-only pull e2e split into `pull_session_lands_bytes_over_the_data_plane` default-TCP + `..._over_in_stream_carrier` fallback; the existing A/B vs old `pull_sync` now runs over the data plane — converge-up bar). fmt/clippy clean. **Slice sizing**: otp-5b decomposed into 5b-1 (single-stream, this) + 5b-2 (resize) mirroring otp-4b-1/-2. Next: **otp-5b-2** (mid-transfer shape-correction resize for the pull direction — the accept-based epoch-N socket: SOURCE responder proposes, DESTINATION initiator dials+acks). (Unrelated `Cargo.lock` drift still unstaged — STATE open item.)

**2026-07-06 07:30:00Z** - **CODER (otp-5a daemon serves both roles / pull-equivalent, in-stream, claude)**: The one served `Transfer` RPC now handles BOTH directions by the client's declared `initiator_role` — no second code path (`84be1cc`). A DESTINATION initiator makes the daemon the SOURCE Responder (streams its module tree, the pull-equivalent); a SOURCE initiator keeps otp-4's push unchanged. **blit-core**: `establish` split into `exchange_hello` + `responder_finish` (the responder half after the open is read) so a serving end can read the `SessionOpen`, learn the initiator's role, and only then pick a driver — `establish` keeps its shape for the direct role drivers (the in-process role suite). New `run_responder` is the daemon's single serving entry: HELLO, read open, dispatch (SOURCE-initiator→local DESTINATION, DESTINATION-initiator→local SOURCE), returns `ResponderOutcome::{Destination,Source}`. New `SourceResponderTarget::{Fixed,Resolve}` mirrors `DestinationTarget`; `Resolve` reuses the SAME `OpenResolver` the destination path uses to map module→root, then builds `FsTransferSource` inside blit-core (read-only ignored for a SOURCE — reading a read-only module is fine). `run_source`/`run_destination` bodies factored into `drive_source`/`drive_destination` (both keep the fault-notify wrapping) so all three entries share one session loop; `source_send_half` now takes `plan_options` + `data_plane_host` directly, not the whole `SourceSessionConfig`. **daemon**: `run_transfer_session` dispatches via `run_responder` (both resolvers from `make_open_resolver`); both outcome arms map to the jobs record as before (kind stays `Push` until cutover). **client**: `run_pull_session` (DESTINATION initiator, `DestinationTarget::Fixed`, in-stream) — not wired to CLI verbs (otp-10). **Staging**: otp-5 split into 5a (in-stream, this) + 5b (data plane), mirroring otp-4a/4b — the data-plane transport/role decoupling (responder binds+grants while SENDING; initiator dials while RECEIVING; today it's keyed to role) is genuinely new work and is otp-5b. **Codex: PASS, no findings** (verdict `.review/results/otp-5a-daemon-as-source.gpt-verdict.md`; raw `.codex.md`) — affirmed the establish split / drive_* factoring preserve otp-4 behavior and the guard is sound. Guard proof (run live pre-commit): force the responder to serve DESTINATION unconditionally → the 3 pull tests FAIL with `PROTOCOL_VIOLATION` (complement check refuses a DESTINATION initiator), the 7 push tests still pass; restored → all pass. Tests 1516 → **1519** (+3 e2e: `pull_session_lands_bytes_and_scores_them`, `old_pull_and_session_produce_identical_trees_and_counts` A/B vs old `pull_sync` byte-identical + equal counters, `unknown_module_refuses_the_pull_session`); role suite (14) still green after the establish refactor. fmt/clippy clean. **Reviewer-incantation gotcha found + fixed in the loop doc**: `codex exec "<prompt>"` on codex-cli 0.142.5 appends stdin as a `<stdin>` block and BLOCKS on EOF when stdin is open — the first review run hung at 0 CPU indefinitely; `</dev/null` closes stdin and it starts. `docs/agent/GPT_REVIEW_LOOP.md` step 4 gained the redirect + a load-bearing note. Known gaps → otp-5b (SOURCE-responder data plane); mid-transfer cancel e2e for pull (mechanism is direction-agnostic, deterministic test needs the data plane like otp-4b-3); mirror otp-6, resume otp-7. Next: otp-5b. (Unrelated: `Cargo.lock` has transitive version-bump drift in the working tree — not part of this slice, left unstaged; STATE Cargo.lock open item.)

**2026-07-06 05:37:00Z** - **CODER (otp-4b-3 deterministic mid-transfer cancel e2e, claude)**: A `CancelJob` fired while payload bytes are in flight over the session's TCP data plane now surfaces to the client as `SessionFault{CANCELLED}` — the peer's framed abort reason — instead of the data-plane `Broken pipe` (INTERNAL) the cancel also causes, and never hangs (`3ae0a5f` + review fixes `a530005`, `46cc4bb`; records `db9b63d`). **Problem found while building the e2e** (throwaway gated-source experiment): the daemon side was already correct (otp-4a codex F1 — the served `Transfer` dispatcher drops `run_destination` and frames `SessionError{CANCELLED}`), but the SOURCE only consulted the control lane when parked at `events.recv()`. During the payload **drain** (`SourceDataPlane::finish`, where a push spends its byte-transfer wall time) it awaited only the data-plane pipeline, so a cancel dropped the destination → the source's socket write hit `Broken pipe` first → the client surfaced INTERNAL, and a worker blocked reading a slow file (never writing) → the socket break never unblocked it → **client hung**. **Fix** (`transfer_session/mod.rs`): `source_send_half` races the drain against a peer-framed fault on the control lane. `recv_peer_fault` (strict; a `biased` `select!` arm against `dp.finish()`) wins if the fault arrives while the drain is still pending — dropping the unfinished `finish()` future drops the `SourceDataPlane`, whose `AbortOnDrop` stops in-flight workers (this is what makes the blocked-reader case terminate as CANCELLED instead of hanging). `prefer_peer_fault` (lenient) handles the socket-break-first ordering at BOTH the `queue()` and `finish()` error paths — prefers the framed reason within `TRANSFER_STALL_TIMEOUT`, skipping still-in-flight `Need`/`ResizeAck` events (a `Need` can queue ahead of `CANCELLED` during the payload loop), else the raw data-plane error. **Codex: 3 passes.** Pass 1 (`3ae0a5f`) NEEDS FIXES, 3/3 accepted: F1 (High) `dp.queue()` not fault-preferred (multi-file backpressure cancel → DATA_PLANE_FAILED); F2 (Med) the e2e's bytes-flowed gate fired on a 256 KiB local duplex before TCP; F3 (Med) `recv_peer_fault` silently dropped non-fault drain events. Pass 2 (`a530005`) NEEDS FIXES, 1/1 accepted: the F1+F3 pair interacted — routing `queue()` errors through the now-strict `recv_peer_fault` could surface a spurious `PROTOCOL_VIOLATION` from a legitimately-queued `Need`; fixed by splitting the helpers (strict `recv_peer_fault` for the live drain select arm only; lenient `prefer_peer_fault` for both error paths). Pass 3 (`46cc4bb`) **PASS**. Guard proofs: e2e `mid_transfer_cancel_surfaces_cancelled_over_the_data_plane` (revert the select → client hangs → 10 s timeout FAIL), unit `prefer_peer_fault_prefers_a_framed_fault` (revert → wrong code) + `prefer_peer_fault_skips_inflight_needs_to_reach_the_fault` (pass-2 regression guard). Suite 1513 → **1516/0**, fmt/clippy clean. Verdict `.review/results/otp-4b3-data-plane.gpt-verdict.md`. Known gap: a cancel while a worker is blocked reading a slow local file *inside* an early `queue()` (channel full) can still hang until the peer's stall guard fires — pre-existing slow-local-read pathology, not cancel-specific. **otp-4b fully closed** (4b-1/4b-2/4b-3). Next: otp-5 (daemon-as-SOURCE / pull-equivalent).

**2026-07-06 00:30:00Z** - **CODER (otp-4b-2 resize + multi-stream + sf-2 pin, claude)**: The unified session's data plane now grows mid-transfer — the zero-knowledge single-stream grant shape-corrects upward as the need list accumulates, over real sockets (`dce56de`). **SOURCE owns the live dial** (`TransferDial::conservative_within(receiver_capacity)`, seeded to the granted epoch-0 count; pool provisioned for `ceiling_max_streams`, matching old push's lazy-alloc rationale). As needs accumulate it re-runs the shape table (`initial_stream_proposal(needed_bytes, needed_count, ceiling)`) and calls `propose_shape_resize` — one ADD per epoch, one in flight (the dial's `pending_epoch` CAS); mints a 16-byte sub-token, sends `DataPlaneResize{ADD}` on the control lane, and on `DataPlaneResizeAck` dials the epoch-N socket (`session_token ‖ sub_token`) and `SinkControl::Add`s it to the now-**elastic** pipeline (`execute_sink_pipeline_elastic`). `source_send_half` gained `needed_bytes/count` accumulators, `maybe_propose_resize`, `process_source_event` (ResizeAck → dial + settle + propose-next ramp), and `resolve_in_flight_resize` (drains the one in-flight proposal BEFORE `dp.finish()` so no armed dest slot orphans). **DESTINATION** runs a resize-aware `accept_loop` (`ResponderDataPlane::spawn` → select over arm-channel / bounded accept / worker-join); the control loop handles `Frame::Resize` (ceiling-checked arm + `resize_live` bump + `DataPlaneResizeAck`), and at SourceDone `finish()`es the run for the settled stream count on `DestinationOutcome.data_plane_streams`. **Orphan-free by construction**: a source resize-dial failure is FATAL (session faults → AbortOnDrop kills the dest accept loop), a deliberate simplification vs old push's arm-TTL recovery (noted in the finding doc). **Bug caught in pre-commit e2e**: the dest accept loop busy-spun once `arm_tx` dropped — a closed `mpsc` resolves `recv()` to `None` every poll and, as the biased-first select arm, starved `join_next`, so finished receive workers were never collected and `finish()` hung (reproduced on the gRPC data-plane e2e); fixed by parking the arm branch on `pending()` once closed (the same guard `execute_sink_pipeline_elastic` uses). sf-2 pin ported: `many_tiny_files_shape_correct_to_more_than_one_stream` (10k tiny files over the data plane settle `data_plane_streams > 1`, guard-proven: neuter the proposal → settles at 1 → pin fails). **Codex: PASS, no findings** (source resize sequencing, dest accept/finish teardown, byte accounting, StallGuard wiring, sf-2 pin all sound; verdict `.review/results/otp-4b2-data-plane.gpt-verdict.md`). Suite 1512 → **1513/0**, fmt/clippy clean. Known gaps → otp-4b-3 (mid-transfer cancel e2e), cheap-dial live tuner still deferred. Next: otp-4b-3.

**2026-07-05 23:00:00Z** - **CODER (otp-4b-1 TCP data plane onto the session, single stream, claude)**: The push-equivalent now rides the real TCP data plane, not the in-stream carrier (`881d412` + review fix `e1aafcc`). New `blit-core/src/transfer_session/data_plane.rs` owns the session's OWN data-plane choreography, reusing blit-core primitives (`DataPlaneSession` framing, `execute_receive_pipeline`, `execute_sink_pipeline_streaming`, `dial_data_plane`, `TransferDial`/`initial_stream_proposal`, `generate_sub_token`) — **no call into `remote::push` or the daemon push service** (those per-direction drivers die at otp-10; codex confirmed the boundary clean). DESTINATION responder binds `0.0.0.0:0`, mints session_token + epoch0_sub_token (16B each), computes `initial_streams = initial_stream_proposal(0,0,ceiling) == 1` (the grant is issued before any manifest — so the session data plane always starts single-stream; multi-stream is resize-only, otp-4b-2), and grants them in `SessionAccept`; a bind/RNG failure falls back to a grant-less accept (in-stream). SOURCE dials up front (before streaming the manifest, so the DEST accept task sees the connection before its bounded-accept timeout), authenticates `session_token ‖ epoch0_sub_token`, and feeds planned payloads to `execute_sink_pipeline_streaming` over a `DataPlaneSink`; `finish()` drains + writes each socket's END before `SourceDone`. DEST arms the accept+receive task concurrent with the diff loop (AbortOnDrop-bounded); control-lane payload frames under a data plane are `PROTOCOL_VIOLATION`. Default carrier flipped to the data plane (`PushSessionOptions.in_stream_bytes`, default false); in-stream stays the requested `--force-grpc`-shaped fallback. **A/B parity over the data plane** vs old push holds byte-identically. **Codex: FAIL, 2/2 accepted+fixed** (`e1aafcc`): **F1 (High)** completion was a weak count proxy (`files_written == needed_paths.len()`) — a peer could substitute/duplicate paths or send non-resume block records and pass; fixed by a shared `outstanding` need set both carriers claim from (control loop inserts before each NeedBatch → insert-happens-before-payload, no race) enforced on the data plane by a new `NeedListSink` decorator (membership + no-duplicate + reject resume blocks), completion is `outstanding.is_empty()` for both carriers; guard-proven by `need_list_sink_enforces_membership_and_rejects_blocks`. **F2 (High)** no read-side StallGuard on the data-plane receive — a peer that auths then stalls hung the DEST at `recv.join()`; fixed by wrapping each accepted socket in `StallGuard::new(socket, TRANSFER_STALL_TIMEOUT)` (matching old push). Nothing rejected. Suite 1509 → **1512/0**, fmt/clippy clean. Re-review of `e1aafcc` (the fix added shared-set concurrency + a sink decorator) in flight. Known gaps → otp-4b-2 (resize + multi-stream + sf-2 pin), otp-4b-3 (deterministic mid-transfer cancel e2e). Next: otp-4b-2.

**2026-07-05 21:30:00Z** - **CODER (otp-4a daemon serves Transfer, client SOURCE, claude)**: The push-equivalent rides the unified session end to end (`4b07bbb` + review fix `25f538b` + sha `fe4ad6d`). The daemon `Transfer` RPC — UNIMPLEMENTED since otp-1 — now runs `run_destination` as Responder; `core.rs::transfer` mirrors `push` (jobs row + cancel/hangup race). New gRPC `FrameTransport` adapters (`transport.rs`: `GrpcFrameRx` over `tonic::Streaming`, client/daemon `FrameTx`, `grpc_{client,daemon}_transport`). New client entry `session_client::run_push_session` runs `run_source` as SOURCE initiator. **Responder-resolution API**: `run_destination`'s 3rd param is now `DestinationTarget::{Fixed,Resolve}`; a `Resolve` responder maps the received `SessionOpen`→root via an async `OpenResolver` threaded through `establish` (after `validate_open`, before `SessionAccept`, so a refusal REPLACES the accept). Daemon closure (`service/transfer.rs::make_open_resolver`) wraps the push module-resolution + F2 containment chain, mapping `tonic::Status`→`SessionFault` (blit-core stays Status-free). **Compare fork resolved** (workflow-verified): the sole push/pull divergence is same-size+dest-newer (push clobbers, pull/session safely skip); session keeps the safe skip (converge-up + data-safety; shared arm untouched → live pull_sync unchanged; no test pinned push's clobber). Pinned by `same_size_newer_destination_is_skipped_not_clobbered`. **⚠ narrow owner-ack flagged**: "byte-identical vs old push" isn't literally achievable in that cell; `--force` overrides (STATE open question). **A/B parity proven**: same fixture through old push (TCP data plane) and the session (in-stream) → byte-identical trees + equal shared summary counters. **Codex: NEEDS FIXES, 1/1 accepted+fixed**: F1 (Medium) cancel emitted `Status::cancelled` not a `SessionError{CANCELLED}` frame → `session_error_frame` helper + `resolve_transfer_session_outcome`, unit-guarded by revert. Nothing rejected. Known gaps → otp-4b: TCP data plane, resize, sf-2 pin ported, deterministic mid-transfer cancel e2e; jobs-row endpoint population; daemon-as-SOURCE (otp-5). Suite 1501 → **1509/0**, fmt/clippy clean. Next: otp-4b.

**2026-07-05 18:30:00Z** - **CODER (otp-3 TransferSession core, claude)**: The unified session exists and moves real bytes (`ef9ffa1` + review fix `d5796a1` + sha append `85bf611`). New `blit-core/src/transfer_session/`: `FrameTransport` (FrameTx/FrameRx halves, `in_process_pair()` on bounded mpsc), ONE `establish()` hello/open/accept phase both role drivers call (strict same-build exact-match refusal naming both ids, D-2026-07-05-2; responder refuses mirror/filters/resume with the slice that lands them — fail-fast, no silent ignore), `run_source` (send half + always-draining recv half — bounded-transport deadlock-freedom; needs validated against the sent-manifest map so buffering is bounded by construction; payloads planned per accumulated need batch via `plan_push_payloads`, in-stream records strictly serialized after ManifestComplete), `run_destination` (sequential loop; 128-entry chunked stat+compare on the blocking pool; file records stream through `FsTransferSink::write_file_stream` via a bounded duplex pipe; tar records buffer to exactly `archive_size` ≤ tar cap then ride the tar-safety unpack; grammar violations → `PROTOCOL_VIOLATION` + abort; diff stats go through the R46-F3 canonical-containment chokepoint). Destination diff = new public `manifest::header_transfer_status` (extracted from `compare_manifests`, which now delegates) — scoping's `filter_unchanged` pointer was unimplementable on a wire destination (stats both sides); deviation recorded. **The owner's invariance property is now tested**: the role suite runs every fixture under BOTH initiator layouts and pins identical need sets + summaries + byte-identical trees. **Codex: FAIL, 2/2 accepted+fixed**: F1 (High) build identity could false-match (unknown collapse; dirty rebuild under stale clean sha) → non-collapsing forms `<sha>.dirty.<content-hash>` / `unknown.<entropy>`, build.rs re-samples on `.git/{HEAD,refs,index}` + dirty paths + blit-core src + proto, contract §Invariants 2 annotated, residual window flagged in the verdict file; F2 (Medium) early NeedComplete accepted silently → arrival-time gate in the recv half, pinned by a 500-entry scripted-peer test (guard proven by revert). Nothing rejected. Known gaps carried: SizeMtime semantic divergence (push exact-equality vs pull newer-than — otp-4 A/B parity pins force the unified pick), checksum-mode always transfers (no header checksums), strict SourceDone (no vanish-tolerance), cadence knobs at otp-4. Suite 1484 → **1501/0**, fmt/clippy clean. Next: otp-4 (daemon serves `Transfer`, client initiates as SOURCE); otp-2 stays rig-gated.

**2026-07-05 10:00:00Z** - **DECISION + CODER (D-2026-07-05-4 Active flip; otp-1 wire contract, claude)**: Owner: "flip the plan and go" + per-slice codex loop re-affirmed → **ONE_TRANSFER_PATH Active** (D-2026-07-05-4, records `3a87ba0`). **otp-1 landed through the codex loop** (`a3e2acb` + review fix `f861579`): `docs/TRANSFER_SESSION.md` (authoritative contract) + the `Transfer` RPC and full session message set in proto + UNIMPLEMENTED stubs (daemon service + five test fakes) + an in-process reachability pin (real client → real service → UNIMPLEMENTED, not UNKNOWN). Contract highlights: ONE role-tagged frame vocabulary both wire directions (no push/pull-shaped message sets exist); SessionHello same-build exact-match handshake FIRST (D-2026-07-05-2, mismatch names both build ids); DESTINATION owns the diff; receiver capacity travels DESTINATION→SOURCE whichever end holds the role (D-2026-06-20-1/-2); sf-2 shape correction = the only stream policy, SOURCE always the resize controller; resume = strictly-ordered RELIABLE exception; mirror deletes destination-local (no delete list on the wire); deliberately absent: PeerCapabilities, spec_version, delete lists. Codex **NEEDS FIXES 6/6 accepted**: closing flow was drawn on initiator/responder lanes (wrong for initiator=DESTINATION — redrawn on ROLE lanes); `initial_streams` respecified as an ACCEPT CEILING so a destination responder can never choose the sender's dial; exact per-socket auth handshake; in-stream carrier record grammar (strict serialization, size-inferred completion, payload-after-ManifestComplete); flow control + NeedComplete ordering named; error-field doc/proto drift. Suite 1483 → **1484/0**, fmt/clippy clean. REVIEW.md gains the otp section. Also this session, before the flip: aarch64-musl cross-build recipe proven (static blit/blit-daemon EXECUTING on zoey, the UNAS zero-copy rig — recipe + rig facts in STATE queue item 5; all zoey activity restricted to the owner's blit-temp folder, zero-copy test pre-authorized there). Next: otp-3 (TransferSession core) — otp-2 (symmetric baseline) is rig-gated and may run out of order, before otp-10.

**2026-07-05 08:45:00Z** - **DECISION (D-2026-07-05-3 — zero-copy receive unparked, claude)**: Owner declared the D-2026-06-12-1 revisit gate met: his UniFi UNAS 8 Pro daemon target is CPU-bound below 10 GbE even from SSD cache — exactly the CPU-bound-receiver case the gate contemplated (skippy's 32-core rig measured 1.43 receive cores at 9.5 Gbit/s and did NOT trip it). Recorded with two clarifications: the dead `zero_copy.rs` module still deletes in w8-1 (EAGAIN busy-wait draft — rewrite, not revival), and the capability returns the one-path way per the same-day owner exchange: a runtime-selected write strategy inside ONE_TRANSFER_PATH's unified receive sink (eval doc's AsyncFd splice design as input; buffered relay stays the universal fallback; selection reads capability + payload type, never role/initiator), sequenced as a follow-on slice set after otp-10 cutover. UNAS is the measurement rig, symmetric-endpoint methodology applies. Resolves the STATE Blocked zero-copy a/b/c question and shrinks the 10 GbE owner declarations from four to three (ue-1, ue-2, REV4 → Shipped). Propagated: eval doc annotated, ONE_TRANSFER_PATH Non-goals + Design write-strategy seam, STATE queue/blocked. Codex review of this docs commit follows.

**2026-07-05 08:15:00Z** - **DECISION (D-2026-07-05-2 — no version compatibility, ever, claude)**: While reviewing the plan-review fixes the owner caught the REV4 "mixed old/new peers must negotiate down" clause surviving into ONE_TRANSFER_PATH ("governs until cutover") and rejected it root-and-branch: **"backward compatibility is NOT a consideration. I expect blit 1.2.3 not to be able to talk to blit-daemon 1.2.3.1. period. same build only. do not engineer tech debt into an unshipped product."** — a rule he had stated repeatedly in chat but which was never recorded, so the written REV4 doc kept winning document-conflict resolutions (exactly the unrecorded-intent failure mode; the codex F1 fix resolved the conflict in the wrong direction). Recorded as **D-2026-07-05-2**: same-build peers only; the unified session's handshake REFUSES mismatched builds at session open (strict build/protocol identity, specified in otp-1, pinned by test); no negotiate-down, no advisory fields, no feature-capability bits (same build = same features); receiver capacity profile explicitly unaffected (hardware negotiation, D-2026-06-20-1/-2). Propagated: REV4 §Constraints clause struck + annotated (effective immediately, not at cutover); SMALL_FILE_CEILING's "mixed-version peers keep working" clause and sf-6's mixed-version-test deliverable struck + annotated; D-2026-07-05-1's "only at cutover" scoping struck; ONE_TRANSFER_PATH Non-goals rewritten + otp-1 gains the same-build handshake deliverable. Codex review of this docs commit follows per D-2026-07-04-1.

**2026-07-05 07:30:00Z** - **DIRECTIVE + PLAN (ONE_TRANSFER_PATH draft, D-2026-07-05-1, claude)**: Owner rejected the push/pull disparity, the mixed-fs benchmark methodology ("tmp on one side, spinning rust on the other is not a valid test"), and the explanation that direction symmetry lives in four separate driver loops — then issued the directive now recorded verbatim in `docs/plan/ONE_TRANSFER_PATH.md`: **"ONE BLOCK OF CODE that does the transfer. no POSSIBILITY OF ANYTHING EVER using anything else because anything else does not exist"**; "I NEVER see a situation where pull is faster than push or vice versa... because of something blit did"; identical whether initiated as push from skippy or pull from this machine. Scope/wire/process explicitly delegated to the agent. **Plan drafted through the plan procedure**: one `TransferSession` (roles SOURCE/DESTINATION selected by initiator/verb, never code paths), one bidi `Transfer` RPC replacing `Push`+`PullSync` (no back-compat, lockstep upgrade — repo precedent PullSyncHeader), one choreography (source streams manifest immediately; destination diffs incrementally; sf-2 shape-corrected dial as the only stream policy — absorbs the pull 1s-start residue), gRPC fallback demoted to a byte-carrier option, delegated pull = daemon-initiated session, local transfers on an in-process transport in the final phase; slices otp-1..12 ending in a **deletion slice** (the four drivers + both RPCs removed, file-by-file proof) and a **symmetric-rig acceptance matrix** (same-fs disk-to-disk verdict cells, cold caches, tmpfs as labeled wire-reference only; initiator/verb A/B within ±10%). Converge-up constraint: per cell the unified path must match the better of today's directions, not the average. **D-2026-07-05-1 recorded**: directive + SMALL_FILE_CEILING paused at sf-2 (sf-3a+ blocked; principle D-2026-07-04-4 stands) + design queue paused (w7-1 likely absorbed by otp-5). STATE rewritten around the new queue. Next: codex plan review of this commit, adjudicate, fix, then STOP for the owner's Active flip — no implementation anywhere until then.

**2026-07-05 06:45:00Z** - **CODER (sf-2 shape-correction stream resize, claude)**: sf-2 landed through the codex loop (`c70c2ac` + review fix `7627e7b`). **Diagnosis correction recorded in the finding doc**: the plan said `initial_stream_proposal` was byte-weighted, but the table has had file-count tiers since ue-r2-1f — the real defect was the INPUT: the daemon proposes the push epoch-0 stream count at its early manifest flush (`FILE_LIST_EARLY_FLUSH_ENTRIES` = 128, `control.rs`), so a 10k-file push negotiated from a ~128-file prefix → 1 stream for the whole transfer (the measured 10 GbE small-cell gap AND the sf-1 probe's 1000-file/1-stream evidence). Pull checked clean (proposes from the complete post-diff need list, `pull_sync.rs:344`). **Fix, no wire change, no daemon change**: client-side shape-correction resize — as need batches accumulate, the client re-runs the shape table over the true transfer shape (need bytes + count) and corrects upward through the existing ue-r2-2 resize wire, one ADD epoch per acked roundtrip. Dial stays the single stream-policy owner (`TransferDial::propose_shape_resize`: one-in-flight, ceiling-clamped, ADD-only, no sustain/cooldown — definite signal; epoch allocation CAS'd in BOTH proposers now that two tasks allocate); tuner REMOVE permanently disables shape corrections for the transfer (live evidence outranks the static table); old daemon / gRPC fallback behavior unchanged. New observable: `RemotePushReport.data_plane_streams`. Tests +4 (suite 1479 → 1483/0): plan-cell table pins (10k×4KiB→8 via count tier; 1 GiB unchanged; mixed→8; sf-1's 1000→2), shape-resize semantics pins, and an in-process loopback e2e (REAL daemon service + REAL client, 10k tiny files) pinning `data_plane_streams > 1` — **guard proven by revert** (forced `propose_shape_resize`→None fails the pin with "settled at 1", the exact pre-fix behavior). Codex **NEEDS FIXES, 1/1 accepted**: the shape target counted `requested_files`, which `prune_unrequested_payloads` drains as payloads match — a 300-file push whose early batches predate the negotiation prunes to 0 and never corrects; now counts the append-only `files_requested` accumulator (verdict: `.review/results/sf-2-shape-correction-resize.gpt-verdict.md`). Known gaps: ramp is 1 stream per roundtrip (7 hops to 8 — fine on LAN, plan-out-of-scope on WAN); the daemon's early-flush proposal itself still lowballs (a PushHeader totals field would be wire-visible, sf-6-class gate, unnecessary while the ramp closes the gap); whether 8 streams IS the hardware ceiling is sf-4's rig question. Windows parity run not done this slice (code is platform-neutral; rides the next owner Windows run). Next: sf-3a (per-file cost limiter analysis, analysis-only).

**2026-07-05 03:03:29Z** - **DECISION + CODER (D-2026-07-04-4 Active flip; sf-1 tripwire harness, claude)**: Owner "go" on the queued flip: `docs/plan/SMALL_FILE_CEILING.md` Draft → **Active** (D-2026-07-04-4, records `6ddbc68`; the four 10 GbE gate declarations were NOT part of the go and stay Blocked). **sf-1 landed through the codex loop** (`7202c1a` + review fix `80633df`): `scripts/bench_tripwires.sh` re-runs the 2026-07-05 baseline against any daemon host in one command — full matrix (blit/rsyncd/rsync-ssh/rclone-sftp × push/pull; blit/rsync/rclone/cp × local; schema-identical CSV to `docs/bench/10gbe-2026-07-05/tool_comparison.csv`), spins blitd (`--root`, module `default`) + rsyncd over ssh with full teardown, fresh never-seen targets every run, per-run `timeout`, tripwire verdict as exit code (0 clean / 3 tripped / 4 incomplete), baseline diff with ±10% flags, plus the stream-scaling probe (files/s vs streams MEASURED from blitd's per-stream completion lines, not the proposal table). Codex **NEEDS FIXES, 6/6 accepted**: teardown could rm a dir it didn't create (plain mkdir + `$$` ownership); partial coverage could read as "clean" (expected tool set enforced — INCOMPLETE cells, exit 4); `SPIN_DAEMONS=0` was broken beyond the flagged rsyncd gating — adjudication found ALL external-daemon paths diverged from the prepared targets (REL prefix + rsyncd liveness probe; verified against externally spun daemons); ±10%/missing-cell flags; two record-accuracy Lows. Verified by execution in all three modes (local-only; loopback spun; loopback external — daemons torn down, session dirs removed). Validation: fmt/clippy clean, suite 1479/0 across 37 suites (baseline held), check-docs OK. **Instrument already earning its keep**: loopback probe shows a 1000-file push riding 1 stream where the dial table (`dial.rs:429`, count≥256→2) says 2 — the sf-2 policy gap, now measurable; noted in the finding doc for sf-2. Local datapoint: `cp -a` trips blit on tiny local copies on the dev box (rig verdicts belong to sf-4). Next: sf-2 (dial file-count weighting).

**2026-07-05 02:07:15Z** - **PLAN + PRINCIPLE (SMALL_FILE_CEILING draft, claude)**: Owner ordered "make a plan to beat rsync" then issued a foundational correction mid-interview that now governs all perf work: **goals are ceiling-driven, never competitor-relative** — blit's guiding principles are FAST/SIMPLE/RELIABLE (every change serves one or is scrapped), blit must be the fastest way to transfer files in ANY scenario, and a "beat X by N%" bar embeds a stopping condition ("you'll stop when you hit it — NOT what we are building"). A margin answer (≥25%) given in a quick Q&A was explicitly retracted ("stop, undo that") — future sessions must not re-litigate margins. First-draft BEAT_RSYNC.md was deleted (name itself was competitor-framed; also caught the interactive-rm alias silently declining deletes over non-tty — verify with ls, not exit codes). **`docs/plan/SMALL_FILE_CEILING.md` (Draft, `78eabfd`)**: principle as §1; goal = the measured small-file/mixed cells become bound by a NAMED hardware limit (wire 34 ms for the 10k-push payload, ~150 µs/file single-pipe fs floor proven on the rig's own ZFS, tmpfs-class pull ceiling) demonstrated by profile + stream-scaling curve; tools (rsyncd/rsync-ssh/rclone-best/cp) are tripwires only — any tool faster anywhere = finding, never finish line. Slices sf-1 (tripwire harness + scaling probe), sf-2 (file-count-aware stream proposal — the measured one-stream-for-10k-files policy gap), sf-3a (profile-first limiter analysis) + sf-3b… (one cut per slice), sf-4 (rig re-measure), sf-5 (resize-on-backlog, ue-2's organic trigger), sf-6 (tar-shard wire lane — gated on full REV4 wire-contract deliverables before code), sf-7 (verdict). Codex plan review: **NEEDS FIXES, 5/5 accepted** (evidence made durable in-repo at `docs/bench/10gbe-2026-07-05/` — DIAGNOSIS.md + all session CSVs; harness=tripwire-set by construction; sf-3 split; sf-6 wire deliverables named — the High; competitor-relative residue purged) → fixed `811a3f2`, records `219cecf`. STATE queue carries the draft; **no code until the owner flips Active** (PROTOCOL plan step 6). skippy torn down at session close: daemons stopped, cmp/bench-data payloads removed; binaries + configs staged at blit-bin/ for sf-4. Owner decisions outstanding: plan Active flip, four 10 GbE gate declarations, zero-copy option a/b/c, push go.

**2026-07-05 00:51:18Z** - **BENCHMARK ADDENDUM (tool comparison + zero-copy gate data, claude)**: Owner-requested follow-ups on the same rig. **(1) Zero-copy revisit gate measured** (D-2026-06-12-1's "receive-side CPU saturation" trigger — previously unevaluated): pull receiver (client, tmpfs sink) 0.45 cores at 9.5 Gbit/s; push receiver (skippy daemon, ZFS sink) **1.43 cores** — above the eval doc's "fraction of one core" estimate but nowhere near saturation on 32-core hosts; gate NOT met on this rig, wording is rig-bound (owner reviewing options: append data + regeneralize gate / amend decision / leave). `zero_copy.rs` status re-confirmed: zero callers ever, `(zero-copy 0 bytes)` is an unfed counter, EAGAIN busy-wait defect means any revival is a rewrite; deletion (owner-ratified) still pending w8-1. **(2) blit vs rsync vs rclone** (4 transports × push/pull × 3 workloads × 2 runs, shared data root on skippy, fresh targets every run; CSV: logs/bench_10gbe_20260704T201804/tool_comparison.csv): **large 1 GiB** — blit fastest both directions (push ~950 ms, pull ~885 ms ≈ wire ceiling); rsyncd (native, unencrypted) ~1.01/1.03 s; ssh-transported rsync 1.9–2.3 s and rclone-sftp 2.9–3.1 s pay the cipher tax. **small 10k×4 KiB** — **rsync beats blit on push** (rsyncd 1.5 s, rsync-ssh 1.6 s vs blit 2.4–3.3 s; the known push-side per-file gap — pull asymmetry is 23k vs 4.4k files/s) and edges it on pull (0.37 s vs 0.45–0.48 s); rclone-sftp ~5–6 s. **mixed** — rsyncd wins push (1.24 s vs blit 1.8–2.2 s), blit wins pull (0.58 s vs 0.69 s). **local tmpfs** — blit wins all three (173/79/119 ms vs rsync 332/87/248, rclone 1011/199/557). Takeaway filed: small/mixed PUSH is blit's one loss to rsync on this rig — push-side per-file receive cost is the actionable gap (queue-candidate alongside w6-2b territory); rsyncd's r1 large-push 7.2 s was a one-time cold outlier (r2 1.01 s representative). rsyncd left running on skippy:8730 alongside blitd for further comparison work; both torn down at session close.

**2026-07-05 00:34:30Z** - **BENCHMARK SESSION (10 GbE, TrueNAS↔Arch, claude + owner)**: The owner-called REV4 sign-off session ran end-to-end on the recorded pair (skippy = TrueNAS SCALE, glibc 2.36, binaries exec'd from the pool because /tmp and /home are noexec; client = netwatch-01 Arch, enp6s0 10 GbE). Env: **MTU 9000 both ends** (client NM profile was silently 1500 — set to 9000 via profile edit + full `connection up` reactivation after `device reapply` refused on immutable fields; 8972-byte DF probe green), **iperf3 baseline 9.88 Gbit/s single-stream / 9.91 both directions** — the parity reference (~1.24 GB/s). **Results (validated matrix, avg of 3 true cold runs each)**: TCP push 1 GiB **907 ms ≈ 9.5 Gbit/s** (first payload 14.5 ms), TCP pull 1 GiB **904 ms ≈ 9.5 Gbit/s** — both at the wire ceiling; gRPC-fallback push 1 GiB 1070 ms ≈ 8.0 Gbit/s (first payload 1.0–2.2 s scaling with manifest size — the design-4 no-mid-manifest-negotiation property, deliberate); gRPC pull ≈ TCP pull on large (880 vs 904 ms — 1 MiB-chunked fallback is wire-competitive on clean LAN), TCP 31% faster on 10k-small pulls; push 10k×4 KiB 2.29 s (~4.4k files/s) vs pull 433 ms (~23k files/s — push side per-file daemon work is the gap, w4-4/w9 territory); local blit beats rsync 2.3× on 1 GiB (157 vs 359 ms), no-op mirror 3 ms vs rsync 42 ms. **ue-1 loopback parity band: HOLDS** — local/push/pull per workload: large 195/178/162 ms, small 83/147/116, mixed 119/163/104; worst spread 1.8×, no 10×/2× gap (record: logs/bench_10gbe_20260704T201804/loopback_parity_band.csv). **Reverse direction validated** (daemon on client, skippy as pusher/puller through ufw): push 7.25 Gbit/s data-plane (first payload 3.9 ms), pull 9.75 Gbit/s. **ue-2 observation**: no organic mid-transfer resize triggered anywhere — a single stream saturates 10 GbE (daemon log: 1 GiB stream at 9.69 Gbit/s; negotiated extra streams close cleanly at 0 bytes), including under deliberate 2-concurrent-push contention (8.30+3.21 Gbit/s, clean completion); organic resize needs a constrained/variable wire — owner call whether the ue-r2-2 deterministic suite + this no-wedge evidence satisfies the gate. **Zero-copy datapoint** (D-2026-06-12-1 revisit): every transfer reported `zero-copy 0 bytes` yet the wire is saturated — splice wiring buys nothing at 10 GbE; CPU is not the bottleneck. **Methodology recorded**: engine-vs-wire isolation by design — tmpfs local ends, ZFS absorbing writes async, ARC-warm re-reads, no sync/ARC-eviction between runs (owner asked; disk-path variants — post-push `zpool sync` timing, cold-ARC pulls via `primarycache` — deferred as owner-gated follow-ups). **Session findings fixed en route**: bench_10gbe.sh had never survived past its local phase — stale endpoint grammar (missing load-bearing trailing slash) + daemon-only `--force-grpc-data` flag on the client, both silent aborts under run_timed's `2>/dev/null` + `set -e` (`b9befb8`); codex review of that fix returned 2 High (run_timed re-runs measure no-ops; shared module root lets gRPC pushes no-op and de-isolates pull labels) — both accepted, fixed `92d6326` (run_timed_fresh / per-run push subdirs / per-workload subpath pulls), validated by a full re-run (avg≈best every row). Verdicts: `.review/results/bench-script-fix.{codex,gpt-verdict}.md`. **New foot-gun filed**: `blit copy src_large dst` with an existing local dir named without `./` parses as an mDNS discovery endpoint and errors "remote source must include a module or root" (blit-app endpoints.rs) — STATE open question. Benchmark payload cleaned off the pool; binaries + bench.toml left at /mnt/generic-pool/video/blit-bin/ for future sessions. **Owner calls now pending**: ue-1 declaration (evidence says band holds), ue-2 declaration, zero-copy revisit verdict, REV4 → Shipped flip, and the GitHub push (w9-3 Windows CI check still queued behind it).

**2026-07-04 23:35:54Z** - **CODER (w9-3-test-harness-builder, claude)**: Landed w9-3 through the codex loop (owner go: "continue, use /playbook reviewloop codex" — no playbooks exist in this repo, resolved to the `slice` operator per `.agents/repo-guidance.md` → topmost ratified open row per the 19th handoff). A 6-agent inventory workflow re-derived the audit's 2026-06-11 evidence at HEAD before coding and found the rot had GROWN: **seven** daemon-harness clones, not five — w9-4 (`readonly_enforcement.rs`) and w9-5 (`jobs_lifecycle.rs`) each added another private spawn_daemon/config-struct copy *because* common couldn't express delegation or a second daemon, proving the finding's "the next one will miss at least one" prediction twice — plus 5 `cli_bin` copies, 7 `run_with_timeout`, 4 `ChildGuard`, and **five** bare `Server::builder()` fake servers (not three: `remote_remote.rs` ×2, `jobs_lifecycle.rs`, `pull_sync_with_spec_wire.rs` ×2) vs production's audit-1 keepalive. Slice `f6e592e`: common/mod.rs is now the single owner — `TestContext::builder()` (`.read_only()`/`.delegation()`/`.extra_daemon_args()`; `new()`/`new_read_only()` signature-stable, zero edits in the 13 pre-existing consumers), `spawn_daemon(workspace, name, module_dir, opts)` + `TestContext::spawn_second_daemon` primitives (config superset: `delegation_allowed` serialized explicit `true` = the daemon's own absent-default, verified in runtime.rs before choosing; `[delegation]` table optional), `ensure_daemon_built()` OnceLock'ing the nested `cargo build` (R16-F1 per-process independence kept; ~75 invocations per full run → ≤1 per binary; also fixes remote_remote's dropped `--target` handling and the tcp_fallback/jobs/readonly spawns that ran NO build), shared `spawn_fake_blit_server` scaffold, and new `blit_core::remote::grpc_server::production_server_builder()` (owns the 2026-05-23 keepalive 30s/20s; daemon main.rs + all five fakes route through it; zero bare `Server::builder()` left, grep-verified; +1 mutation-verified pin test). Mid-slice the validation run itself caught the **daemon-spawn load-flakiness live**: `test_admin_find` got an empty listing from another test's daemon — `pick_unused_port`'s probe-drop-to-bind TOCTOU, previously masked by the per-test cargo builds serializing bring-ups; fixed two-layer (process-global claimed-port set — cargo runs test binaries sequentially, so per-process scope is exactly right — plus a `try_wait` child-death check in the readiness poll so an externally stolen port panics with the real reason instead of silently testing a foreign daemon). stderr policy unified to null (was piped-but-never-read; real capture stays w9-6). Review: codex **NEEDS FIXES (1 Medium, accepted — a genuinely sharp catch)**: `spawn_fake_blit_server` still bound `:0` OUTSIDE the claimed set, so a fake could take a port promised to a not-yet-bound daemon in mixed binaries (remote_remote, jobs_lifecycle) — same wrong-listener class, missed path; fixed `8641bc6` (`claim_port()` shared by both paths; the fake keeps its probe listener so its path has no gap at all). Records `c62d15b`. Net −1,251 test-tree lines. Validation: fmt/clippy clean; test-count gate proven by same-method A/B via `git stash` — HEAD 1478/0/2, slice 1479/0/2 across 37 suites, exactly +1, per-file `#[test]` counts identical (STATE's recorded "1479" baseline was a different aggregation, off-by-one vs the same tree); full suite ×2 + `admin_verbs` ×10 post-fix all green. All on master, unpushed. Next: strict design-queue order gives **w7-1** (mirror-executor consolidation) as topmost ratified open row; filed alternatives w6-2a/b/c + relay-1, coder's pick.

**2026-07-04 22:09:54Z** - **OWNER Q&A + PUSH (benchmark host plan, claude)**: Owner pushed `master` → `github` at `10d89e0` (first push carrying the w9-1/w9-4 ungated suites since the Windows triage — windows-latest CI on it is the "meaningfully green" check; `origin` gitea mirror not pushed, lags). Benchmark host question settled in Q&A: the 10 GbE **sign-off pair is TrueNAS (skippy) ↔ Arch client, all-Linux** — the zero-copy/splice revisit gate (D-2026-06-12-1) needs a Linux consumer, and ue-1's parity band should measure the engine without Windows I/O confounders (win_fs, TCP autotuning, filter drivers). Key facts recorded: the client box **dual-boots Win 11/Arch on identical hardware** (so a later TrueNAS→Win 11 bare-metal pull in the same hardware window gives a clean platform delta as a deployment-parity datapoint, not a gate), and a **Windows VM on the Arch install** exists for Windows-specific functional checks — explicitly never for perf numbers (virtio/NAT skews throughput). iperf3 baseline per pair before any Blit numbers. Plan recorded in STATE.md Queue item 2. Handoff commit follows; push of the handoff itself pending the owner's ref-listing go per push policy.

**2026-07-04 21:46:39Z** - **CODER (design-3-unbounded-data-plane-connects, claude)**: Landed design-3 through the codex loop (same session, fourth slice; coder's pick of the long-sanctioned smaller alternative over the large w9-3 harness consolidation — queue policy leaves sequencing to the coder). Both TCP data-plane client connects ran unbounded — the audit-2 wave bounded every control-plane connect at 30 s but never reached the data plane, so a firewalled/black-holed data port (the daemon advertises a fresh ephemeral port per transfer; asymmetric firewalls passing 9031 but blocking ephemerals are common) hung for the kernel SYN timeout (60–127 s) with no message. Sites re-verified at HEAD: the pull site is now `connect_pull_stream` (split at ue-r2-2, shared by resize-ADD dials), the push site `DataPlaneSession::connect_with_probe` (elastic dials included). Slice `49dcec6`: `remote::transfer::socket::dial_data_plane(addr, handshake, tcp_buffer_size)` — the client-side mirror of the daemon's bounded accept, in the w1-family policy module: connect bounded by the shared `DATA_PLANE_ACCEPT_TIMEOUT` (the row's sanctioned constant — no fifth 30 s literal), `configure_data_socket` applied, handshake write bounded by `DATA_PLANE_TOKEN_TIMEOUT` (mirrors the acceptor's bounded token read — the finding's "also the token write" clause); on either timeout the chain carries an `io::ErrorKind::TimedOut` source with text naming addr + the likely-firewall cause, so `remote::retry::is_retryable` classifies it transient and `--retry` re-dials. Both call sites collapsed onto the helper; socket.rs's w1-2-era "connect timeouts live at the call sites" module-doc paragraph rewritten (comment-truth). Tests +3 (blit-core 389 → 392): happy path (policy + handshake delivery), deterministic timeout SHAPE via an accepting-but-never-reading peer against a 64 MiB handshake (TimedOut chain + retryable — mutation-verified: swapping the timeout error for a plain eyre message fails the pin), TEST-NET black-hole connect bounded (environment-tolerant: fast-reject networks skip the shape assertions, the bound is asserted always). Review: codex **PASS, zero findings** (independently confirmed the pull resize-ADD non-fatal-dial posture survived and StallGuard/cancellation/byte accounting untouched). Validation: fmt/clippy clean, `cargo test --workspace` 1476 → 1479/0/2 across 37 suites. All on master, unpushed. Session total: w6-1 (+design-1), w6-2 (filed w6-2a/b/c), w4-4, design-3 — four rows closed, six commits of records. Next: w9-3 (test-harness builder) is the topmost ratified open row and the right size for a fresh session; filed alternatives w6-2a/b/c + relay-1.

**2026-07-04 21:33:31Z** - **CODER (w4-4-blocking-work-off-runtime, claude)**: Landed w4-4 through the codex loop (same session, standing "reviewloop" go → topmost ratified open row after w6-1/w6-2). Both audit halves re-verified at HEAD before coding — the 2026-06-11 sites moved: daemon pull.rs died at ue-r2-1h, its enumeration relocated into pull_sync.rs; the push manifest loop's per-entry blocking is control.rs `file_requires_upload` (canonical-containment ancestor walk ≥2 canonicalize syscalls + stat, inline on a tokio worker — ~3M+ blocking syscalls for a 1M-file push). Slice `0feca34`: **(A)** manifest entries buffer into `PendingManifestEntry` and drain through `drain_manifest_checks` — ONE spawn_blocking per chunk runs the batch's checks (batch moved in/out, no clones), need-list pushes follow in async context in manifest order; `MANIFEST_CHECK_CHUNK` = FILE_LIST_EARLY_FLUSH_ENTRIES (128); the mid-manifest TCP data-plane spin-up moved from per-entry to post-chunk-drain (same guard — design-4's no-mid-manifest-fallback-negotiation invariant untouched); ManifestComplete drains the sub-chunk remainder (no spin-up — post-manifest negotiation owns it); the spec's lexical-containment alternative was REJECTED as weakening the F2 canonical posture (an in-module symlink could redirect the stat outside; check stays canonical, now on a blocking thread). **(B)** `collect_pull_entries_with_checksums` now runs entirely on one spawn_blocking thread (thin async wrapper + `collect_pull_entries_sync`; the directory branch's inner spawn_blocking unwrapped): previously the single-file branch ran its metadata probes and — with --checksum — a full synchronous Blake3 of an arbitrarily large file inline on a runtime worker, plus the top-level is_file/is_dir probes. Review: codex **NEEDS FIXES (1 Medium, accepted — a genuinely good catch)**: chunk-only draining muted the need-list batcher's 64 KiB/5 ms early-flush triggers between chunk boundaries (they only evaluate inside push(), which chunking confines to drain time) — a slowly-enumerating client's first FilesToUpload + TCP spin-up would wait for 128 trickled entries instead of ~5 ms; fixed `768e7e3` with `manifest_drain_due(pending_len, oldest_buffered)` — drain on chunk-full OR oldest-entry age ≥ MANIFEST_CHECK_MAX_DELAY (= the batcher's own delay bound), evaluated on next arrival exactly like the batcher's push-time semantics; fast streams still hit the cap first. Tests: +4 `manifest_check_batch_tests` (decision parity incl. POSIX wire paths + order, empty-drain no-op, containment-escape rejection through the batched path, trigger contract), mutation-verified (all-true check → parity pin fails); half B rides existing pins (single_file_filter_tests; the 500-file design-5 e2e drives ≥3 chunk drains + remainder + spin-up end-to-end). Validation both commits: fmt/clippy clean, `cargo test --workspace` 1472 → 1476/0/2 across 37 suites. All on master, unpushed. Next: strict design-queue order gives w9-3 (test-harness builder) as topmost ratified open row; filed alternatives w6-2a/b/c + design-3 + relay-1, coder's pick.

**2026-07-04 21:13:10Z** - **CODER (w6-2-progress-residue-verify, claude)**: Landed w6-2 through the codex loop (same session as w6-1; owner's standing "reviewloop with codex as each slice lands" go → next topmost open row). The ratified W6.2 spec makes this a verify-then-file slice — verification is step 1, each confirmed item becomes its own follow-on — so the deliverable is records, not code. All three §1.6 residue claims **CONFIRMED** at HEAD `8fd8978` (evidence derived twice: the w6-1 mapping workflow, then hand spot-checks): (1) delegated live progress is wire-dead — `BytesProgress` has zero production producers (proto + blit-app consumer/tests only; the dst daemon sends `Started`, silence during transfer per the deliberate 0.1.0 gap at delegated_pull.rs:363-369, then one post-hoc `ManifestBatch{file_count=files_transferred}`), while the consumer bridge sits ready and the dst daemon ALREADY meters the bytes (core.rs:667 feeds the row atomic) — the fix is a bridge, not new instrumentation; (2) daemon row byte counters stay 0 for push receive (sink built without `.with_byte_progress()`, `execute_receive_pipeline(.., None)` at push/data_plane.rs:1086) and pull_sync serve (no counter at any of the 3 send pipelines, pull_sync.rs:635/:765/:795) — `job.bytes_counter()` is wired exactly once in service code, the delegated dispatch; (3) no denominators or file counts anywhere on the daemon event stream — `TransferProgress` hardcodes bytes_total/files_completed/files_total 0 (core.rs:240-242), `TransferComplete.files` 0 (:322-325, `tcp_fallback_used` false :329), `GetState` bytes_total 0 (:994-996). Filed as three INDEPENDENT rows in REVIEW.md's pending-review section (the design-1..5 entry route, not new ratified-queue rows): **w6-2a** delegated BytesProgress producer (bridge the already-fed row atomic onto the DelegatedPullProgress stream; client needs nothing — w6-1's aggregate lane + `report_bytes_progress` are ready), **w6-2b** wire the counter through push receive + pull_sync serve, **w6-2c** denominators + files counter on ActiveJobs rows (absorbs the tcp_fallback_used=false honesty note). Slice `0aba593` (docs gate green). Review: codex **NEEDS FIXES (2 Low, both doc-coherence, both accepted)** — "no code anywhere constructs BytesProgress" overstated (blit-app unit tests construct it; clarified to zero PRODUCTION producers) and "2b is substrate for 2a" was wrong (the delegated counter is already fed — codex cited my own Claim-1 evidence back at me; slices reworded as independent, 2b→2a→2c smallest-first only); fixed `8b7829d`. No code change; workspace stays 1472/0/2 as of `8fd8978`. All on master, unpushed. Next: strict design-queue order gives w4-4 (spawn_blocking the stat/canonicalize batch) as topmost ratified open row; filed alternatives now w6-2a/b/c + design-3 + relay-1 — sequencing coder's pick unless the owner orders.

**2026-07-04 21:02:20Z** - **CODER (w6-1-progress-event-contract, claude)**: Landed w6-1 through the codex loop (owner go: "continue. reviewloop with codex as each slice lands" → topmost open row per the 14th handoff). A 5-agent mapping workflow re-derived the audit's 2026-06-11 §1.6 inventory against post-REV4 HEAD before coding (producer census / consumer census / push-delegated-daemon boundary / test inventory / gapcheck critic that re-grepped every `report_*`/`ProgressEvent` hit — zero unmapped sites): the CLI's TCP-pull byte double-count (design-1) was live at `pipeline.rs` FILE receive (`Payload{0,N}` + `FileComplete{path,N}`, same bytes twice); push rode `FileComplete` only; delegated rode `Payload{fΔ,bΔ}`; plus four quieter producer sins — gRPC pull `finalize_active_file` leaked the ABSOLUTE local dest path (every other producer emits wire-relative), tar-shard member files were never counted on either transport, TCP resume records emitted nothing, and the two dead emitters encoded contradictory semantics. Slice `8fd8978`: contract defined ON the enum in blit-core — **bytes ride `Payload` only; `FileComplete` loses its `bytes` field entirely** (the design-1 class is now unrepresentable at the type level, not avoided by convention); files count exactly once via either one byteless `FileComplete{wire-relative path}` (per-file lane) or `Payload.files` deltas (aggregate lane: delegated bridge, gRPC tar-shard applier); `ManifestBatch` documented as the direction-flavored denominator (pull full-manifest / push need-list / delegated post-hoc — unification is wire/UX scope, deliberately not this slice). All producers normalized: receive FILE double-emit fixed; receive TAR_SHARD gains per-member completions; BLOCK/BLOCK_COMPLETE lanes gain `Payload`+`FileComplete` (TCP resume was progress-invisible); send-side sink worker (push TCP + gRPC fallback share it) moves planned sizes onto `Payload`; gRPC pull TarShardComplete counts via `Payload{stats.files,0}`, BlockComplete completes per-file, finalize carries the wire path through `active_file`; dead `send_payloads_with_progress` + `transfer_payloads_via_control_plane` conformed pending w8 deletion. Consumers collapsed onto new `ProgressTotals` (blit-core, next to the enum, `started()` gate included): CLI monitor rewritten (fixes design-1's ~2× `[progress]` lines; `--json` `file_complete` keeps its key shape with constant `bytes:0`; verbose gRPC-pull lines now print wire-relative paths) and all three TUI forwarders; the TUI's three `accumulate_*` rules deleted, its 7 tests rewritten in place against `ProgressTotals`. Tests +12 in blit-core (6 contract, 4 pipeline emission incl. exact-sequence pins driven over in-memory wire records via a new `RecordingSink`, 2 finalize) — the emission tests exposed that the fuzz helper `encode_block_complete` wrote truncated records (missing mtime/perms vs the reader), fixed; two mutation checks verified the pins bite (drop FileComplete counting → 3 fail; drop the FILE arm's `report_payload` → emission pin fails). Review: codex **PASS, zero findings** (explicitly checked the W6.1/W6.2 scope split — daemon/ByteProgressSink counters untouched by design, that residue is w6-2). design-1's row closed `[x]` alongside (fixed structurally, graded in the same round). Validation: fmt/clippy clean, `cargo test --workspace` 1460 → 1472/0/2 across 37 suites. All on master, unpushed. Next: w6-2 (progress-residue verify-then-fix) tops the open queue; design-3 remains the sanctioned smaller alternative. Process note: mutation-testing uncommitted work must restore from a scratchpad copy, not `git checkout` (one self-inflicted clobber of `progress.rs` was fully re-applied and re-verified this session).

**2026-07-04 20:04:30Z** - **CODER (w3-1-memory-aware-buffer-pool, claude)**: Landed w3-1 through the codex loop (owner go: "continue" → topmost open row per the 13th handoff; its "after W2.2 settles the tuning owner" prerequisite held — owner is `engine::TransferDial`, so the spec's `for_data_plane(tuning, streams)` became `for_data_plane(chunk_bytes, streams)`, the chunk read from the dial at each site). A 5-agent audit workflow re-verified the 2026-06-11 evidence against post-REV4 HEAD before coding: exactly three formula sites survive (push client, pull_sync multistream, pull_sync resume — the daemon pull.rs copy died with the Pull RPC at ue-r2-1h); the resume site is a hand-rolled `pool_size = 4` whose pool is **inert at runtime** (resume only sends via `send_block*`, which write caller slices — `pool.acquire` is reached solely from `send_file_double_buffered`), so unifying it to the formula's 6 is behavior-neutral; the double-buffered sender holds TWO pool buffers acquired sequentially (hold-one-wait-for-second), so any memory cap admitting fewer than 2 buffers per live stream can deadlock a sender against itself; shrinking pool buffers below the session's chunk_bytes is wire-safe (file bytes travel raw under a size-carrying header — no per-chunk framing; the effective send granularity already IS the pool buffer size); and `dial.ceiling_max_streams()` is a hard 3-layer-enforced bound on live streams under resize, with ADDed streams sharing the epoch-0 pool on both elastic paths. Slice `f49f8f6`: `BufferPool::for_data_plane` in buffer.rs backed by pure `data_plane_pool_params(chunk_bytes, streams, available_memory)` — formula unchanged when memory is plentiful (pinned pure-hoist parity), budget capped at available/4 (the pool's own doc example, ignored by every call site until now), liveness floor `budget ≥ buffer_size × streams × 2` always beats the cap with buffer_size shrinking toward the shared floor instead of concurrency; elastic paths authorize `ceiling_max_streams()` up front (lazy allocation makes the ceiling authorization free until resize ADDs streams — closes both sites' "growing the pool live is a W3.1 concern" deferral without live-growth machinery); resume passes 1. **Bonus real bug fixed**: the hoisted `available_memory_bytes()` drops the old helper's `* 1024` — sysinfo 0.38 returns BYTES not KiB (verified against the vendored crate source), so available memory was over-reported 1024×, which had made BufferSizer's /10 cap vacuous forever and would have made the new /4 cap vacuous too; also `System::new()`+`refresh_memory()` replaces the process-table-walking `new_all()`, zero-report 512 MiB fallback kept. `DATA_PLANE_BUFFER_FLOOR` (64 KiB) exported and adopted at the same-semantic floor sites (session chunk clamp, receive-buffer clamp, dial inflight clamp — whose comment already said "matching the session's minimum buffer"); coincidentally-equal 64 KiB literals deliberately untouched. Comment-truth: `RECEIVE_CHUNK_SIZE`'s false "matches the send side" claim rewritten (receive is deliberately 1 MiB vs the sender's 16–64 MiB; the asymmetry is legal — no per-chunk framing); BufferPool header example now shows the constructor. Tests: +8 params-layer pins (legacy parity, floor, cap, shrink-preserving-liveness, tiny-cap liveness override, full-grid liveness/floor property sweep, zero-memory fallback, real-sysinfo smoke), mutation-verified (cap+liveness line reverted → 3 pins fail; restored → green). Review: codex **PASS, zero findings** (it independently walked the two-buffer acquisition, the audit-11 exact-size guard, tar-shard chunking, and the ceiling authorization; first invocation was killed by a session restart before output — the record is the complete re-run). Known gaps in the finding doc: receive-side dial tuning stays out of scope (rest of constants-receive-chunk-1mib-asymmetry, separate slice if wanted); resume path's inert pool + dead prefetch literal left as-is; no memory-capped-host e2e (capped regimes pinned at the params layer; the pull-sync deadlock canary passes). Validation: fmt/clippy clean, `cargo test --workspace` 1460/0/2 across 37 suites (baseline 1452). All on master, unpushed. Next: w6-1 (progress-event contract) tops the open queue; design-3 remains the sanctioned smaller alternative.

**2026-07-04 15:24:23Z** - **CODER (w2-2-stream-ladder-owner, claude)**: Landed w2-2 through the codex loop (owner go: "continue" → topmost open row per the 12th handoff). The row as filed (2026-06-11) predates REV4, which already delivered its three stream-count legs: the `determine_remote_tuning` ladder died at ue-r2-1e (live dial), daemon `desired_streams` at ue-r2-1f (`engine::initial_stream_proposal` — byte- AND file-count-keyed, satisfying the spec's "takes file_count"), and `pull_stream_count` with the Pull RPC at ue-r2-1h; D-2026-06-20-1 recorded the absorption in v1 slice IDs. The remaining leg — the transfer_plan 16/32 MiB chunk ladder — turned out to be **entirely dead policy**, established by a 5-agent audit workflow + hand verification: every remote path overrode it with `Some(dial.chunk_bytes())` (push client 5 refresh sites + ensure_dial, pull_sync both literals); the only paths where the ladder won (local engine, test callers) discarded the value (`PlanUpdate` carries payloads only); the single workspace read of `PlannedPayloads.chunk_bytes` sat behind a `chunk_bytes == 0` guard no live caller can trigger (all pass the dial value, floored ≥ 64 KiB). The spec's "make transfer_plan take chunk_bytes as input" predates the dial — with zero consumers, threading a value through the planner would be plumbing with no reader, so the honest single-owner outcome was deletion. Slice `01209bc`: ladder + `Plan` wrapper deleted (`build_plan` → `Vec<TransferTask>`); `PlannedPayloads` deleted (`plan_transfer_payloads` → `Result<Vec<TransferPayload>>`, ripple through diff_planner/streaming_plan/pipeline tests/re-exports); `PlanOptions.chunk_bytes_override` + all refresh sites deleted (push `plan_options` now immutable default; two arms keep bare `ensure_dial` calls — first-need creation and first-wins ceilings unchanged); unreachable fallback guard in `stream_fallback_from_queue` deleted; `plan_to_daemon_format` deleted (git log -S: never called in repo history — its "server pull mode" comment was never true); orphaned `TuningParams` deleted (producer died at ue-r2-1e); write-only kickoff histogram collapsed to the `total_bytes` accumulator that was its only read. Comment-truth sweep: dial.rs mutability-model doc no longer claims chunk/prefetch are "read at each use site" (consumers snapshot at session/pipeline/batch setup; steps reach epoch-N sockets and later fallback batches); buffer.rs example cites the dial, not `TuningParams`. Behavior byte-identical on every live path. Tests: +4 transfer_plan unit pins (module had zero) — tier classification/interleave, single-small-file no-tar, force_tar single-file, count-target shard splitting with the 128 clamp; deletions are compile-guarded (w2-1 evidence shape); zero tests deleted. Review: codex **NEEDS FIXES (1 Low)** — the first bare ensure_dial comment said "fallback batch" inside the `TransferMode::DataPlane` branch; accepted (mislabel sits exactly on the invariant under review), fixed `27f53a0` (one word). W3.1's "after W2.2 settles the tuning owner" prerequisite is now settled: the owner is `engine::TransferDial`. New discoveries → STATE Open questions: `725aa07` tracked a 236-file stale worktree snapshot (`.claude/worktrees/vigilant-mayer/`) into the repo; WHITEPAPER still describes the pre-dial tuning world (stale since ue-r2-1e, w10 territory). Validation both commits: fmt/clippy clean, `cargo test --workspace` 1452/0/2 across 37 suites (baseline 1448). All on master, unpushed. Next: w3-1 (memory-aware BufferPool) tops the open queue; design-3 remains the sanctioned smaller alternative.

**2026-07-04 14:25:35Z** - **CODER (w4-5-supports-cancellation-flip, claude)**: Landed w4-5 through the codex loop (owner go: "continue" → topmost open row per the 11th handoff). D-2026-07-04-3 executed: `ActiveJobKind::supports_cancellation` flipped from DelegatedPull-only to `!matches!(self, Pull)` — every kind that can hold an active row is now cancellable; only history-only `Pull` stays gated (its RPC died at ue-r2-1h, so the Unsupported/FailedPrecondition/exit-2 arm survives end-to-end purely as the contract's escape hatch). `blit jobs cancel` and the TUI `K`/`Shift+X` now fire the row token for attached Push/PullSync; the w4-3 dispatcher race makes teardown + client `Status::cancelled` real. Slice `05a8b39`. A 3-agent workflow sweep (prod code / tests / docs) enumerated every surface encoding the old policy before editing: confirmed the TUI has NO kind-based cancel gating and the CLI 0/1/2 mapping is outcome-based — the daemon-side predicate is the entire behavior change; zero CLI/TUI logic edits. Comment surfaces updated: `supports_cancellation` rustdoc rewritten as the decision requires (old "disconnecting IS the cancel" rationale recorded as superseded), `CancelOutcome::Unsupported` + blit-app `CancelJobOutcome::Unsupported` docs, `cancel_job`'s FailedPrecondition message de-rationalized, `resolve_streaming_outcome`'s "production-unreachable" note now says the arm is live, `proto/blit.proto`'s CancelJob wire-contract comment (the last flat old-policy statement), `jobs_lifecycle.rs` header, w4-3 finding doc scope note annotated per the decision's Supersedes line. Tests (blit-daemon 168 → 170): policy pin flipped; `cancel_fires_token_for_push_and_pull_sync` + RPC-level `cancel_job_ok_for_push_and_pull_sync` pin the new contract; Pull-only Unsupported pins split out; authz-precedence test re-anchored on Pull AND extended — a different-host caller on a Push row now gets `Unauthorized`, pinning that flipped kinds went under the audit-9 authz gate, not around it. Mutation-verified: reverting the predicate fails exactly those four tests (4/15 under the `cancel` filter), restore → all 19 green. Review: codex **NEEDS FIXES (1 Low)** — the module-level "Scope so far" changelog still said the `pull` dispatch site is wired and "all four kinds are now constructed on the wire path", contradicting the new policy rustdoc 140 lines below; accepted (same doc-drift class codex caught in w1-4), fixed `1708075` (bullets annotated in place, changelog framing kept). Known gap: no e2e drives a live mid-flight attached cancel (needs a test seam or timing the w9-3-flaky daemon-spawn family can't afford; the pin chain covers every link the e2e would compose — same evidence shape w4-3 was graded on). Validation both commits: fmt/clippy clean, `cargo test --workspace` 1448/0/2 across 37 suites (baseline 1446). All on master, unpushed. Next: w2-2 (stream-ladder owner) tops the open queue; design-3 remains the sanctioned smaller alternative.

**2026-07-04 13:53:22Z** - **DECISIONS (owner Q&A, claude)**: The owner asked for the four standing questions "one at a time, no idea what these refer to" — each was presented in plain English with options and answered: (1) **commit erratum → leave as-is** (D-2026-07-04-2; mirrors the D-2026-06-07-1 no-rewrite calculus — two bisect-skippable commits beat force-pushing shared history); (2) **10 GbE session → "soon, but keep coding first"** (STATE Blocked reworded: not a daily blocker; owner will call "benchmark"); (3) **D-2026-06-20-1 stale warmup/size-gate wording → "follow the existing pattern"** — the ledger's own precedent (D-2026-06-20-2's veto annotation, D-2026-06-20-6's struck scope clause) IS edit-in-place-with-annotation, so the superseded framings are struck with pointers to -2 q1 and REV4/-5 (bounded-unilateral untouched — still true), and -5's "remains an open question" note resolved; (4) **supports_cancellation → flip it** (D-2026-07-04-3): CancelJob + TUI F2 will work on attached Push/PullSync transfers; policy-only after w4-3's race wiring; contract change (exit 2→0) recorded; implementation queued as **w4-5-supports-cancellation-flip**, now the topmost open REVIEW.md row. Batch `2a21d6f` through the codex loop per D-2026-07-04-1: **NEEDS FIXES (1 Medium + 1 Low, both STATE.md coherence)** — the Now bullet still called the erratum an open owner call, and the queue rewrite dropped the coder's-pick clause (design-3-vs-w2-2 ordering contradiction); both accepted, fixed `a928193`. The decision content itself passed all cross-checks (ledger consistency, w4-3 scope-note agreement, strike precision). check-docs.sh green. All on master, unpushed.

**2026-07-04 13:36:48Z** - **CODER (w1-4-accept-token-constants, claude)**: Landed w1-4 through the codex loop (same session as w1-2/w1-3 — the W1 transport-policy family is now complete). The accept(30s)/token(15s) bound pair was declared three times at HEAD (the audit counted four; one died with the Pull RPC at ue-r2-1h): the push pair with the full R46-F7 rationale, pull_sync's module-scope `PULL_*` pair, and the resume path's function-local pair behind a `StdDuration2` alias. Slice `6a19e1d`: the pair now lives in `blit_core::remote::transfer::socket` (`DATA_PLANE_ACCEPT_TIMEOUT`/`DATA_PLANE_TOKEN_TIMEOUT`, the push side's doc comments moved with it — the data-plane policy module from w1-2/w1-3 is the natural home), all three local declarations deleted, every daemon use site renamed to the one name (`RESIZE_ARM_TTL` still equals the accept bound); values byte-identical, no behavior change, no new tests (a constant-equality test would be vacuous; the bounded-accept behavior is pinned by the existing audit-h3a-family tests). Records `484e70b`. Review: codex **NEEDS FIXES (1 Low)** — stall_guard.rs's module doc + `TRANSFER_STALL_TIMEOUT` rustdoc still named the deleted `PULL_*` pair (doc drift); accepted, fixed `d17b089` (plus the same lines' stale `daemon::service::{pull, pull_sync}` reference — the pull service died at ue-r2-1h; disclosed in the verdict, not silently bundled). Codex's sweep confirmed no fourth declaration or stray data-plane 30s/15s literal survives (remaining `from_secs(30)` hits are control-plane connect bounds, test harness timeouts, and `TRANSFER_STALL_TIMEOUT` — different policy families, deliberately untouched per the finding doc's Known gaps). Environment note recorded: the codex first attempt hung on the known inherited-stdin quirk (chained `git && codex` invocation); killed, re-run standalone with `< /dev/null` — worked. Validation: fmt/clippy clean, `cargo test --workspace` 1446/0/2 across 37 suites (unchanged). All on master, unpushed. Next open ratified row: design-3 (data-plane connect timeouts — now trivially placeable, the call sites are consolidated and the bound can import the shared pair).

**2026-07-04 13:06:08Z** - **CODER (w1-3-tcp-keepalive-honesty, claude)**: Landed w1-3 through the codex loop (same session as w1-2, fourth slice under D-2026-07-04-1). The audit's finding: both `set_keepalive(true)` sites ran OS-default timing (~2 h idle before the first probe) while their comments claimed idle-stream liveness — overselling by two orders of magnitude. w1-2 had already collapsed both sites into the shared helper (and satisfied the "daemon copy logs failure" clause structurally — the daemon copy no longer exists), so this slice took the ratified row's real-timing exit at that single site (`865fc1e`): `TcpKeepalive` idle 60 s / interval 10 s / retries 5 via `set_tcp_keepalive` (which also enables SO_KEEPALIVE — the bare call is replaced, not supplemented; best-effort-logged posture unchanged). A vanished peer on an IDLE data socket (armed resize slot, stream awaiting work) is now detected in ~2 minutes; StallGuard's 30 s keeps owning the stalled-peer-with-data-in-flight case; the window is deliberately more conservative than the control plane's HTTP/2 keepalive (audit-1b) so the data plane never declares death first. blit-core socket2 → `features=["all"]` (unlocks `with_retries` + the getters; API surface only, no new deps, no lockfile delta). +1 test (`keepalive_timing_is_explicit`, `#[cfg(unix)]` — getters don't exist on Windows): reads idle/interval/retries back THROUGH THE KERNEL and pins each to the policy constant; mutation-verified (bare-enable revert fails on the idle-time assertion). Records `894f5e8`. Review: codex **PASS, zero findings** — independently confirmed via socket2 source that Windows sets retries through `TCP_KEEPCNT`, that healthy-idle connections answer probes (no false-positive kill class), and via ci.yml that all three OS runners compile the feature surface; verdict recorded, no fix commit. Validation: fmt/clippy clean, `cargo test --workspace` 1446/0/2 across 37 suites (blit-core 417 → 418). All on master, unpushed. Next: w1-4 (accept/token constant pair) heads the ratified queue.

**2026-07-04 12:54:24Z** - **CODER (w1-2-data-socket-policy-helper, claude)**: Landed w1-2 through the codex loop (third slice fully under D-2026-07-04-1). The NODELAY/keepalive/tuned-buffer socket policy existed as two divergent push-only copies — `DataPlaneSession::connect_with_probe` (nodelay hard, keepalive/buffers logged, via an `into_std`/`from_std` round trip) and a private daemon twin of the same name that silently swallowed both options (`let _ =`) and never set buffers — while the entire pull direction ran bare: Nagle on at both ends of the path that carries pull bytes, and the pull_sync daemon's `TransferDial.tcp_buffer_bytes` ramping to 8 MiB and never being read (computed-and-discarded, exactly as the audit filed). Site map re-derived this session (5-reader workflow fan-out): the audit's "pull connect + 3 daemon accepts" predates REV4 — live sites at HEAD are the pull client `connect_pull_stream`, the push-accept family (3 call sites through the twin), and the pull_sync epoch-0/resize/resume accepts. Slice `16237e2`: `blit_core::remote::transfer::socket::configure_data_socket(&TcpStream, Option<usize>)` — `SockRef`-based in-place (round trip + its two error paths deleted), nodelay hard / keepalive+buffers logged; `connect_with_probe` routes through it; `connect_pull_stream` calls it with `None` (the pull dial lives on the daemon — this is the pull-client Nagle fix); daemon push accepts call it with `None` (receiver holds no dial; twin deleted, daemon `socket2` dep dropped; posture unified — option failures now Status::internal on the two fatal accept paths, logged-lapse on the resize arm, instead of silent); pull_sync accepts pass the dial — `accept_and_wrap_sinks` gains a `tcp_buffer_size` param (epoch-0 snapshot = `None`, matching push epoch-0 semantics), `accept_one_resize_socket` reads `dial.tcp_buffer_bytes()` LIVE (the one pull socket that actually gets the ramped 8 MiB — mirror of push `add_stream`), resume passes the snapshot (no tuner → `None` in practice). Deliberately untouched: design-3 connect timeouts (same call sites, disjoint lines — that slice just got smaller), w1-3 keepalive-timing honesty (though its "daemon copy logs failure" half is now satisfied structurally — there is no daemon copy), w1-4 constants. +3 tests (helper policy Some/None on a loopback pair asserting via SockRef getters; `connect_pull_stream_applies_socket_policy` pinning the call-site wiring on the RETURNED stream), mutation-verified (M1 unwire pull call → wiring test fails while helper tests stay green; M2 drop nodelay → all 3 fail; M3 drop buffer block → Some-test fails, None-test rightly survives). Records `f60942e`. Review: codex **PASS, zero findings** — independently confirmed socket policy stayed out of framing/session construction (StallGuard/accounting untouched) and the resize fatal/non-fatal postures held; verdict recorded, no fix commit. Validation: fmt/clippy clean, `cargo test --workspace` 1445/0/2 across 37 suites; blit-core 414 → 417 (stash-measured HEAD baseline), blit-daemon 168 unchanged (the w4-3 record's "167" was the Windows-host count). All on master, unpushed. Next: w1-3 (keepalive honesty) heads the ratified queue.

**2026-07-04 05:59:38Z** - **CODER (w4-3-daemon-disconnect-racing, claude)**: Landed w4-3 through the codex loop (D-2026-07-04-1, second slice fully under it). The push and pull_sync dispatchers bare-awaited their handlers, so a client that disconnected during a send-free compute phase (pull_sync's enumerate+checksum collection is the longest window) left the daemon running the whole remaining handler for a dead peer — while `active_jobs.rs` claimed a `tx.closed()` drop mechanism only `delegated_pull` actually had. Slice `37d7f91`: `resolve_delegated_pull_outcome` generalized to `resolve_transfer_outcome<T>` (same audit-10 biased handler-first select, single owner of the three-way race); new `resolve_streaming_outcome` races a streaming handler against `tx.closed()` + the row's cancel token and maps a race loss onto the ring pair ("client cancelled" / "cancelled via CancelJob", delegated_pull's vocabulary, terminal `Status::cancelled` to a still-connected client); both spawn closures rewired (revert-to-bare-await leaves the helper dead → clippy `-D warnings` catches the unwiring); the false `supports_cancellation` comment rewritten as policy-vs-capability (+ the stale test comment and `CancelOutcome::Unsupported` doc). The audit's "three spawn closures" is two at HEAD — the pull closure died with the Pull RPC at ue-r2-1h. **Deliberately out of scope**: `supports_cancellation` policy unchanged (ratified spec asked for the race + comment fix, not a CancelJob contract change); the token arm is production-unreachable for push/pull_sync until the owner flips the policy — now policy-only, filed as an open question. +5 deterministic unit tests (hangup, CancelJob, bias, error-forward, clean-success; ready/pending futures, no timing), each select arm mutation-verified (M1 drop tx_closed arm → hangup test hangs, cancel test passes; M2 drop cancelled arm → both cancel tests hang, hangup passes; M3 handler-last bias → both bias tests fail). Records `b7382ac`. Review: codex **PASS, zero findings** — it independently traced sink-pipeline drop-propagation ("does not create unbounded orphaned transfer work") and confirmed the policy freeze intentional; verdict recorded, no fix commit. Validation (on the owner's Windows host = parity run): fmt/clippy clean, `cargo test --workspace` all 37 suites green, blit-daemon 162 → 167. Known gaps: in-flight `spawn_blocking` enumeration/checksum batches still run to natural end on drop (the audit's stated follow-up slice); `pull_sync.rs` resize-validation `Vec<JoinHandle>` noted for the w4-1 family ledger. Environment note: `cargo test` under Git Bash fails to link (coreutils `link` shadows MSVC `link.exe`) — use PowerShell. All on master, unpushed. Next: w1-2 (data-socket policy helper) heads the ratified queue.

**2026-07-04 03:37:49Z** - **CODER (ue-r2-2, claude)**: Landed stream resize (REV4 `ue-r2-2`, the ninth and FINAL slice — **REV4 is complete**; all three static ladders died at 1e/1f/1h and mid-transfer stream add/drop now runs on the 1a elastic queue + 1e mutable dial + 1b wire contract, "a wire-up, not a restructuring" as specified). Stack: `042ca4b` engine policy (streams are the EXPENSIVE dial — `resize_tick` proposes ±1 stream/epoch only with cheap dials pinned at a bound, the signal sustained 2 busy ticks, 4-tick cooldown, one-in-flight, bounded 1..=profile ceiling; `resize_settled` records what ACTUALLY happened; growable `SharedStreamProbes` tuner registry), `ce0e396` elastic pipeline (`SinkControl::Add`/`RetireOne`; retire drains at the payload boundary via a per-worker watch and its `finish()` END is the receiver-side teardown — REMOVE needs zero receiver coordination; floor at 1; exactly-once pinned), `04c9c6d` push (client controller/dialer: pre-dial ADD registration, dial-on-ack, non-fatal failed dials; daemon armed-only acceptor — sockets identified by WHICH sub-token they echo, not arrival order, dissolving the epoch race; epoch-N failures non-fatal, epoch-0 fatality parity; control stream serviced during transfer), `b08d80e` pull (daemon controller in `stream_via_data_plane`: proposals→commands, client acks, TTL-armed single accept slot; client growable JoinSet receiver — same AbortOnDrop cascade, now covering added workers; delegated inherits whole via `dst_capabilities`), `0788e83` fmt+wire-test+finding doc. Scope interpretation stated for review: pull 1s-start is post-REV4 residue (moving pull negotiation ahead of enumeration IS a restructuring); resume/fallback/relay excluded per their own contracts; the 1g W1 deferral re-ratified via armed-only accepts; 10 GbE remains the plan-named sign-off for telemetry-triggered behavior. Review: codex **NEEDS FIXES (3)** + 3-lens panel (4 more), fixes in `ec4a3fe` — headline: the panel caught that **pull resize was dead on the CLI path** (the capability flip was a dropped edit — an Edit failed mid-session and was never re-issued; the hand-built-spec wire tests masked it); codex caught the missing CUMULATIVE stream bound at both acking ends (replayable ADDs) and the pull controller's inline 15s handshake freeze; plus END-on-dead-pipeline closes, biased control-first supervisor + post-break Add drain, optional-worker zero-received leniency (the teardown race is now genuinely non-fatal), tuner idle-tick sustain reset, acceptor expiry wake, REMOVE settle-at-retire. 1 deferred (epoch-0 loop vs a <2.5s ADD — improbable at current constants, Known gaps). Validation: fmt/clippy clean; tests **1405 / 0 / 3 on Windows** (entering 1393; +12: 6 dial policy, 4 elastic pipeline, 2 pull-resize wire incl. a real-listener 48-byte handshake pin). All on master, unpushed (origin at `7603177`). Next: REV4 acceptance rides the owner's 10 GbE session (`ue-1`/`ue-2`); the design-review queue (w4-1 first) is the working queue.

**2026-07-04 01:59:04Z** - **CODER (ue-r2-1h, claude)**: Landed the deprecated-Pull-RPC deletion (REV4 `ue-r2-1h`, eighth slice through the code→GPT-review→fix loop) — and with it the LAST of the three static ladders (`pull_stream_count`, which died untested: nothing anywhere pinned it). Slice `2a13f53`: proto loses `rpc Pull` + `PullRequest` + `PullChunk` whole (BlitAuth-style dated tombstones; PullSummary/ManifestBatch/FileHeader/FileData/Negotiation survive — PullSync/push/delegated share them); daemon loses `service/pull.rs` (953 lines, incl. 2 of design-2's 3 orphaned-data-plane spawn sites — finding doc annotated, w4-1 row rescoped) with `PullEntry`/`collect_pull_entries_with_checksums`/`build_file_header` + their 2 tests relocated byte-identical into `pull_sync.rs` (the 1g borrow-back closes); client loses the dead `RemotePullClient::pull` (zero callers). The relay escape hatch's `scan_remote_files`/`open_remote_file` (owner-mandated port, REVIEW.md w2-4 / D-2026-06-11-2 — REV4's slice text omitted the relay; interpretation stated in the finding doc) now ride PullSync sessions: new additive `TransferOperationSpec.metadata_only = 13` (receiver_capacity precedent, no spec_version bump; old daemon ignores it and streams data the scan discards — correct-but-wasteful, pinned by a mock test) for the header scan, and single-file force_grpc sessions read through a rewritten `RemoteFileStream` for byte streaming. +6 tests (5 CannedFramesServer wire tests, 1 nested-tree relay e2e). **First slice validated on the owner's Windows host, which surfaced two pre-existing problems fixed in their own commits**: `9f37a7a` — 15 Windows-only clippy violations (the ps1 script never ran clippy); `48c5a11` — **win-1 (High)**: the daemon's push need-list echoed `PathBuf::to_string_lossy` backslash paths, so every nested push to a Windows daemon planned zero payloads for separator-bearing files and stalled 30s (`test_push_nested_directories` + `forced_grpc_push_many_files_completes` failed on Windows for this; the new relay e2e exposed it; one-line `relative_path_to_posix` fix, 300s→0.77s). **Commit erratum**: the `pull.rs` deletion was staged early and rode into `9f37a7a` — the two intermediate commits don't build (bisect must skip); disclosed in the finding doc, owner may authorize a history fix. Review: codex **NEEDS FIXES (3)** + 3-lens adversarial panel, adjudicated in `.review/results/ue-r2-1h.gpt-verdict.md`; fixes in `f6f52d7`: delegated pull now REJECTS forwarded `metadata_only` (both reviewers independently: the dst would materialize bare headers as zero-byte files with a success summary — comments corrected from 'ignores' to 'rejects', test pins); `RemoteFileStream` recursion → loop (hostile-chatter stack growth) + second-header protocol error (silent splice); docs/API.md + ARCHITECTURE.md purged of the deleted rpc. Rejected with citation: the 'outside recv_fallback_message' half (audit-h3c's own doc scopes this adapter shape out). Deferred: pre-existing relay subpath double-join (`relay-1-subpath-double-join.md` + REVIEW row). Validation: fmt/clippy clean; tests **1393 / 0 / 3 on Windows** (unix baseline 1413/0/2 not host-comparable — unix-gated tests compile out; +8 net new this slice). All on master, unpushed (origin at `7603177`). `Cargo.lock` drift (dep bumps, pre-session) left uncommitted for the owner. REV4 remaining: `ue-r2-2` (stream resize — the last slice).

**2026-07-03 19:33:59Z** - **CODER (ue-r2-1g, claude)**: Landed PullSync multistream through the engine (REV4 `ue-r2-1g`, seventh slice through the code→GPT-review→fix loop; absorbs `MULTISTREAM_PULL.md`). Key discovery that shaped the slice: the CLIENT side of multistream PullSync already existed — `pull_sync_with_spec` has routed negotiations through the `stream_count`-honoring fan-out (`receive_data_plane_streams_owned`) since `69d8599` (2025-11-15), months before any client sent a capacity profile (`a0d2c9f`, 1e) — proven from git history, so the profile-presence gate cannot strand any committed client. The slice (`48e583e`) is therefore daemon-side: `negotiated_pull_streams` proposes from `engine::initial_stream_proposal` (the daemon is the byte sender AND shape-knower on pull; the engine fn's doc now states the proposer is the shape-knowing end either direction), gated on the client's advertised `receiver_capacity` (absent/unknown `max_streams` → 1 stream, today's behavior byte-for-byte per REV4 Design §5), recorded on the dial; `accept_and_wrap_sinks` (accept N, bounded token auth, N `DataPlaneSink`s) HARVESTED verbatim from the deprecated Pull RPC into `pull_sync.rs` (the deprecated handlers borrow it back until 1h deletes them); the 1a elastic work-stealing pipeline does the fan-out across N sinks. NO proto changes. Resume keeps its dedicated single-stream path (ordered JIT block-hash protocol — explicit RELIABLE exception); gRPC fallback untouched; delegated daemon→daemon inherits free via the dst-stamped profile. Deliberate deltas called out: prefetch 8→`dial.prefetch_count()`, pool scales with streams. Pull 1s-start explicitly NOT met and cannot be yet — the shape-keyed proposal inherently follows the full scan; it rides on `ue-r2-2` resize (recorded in Known gaps, not silently skipped). Review: codex **NEEDS FIXES → 2 accepted + fixed** (`4a2e58d`): cancellation-mid-transfer test with live sockets (TCP-level teardown observability) + dial bookkeeping on the conservative arm. Additionally ran a 3-lens adversarial self-review panel (concurrency/compat/RELIABLE): 2 more accepted + fixed same commit — client now clamps daemon-advertised `stream_count` to its own advertised ceiling (`bounded_stream_count`, REV4 §4 "weak end protects itself" made real receive-side) and the harvested helper's token-mismatch status restored to UNAUTHENTICATED (pull_sync wire behavior pre-slice-exact; the delta moved to the deprecated path) — 1 deferred (sequential-accept pin growth ~N×, bounded + precedented → W1 socket-policy row). e2e proves >1 stream observably (300 files → 2 streams, marker + byte-identical, revert-proven). Validation: fmt/clippy clean, `cargo test --workspace` **1413 / 0 / 2** (baseline 1403; +8 slice, +2 review). All on master, unpushed (origin at `7603177`). Ladder #3 (`pull_stream_count`) now dies with its RPC at `1h`. Next: `ue-r2-1h` (delete deprecated Pull RPC; must relocate `PullEntry`/`collect_pull_entries_with_checksums` — noted in the finding doc).

**2026-07-03 18:32:57Z** - **CODER (ue-r2-1f, claude)**: Landed push convergence (REV4 `ue-r2-1f`, sixth slice through the code→GPT-review→fix loop). The daemon-push `desired_streams` ladder — the one the old `tuning.rs` doc said "wins" — is retired into the engine (`a4a9f70`): `engine::initial_stream_proposal` carries the shape table verbatim (bytes OR file-count keyed), clamped to the proposer's advertised receiver ceiling; both daemon negotiation sites call it; the private ladder is deleted (it had zero tests — the engine fn now has full tier-boundary coverage, extended ±1 per review). Wire-identical negotiations today (table max 16 < ceiling 32); the client's dial still clamps sender-side (1e). Second of three ladders gone; `pull_stream_count` retires at 1g/1h. The finding doc states the interpretation of "route push through the engine" explicitly (push's gRPC manifest/need-list loop = protocol boundary per REV4 Design §1's own list; the slice's substance = decision-layer ownership) and put it to the reviewer: codex **PASS with one Low** (boundary-value test gap, fixed `0c8da50`) and explicitly judged the interpretation **plan-conformant**. Validation: fmt/clippy clean, `cargo test --workspace` 1403 / 0 / 2. All on master, unpushed (origin at `7603177`). Next: `ue-r2-1g` (PullSync multistream through the engine).

**2026-07-03 18:17:06Z** - **CODER (ue-r2-1e, claude)**: Landed the live cheap dials (REV4 `ue-r2-1e`, fifth slice through the code→GPT-review→fix loop). `engine/dial.rs` `TransferDial` (`3be9105`): conservative floor start (16MiB chunk / prefetch 4 — no probe phase, no size-gated start per D-2026-06-20-1/-2), ceilings = old top tier clamped by the receiver `CapacityProfile` (0 = unknown = keep conservative defaults; in-flight budget bounds chunk THEN prefetch), atomic live reads, hysteresis stepper (<5% blocked → up, >30% → down). Receivers now actually advertise profiles (`a0d2c9f`): daemon push negotiations, pull_sync client spec, delegated dst override (replaces CLI-supplied profile with its own). Ladder retired (`98943b7`): `remote/tuning.rs::determine_remote_tuning` DELETED with its 4 tier tests (offset by 7 dial tests, called out); all six callers read per-transfer dials — push client `ensure_dial` (first-wins; negotiated streams clamp via the dial; per-batch `chunk_bytes_override` refreshed from the LIVE dial), pull_sync daemon dial from the client's spec profile (`NormalizedTransferOperation` now carries `receiver_capacity` — the 1b known-gap consumer), deprecated Pull conservative (dies 1h). Live tuner (`15968f4`): `spawn_dial_tuner` samples PR1 per-stream telemetry every 500ms; push data plane gets the first production `LiveProbe` wiring via `DataPlaneSession::connect_with_probe`; tuner aborts on finish, self-terminates via Weak. Carried 1a finding fixed: `write_blocked_nanos` times ONLY the socket write (async-block clock inside the join), not the overlapped write+read. codex review: **NEEDS FIXES → 3 Mediums, all accepted + fixed** (`46da929`): in-flight budget bounds the chunk ceiling too; idle tuner ticks (no bytes) are no-signal, not clean-pipe; tar-shard writes now feed the blocked signal. (First codex attempt hung on an inherited-stdin quirk — killed, re-run standalone.) Validation: fmt/clippy clean, `cargo test --workspace` 1402 / 0 / 2 (slice baseline 1399; 4 deleted ladder tests called out in the finding doc). All on master, unpushed (origin at `e1a21a1`). Next: `ue-r2-1f` (push converge; retires `desired_streams`).

**2026-07-03 16:54:30Z** - **CODER (ue-r2-1d, claude)**: Landed the streaming plan foundation (REV4 `ue-r2-1d`, fourth slice through the code→GPT-review→fix loop). The engine's local streaming leg now plans from a PARTIAL header stream (`c08a5c1`): new `engine/streaming_plan.rs` with `InitialPlan`/`InitialPlanStrategy` (Design §3 novel/known telemetry split made explicit from the tuning window) + `PlanUpdate`; batcher flushes on 512 headers / 250ms timer (the slow-enumeration mitigation holding the ~1s-start budget) / scan end; per-batch `plan_local_mirror` (diff semantics unchanged — per-header dest stats) feeds `execute_sink_pipeline_streaming` concurrently via `tokio::join!`. RELIABLE: mirror deletion still gated on a complete clean scan; failure envelope documented (hard scan error can follow completed file writes). Phase split redefined for the predictor (planner = time-to-first-payload; estimate query after join, before observe). Structural proof: gated-source test deadlocks (30s timeout) against collect-all, passes 0.25s streaming — revert-proven. codex/GPT-5.5: **FAIL → fix-then-ship, 2 findings, both accepted + fixed** (`29159ca`): High — nested destination (dest under src) could self-copy recursively with writes concurrent to the walk → streaming planner drops headers under the destination's source-relative prefix (`exclude_dest_subtree`) + two-run regression test; Medium — scan JoinHandle unobserved on early error returns → awaited right after the join, before pipeline→planner→scan precedence. Discovered pre-existing gap surfaced (finding doc Known gaps): streaming summaries never populate tar/raw bucket stats, so the tuning window can never admit a streaming record and `derive_local_plan_tuning` is dead at HEAD — Known/Novel resolves Novel in practice; deliberately not activated this slice (1e/w2-2 territory). Validation: fmt/clippy clean, `cargo test --workspace` 1399 / 0 / 2 (baseline 1394, +5). All on master, unpushed (origin at `f648784`). Next: `ue-r2-1e` (live cheap dials).

**2026-07-03 16:26:55Z** - **CODER (ue-r2-1c, claude)**: Landed the engine shell (REV4 `ue-r2-1c`, third slice through the code→GPT-review→fix loop). New `crates/blit-core/src/engine/` module: `TransferEngine::execute(EngineRequest)` owns strategy selection (single-file → journal probe → fast-path walk → streaming) and the streaming leg (tuning window → scan → diff/plan → sink pipeline → mirror deletions → journal checkpoints → history/predictor); `TransferOrchestrator` shrank to the local adapter (preconditions, `FsTransferSource`/`FilteredSource` + `FsTransferSink`/`NullSink` construction, option translation) with the public API preserved via re-exports — engine type thereby ratified per REV4 Design §1 (new engine + adapter, not a rename). Sequence: behavior pins first (`7730eb1`: NoWork/UpToDate + `no_work` tag, NoWork/SourceEmpty — both previously untested), the move (`dc9b0ed`: `fast_path.rs`→`engine/strategy.rs`, options/summary/history moved, tuning/mirror/journal/single-file split out; move fidelity machine-checked — 75 diff lines vs origin, all six documented transformations, zero drift), then the REV4-named accounting gap (`29e210b`: single-file strategy now records perf history tag `single_file` / `null_sink` lane + predictor, guard test proven by revert). Review: first codex run died on provider quota (121k tokens, recorded); owner restored access same day; retry PASS with 1 Low, accepted + fixed (`15e6334`: engine back-references through `crate::orchestrator` → engine-local paths; swept one more the reviewer missed). Also recorded owner-provided 10GbE test env (`admin@skippy:/mnt/generic-pool/video/test`) in STATE queue item 4. Validation: fmt/clippy clean, `cargo test --workspace` 1394 / 0 / 2 (baseline 1391, +3). All on master, unpushed. Next: `ue-r2-1d` (streaming plan foundation) — or `1e`/w4-1 per queue.

**2026-07-03 15:43:45Z** - **CODER (ue-r2-1b, claude)**: Landed the wire dial contract (REV4 `ue-r2-1b`, second slice through the code→GPT-review→fix loop, D-2026-06-20-6). Proto (`2741dc8`): new `CapacityProfile` (cpu_cores, `DrainClass` enum, load_percent, max_streams, drain_rate_bytes_per_sec, max_chunk_bytes, max_inflight_bytes; 0 = unknown = stay conservative) carried as `DataTransferNegotiation.receiver_capacity = 11` (push: daemon is byte receiver) and `TransferOperationSpec.receiver_capacity = 12` (pull_sync/delegated: client/dst is byte receiver) — **spec_version deliberately stays 2** (exact-match gate at `operation_spec.rs:107` means a bump would make old daemons reject new clients; the profile is a skippable hint, unlike v2's safety-critical field). Daemon-authoritative `resize_enabled = 12` + `epoch0_sub_token = 13` on the negotiation; capability bits `PushHeader.supports_stream_resize = 8` / `PeerCapabilities.supports_stream_resize = 5` (all false until ue-r2-2); `DataPlaneResize`/`DataPlaneResizeAck` from adaptive-PR3 prior art (`d9d4ec7`) as oneof variants in all four control streams (ClientPushRequest=9, ServerPushResponse=5, ClientPullMessage=5, ServerPullMessage=16). PR3's field-number clash (its negotiation 11-14) resolved: min/max stream bounds subsumed by `CapacityProfile.max_streams`, floor 1. Zero behavior: all literals stamped with defaults; new variants ignored on receive like unknown payloads; one intentional semantic addition — the delegated dst override now also strips CLI-supplied `receiver_capacity` (R25-F2 boundary; prevents a fabricated ceiling leaking once ue-r2-1e reads the field). Compat tests (`crates/blit-core/tests/proto_wire_compat.rs`, first use of test-local `#[derive(prost::Message)]` old-shape replicas): old→new + new→old for negotiation/spec/PushHeader/caps including normalization through the real `from_spec` chokepoint; resize frames decode as `payload: None` on old peers for all four oneofs (with known-variant controls); new↔new round trips. Review: codex/GPT-5.5 **PASS, zero findings**; supplementary 4-lens adversarial self-review (ultracode session) found 1 Low, accepted — the `receiver_capacity` comment falsely implied deprecated Pull carries a client→daemon profile (PullRequest has no spec channel); comment fixed in `5bd345a`. Validation: fmt/clippy clean, `cargo test --workspace` 1391 passed / 0 failed / 2 ignored (baseline 1378, +13). All on master, unpushed. Next: `ue-r2-1c` (engine shell + local adapter).

**2026-06-21 03:02:29Z** - **CODER (ue-r2-1a, claude)**: Landed the adaptive-streams substrate (REV4 `ue-r2-1a`, first slice; first end-to-end run of the code→GPT-review→fix loop, D-2026-06-20-6). Cherry-picked over the `-s ours` octopus trap (D-2026-06-07-2, where a plain merge no-ops): PR1 per-stream telemetry zero-cost `Probe` (`e569eea`), PR2 shared work-stealing flume queue (`3844a15`), PR2 forwarder-halt-on-error fix (`ec561f2`). Hand-resolved the `data_plane.rs` StallGuardWriter-vs-`Probe` conflict (compose: stream stays `StallGuardWriter<TcpStream>`, struct gains generic `<P: Probe = NoProbe>`, `from_stream_with_probe` wraps the guard) and `mod.rs` re-exports (dropped `Phase`/`TransferProgress`/`TransferProgressSnapshot` — master had removed them; added telemetry types + the `AtomicU8` import). Excluded `eafb187` (doc-shuffle; embeds `C:/Users` paths) and `d9d4ec7` (PR3 WIP, does not build). Work-stealing behaviour tests added (`771a632`): byte/file exactly-once accounting + producer-cancel graceful wind-down. codex/GPT-5.5 read-only review → fix-then-ship, 4 findings, all accepted, fixed in `90ed43d`: F1 (High→Medium) workers re-check `cancelled` before each recv (bounds survivor work after first error; interrupting in-flight / hard-abort-on-drop stays w4-1); F2 `send_block` now calls `probe.record_bytes`; F3 exactly-once path assertion; F4 multi-sink cancel-under-backpressure test. Carried to `ue-r2-1e`: PR1 `write_blocked_nanos` `join!` over-measure (Medium) + tar-shard timing (Low) — telemetry has no live consumer until the dial. Validation at each step: fmt/clippy clean, `cargo test --workspace` 1378 passed / 0 failed / 2 ignored (baseline 1370). All on master, unpushed.

**2026-06-20 23:50:40Z** - **PLAN REVIEW + DECISION (claude)**: Owner asked to review the three `UNIFIED_TRANSFER_ENGINE` candidates and produce a final revision *only if warranted*. Reviewed all three against the tree at `HEAD` `09268eb` (Explore agent + direct greps/reads). **A REV4 was warranted**: REV3's headline "corrected against code" claim was itself wrong, and the error was load-bearing. The previous DEVLOG entry (2026-06-20 20:57:45Z) recorded REV3's "**two** static tables, not three; `push/control.rs::desired_streams` and `pull.rs::pull_stream_count` were stale" — **that is incorrect** (correcting forward, since DEVLOG is append-only): all three ladders are live — `remote/tuning.rs::determine_remote_tuning` (tuning.rs:11), `desired_streams` (`push/control.rs:476`, already file-count-aware), `pull_stream_count` (`pull.rs:904`) — and `tuning.rs:7-10`'s own doc comment says "the daemon's push negotiation currently runs its own ladder and wins." v1's "three ladders" enumeration was substantially right. Two further REV3 slips: `determine_remote_tuning` does NOT drive local (callers are push client + daemon pull/pull_sync; local copy uses its own orchestrator runtime), and "pull is single-stream" is true only of **PullSync** (`pull_sync.rs:568` hardcodes `stream_count = 1u32`) — the deprecated `Pull` RPC is already multistream. Strategy names were off too: real keys are `journal_no_work` (not `journal_skip`), `tiny_manifest`, `single_huge_file`, `no_work`; no distinct `single_file`. Verified-correct REV3 additions kept: proto `stream_count = 4` / `reserved 5-10` so `receiver_capacity = 11` is first free; all five salvage SHAs exist (`e6ef095`/`af66ff5`/`b797b73`/`eafb187`, excl. `d9d4ec7`); `perf_history::PerformanceRecord` + `perf_predictor` per-profile coefficients; `TransferSource`/`TransferSink`; the pipeline leaves; `plan_transfer_payloads`; `TransferOrchestrator` local-shaped. **Authored `docs/plan/UNIFIED_TRANSFER_ENGINE_REV4.md`** = REV3 with the code-reality section corrected and every symbol grounded with `file:line`, the under-converged-ladders risk added, and v1's lineage/absorption header carried forward (PIPELINE_UNIFICATION/UNIFIED_RECEIVE_PIPELINE Historical; MULTISTREAM_PULL absorbed as `ue-r2-1g`; H10b veto D-2026-06-20-3). **Owner then decided "rev4 replaces v1"** → recorded **D-2026-06-20-5** and propagated it: REV4 flipped Draft→Active; v1, REV2, REV3 flipped to Superseded (each with a `Superseded-by` header note); STATE.md Now/Queue/Authoritative-docs/Blocked/Open-questions + the `09268eb` handoff entry rewritten; the D-2026-06-20-4 freeze lifted *as to the plan decision only*. No code touched; coding still gated by AGENTS.md §9 (needs a fresh per-slice go-ahead; first slice `ue-r2-1a`). No git commit (owner gate); `Cargo.lock` left unstaged (unrelated dep refresh). Still open: authorize `ue-r2-1a`; edit D-2026-06-20-1's superseded warmup/size-gate wording (owner: not sure); push approval for `439a2a7`.

**2026-06-20 20:57:45Z** - **PLAN REVIEW (claude)**: Continued the frozen unified-transfer-engine plan review (D-2026-06-20-4). Authored `docs/plan/UNIFIED_TRANSFER_ENGINE_REV3.md` (Draft) = REV2 with the Risks section restored, the "C-ready by construction" acceptance criterion restored (both dropped in REV2), the static-ladder references corrected against code (there are **two** static tables — client `remote/tuning.rs::determine_remote_tuning` and daemon-side `DataTransferNegotiation.stream_count` (proto field 4) — plus pull's single-stream `force_grpc` path; the REV2/v1 "three ladders" / `push/control.rs::desired_streams` / `pull.rs::pull_stream_count` references were stale), explicit slice dependencies added, and labeled agent recommendations on the open questions. Verified `receiver_capacity = 11` is the first free proto field (1–4 used, 5–10 reserved RDMA). Two owner decisions recorded in REV3 + STATE: (a) first-byte-within-~1s is a hard invariant for every mode except the modes where moving any byte before full knowledge would be unsafe (mirror/delete, resume, checksum-refusal); novel vs known workload is a tuning-strategy choice (start-something-and-tune vs replay-optimal-last-run via the in-tree `perf_history`/`perf_predictor`), not an exception — both meet 1s; (b) deprecated `Pull` deletion stays in-plan as `ue-r2-1h`. Still open: replace-vs-amend (owner: no flip yet, REV3 stays Draft, v1 stays Active-but-parked) and the D-2026-06-20-1 superseded-wording cleanup (owner: not sure). No code touched; coding remains frozen per D-2026-06-20-4. Commit local-only; Cargo.lock (unrelated dep refresh) left unstaged.

**2026-06-12 16:32:00Z** - **REVIEW (gemini-reviewer)**: Graded the two pending sentinels on `master` sequentially: **design-4-fallback-midmanifest-negotiation** (`ddfeb58`) accepted (daemon early-flush branch is now TCP-only; client-side fallback_negotiated flag added to hold need-list/manifest fallback payloads in pending_queue; 2000-file regression test passes) and **design-5-send-failure-masks-rejection** (`08d71a2`) accepted (prefer_server_error helper harvests daemon's terminal status on manifest-phase send failures; 500-file readonly_enforcement regression test passes repeatedly). Both verified.json verdicts created, sentinels deleted via git rm, and REVIEW.md status rows updated to `[x]`. Validation suite re-run: cargo fmt, clippy, and cargo test --workspace all passed successfully (1370 passed, 0 failed, 1 ignored).

**2026-06-12 12:43:38Z** - **REVIEW (gpt-reviewer)**: Graded the 4 pending local-only sentinels on `master`, one at a time per `.review/README.md`: **w4-2-delete-push-upload-channel** (`03bcb1d`) accepted (channel plumbing gone from live code; only historical docs still mention upload_tx/upload_rx; 50-file forced-gRPC test passes, 270k repro honestly ignored behind design-4), **w5-2-retry-classifier-consolidation** (`9c960dc`) accepted (dead `blit-core/src/errors.rs` deleted; live retryable-kind table byte-identical to old app classifier aside from `pub`; 4 deleted dead-module tests called out), **w7-4-hash-reader-helper** (`6b2f433`) accepted (hash_reader extraction preserves hash_file/partial-hash/daemon behavior; >256 KiB 3-algorithm equivalence test pins outputs), **w7-6-default-port-pub** (`de04054`) accepted (DEFAULT_PORT remains 9031; production literals route through it; remaining literals are documented tests/comments/display/docs). Verdict commits: `1246398`, `8c32eb3`, `5910e29`, `0213896`; `.review/ready/` empty and `REVIEW.md` has 0 `[~]` rows. Validation re-run per sentinel: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` all green; tests required unsandboxed execution because the sandbox denies loopback listener binds. Handoff pruned the oldest STATE handoff entry for `0af904e`; that autonomous coder session remains represented by the 2026-06-12 03:55 DEVLOG entry.

**2026-06-12 13:30:00Z** - **CODER (continuation session, owner: "Continue with 1")**: 4 more design-review slices sentineled (commits local-only after the owner-approved push of `1adbe0c..bf63a6e` earlier in the session). **w4-2-delete-push-upload-channel** (`03bcb1d`) — the 262,144-slot drain-and-discard mpsc is gone (control.rs channel+send+takes; data_plane.rs files/cache params + per-stream drain task); the slice's regression net also EXPOSED A PRE-EXISTING HIGH BUG, filed as **design-4-fallback-midmanifest-negotiation**: forced-gRPC pushes fail at ≥128 files (exactly FILE_LIST_EARLY_FLUSH_ENTRIES; ~100 timing-flaky — 97/100 landed then 'failed to send push request payload'; ≤80 reliable), verified pre-existing by stash-bisect on the unmodified tree; it made the 262k wedge unreachable in practice. CI test pinned at 50 files; 270k wedge repro is #[ignore] as the joint design-4+w4-2 acceptance test. **w5-2-retry-classifier-consolidation** (`9c960dc`) — dead blit-core/errors.rs deleted (zero importers, retry policy inverted on 5 kinds, proto name collision); is_retryable/is_retryable_io_kind now in blit_core::remote::retry (blit-app re-exports; API stable) with kind-by-kind contract test + deep-chain test (W1.1 net); the dead module's 4 tests deleted with it (called out). **w7-4-hash-reader-helper** (`6b2f433`) — checksum::hash_reader owns the 256 KiB read loop; hash_file / partial-hash small branch / daemon build_file_header (the fifth hand-rolled copy, 256 KiB stack array) all route through it; 3-algorithm equivalence test pins streaming==one-shot. **w7-6-default-port-pub** (`de04054`) — RemoteEndpoint::DEFAULT_PORT pub; the 3 production 9031 restatements (daemon bind default, scan display elision, TUI local-row fallback) route through it; ~90 test-fixture literals deliberately left. Also this session: D-2026-06-12-1 recorded (zero_copy delete, supersedes D-2026-06-11-2(b)); docs/plan/MULTISTREAM_PULL.md drafted (owner-delegated parameters; sequencing w2-2 → w2-3 → adaptive-streams → w2-4 → w3-1); owner-approved push to origin+gitea and named-branch prune (phase5/a1, phase5/blit-app-extract) executed and verified; w5-1's per-file-output question resolved empirically (lines real since Nov-15 'speed tests' commit, buried by the -p progress bar; owner accepted the demotion). Suite at 1369; fmt/clippy/test green per slice. Stopped: remaining rows overlap the 4 pending sentinels, need owner gates (design-4 ratification, w2-3 Active flip), or deserve a fresh session.

**2026-06-12 11:45:00Z** - **REVIEW (claude-reviewer persona)**: Followed `.review/README.md` exactly (woke via `.review/reviewer-wait.sh`; one-shot per-sentinel; re-ran validation as first grading step; inspected `<prev>..<sha>` + finding scope; wrote verdicts only to `.review/results/` + REVIEW.md; no code edits). Graded all 7 design-review sentinels (alpha order): w2-1 (delete-warmup, static tuning table + 4 tier tests), w5-1 (log backend in 4 binaries + prefix convergence + 3 unit tests; follow-up for multi-line), w8-1b (analysis-only; delivered ZERO_COPY_RECEIVE_EVAL.md Draft recommending delete), w9-1 (27 #[cfg(unix)] deletions from 8 remote test files; kept 4 genuine), w9-2 (revived root tests/ into blit-core/tests +24 tests + AGENTS.md §4 fix), w9-4 (new_read_only knob + 3 e2e enforcement tests for push/purge/delegated), w9-5 (5 jobs/detach lifecycle e2e; exit+JSON only; deterministic active-cancel via pending future). All **accepted** (no reopens): validation green on re-runs (fmt/clippy clean each time; test --workspace ok, 0 failures); diffs, commit messages ("Fix <id>: ..."), finding docs ("What/Approach/Files/Tests/Known gaps"), and REVIEW rows aligned perfectly. 7 verified.json with "reviewer":"claude-reviewer"; ready/ cleared (final wake: NO_READY); 7 local commits. Unblocks w4-2 etc. (see updated STATE.md). No push performed.

**2026-06-12 03:55:00Z** - **CODER (autonomous overnight session)**: Owner authorized autonomous queue execution ("work on as much as you can. commit every slice as it lands. if anything gets questionable, stop." — scope: master only, no branches, local commits, never push, faster-mode WIP iff files-changed disjoint across pending sentinels; recorded in STATE.md). **7 slices completed and sentineled** (18 local commits, `1adbe0c..0af904e`): **w5-1-log-backend** (`56bda09`+`7145202`) — new `blit_core::stderr_log` backend (warn default, `BLIT_LOG` override, `<binary>: <level>: <msg>`) installed in all 4 binaries; ~22 formerly-discarded log::warn/error sites now visible; 9 bracketed stderr prefix families converged (warnings→facade, operator lines→`blit:`/`blitd:` prefixes, per-file pipeline spam→log::debug); trace-gated `[data-plane-client]` family kept (remote_parity.rs asserts the prefix); jobs-watch ticker vocabulary kept (single-command status output). **w9-5-jobs-lifecycle-e2e** (`ad773d8`) — first e2e net for detach/jobs verbs: 5 tests incl. cancel-active-delegated-job exit-0 made deterministic via a fake tonic source whose pull_sync never answers. **w2-1-delete-warmup-machinery** (`2a8a490`) — analyze_warmup_result + determine_tuning + warmup_gbps deleted; determine_remote_tuning is one honest static table (production values bit-identical); 4 tier tests added. **w9-1-ungate-windows-tests** (`9324559`) — 27 blanket #[cfg(unix)] removed from 8 remote-transfer test files (none unix-specific); 4 genuinely unix files kept gated; Windows CI will now actually compile parity-critical paths. **w9-2-revive-root-tests** (`461525d`) — dead root tests/ relocated into crates/blit-core/tests (+24 tests incl. the only MirrorPlanner coverage); connection.rs deleted; drift fixed (for_tests→load(), compare_mode, 32→300 files vs TINY_FILE_LIMIT=256, serial Mutex for config-dir globals); AGENTS.md §4 corrected. **w9-4-readonly-enforcement-tests** (`4d67210`) — TestContext::new_read_only() knob + 3 e2e tests covering all 3 read-only write gates (push/purge/delegated-pull), asserting error text + no destination mutation. **w8-1b-zero-copy-fast-eval** (`6189d82`) — analysis-only: `docs/plan/ZERO_COPY_RECEIVE_EVAL.md` (Draft) recommends DELETING zero_copy.rs (wire path is splice-friendly but the dead impl busy-waits on EAGAIN, the AsyncRead-generic pipeline resists raw-fd wiring, and the CPU win is unmeasured pending the 10GbE rig); owner verdict is the gate. **Suite: 1331 → 1368 tests, fmt/clippy/test green after every slice.** Remaining `[ ]` rows are all overlap-blocked behind the 7 pending sentinels (skip map in STATE.md), owner-gated (w2-3 plan interview), or dependent (w2-4, w6-2, w8-1). Transient note: first post-w5-1 workspace test run showed 11 "daemon failed to listen" failures under cold parallel load; isolated and clean re-runs green — same harness-contention class w9-3 addresses.

**2026-06-11 04:45:00Z** - **DECISION + QUEUE**: Owner ratified the full design-review queue (D-2026-06-11-2). 38 new slices entered in REVIEW.md's new "Design-review queue" section in execution order (w5-1-log-backend first). Embedded decisions: w2-4 deletes the deprecated Pull RPC after w2-3 harvests its multi-stream pattern (criterion: not needed for FAST/SIMPLE/RELIABLE in any scenario); zero_copy.rs excluded from the w8-1 deletion sweep in favor of evaluation slice w8-1b (owner judges splice has FAST potential; outcome = plan doc or deletion); w2-3 multi-stream-pull plan doc authorized. DESIGN_COHERENCE_REVIEW.md flipped Active → Shipped. STATE.md queue rewritten: design queue is item 1; old "audit-h3c slice 2" item absorbed into slice family W1; adaptive-streams note added re w2-3/w3-1 data_plane interaction; Round 1 cross-check note added (several Round-1-class items now covered by design slices). Phase C report slice-count corrected 33 → 39 IDs (38 new + queued W1.1).

**2026-06-11 04:15:00Z** - **AUDIT**: Design-coherence Phase C synthesis delivered (`docs/audit/AUDIT_REPORT_2026-06-11_DESIGN.md`), executed inline (no agent fleet — judgment work over verified material). The 70 confirmed Phase B findings + filed design-1/2/3 + queued slice-2 transport work collapse into 10 workstreams / 33 slices (4-way-confirmed dead classifier = 1 deletion slice; buffer/ladder/pull-tuning findings = facets of 1 ownership fix). Proposed queue order leads with cheap load-bearing slices (W5.1 log backend — every log::warn in the workspace is currently discarded; W4.2 push-upload-channel deletion — fixes a real >262k-file fallback wedge), then the AbortOnDrop family (W4.1 + design-2), then transport (W1.x), then the pull-parity FAST program (W2.1→W2.4, W2.3 requires a plan doc). Three embedded owner decisions: W2.4 deprecated-Pull-RPC deletion (wire-breaking, proto unfrozen per D-2026-06-11-1), W8.1 zero_copy delete-vs-revive, W2.3 plan-doc go-ahead. Caveat recorded: Phase B had no progress dimension, so map §1.6 residue (delegated zero progress, daemon counters 0 for 3/4 kinds, no denominators) is slice W6.2 verify-then-fix, not yet adversarially verified. Map erratum #2 applied (TransferOrchestrator exists; transfer_engine/PLAN_OPTIONS are the real AGENTS.md ghosts). Awaiting owner ratification by slice ID.

**2026-06-11 03:30:00Z** - **AUDIT**: Design-coherence Phase B executed and delivered (`docs/audit/DESIGN_FINDINGS_2026-06-11_PHASE_B.md`; 164 agents, ~7.0M tokens). 8 dimension reviewers (boundaries/duplication/errors/constants/async/deadcode/tests/drift) re-derived mechanisms from code, then every finding faced 2 adversarial verifiers (mechanism + impact lens) with tiebreakers. 76 reported → 70 confirmed / 6 refuted; ~28 severity downgrades (impact lens strictest). Effective HIGHs (4): network buffer-pool budget is a frozen streams*2+4 formula authorizing multi-GiB with no memory awareness (3 pasted sites; OOM-by-constant); live pull-sync computes full tuning then discards all but chunk_bytes (1 stream + pool=4 vs push's 32 streams); deprecated Pull RPC half-migration (~500 unreachable daemon lines that are ALSO the only multi-stream pull); blanket #[cfg(unix)] on remote transfer tests with nothing unix-specific (Windows parity untestable). Notable mediums: log:: facade has NO backend anywhere (all log::warn/error discarded); workspace-root tests/ never compiled by cargo; daemon compute phases blind to client disconnect; 2 more detach-on-drop families beyond design-2. Refuted kills recorded in the doc so they are not re-found (2 of 6 were 'textually true but unreachable' — same lesson as the Phase A erratum). At the Phase B checkpoint: owner go/no-go for Phase C synthesis.

**2026-06-11 02:40:00Z** - **AUDIT**: Phase A bug-class candidates dispositioned (owner delegated the call) + Phase B launched (owner: "Full Phase B"). Each of the map's four immediate candidates was re-verified by hand before filing. Three confirmed → filed as `[ ]` Open: `design-1-cli-pull-byte-double-count` (Medium; pipeline.rs:233-235 reports full bytes on BOTH Payload and FileComplete, blit-cli/transfers/remote.rs fold adds both → ~2x), `design-2-orphaned-daemon-data-planes` (High; bare JoinHandles at daemon pull.rs:180/:297 + push/control.rs:57 detach on control-stream death; AbortOnDrop fix exists but is pub(crate) in blit-core and was never propagated), `design-3-unbounded-data-plane-connects` (Medium; TcpStream::connect with no timeout at data_plane.rs:92 + pull.rs:1710 — audit-2's fix never reached the data plane). The fourth (>4 MiB manifest batch) was REFUTED in mechanism: ENUM_BATCH_SIZE=500 batches an internal channel, legacy Pull sends one header per PullChunk (service/pull.rs:583), push need-list is byte-capped at 512 KiB (push/control.rs:25-26); erratum recorded in the map under headline 8, residual decode-limit/tar-shard-manifest question handed to Phase B. Lesson institutionalized: Phase B verifier prompts instruct refutation-by-reading-code, never trust-the-map. Git: owner approved push; `master` pushed to origin + gitea at `ab0d8a0` (was 1be16bc).

**2026-06-11 02:05:00Z** - **AUDIT**: Design-coherence review Phase A executed and delivered (`docs/audit/DESIGN_MAP_2026-06-11.md`, 1766 lines; plan D-2026-06-11-1). 17 read-only agents (9 concept-mappers, 6 crate stratum inventories, 2 plan-doc graveyard tracers; ~2.3M tokens) mapped 327 sites, 62 duplicate groups, 128 constants, 26 idiom strata, 21 plan docs. Verdict: 8 of 9 cross-cutting concepts have no owner (only mDNS and wire-path-form are healthily owned; retry has a nominal owner in the wrong layer). Cross-validated headlines: every client-side gRPC await unbounded (no keepalive/deadline on any client channel); pull-direction TCP sockets get no NODELAY/keepalive/tuned buffers on either end and both data-plane connects are unbounded; --retry silently inert on gRPC paths (Status→String strips the chain; dead contradictory classifier blit-core/errors.rs exported); advertised adaptive tuning is dead code (frozen disagreeing ladders; pull-sync single-stream vs push 24-32 streams; only multi-stream pull lives in the deprecated RPC); CLI double-counts pull bytes ~2x; delegated transfers show zero live progress; cancel reaches 1 of 4 transfer kinds; blitd has no graceful shutdown; Windows remote parity structurally untestable (all remote tests cfg(unix)); 4 MiB tonic decode default is an undeclared invariant that 500-entry manifest batches can exceed. Four bug-class candidates flagged for immediate filing (CLI byte double-count, manifest-batch >4 MiB abort, orphaned daemon data planes, unbounded data-plane connects). Graveyard: WORKFLOW_PHASE_2.md marked Shipped certifies a streaming planner/heartbeat with zero trace in code; AGENTS.md §4/§6 name nonexistent modules (transfer_engine). At the Phase A checkpoint — Phase B is a fresh owner go/no-go per the plan.

**2026-06-11 00:42:30Z** - **REVIEW**: Verification and acceptance of `audit-h3c-slice1-grpc-fallback-frame-contract` (commit `bf4cc82`, verdict declared by owner). Validation suite re-run at HEAD `1be16bc`: fmt/clippy/test all green (exit 0), blit-core test-function count identical at `bf4cc82` and HEAD (344) — no test drop. The review assessment surfaced four facts the finding doc didn't know, recorded here as input to the slice-2 re-scope: **(1)** the 1 MiB frame cap is load-bearing correctness, not just cadence — tonic 0.14's default 4 MiB decode limit (`DEFAULT_MAX_RECV_MESSAGE_SIZE`, no `max_decoding_message_size` override anywhere in the workspace) means pre-slice-1 gRPC-fallback transfers of files >4 MiB should fail at decode, since `tuning.rs` emits 16/32/64 MiB chunks and `pull_sync.rs:515` passed `tuning.chunk_bytes` straight into `GrpcServerStreamingSink`. Version-skew residual: a new client against an old daemon still receives uncapped frames. **(2)** The bigger H3 hole is missing client-side HTTP/2 keepalive: daemon pings clients (`main.rs:138`) but all three client channel builders (`pull.rs:239`, `push/client/mod.rs:307`, `blit-app/src/client.rs:38` — already-drifted near-duplicates) set only `connect_timeout`, so a dead daemon hangs `message().await` forever; ~3 lines of `Endpoint` keepalive config cover dead-peer/black-hole at transport level and shrink slice 2's watchdog scope to wedged-but-alive peers only. **(3)** Fallback throughput is governed by HTTP/2 flow-control windows, not frame size — hyper defaults: client 2 MiB stream / 5 MiB conn; daemon server 1 MiB conn total → hardware-independent push-fallback ceiling on high-BDP links; `http2_adaptive_window(true)` is the no-tuning fix. **(4)** `clamp_fallback_chunk_size(x.max(1MiB))` ≡ const 1 MiB — the sinks' `chunk_bytes` param is now inert and misleading; root cause is a TCP transport parameter (`chunk_bytes`) embedded in the transport-agnostic `PlannedPayloads` (planner payload composition itself is sound). Owner direction: slice 2 re-scope deferred until after a repo-wide design-coherence review (plan doc next). Sentinel cleared, `REVIEW.md` updated to verified (`[x]`).

**2026-06-07 07:32:00Z** - **RULE**: Merged the agent-kit branch into `master`, deleted the local `chore/agent-kit-install` branch, and codified the branch-free policy in [AGENTS.md](file:///home/michael/dev/Blit/AGENTS.md). The policy dictates that agents are strictly forbidden from creating branches on their own decision and that the owner is the sole gatekeeper for all Git operations. Verified that the codebase compiles and document validation checks pass.

**2026-05-23 17:18:00Z** - **REVIEW**: Verification and acceptance of audit finding `audit-7d2-extract-display-f3` (commit `315f923`). Verified that the verbatim extraction of F3 status-to-display mapping helpers (`f3_pull_to_display` + `confirm_detail`, `f3_du_to_display`, `f3_del_to_display`) from `main.rs` to the new `display_f3` module is clean and behavior-preserving. Crate-root re-exports keep rendering call sites and unit tests intact. Verified with clean clippy/fmt and all 630 tests passing. Sentinel cleared, `REVIEW.md` updated to verified (`[x]`).

**2026-05-23 16:57:00Z** - **REVIEW**: Verification and acceptance of audit findings `audit-13`, `audit-14`, and `audit-15`. (1) Verified `audit-13-buffer-pool-double-locking` (commit `f9d3f2f`): single-lock return path and no-pre-zeroing optimization on `BufferPool` release/return routes are correct, performance validated, and dirty reuse contract pinned by test. (2) Verified `audit-14-resume-copy-redundant-seek` (commit `b7f8177`): sequential source `seek` elimination and destination cursor tracking successfully optimize syscall count during block-level resume checks; verified against existing resume test suites. (3) Verified and accepted resolution for `audit-15-grpc-missing-connection-timeouts` (commit `f0ed9e5`): accepted the recommend-defer decision rejecting global `Server::timeout` due to stream abortion risk, confirming HTTP/2 keepalive reaps dead peers safely. Sentinels cleared, `REVIEW.md` and `TODO.md` updated to verified (`[x]`).

**2026-05-23 16:40:00Z** - **AUDIT**: Codebase review and findings documentation. Added detailed findings `audit-13-buffer-pool-double-locking.md`, `audit-14-resume-copy-redundant-seek.md`, and `audit-15-grpc-missing-connection-timeouts.md` to `.review/findings/` to document concurrency double-locking, redundant seeks in resume copies, and missing gRPC control plane timeouts. Updated `REVIEW.md` and `TODO.md` checklists accordingly.

**2026-05-14 14:00:00Z** - **FIX**: post-edc11aa review followups. Two findings: **#1 Medium (delegated_pull logs failures as ok).** `service/core.rs:291` used `ok = !cancelled`, but `handle_delegated_pull` returned `()` and converted `run_delegated_pull` failures into an error-progress message on `tx`. So a real handler failure with the client still connected logged `delegated_pull ok ... errors=N` with `N` unchanged — the operator-facing metric contradicted the actual RPC result. Fix: `handle_delegated_pull` now returns `bool` (true=success, false=failure). The phased error is still surfaced on `tx` as before; the bool is purely the success bit. Caller in `service/core.rs` consumes via `tokio::select!` into `Option<bool>` — `None` = client cancelled, `Some(true)` = handler ok, `Some(false)` = handler failed. `ok = matches!(outcome, Some(true))`; `inc_error` fires only on `Some(false)` (handler failure), not on client cancellation. New regression test `handle_delegated_pull_returns_false_on_handler_failure` drives a blank-host source locator to trip `DelegationRejected` and asserts `false` is returned — pins the return-bool contract without mocking the full pipeline. **#2 Low (release-plan inconsistency).** Top-tracker said §3.1/§3.2/§3.3 closed, but §6 commit-sequence table still labeled steps 9–11 "Not started," step 1 said D4/D5/D6 still open, step 13 still claimed tagging is blocked on "product-owner ack of P1 deferrals," and §7 heading still said D4/D5/D6 are open. All four sites rewritten to reflect 2026-05-13 reality: steps 9–11 carry closing commits (`0d76c4f`, `6e750b9`, `6e750b9`); step 1 says "all taken — D5 modified by owner"; step 13 shows tagging blocked on step 2 only; §7 heading says "all D1–D9 decided, nothing owed anymore" with the section name retained for historical reference. **Tests**: `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test --workspace` all pass (+1 delegated_pull regression test).

**2026-05-14 12:00:00Z** - **FIX**: post-6e750b9 review followups against the --metrics work. Four findings: **#1 Medium (active-gauge race).** `format_completion_line` was reading `active_transfers` while the just-finished RPC's `_guard` was still in scope, so a single completed transfer logged `active=1` — misleading for an end-of-RPC summary. Fix: rename `let _guard = guard;` to `let guard = guard;` and `drop(guard);` explicitly before the `log_completion` call in all three spawn closures (push / pull / pull_sync at `service/core.rs`). Now `active=N` reflects state AFTER the just-finished RPC is removed from the gauge. **#2 Medium (missing RPC coverage).** `delegated_pull` increments `pull_ops` + the active gauge inside `run_delegated_pull` (`delegated_pull.rs:227`) but never emitted its own completion line — the field would only become visible on a later push/pull log. Same for `purge`. Fix: (a) wrapped the delegated_pull spawn closure in core.rs with a start-time capture and a `log_completion("delegated_pull", ...)` call after the cancellation `select!`; `ok = !cancelled` per the cancellation-race semantics. (b) Extracted `BlitService::purge_inner` so the trait `purge` method can wrap a single call site with start-time capture + `log_completion("purge", ...)` + `inc_error` on `Err`. Both paths now contribute to `--metrics` per-RPC summary symmetrically. **#3 Medium (rustfmt clean).** Already clean on `HEAD` (the prior `cargo fmt --all` at the tail of `6e750b9` had landed); reviewer was working off an older snapshot. No action. **#4 Low (stale scope text).** Three sites still claimed `--metrics` was dormant / scaffolding-only after the §3.1 closure: (a) `crates/blit-daemon/src/main.rs:82` comment block rewritten to reference `log_completion` and call out the new operator-facing summary lines; the `--metrics` `eprintln!` info line now says "per-RPC summary lines on stderr." (b) `docs/plan/RELEASE_PLAN_v2_2026-05-04.md:500` — old "No reader / dormant" analysis paragraph quoted as historical context inside a "Resolved 2026-05-13" callout, so the section reads consistently as closed without losing the audit-time reasoning. (c) `TODO.md:248` — AI telemetry line struck through with explicit `Removed from scope 2026-05-13` callout pointing at §5.4 + deletion of the doc; predictor note added to clarify perf-history collection is unaffected. **New regression test:** `completion_line_reflects_active_after_guard_drop` constructs an in-flight transfer + a finishing transfer, drops the finishing guard, and asserts `active=1` in the formatted line — pins the call-site contract that the formatter sees post-drop state. **Verified:** `cargo fmt --all -- --check` clean, `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo test --workspace` all pass (+1 new metrics test, 9 total in `metrics::tests`).

**2026-05-13 18:30:00Z** - **FEAT + SCOPE**: §3.1 / §3.3 / §5.2 / §5.4 closed (D5 modified, D6 default taken, BlitAuth + AI telemetry removed). Three owner decisions landed in one commit set: **(1) §3.1 D5 modified.** Rather than ship `--metrics` as dormant scaffolding, the daemon now emits a one-line stderr summary at end of each push/pull/pull_sync RPC when `--metrics` is on. Format: `[metrics] push ok in 1.23s (push_ops=N pull_ops=N purge_ops=N active=N errors=N)` — rsync/rclone-style operator visibility under systemd / foreground without needing the TUI. `crates/blit-daemon/src/metrics.rs` gains `log_completion(op_kind, duration, ok)` (no-op when disabled — atomics never loaded) and `format_completion_line` (extracted so the format contract is unit-testable without stderr capture). 3 new unit tests in `metrics::tests` pin the line format and error tagging. Counters themselves stay in place and feed future TUI work; the change just gives `--metrics` a real consumer for 0.1.0. **(2) §3.3 D6 default taken.** Phase 4.8 daemon FS-capability work (4.8.2 startup/idle daemon probes + 4.8.3 `blit diagnostics profile` capability probes) explicitly deferred to 0.2.0. `WORKFLOW_PHASE_4.md` §4.8 now carries a scope-note callout marking 4.8.2 and 4.8.3 as deferred; 4.8.1 client-side `fs_capability` cache ships in 0.1.0 as today. **(3) §5.2 BlitAuth removed.** `service BlitAuth { Authenticate(AuthRequest) returns (AuthResponse) }` plus the two messages stripped from `proto/blit.proto`. `RemoteSourceLocator.delegated_credential` (forward-compat auth passthrough hook for delegated pull) also removed — field 10 reserved against future reuse. `crates/blit-cli/src/transfers/remote_remote_direct.rs` no longer sets the field. `DAEMON_CONFIG.md` Trust Model section rewritten to say "no auth, intentional, out of scope — terminate auth at the network edge" (was "no auth in 0.1.0; passthrough field exists for forward-compat"). `docs/plan/REMOTE_REMOTE_DELEGATION_PLAN.md` got a top-of-file auth note flagging body references to BlitAuth/`delegated_credential` as obsolete (body retained as historical design record). **(4) §5.4 AI telemetry removed.** `docs/plan/AI_TELEMETRY_ANALYSIS.md` deleted entirely. References in `docs/plan/README.md`, `docs/plan/PROJECT_STATE_ASSESSMENT.md`, and `CHANGELOG.md` dropped. `WORKFLOW_PHASE_4.md` §4.9 marked as removed from scope. Predictor (§2.8) is unaffected — performance history is still collected; no "analyze my history" feature is planned. **Release-plan tracker:** §3.1 / §3.2 / §3.3 all ✅ closed. §5.2 / §5.4 changed from "deferred" to "removed." `--metrics` decision (D5) modified from default; all other D-decisions taken at their plan defaults. **Net release-blocker count:** §2.6 (hardware-bound 10 GbE benchmark capture) is the last open P0. **Tests**: `cargo test --workspace` all pass (+3 metrics tests); `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check` clean.

**2026-05-13 17:30:00Z** - **FEAT**: §3.2 mDNS TXT enrichment closed (D4 default taken). `crates/blit-core/src/mdns.rs::advertise` now publishes two new TXT keys: `module_count` (always set; the authoritative module count, distinct from `modules` which gets truncated past the ~180-byte TXT cap when a daemon exports many modules) and `delegation_enabled` (`1`/`0`; whether the daemon accepts `DelegatedPull` requests). `AdvertiseOptions` gained a `delegation_enabled: bool` field; the daemon at `crates/blit-daemon/src/main.rs:65` passes `delegation.allow_delegated_pull` through so the advertised value tracks the runtime gate. `MdnsDiscoveredService` gained `.module_count() -> Option<u32>` and `.delegation_enabled() -> Option<bool>` accessors — both return `None` for pre-§3.2 daemons so back-compat is graceful. `blit scan` surfaces both: JSON output adds `module_count` and `delegation_enabled` fields (omitted when absent via `skip_serializing_if`); text output shows `Modules (N): ...` when the names list is truncated relative to the count, `Modules: N (names not advertised)` when only the count came through, and `Delegation: accepts DelegatedPull` when the daemon opts in. 5 new unit tests in `mdns::accessor_tests` pin the wire-shape contract (truthy parsing, absent-field None, modules-accessor non-regression). Verified: `cargo test --workspace` all pass, `cargo clippy --workspace --all-targets -- -D warnings` clean, `cargo fmt --check` clean. **Tests**: `cargo test --workspace` +5 (was N, now N+5).

**2026-05-13 17:00:00Z** - **DOCS**: §2.2 + §4 cleanup followup — `docs/plan/greenfield_plan_v6.md` slipped through the original sweep (`aac13bf` + `8d43e4d`). Seven unconditional `blit-utils` references presented as live plan items: workspace-init step (line 82), Phase 1 layout (line 181), "Discovery & Admin Utilities (`blit-utils`)" feature section (line 337), Phase 4 tooling item (line 370), packaging step (line 399), acceptance-checklist entries (lines 410, 412). `docs/plan/README.md` labels this file as "Active architectural plan," so a new agent landing on it could reasonably infer the `blit-utils` artifact still ships. Added a post-phase implementation-note callout right below the header — same pattern as `WORKFLOW_PHASE_3.md` / `WORKFLOW_PHASE_4.md` — pointing at `RELEASE_PLAN_v2_2026-05-04.md` §1/§2.2 and explaining that the as-shipped surface is `blit <subcommand>` from `crates/blit-cli`. Body kept verbatim per §4 closing-action style ("retained as design rationale"). **Tests**: not rerun — docs only.

**2026-05-07 14:30:00Z** - **DOCS**: Followup #3 from review of `5c36c29`. **Low**: the "v2.1 changes vs v2" block at the top of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` referred to the predictor as §3.1 and POST_REVIEW_FIXES Round 1 as §3.2 — accurate against the original v2 layout (where they sat under "Quality work P1") but stale as forward references in v2.1, where promotion moved them to §2.8 and §2.7 respectively, and §3.1/§3.2 are now `TransferMetrics` and mDNS TXT. Annotated each bullet with both the v2.1 section number (current layout) and the original v2 section number, plus an explicit "§3.1 is now TransferMetrics, not the predictor" / "§3.2 is now mDNS TXT, not POST_REVIEW_FIXES" callout so anyone using v2.1 as the live source can't be misled into chasing the wrong section. Block now reads as a true changelog rather than a forward reference. **Tests**: not rerun — docs only.

**2026-05-07 14:00:00Z** - **DOCS**: Followup #2 catching review of `8d43e4d` — the RELEASE_PLAN_v2 itself was still presenting closed P0s as open work, which was a serious risk because `docs/plan/README.md` points new agents at it as the live source of truth for what remains before shipping. **Medium**: added a top-of-file "Closure tracker (last updated 2026-05-07)" block with a one-row-per-section table showing closing commits for §2.1/§2.2/§2.3/§2.4/§2.5/§2.7/§2.8 and §4, plus pending markers for §2.6 (hardware-bound benchmark — last open P0) and §3.1/§3.2/§3.3 (P1, doc-only). Each P0 section now has its own per-section `**Status (2026-05-07):** ✅ Closed <commit>` block at the top so a reader landing on a specific section sees current state without scrolling back to the top. §2.5 line 265 also still showed the old `blit completions remote <prefix>` syntax in the recommendation paragraph — corrected to the actual `<REMOTE> [--prefix <STR>] [--files] [--dirs]` form, matching the existing line-21 manpage. §6 commit-sequence rewritten as a status table with closing commits per row (P0 done except step 2 §2.6; P1 deferrable). §7 decisions table flipped from "Default recommendation" / "Owner" columns to "Default" / "Outcome" with closing commits where taken — D1/D2/D3/D7/D8/D9 ✅ taken, D4/D5/D6 still open. Test totals updated from the 2026-05-04 baseline (383) to current (407, after R41-R45 review-fix coverage). Footer "Last updated" + "Next review" rewritten. **Low**: `docs/plan/REMOTE_TRANSFER_PARITY.md:83` Success Criteria still said `blit-cli mirror -p -v local remote`; updated to `blit mirror`. Unlike POST_REVIEW_FIXES.md, this file is listed as still-applicable design in `docs/plan/README.md`, so a stale binary name there would mislead. **Tests**: `cargo test --workspace` 407 pass / 0 fail (no code touched).

**2026-05-05 22:00:00Z** - **DOCS**: Followup to the §2.2+§4 sweep — three doc misses caught by review of commit `aac13bf`. **Medium**: `docs/plan/BENCHMARK_10GBE_PLAN.md` lines 36 + 99-100 still pointed users at `./target/release/blit-cli` and `/tmp/blit-cli` for the active 10 GbE benchmark playbook (the plan that's currently driving the §2.6 capture). Replaced with `blit` paths. **Medium**: `docs/WHITEPAPER.md:738-739` build/run example used the same stale `target/release/blit-cli`; rewritten. WHITEPAPER:7 also still described `blit-cli` as the "user binary" without noting the `[[bin]] name = "blit"` rename + admin-verb merge — corrected. **Low**: `README.md:149` documented the daemon-backed completion fallback as `blit completions remote <prefix>`, but the actual CLI signature is `blit completions remote <REMOTE> [--prefix <STR>] [--files] [--dirs]` (matching `docs/cli/blit.1.md:21`). Rewrote the README line to show the correct positional `<REMOTE>` plus `--prefix` flag form. Two intentionally-left-alone files: `docs/plan/POST_REVIEW_FIXES.md:85` (real-world incident report dated 2026-05-01 capturing the exact pre-rename command that triggered the bug — historical evidence) and `docs/bugs/{single-file-source-silent-noop,copy-destination-semantics}.md` (bug reports also dated against the pre-rename binary). Verified by `grep -rn 'release/blit-cli\|/tmp/blit-cli\|--bin blit-cli'` across `docs/`, `scripts/`, `README.md`, `CHANGELOG.md` — only the three intentional historical references remain. **Tests**: `cargo test --workspace` 407 pass / 0 fail (no code touched).

**2026-05-05 21:30:00Z** - **DOCS**: §2.2 + §4 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` — closed the last P0 by removing/contextualising every `blit-utils` doc reference, since the admin verbs ship as subcommands of the single `blit` binary. **Per the §4 table**: (a) `docs/plan/PROJECT_STATE_ASSESSMENT.md` — added top-level "Superseded" callout pointing at the release plan; CLI Surface and Documentation sections rewritten so the `blit-utils` line is gone; Architecture Overview ASCII diagram now lists `blit` (admin verbs included) alongside `blit-daemon` only. (b) `docs/plan/README.md` — Current Status block flipped to point at the release plan as live source of truth; document index labels PSA as superseded snapshot, calls out BLIT_UTILS_PLAN as design-rationale-only. (c) `docs/plan/MASTER_WORKFLOW.md` — Feature-Completeness Goals, Phase 3 gates, and Telemetry row reword "blit-utils" → "blit" admin subcommands. (d) `docs/plan/BLIT_UTILS_PLAN.md` — added prominent Superseded header at top stating the `blit-utils` artifact never shipped and verbs live as `blit <verb>`; document body retained as design rationale per plan §4. (e) `docs/plan/WORKFLOW_PHASE_3.md` and (f) `WORKFLOW_PHASE_4.md` — added post-phase notes at the top so historical body text describing `blit-utils` stays as workflow record but is not read as current state. (g) `docs/ARCHITECTURE.md` — User Layer ASCII diagram redrawn (no blit-utils column), `### blit-cli (produces the blit binary)` rewritten with the merged structure including all admin verb modules, `### blit-utils` section replaced with `### Admin verbs` cross-referencing BLIT_UTILS_PLAN as superseded for artifact shape. (h) `docs/cli/blit.1.md` — SYNOPSIS adds `list-modules`, `ls`, `completions shell <SHELL>`, `completions remote <REMOTE>`, and `profile`; Admin Commands section documents all of them plus the `list` smart-dispatch wording matching §2.3 implementation. (i) `README.md` — annotated `crates/blit-cli/` repo-structure entry to clarify it produces the `blit` binary with admin verbs included. **Drive-bys outside the §4 table**: `docs/plan/AI_TELEMETRY_ANALYSIS.md` Phase 3 ("blit-utils Integration") section retitled and rewritten — the standalone-utils-binary integration path is moot post-merge; `docs/plan/TUI_DESIGN.md` ScanResponse-effort estimate updated to point at `blit-cli`'s scan parsing (was "blit-utils-merged"). Files explicitly **left alone**: `TODO.md` historical completed-task lines, `CHANGELOG.md` (already correctly says "merged into blit"), `docs/plan/greenfield_plan_v6.md` (architectural plan, treated as historical), the GPT/audit review artifacts under `docs/`, and the `RELEASE_PLAN_v2_2026-05-04.md` itself (the source-of-truth describing the cleanup). Verified by `grep -rln blit-utils docs/ README.md TODO.md CHANGELOG.md` — every remaining hit is either an explicit "no separate blit-utils" callout, a historical TODO completed line, the CHANGELOG merge note, or the design-rationale BLIT_UTILS_PLAN now headed by its Superseded banner. **Tests**: `cargo test --workspace` 407 pass / 0 fail (no code touched).

**2026-05-05 20:30:00Z** - **FIX**: Round 45 review of commit `f83a208`. **R45-F1 (High)**: while plumbing scanned features I left a `let total_bytes = scanned_bytes` alias in `crates/blit-core/src/orchestrator/orchestrator.rs:480` and the streaming summary at `:567` then read that local for `summary.total_bytes`. Net effect on the wire: `summary.total_bytes` (contracted as "bytes the pipeline wrote") was reporting scanned bytes, so any incremental skip-unchanged run with all-up-to-date files would have reported the full source size as bytes-written and overcounted throughput. Fix: removed the alias; streaming summary now reads `pipeline_outcome.bytes_written` from `SinkOutcome` directly. The verbose "Planning enumerated" line correctly references `scanned_bytes`. **R45-F2 (Low)**: planner + transfer sub-predictions were still passing `(all_headers.len(), total_bytes)` (which were aliases of scanned_* but only by accident) while the total prediction passed `(scanned_files, scanned_bytes)` explicitly. Made all three predictor calls share the explicit `(scanned_files, scanned_bytes)` feature vector so a future maintainer editing one branch can't reintroduce drift by missing another. New regression test `incremental_run_total_bytes_excludes_skipped_files` in `orchestrator::orchestrator::async_runtime_tests` runs the full local mirror pipeline twice (mirror=true forces streaming planner; skip_unchanged=true means run 2 writes 0 bytes) and asserts `summary.total_bytes == 0` on the second run. Verified the test catches the bug by reintroducing the alias — assertion fires with `total_bytes=4096, scanned_bytes=4096` matching the pre-R45 state. **Tests**: `cargo test --workspace` 407 pass / 0 fail (was 406, +1 new R45 regression test).

**2026-05-05 19:30:00Z** - **FIX**: Round 44 review of commit `da6ced2`. **R44-F1 (Medium)**: predictor train/query feature mismatch. The pre-pipeline query in `crates/blit-core/src/orchestrator/orchestrator.rs` used `(all_headers.len(), total_bytes)` (scan-side workload), but `record_performance_history` recorded `summary.copied_files` for `PerformanceRecord.file_count`. On any incremental run the predictor saw "predict 1000 files" then trained on "5 files actually copied" — estimates drifted badly. Fix: added `scanned_files: usize` and `scanned_bytes: u64` to `LocalMirrorSummary` (`crates/blit-core/src/orchestrator/summary.rs`) with a load-bearing rustdoc explaining the scan-vs-copy split; populated at every summary construction site (streaming pipeline, NoWork, Tiny, Huge, JournalSkip, single-file copy); `record_performance_history` now reads `summary.scanned_{files,bytes}` for the record's feature fields. The bucket-shape counters (`tar_shard_*`, `raw_bundle_*`, `large_*`) still reflect actual writes — they feed `derive_local_plan_tuning` which heuristically sizes destination buckets from past apply behavior, not scan size. Extracted `build_performance_record` from `record_performance_history` so the contract is unit-testable without polluting the global JSONL. Two new tests in `orchestrator::history::tests`: `record_uses_scanned_features_not_copied` (would have failed pre-fix on a 1000-scanned/5-copied incremental) and `bucket_counts_still_reflect_actual_writes` (regression guard against accidentally rerouting bucket counters through scan stats). **R44-F2 (Low)**: `blit profile --json` shape was unasserted. The pre-fix commit message claimed top-level `"predictor": null` for the empty-state, but actual output emits `{"copy": null, "mirror": null}` (predictor file loads even when no profile yet has training data). Picked the structured shape — strictly more informative — and pinned it in `test_utils_profile_json` (`crates/blit-cli/tests/blit_utils.rs`) with assertions covering both empty and trained states. **R44 plan note**: §2.8 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` is correctly framed now as "predictor observability and training," not "adaptive planning behavior" — the predictor is queried, surfaced via `--verbose` and `blit profile --json`, and audit-able, but the Tiny fast-path still does not consult it for routing. Added a Status block to the plan section documenting this. **Tests**: `cargo test --workspace` 406 pass / 0 fail (was 404, +2 history regression tests).

**2026-05-05 18:30:00Z** - **ACTION**: §2.8 phase 2 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` — orchestrator and `blit profile` now actually query the predictor. Three load-bearing changes: (1) `crates/blit-core/src/orchestrator/orchestrator.rs` introduces a `plan_done = Instant::now()` checkpoint between the planner stage and the pipeline so `planner_duration_ms` and `transfer_duration_ms` are correctly attributed; pre-fix both fields fed `record_performance_history` got the same whole-run time, so v1 predictor training collapsed into a single regressor. (2) Just before kicking off `execute_sink_pipeline`, the orchestrator queries `predictor.predict(DurationKind::Total/Planner/Transfer, ...)` with the just-scanned `all_headers.len()` and `total_bytes`; populates `LocalMirrorSummary.predictor_estimate` (new field of type `PredictorEstimate { planner_ms, transfer_ms, total_ms, observations, fallback_depth }` in `summary.rs`). `--verbose` now emits two lines: a "Predictor estimate: ..." pre-run line and a "Predictor delta: ..." post-run line showing predicted-vs-actual percent error per phase. (3) `crates/blit-cli/src/profile.rs::run_profile` now calls `predictor.coefficients_for(mode-only, ...)` for both Copy and Mirror, surfaces the result as a coefficients block in human output (`alpha_ms_per_file`, `beta_ms_per_mb`, `gamma_ms` per planner/transfer pair) and as a `predictor.{copy,mirror}` JSON object under `--json`. Smoke test: `blit profile --json` returns the new `predictor` key (null when no v2 profile yet); `blit copy` --verbose under streaming-planner workloads will emit the new predictor lines once 5+ observations accumulate. Tiny fast-path extension (the planner-overhead-dominance signal in the plan) deferred to post-0.1.0 — extending the fast_path scan budget risks edge cases on memory-bounded large-tree workloads, and the predictor's now-load-bearing model is only freshly trained on v2 data; revisit once we have multi-week observability under the new schema. **Tests**: `cargo test --workspace` 404 pass / 0 fail.

**2026-05-05 18:00:00Z** - **ACTION**: §2.8 phase 1 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` — extended the predictor data model so its predictions can actually drive decisions. Schema bumped to v2; existing `PerformancePredictor::load` already drops state on version mismatch, so the bump is transparent. New types: `DurationCoefficients` (planner + transfer coefficient pair per profile), `DurationKind { Planner, Transfer, Total }`, `Prediction { predicted_ms, observations, fallback_depth }`. New API: `predict(kind, mode, src_fs, dst_fs, fast_path, skip_unchanged, checksum, file_count, total_bytes) -> Option<Prediction>` walks a fallback chain — exact key → drop fast_path → drop dest_fs → drop src_fs → mode-only — gated at each step by `MIN_OBSERVATIONS_FOR_CONFIDENCE = 5`. `predict_planner(record)` / `predict_transfer(record)` / `predict_total(record)` are convenience wrappers around `predict(...)`. `coefficients_for(...)` returns the same `DurationCoefficients` clone that `blit profile --json` will surface in phase 2. `observe(record)` now updates BOTH coefficient sets in lockstep (single shared observation counter) so `transfer_duration_ms` becomes a load-bearing input. Removed the old `predict_ms(record) -> f64` and `predict_planner_ms(...) -> Option<(f64, u64)>` — both pre-v2 entry points returned values without confidence gating. Eight new unit tests added: `predict_below_confidence_threshold_returns_none`, `predict_above_confidence_threshold_returns_value` (verifies `fallback_depth == 0`), `predictor_learns_planner_and_transfer_independently`, `predict_total_sums_planner_and_transfer`, `fallback_chain_drops_fast_path_then_dest_then_src`, `schema_version_mismatch_resets_state_on_load`. Existing tests rewritten to use the new API. Phase 2 (orchestrator wire + verbose surface + `blit profile --json` exposure + Tiny fast-path extension when planner-dominant) lands in the next commit. **Tests**: `cargo test --workspace` 404 pass / 0 fail (was 399, +5 new predictor tests + 3 retained-with-new-API).

**2026-05-05 17:00:00Z** - **FIX**: R43 follow-up to R42-F2. The R42 commit added a comment on `MultiStreamSender::finish` claiming it routed through `drain_pipeline_error`, but the body still hand-rolled the same match arms — comment lied about the factoring. Real fix: extracted `drain_pipeline_outcome(handle: JoinHandle<...>) -> Result<SinkOutcome>` as the underlying primitive (Ok-on-clean / wrap-on-err / panic-prefix-on-join). `drain_pipeline_error` now delegates to it for the failure cases and only adds the "Ok-but-channel-closed" race-diagnostic on top. `finish()` calls `drain_pipeline_outcome(handle).await?` directly. Added 3 new unit tests in `drain_pipeline_error_tests`: `drain_outcome_returns_value_on_clean_finish` (passes the SinkOutcome through), `drain_outcome_wraps_pipeline_error` (same "data plane pipeline failed" wrapping that `queue()` produces, regardless of which call site triggered it), `drain_outcome_surfaces_panic_message`. **Tests**: `cargo test --workspace` 399 pass / 0 fail (was 396, +3).

**2026-05-05 16:30:00Z** - **FIX**: Round 42 review of commit `96cbb10`. **R42-F1 (Medium)**: §1.1 sweep was incomplete — three sites still silently dropped metadata errors. (a) `crates/blit-core/src/remote/transfer/tar_safety.rs::write_extracted_file` mtime + permissions (used by both pull tar-shard receive and the daemon gRPC push fallback); (b) `crates/blit-core/src/orchestrator/orchestrator.rs::execute_single_file_copy` mtime — single-file copy fast-path was bypassing the warn-don't-silence policy. All three converted to `if let Err(e) = ... { log::warn!(...) }`. Final sweep confirms no `let _ = ` over `set_file_mtime` / `set_permissions` / `set_keepalive` / `.flush()` remains in production paths (the two leftovers — `diff_planner.rs::sync_mtimes` test helper and `win_fs.rs::clear_readonly_recursive` cleanup utility — are appropriately best-effort). **R42-F2 (Low)**: the new pipeline-streaming regression test only exercised `execute_sink_pipeline_streaming` directly, leaving `MultiStreamSender::queue`'s producer-side join-error-drain logic untested. Extracted the load-bearing match arms into a free `drain_pipeline_error(handle: JoinHandle<...>) -> eyre::Report` helper in `crates/blit-core/src/remote/push/client/mod.rs`. `MultiStreamSender::queue` and `MultiStreamSender::finish` both now route their failure paths through it, so the wrapping reads consistently as "data plane pipeline failed: <cause>" or "data plane pipeline panicked: <join>" regardless of which branch noticed the failure first. Three new unit tests in a fresh `drain_pipeline_error_tests` mod: `drain_returns_pipeline_error_with_failed_prefix` (Ok(Err(_)) path), `drain_returns_panic_message_for_aborted_task` (Err(JoinError) panic path), `drain_diagnoses_clean_close_race` (Ok(Ok(_)) clean-close-race diagnostic — proves we don't silently swallow it). **Tests**: `cargo test --workspace` 396 pass / 0 fail (was 393, +3 drain-helper tests).

**2026-05-05 16:00:00Z** - **FIX**: §2.7 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` — closed `docs/plan/POST_REVIEW_FIXES.md` Round 1. **§1.1**: `let _ = file.flush().await` in `sink.rs::write_file_stream` now propagates as a `Status::internal`-style error via `with_context("flushing {dst}")?` — flush failure is a data-loss signal (user believes file is durable when it isn't). Six other "best-effort metadata" sites in `sink.rs` (block-complete mtime/perms, tar-shard mtime/perms, file-payload mtime, write_block_complete mtime/perms) plus three TCP socket tuning sites in `data_plane.rs` (`set_keepalive`, `set_send_buffer_size`, `set_recv_buffer_size`) plus two `local_worker.rs::single_file_mmap_copy` mtime sites converted from silent `let _ = ...` to `if let Err(e) = ... { log::warn!(...) }` so misconfigurations are visible without making them hard transfer errors. **§1.1b**: `MultiStreamSender::queue` in `crates/blit-core/src/remote/push/client/mod.rs` no longer turns "tx.send failed" into the generic `"data plane pipeline closed unexpectedly"` string. `pipeline_handle` is now `Option<JoinHandle<...>>`; on send-failure, `take()` it, await, and surface the underlying error (`Ok(Err(e))` → `e.wrap_err("data plane pipeline failed")`, `Err(join)` → panic message). New regression test `pipeline_streaming_surfaces_underlying_sink_error` in `pipeline.rs::tests` uses a `FailingSink` to assert the underlying error message reaches the caller verbatim. **§1.2**: `TarShardExecutor` decision — kept with a thorough docstring explaining (a) gRPC-fallback streaming framing differs from TCP path's contiguous-buffer apply, requiring a `TarShardReceiver` trait extraction to unify, and (b) buffer-pool sizing benefits from real fallback throughput numbers. Deferred to post-0.1.0; tracked in `POST_REVIEW_FIXES.md` §1.2. **§1.3**: `WHITEPAPER.md` §3 "Data plane wire format" updated — `BLOCK_COMPLETE := 0x03 path_len:u32 path:bytes total_size:u64 mtime:i64 perms:u32` (was missing the trailing `mtime + perms` shipped in `a7d659f`); added an explanatory paragraph on why the metadata trails the terminator. `POST_REVIEW_FIXES.md` Round 1 status block flipped to "Closed 2026-05-05". **Tests**: `cargo test --workspace` 393 pass / 0 fail (was 392, +1 new pipeline-streaming regression test).

**2026-05-05 15:00:00Z** - **ACTION**: §2.5 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` — `blit completions` now generates real shell-completion scripts via `clap_complete`, honoring the `README.md:33` "shell completions" promise. Reshaped `Completions(CompletionArgs)` into a subcommand group: `blit completions shell <SHELL>` (bash/zsh/fish/powershell/elvish) writes a clap_complete-generated script to stdout; `blit completions remote <REMOTE> [--prefix <STR>] [--files] [--dirs]` is the existing daemon-backed CompletePath RPC the generated shell scripts call internally for `server:/module/<Tab>` style remote-path completion. New dep `clap_complete = "4"` on `blit-cli`. Three lib unit tests added: `clap_definition_validates` (forces clap's `debug_assert`), `shell_completions_generate_for_bash` (script defines `_blit()` and references known verbs), `shell_completions_generate_for_each_supported_shell` (totality across `clap_complete::Shell`). New integration test `test_utils_completions_shell_bash_emits_script`. Existing tests `test_utils_completions`, `test_utils_completions_dirs_only`, `test_admin_completions_*` updated to call `blit completions remote <target>` (subcommand split). README.md `### Admin Utilities` has new install snippets for bash/zsh/fish; `find --pattern ".csv"` updated to `"*.csv"` to match the post-§2.4 glob semantics. CHANGELOG entry rewritten to describe the two completion subforms. **Tests**: `cargo test --workspace` 392 pass / 0 fail (was 386, +3 cli tests + 2 list smart-dispatch from §2.3 + 1 shell-bash integration).

**2026-05-05 14:00:00Z** - **ACTION**: §2.3 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` — `blit list` now smart-dispatches by target shape. Bare-host targets (`server`, `server:9031`, `[::1]:9031`) route to the `list-modules` RPC; targets with a module/path tail (`server:/module/`, `server:/module/sub/`) keep going through the `ls` directory listing. Implementation: factored `run_list_modules`'s body into `pub(crate) async fn list_modules_remote(remote, json) -> Result<()>` in `list_modules.rs`; `ls::list_remote_path` short-circuits to that helper when `remote.path == RemotePath::Discovery`. Both code paths reuse the same `--json` and human shapes, so `blit list-modules <host>` and `blit list <host>` produce identical output. Two new integration tests in `crates/blit-cli/tests/admin_verbs.rs`: `test_admin_list_smart_dispatch_bare_host` (bare host gets the "Modules on …" header), `test_admin_list_smart_dispatch_module_path_routes_to_ls` (module path stays as `ls`-style streamed entries; explicit regression guard against routing everything to list-modules). The existing `test_admin_list_modules`, `test_admin_list`, `test_admin_list_subdirectory` continue to pass. `docs/cli/blit.1.md:13` (`blit list <REMOTE>`) and `:75` already documented the smart-dispatch wording — no manpage change needed. **Tests**: `cargo test -p blit-cli --test admin_verbs test_admin_list` 5 pass.

**2026-05-04 22:00:00Z** - **FIX**: Round 41 review (R41-F1, R41-F2, R41-F3) of commits `0ca489b` + `090f5cd`. **R41-F1 (High)**: binary-rename sweep was incomplete — five test files (`diagnostics_dump.rs`, `remote_tcp_fallback.rs`, `remote_remote.rs`, `remote_checksum_negotiation.rs`, `remote_pull_mirror.rs`) still constructed `bin_dir.join("blit-cli[.exe]")`, so `cargo test --workspace` on a clean target tree failed with `spawn: No such file or directory`; the previous "385 pass" report only held because stale `target/release/blit-cli` was still around. All five updated to `"blit" / "blit.exe"`. Cosmetic panic-message strings in `remote_resume.rs`, `remote_regression.rs`, `remote_parity.rs`, `remote_push_single_file.rs` swept for consistency. **R41-F2 (High)**: bench scripts still pointed at the old binary — `scripts/bench_remote_remote.sh` (BLIT default + comment), `scripts/bench_10gbe.sh` (BLIT path), `scripts/bench_local_mirror.sh` (build invocation `--bin blit-cli` and resulting BLIT_BIN path) all updated. **R41-F3 (Medium)**: `find --pattern` glob set `literal_separator(false)`, letting `*` cross `/`. With the basename fallback this caused over-matches like `foo*.csv` matching `foo/bar.csv`. Switched to `literal_separator(true)` (POSIX shell-glob semantics; `*` does NOT cross `/`); basename fallback still gives "find files at any depth" via `*.csv`. Added regression test `test_admin_find_glob_star_does_not_cross_path_separator`. CLI `FindArgs.pattern` rustdoc rewritten to spell out the no-cross-separator behavior and the basename fallback. **Tests**: clean `target/`, `cargo test --workspace` 386 pass / 0 fail (was 385, +1 new R41-F3 test).

**2026-05-04 21:30:00Z** - **ACTION**: §2.1 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` — renamed the produced CLI binary from `blit-cli` to `blit`, matching every doc/manpage/README example. `crates/blit-cli/Cargo.toml` keeps the workspace-member name `blit-cli` and adds `[[bin]] name = "blit"` so only the produced binary renames; cargo `--package blit-cli` and `-p blit-cli` references stay valid. Test helpers (`tests/common/mod.rs::TestContext::new`, `tests/single_file_copy.rs::cli_bin`) walk the build dir and previously hardcoded `"blit-cli"` / `"blit-cli.exe"`; updated to `"blit"` / `"blit.exe"`. CI artifact path in `.github/workflows/ci.yml` and the binary list in `scripts/build-release.sh` + `scripts/windows/build-release.ps1` flipped accordingly. Per-platform run/bench scripts (`scripts/{linux,macos,windows}/run-journal-fastpath.{sh,ps1}`, `run-remote-fallback.sh`, `bench-local-mirror.ps1`) now reference `target/.../release/blit{.exe}`. Build-instruction strings in scripts updated from `--bin blit-cli` to `--bin blit`. Stale `target/release/blit-cli` and `target/debug/blit-cli` artifacts removed to confirm nothing was silently relying on them. **Tests**: `cargo build --release -p blit-cli --bin blit` produces `target/release/blit`; `cargo test --workspace` 385 pass / 0 fail.

**2026-05-04 21:00:00Z** - **ACTION**: §2.4 of `docs/plan/RELEASE_PLAN_v2_2026-05-04.md` — `find --pattern` now glob-matches via `globset`, replacing pre-0.1.0 substring containment. Daemon-side `stream_find_entries` in `service/admin.rs` builds a `globset::GlobBuilder` matcher once with `case_insensitive(!case_sensitive)` and `literal_separator(false)`; matcher tested against both the entry's relative path and its file-name basename so `--pattern '*.csv'` finds nested files via the basename fallback even without `**/`. Invalid globs surface as `Status::invalid_argument` from the daemon and propagate to a non-zero CLI exit. CLI rustdoc on `FindArgs.pattern` updated to describe glob syntax. CHANGELOG: removed "find uses substring matching" known-limitation row, added a glob behavior note under Admin Utilities. Two new integration tests in `crates/blit-cli/tests/admin_verbs.rs`: `test_admin_find_glob_invalid_pattern_rejected` (malformed `[unterminated` rejected) and `test_admin_find_glob_nested_with_double_star` (`**/*.csv` finds top + deep entries). Updated existing `test_admin_find_with_pattern` and `test_utils_find` / `test_utils_find_json` to use glob patterns (`*.csv`, `*.md`, `*.log`) instead of substring fragments. New daemon dep: `globset = "0.4.18"`. **Tests**: `cargo test --workspace` 385 pass / 0 fail (was 383, +2 new glob tests).

**2026-05-04 19:55:00Z** - **FIX**: Closed F14 from `docs/reviews/codebase_review_2026-05-01.md`. Migrated `crates/blit-core/src/change_journal/snapshot.rs::macos::capture_snapshot` from `fsevent_sys::FSEventsGetCurrentEventId` (deprecated by upstream in favour of `objc2-core-services`) to `objc2_core_services::FSEventsGetCurrentEventId` under the `FSEvents` cargo feature. Same call shape (`unsafe { ... }`), same `u64` return — only the binding crate changed. `crates/blit-core/Cargo.toml` macOS-target dep flipped from `fsevent-sys = "5"` to `objc2-core-services = { version = "0.3", features = ["FSEvents"] }`. Both deprecation warnings on macOS builds are gone (`cargo build -p blit-core` is now warning-free apart from the pre-existing unused-variable in `fs_capability/macos.rs`). With F14 closed, **14 of 15 baseline findings** are closed; only F15 (deferred structured-logging epic) remains. **Validation**: `cargo check --workspace`; `cargo build -p blit-core` (no fsevent/deprecation warnings); `cargo test --workspace` (383 pass / 0 fail).

**2026-05-03 01:25:00Z** - **DOCS/TOOLS**: Completed Phase 3 cleanup/harness items from `docs/plan/REMOTE_REMOTE_DELEGATION_PLAN.md` except live benchmark execution. Documented `RemoteTransferSource` as the explicit legacy `--relay-via-cli` primitive now that default remote→remote uses destination-side `DelegatedPull`. Added `scripts/bench_remote_remote.sh` to compare default direct delegation against explicit relay using identical remote payloads and the existing `BLIT_TEST_COUNTER_FILE` byte-path counter. Added `docs/perf/remote_remote_benchmarks.md` as the results capture template. Updated the plan status/checklist; remaining unchecked item is running the benchmark on the target network and filling real numbers. **Validation**: `cargo fmt -- --check`; `cargo test -p blit-cli remote_remote_direct`; `bash -n scripts/bench_remote_remote.sh`.

**2026-05-03 01:05:00Z** - **FIX**: Closed Round 37 findings from `docs/reviews/followup_review_2026-05-02.md`. **R37-F1**: `RemotePullClient::pull_sync_with_spec` now preserves a typed `PullSyncError::Negotiation` for source-side pull-sync refusals before negotiation completes, and the destination `DelegatedPull` handler maps that to `DelegatedPullError::NEGOTIATE` instead of generic `TRANSFER`. Added core wire test coverage for initial RPC rejection classification and remote→remote CLI integration coverage where a fake source rejects `pull_sync`; the CLI surfaces "source refused delegated pull" and relay counters stay zero. **R37-F2**: delegated `BytesProgress` is now treated as cumulative state and converted to deltas before feeding the CLI progress accumulator; duplicate cumulative updates are not double-counted. Proto comments now document the cumulative semantics. **Tests**: `cargo fmt`; `cargo fmt -- --check`; `cargo test -p blit-core pull_sync_with_spec`; `cargo test -p blit-cli remote_remote_direct`; `cargo test -p blit-cli --test remote_remote`; `cargo test -p blit-daemon delegated_pull`; `cargo test --workspace` (all passed; existing macOS FSEvents deprecation/F14 warnings and the pre-existing macOS test unused-variable warning remain).

**2026-05-03 00:30:00Z** - **ACTION**: Implemented Phase 2 of `docs/plan/REMOTE_REMOTE_DELEGATION_PLAN.md`. Remote→remote `copy`/`mirror`/`move` now dispatch to destination-side `DelegatedPull` by default, with `--relay-via-cli` as the only legacy relay escape hatch. Added `crates/blit-cli/src/transfers/remote_remote_direct.rs` to build the same `TransferOperationSpec` as pull-sync, call `DelegatedPull`, surface stale-daemon/gate/connect/transfer errors without silent fallback, and print delegated summaries. Added env-gated test instrumentation in `blit-core::remote::instrumentation` so integration tests can assert the CLI sent zero data-plane payload bytes and never constructed `RemoteTransferSource` on the direct path, while `--relay-via-cli` does both. Reworked `crates/blit-cli/tests/remote_remote.rs` to cover direct byte-path isolation, gate-rejection no-fallback, stale destination `Unimplemented` no-fallback, and explicit relay. Updated CLI docs/help for `--relay-via-cli`. **Tests**: `cargo fmt`; `cargo check -p blit-cli`; `cargo test -p blit-cli remote_remote_direct`; `cargo test -p blit-cli --test remote_remote`; `cargo test -p blit-core pull_sync_with_spec`; `cargo test -p blit-daemon delegated_pull`; `cargo test --workspace` (all passed; existing macOS FSEvents deprecation/F14 warnings remain).

**2026-05-02 23:45:00Z** - **DOCS**: Round 18 finding R18-F1 fix — synced TODO.md with closed baseline + followup findings (commit `767f8ee`). Marked F2/F3/F4/F5/F7/F8/F9/F10/F11/F12/F13 + the pipeline-unification sequence as complete, leaving only F14 (FSEvents) and F15 (deferred logging). Round 19 follow-up R19-F1 + R19-F2 fix in commit-after-this: moved "Remote→remote re-evaluation" out of the executable Current Review Follow-up checklist into a new "Deferred design calls" subsection so it's not mistakenly picked up as the first unchecked actionable item; replaced the stale "16-round followup series" wording with a count-free phrase; backfilled this DEVLOG entry per R19-F2.

**2026-05-02 23:00:00Z** - **ACTION**: Closed F13 + F3 from `docs/reviews/codebase_review_2026-05-01.md` — the last remaining baseline polish items. **F13** (docs drift): with F2 making canonical-path containment always-on, the `use_chroot` config field was loaded-but-unused and the docs claimed enforcement that pre-F2 didn't exist. Removed `use_chroot` and `root_use_chroot` entirely from `ModuleConfig`/`RootExport`/`RootSpec`/`RawModule`/`RawDaemonSection`, the daemon startup banner, and `service::util::resolve_module`. TOML configs containing those keys are silently ignored (serde unknown-field tolerance). `docs/DAEMON_CONFIG.md`: removed the option from example, table, and added a new "Path containment" subsection explaining always-on enforcement with an honest TOCTOU caveat. Updated `docs/plan/WORKFLOW_PHASE_3.md`, `MASTER_WORKFLOW.md`, `BLIT_UTILS_PLAN.md`, `greenfield_plan_v6.md` to describe the post-F2 reality. Drive-by cleanup: deleted the orphaned `crates/blit-daemon/src/types.rs` and `crates/blit-daemon/src/config.rs` files (never declared as modules, dead code from a prior refactor). 5 test files migrated to drop `use_chroot` from their `ModuleSection` definitions. **F3** (reframed in triage as docs-only): added a "Trust Model and Network Exposure" section to `docs/DAEMON_CONFIG.md` stating explicitly that `0.0.0.0` default is intentional for a network file daemon, security comes from operator network controls + per-transfer tokens (not loopback-by-default), and TLS/mutual-auth fronting is out of scope. **Tests**: `cargo fmt`; `cargo test --workspace` (188 lib + 23 daemon + integration suites, all green). With F13 + F3 closed, the original 2026-05-01 baseline review has 13 of 15 findings closed; remaining are F14 (FSEvents deprecation) and F15 (structured logging — explicitly deferred).

**2026-05-02 22:15:00Z** - **ACTION**: Closed Round 15 finding R15-F1 (Low) — F11's user-facing closure. The previous F11 fix added daemon ack-mismatch handling in `RemotePullClient::pull_sync`, but `transfers/endpoints.rs::ensure_remote_transfer_supported` rejected `--checksum` for ALL remote transfers before the new code could fire. F11's CLI behavior was therefore unreachable. Made the gate direction-aware: `ensure_remote_pull_supported` allows `--checksum` (the F11 ack negotiates), `ensure_remote_push_supported` keeps it rejected for push/remote-remote (no equivalent capability negotiation in the push protocol yet). New integration test `remote_checksum_negotiation.rs` spins up a daemon with `--no-server-checksums`, runs `blit copy --checksum server:/test/file.txt dest`, and asserts (a) the client refuses with a clear ack-mismatch error, (b) no file is copied via silent fallback. Companion test confirms the same command succeeds against a daemon with checksums enabled. **Tests**: `cargo fmt`; `cargo test --workspace` (188 lib + 23 daemon + 27 metrics-test bin + 2 new checksum-negotiation integration tests, all green).

**2026-05-02 21:30:00Z** - **ACTION**: Closed F5, F11, F12, F9 from `docs/reviews/codebase_review_2026-05-01.md` — the lower-priority items from the baseline triage. **F5** (Medium): replaced inc_active/dec_active scattered around `service/core.rs` with `TransferMetrics::enter_transfer() -> ActiveGuard`, an RAII handle that decrements on Drop so panics/cancellation can't leak the gauge. Moved `inc_purge` to dispatch boundary so it counts attempts (matching push/pull semantics) instead of successes. Module doc updated with the attempts/errors/gauge contract. 5 metrics tests including panic-recovery assertion. **F11** (Medium): pull_sync receive loop now stores `ack.server_checksums_enabled` on `RemotePullReport` and bails early at the ack if the user requested `--checksum` but the daemon has checksums disabled — silent fallback to size+mtime would lie about the comparison strength the user asked for. **F12** (Low–Medium): documented `blit check` equivalence model in CLI `--help` (new `CHECK_SEMANTICS_HELP` constant, `after_long_help` on `CheckArgs`) and `CheckArgs` rustdoc. Pinned the documented behavior with 6 unit tests covering: matching-files-zero-diffs, empty-dirs-not-in-equivalence, symlinks-skipped, file-vs-dir-mismatch-on-file-side, missing-on-dest, --one-way ignores extras. **F9** (Medium): split `execute_local_mirror` into a sync wrapper + new `execute_local_mirror_async`. The async core is callable from any Tokio runtime; the sync wrapper just builds a runtime and block_ons. 2 tests prove async-from-async-context works and the sync wrapper is unchanged. **Tests**: `cargo fmt`; `cargo test --workspace` (188 lib + 23 daemon + 27 metrics-test bin + integration suites, all green).

**2026-05-02 20:15:00Z** - **ACTION**: Closed Round 13 finding R13-F1 (Medium). The F2 fix protected per-file writes via `resolve_contained_path`, but the push-mirror purge path enumerates `module.path` (post-mutation by `destination_path`) before the per-entry containment check fires. If the supplied `destination_path` traversed an in-module symlink pointing outside the module root, the purge would enumerate outside the canonical root before refusing any individual delete. Two-layer fix: (1) `push/control.rs` now calls `verify_contained(&canonical_root, &new_path)` immediately after the destination_path mutation, rejecting the push at handshake; (2) `purge_extraneous_entries` calls `verify_contained` before enumeration, defense-in-depth for any future caller that bypasses the handshake check. New integration test `f2_push_destination_path_symlink_rejected` spins up a daemon, places `module/escape -> /sibling`, attempts `blit mirror src/ server:/test/escape/`, and asserts both the daemon refusal and that `/sibling`'s files were not touched. **Tests**: `cargo fmt`; `cargo test --workspace` (186 lib + 20 daemon + 3 F2 integration tests, all green).

**2026-05-02 19:30:00Z** - **ACTION**: Closed F2 from `docs/reviews/codebase_review_2026-05-01.md` — the highest-severity remaining baseline finding. The lexical `safe_join` chokepoint catches `../`/abs/UNC/Windows-shaped wire paths but doesn't follow symlinks, so a pre-existing symlink inside a module pointing outside the root (e.g. `module/escape -> /etc`) would have any daemon read/write follow it. New `path_safety::contained_join` (and `verify_contained` for already-resolved targets) walks to the deepest existing ancestor, canonicalizes, and confirms `starts_with(canonical_module_root)`. 10 unit tests cover symlink-escape rejection (top-level, deep, parent-of-nonexistent), legit intra-module symlinks, lexical-traversal rejection, and the empty-rel single-file case. Wired into every daemon site that touches a path under a module root: `service/core.rs` (list, find, du, purge), `service/admin.rs` (delete_rel_paths_sync, stream_disk_usage, stream_find_entries), `service/pull.rs` and `service/pull_sync.rs` (pull entry-point), `service/push/data_plane.rs` (file dest + tar shard apply), `service/push/control.rs` (file_requires_upload). Added a separate `canonical_root: PathBuf` field on `ModuleConfig` because the push handler mutates `module.path` to bake in destination subpaths (rsync "copy into here" semantics) — containment must check against the never-mutated original. New integration tests in `crates/blit-cli/tests/f2_chroot_containment.rs` spin up a real daemon, place a symlink inside the module pointing outside, attempt to pull through it, assert the daemon refuses with a containment error, and verify the sensitive sibling file was not copied. Companion test confirms legit intra-module symlinks (`latest -> v1`) still work. **Tests**: `cargo fmt`; `cargo test --workspace` (186 lib + 20 daemon + integration suites, all green). F2 closes the last release-blocking security finding from the original baseline review.

**2026-05-02 18:00:00Z** - **ACTION**: Closed Round 11 finding R11-F1 (High) — completed F7 properly. The previous F7 fix bounded the *reservation* via `try_reserve_exact` but the actual `read_to_end` call would still grow the Vec past that bound if the remote source streamed extra bytes. A peer could declare `header.size = 4` and stream 1 GiB; the post-read length check would catch the discrepancy but only after the buffer had grown past the bound. Fix: extract `read_remote_entry_bounded` as a generic helper that wraps the reader with `tokio::io::AsyncReadExt::take(size + 1)` so the read itself is capped; the +1 is the over-read canary that triggers the post-read length check. Helper is `R: AsyncRead + Unpin` so tests use `std::io::Cursor` instead of a real `RemotePullClient` mock. 5 new tests in `remote_bounded_read_tests`: happy path, under-read, over-read (verifies buffer caps at size+1 even when source sends 1 MiB), defensive cap rejection, empty file. **Tests**: `cargo fmt`; `cargo test --workspace` (176 lib + 20 daemon + integration suites, all green). F7 now fully closed.

**2026-05-02 17:30:00Z** - **ACTION**: Closed F7 + F8 from `docs/reviews/codebase_review_2026-05-01.md` (the original baseline review) in one small hardening pass. **F7** (now elevated to High in the triage): `RemoteTransferSource::prepare_payload` was doing `Vec::with_capacity(header.size as usize)` from a daemon-controlled `FileHeader.size` — the send-side mirror of R6-F1 that affects remote→remote transfers. Extracted `validate_remote_tar_shard_sizes` as a private helper that bounds both per-entry and cumulative shard sizes against `tar_safety::MAX_TAR_SHARD_BYTES`, uses `checked_add` for the sum, and is unit-testable without a `RemotePullClient` mock. Replaced the unbounded `Vec::with_capacity` with `try_reserve_exact`, plus an explicit length-match check after `read_to_end` so a remote source that lies about a file's size produces a hard error rather than a malformed tar. **F8**: `MAX_WIRE_TAR_SHARD_BYTES` in pipeline.rs was 1 GiB while the helper's `MAX_TAR_SHARD_BYTES` is 256 MiB — the wire reader accepted shards the helper would reject. Single source of truth: `MAX_WIRE_TAR_SHARD_BYTES = tar_safety::MAX_TAR_SHARD_BYTES as usize` (const). 5 new tests in `remote_tar_size_tests` (within bounds, at cap, per-entry above cap, cumulative above cap, u64 overflow). **Tests**: `cargo fmt`; `cargo test --workspace` (171 lib + 20 daemon + integration suites, all green).

**2026-05-02 16:30:00Z** - **ACTION**: Closed Round 9 finding R9-F1 (Low). The R8-F2 fix (rejecting stream EOF without UploadComplete) was previously verified by code review only — the new framing-helper unit tests covered R8-F1 size/cap rules but not the actual EOF path through `receive_fallback_data`. To make the function unit-testable without a real gRPC server, generic-ified its signature over `tokio_stream::Stream<Item = Result<ClientPushRequest, Status>>` (tonic::Streaming already implements this trait, so production callers are unchanged) and switched the loop body from `.message()` to `.next().await.transpose()` — semantically identical. 3 new regression tests in `data_plane::tests` drive the function over `tokio_stream::iter` of synthetic message vectors: EOF after `FileManifest`, EOF after `TarShardHeader`, and EOF on an empty stream (no UploadComplete ever). All three assert the rejection now lives in the test suite, not just code review. **Tests**: `cargo fmt`; `cargo test --workspace` (166 lib + 20 daemon + integration suites, all green).

**2026-05-02 15:45:00Z** - **ACTION**: Closed Round 8 review findings R8-F1 and R8-F2 from `docs/reviews/followup_review_2026-05-02.md`. Both bugs were in the daemon's gRPC push fallback receive loop (`receive_fallback_data` in `crates/blit-daemon/src/service/push/data_plane.rs`) — the framing layer that runs *before* the shared `tar_safety` helper sees the buffer. **R8-F1** (Medium — DoS): the chunk accumulator was capped only by an 8 MiB initial reservation; the actual `Vec` grew unbounded. `archive_size == 0` skipped the overflow check entirely, and `archive_size > MAX_TAR_SHARD_BYTES` was bypassed at the framing layer. Reject zero archive_size when files are present, reject `archive_size > MAX_TAR_SHARD_BYTES` at TarShardHeader, and enforce both the declared size and the local cap on every chunk via a new `check_fallback_chunk_overflow` helper. **R8-F2** (Medium — silent partial success): stream EOF was treated as graceful end. But FileManifest and TarShardHeader remove entries from `pending` before bytes arrive, so a client could send a header, close the stream, and the final `pending.is_empty()` check would pass despite no data ever landing. Track `upload_complete_seen` and bail on EOF without UploadComplete or with `active.is_some()`. Extracted `validate_fallback_shard_archive_size` and `check_fallback_chunk_overflow` as private helpers so the rules are unit-testable without spinning up a full gRPC server. 7 new framing tests pin every boundary condition (zero size, at-cap, above cap, declared overflow, local-cap overflow, u64 overflow, within-bounds). **Tests**: `cargo fmt`; `cargo test --workspace` (166 lib + 17 daemon + integration suites, all green).

**2026-05-02 14:30:00Z** - **ACTION**: Round 7 cleanup — extracted shared `tar_safety` primitive in `blit-core/src/remote/transfer/tar_safety.rs` and migrated all three tar-shard receive sites onto it: `pull.rs::apply_pull_tar_shard` (gRPC fallback), `sink.rs::write_tar_shard_payload` (TCP data plane + local-local), `blit-daemon/src/service/push/data_plane.rs::apply_tar_shard_sync` (daemon push receive). The migration closed a previously-latent High-severity bug on the daemon side: `apply_tar_shard_sync` was calling `Entry::unpack`, so an authenticated push client could ship a tar shard with a Symlink entry at a benign path pointing at `/etc/passwd` and a later push to that path would write through the symlink — same R5-F2 class as the pull side, but server-side. New regression test `apply_tar_shard_rejects_symlink_entry` proves the daemon now rejects non-regular entries up front. The shared helper also brought R6-F1 size bounds and R6-F3 mtime/permissions preservation to `sink.rs` and the daemon, where they were missing. 8 new unit tests in `tar_safety::tests`; net +8 lib tests workspace-wide (166 total). **Tests**: `cargo fmt`; `cargo test --workspace` all green.

**2026-05-02 13:15:00Z** - **ACTION**: Closed Round 6 review findings R6-F1 through R6-F3 from `docs/reviews/followup_review_2026-05-02.md`. **R6-F1** (Medium — DoS via per-entry size): `apply_pull_tar_shard` was preallocating with `Vec::with_capacity(header.size as usize)`, where `header.size` came from the daemon-authored `TarShardHeader.files`. A daemon could keep the shard `archive_size` under the 256 MiB cap while putting `u64::MAX` in a per-file header, forcing a huge initial allocation. Now validates `entry.size() == header.size` and `header.size <= shard.declared_size <= MAX_TAR_SHARD_BYTES` before any allocation, then uses `Vec::try_reserve_exact` so a still-pathological size returns `AllocError` instead of aborting. **R6-F2** (Medium — silent partial success): if the gRPC stream ended with `active_shard = Some(_)` (Header sent, no Complete), the receive loop dropped the buffered shard and returned success. Extracted the post-loop check into `ensure_no_open_shard` and made it bail with a wire protocol error — same treatment as `FileData` without a preceding `FileHeader`. **R6-F3** (Medium — metadata desync): manual tar-shard writes weren't applying `header.mtime_seconds` or `header.permissions`, so a forced-gRPC pull of many small files succeeded byte-for-byte but left destination mtimes at "now", causing the next size+mtime sync to re-transfer everything. Now applies mtime via `filetime::set_file_mtime` when `> 0` and Unix permissions when nonzero, mirroring `FsTransferSink`. 3 new unit tests in `tar_shard_safety_tests` (rejects oversized per-entry size, rejects open shard at end-of-stream, preserves mtime). **Tests**: `cargo fmt`; `cargo test --workspace` (158 lib tests + integration suites, all green).

**2026-05-02 11:30:00Z** - **ACTION**: Closed Round 5 review findings R5-F1 through R5-F5 from `docs/reviews/followup_review_2026-05-02.md`. **R5-F1** (High — data-loss vector): `delete_listed_paths` used a lexical `target.starts_with(dest_root)` check that lets `dest_root.join("../victim")` pass. Routed every wire-supplied path through `path_safety::safe_join`, switched `RemotePullReport::paths_to_delete` from `Vec<PathBuf>` to `Vec<String>` so wire input never gets blessed into a path before validation, and added 4 unit tests in `delete_list_safety_tests` covering parent-traversal, absolute paths, root-self-reference, and the happy path. **R5-F2** (High — wire-controlled symlink/hardlink creation): `apply_pull_tar_shard` was calling `Entry::unpack` which honors tar's symlink/hardlink/device entry types. Rewrote to reject anything that isn't `EntryType::Regular` or `EntryType::Continuous`, validate the path through `validate_wire_path`, copy bytes manually, and verify per-entry size matches the corresponding `FileHeader.size`. Added 4 unit tests including a hand-crafted malicious tar with `../escape.txt` in its header. **R5-F3** (Medium — DoS via huge `archive_size`): `Vec::with_capacity(header.archive_size as usize)` was a daemon-controlled allocation. Added `MAX_TAR_SHARD_BYTES = 256 MiB` cap, modest initial reservation (capped at 1 MiB), per-chunk overflow check against `declared_size` and the local cap, and a final length check on `TarShardComplete`. **R5-F4** (Low — comment lied about validation): `filter_from_spec` claimed to validate glob patterns but only copied them into `FileFilter` for later silent dropping. Now compiles each include/exclude pattern through `globset::Glob::new` at the spec normalizer; 2 new tests pin rejection of malformed brackets. **R5-F5** (Low — DEVLOG ordering): added this header and moved newest entries to the top so review workflow doesn't depend on knowing how to find appended-at-the-bottom changes. **Tests**: `cargo fmt`; `cargo test --workspace` (152+ lib tests, all integration suites green).

**2026-05-02 09:30:00Z** - **ACTION**: Step 4C — tar-shard support on the gRPC pull fallback. The push gRPC fallback runs the unified planner and emits TarShardHeader/Chunk/Complete for batched small-file workloads; the pull side previously sent files one-by-one via a hand-written loop. New `GrpcServerStreamingSink` (mirror of `GrpcFallbackSink`) in `blit-core/src/remote/transfer/sink.rs` lets the daemon's `stream_via_grpc` run `execute_sink_pipeline` with the same planner output as the TCP data plane. Proto adds three new ServerPullMessage variants (fields 13/14/15) reusing the existing TarShard message types. Client receive loop in `pull_sync` adds three match arms with an `InProgressShard` buffer and an `apply_pull_tar_shard` extractor (mirror of the daemon's push-side `apply_tar_shard_sync`). New integration test `test_pull_grpc_fallback_many_small_files` exercises 50 small files via `--force-grpc`. **Tests**: `cargo fmt`; `cargo test --workspace` (all green); `cargo test -p blit-cli --test remote_parity` (5 tests including the new one, all pass).

**2026-05-02 08:45:00Z** - **ACTION**: Step 4B — closed F10 (filter parity on pull) and F4 (mirror deletion correctness). The daemon now builds a `FileFilter` from `spec.filter` and threads it into `collect_pull_entries_with_checksums` so source enumeration honors `--exclude/--include/--min-size/--max-size/--min-age/--max-age/--files-from` exactly as a push would. After diff, a new `scope_deletions(...)` helper applies `MirrorMode` + filter scope and produces the authoritative deletion list, which the daemon ships to the client via a new `ServerPullMessage::DeleteList` variant. The CLI replaces the prior dest-tree-walking `purge_extraneous_local` with `delete_listed_paths`, which executes exactly what the daemon said (with bounded-path safety). New `--delete-scope <subset|all>` flag selects between `MirrorMode::FilteredSubset` (default — out-of-scope files preserved) and `MirrorMode::All` (every absent file purged). 6 new unit tests for scope_deletions; 2 new integration tests in `remote_pull_mirror` covering FilteredSubset preservation and `--delete-scope all` purge behavior. **Tests**: `cargo fmt`; `cargo test --workspace` (all green); `cargo test -p blit-cli --test remote_pull_mirror` (3 integration tests, all pass).

**2026-05-02 06:30:00Z** - **ACTION**: Closed R2-F1, R4-F1, R4-F2, R4-F4 from `docs/reviews/followup_review_2026-05-02.md`. (1) DiffPlanner's `local_needs_copy` now has concrete behavior for every `ComparisonMode` (SizeOnly, IgnoreTimes, Force, Checksum, SizeMtime/Unspecified) instead of silently falling through to size+mtime; 10 new per-mode tests. (2) Daemon PullSync handler routes through `NormalizedTransferOperation::from_spec` so spec_version mismatch and unknown enum values become hard `invalid_argument` errors at the wire boundary. (3) `ignore_existing` is now an orthogonal `bool` field on `TransferOperationSpec` (proto field 10; old enum value 5 reserved); `compare_mode = Force && ignore_existing` is rejected at the spec normalizer; CLI adds `conflicts_with = "force"` on `--ignore-existing`; orchestrator threads the flag through `LocalMirrorOptions → LocalDiffInputs` so local-local copies enforce it through the same planner as remote paths. (4) Refreshed stale proto comments referring to removed `PullSyncHeader` and the old "supports_filter_spec fallback" plan. **Tests**: `cargo fmt`; `cargo test --workspace` (all green, 144+ core tests).

**2026-05-02 04:15:00Z** - **STEP 4A (PullSync wire migration to TransferOperationSpec)**: With no backwards-compat constraint into the next release, replaced the bool-soup `PullSyncHeader` (5 booleans for comparison mode + separate fields for resume/mirror/force_grpc) with the unified `TransferOperationSpec` defined in step 2. `ClientPullMessage::Header` is now `ClientPullMessage::Spec`. `PullSyncHeader` deleted from the proto entirely. Daemon parses the spec and translates its `ComparisonMode` enum onto the existing internal `CompareMode` via a new `compare_mode_to_internal` helper that also splits out `IgnoreExisting` as the orthogonal axis it actually is. Client (`RemotePullClient::pull_sync`) constructs the spec from CLI args, advertising full `PeerCapabilities` (resume, tar shards, data plane TCP, filter spec — all true), and defaulting `mirror_mode` to `MirrorMode::FilteredSubset` rather than the old "any-mirror-mode-true" boolean. Filter not yet honored on the daemon — that's step 4B. **The bool-soup → typed-enum win is real**: nonsensical combinations like `size_only=true && checksum=true && ignore_times=true` are no longer constructible. **Tests**: 214 pass (unchanged) — pure wire-shape migration, behavior preserved end-to-end.

**2026-05-02 03:30:00Z** - **STEP 3b (push client routes through diff_planner)**: Smaller than the orchestrator migration in step 3a because push's actual diff lives on the *daemon* side — the client sends its source manifest, the daemon returns a NeedList, and the client filters its headers to that intersection before planning. There's no client-side comparison stage to consolidate. What changed: added `plan_push_payloads(headers, source_root, plan_options)` to `diff_planner` as the unified entry point for push planning (a thin wrapper over `plan_transfer_payloads` that adds a `Context` for error messages); push client's import now goes through `diff_planner` so future readers find the planning entry point in the unified module rather than reaching into `payload.rs`. Documented inline that push's diff is daemon-driven and that step 5 (remote→remote re-evaluation) might let push share daemon-side diff machinery with pull. **Tests**: 214 pass (unchanged; this is a re-export + import rename, not a behavior change).

**2026-05-02 03:00:00Z** - **F1 follow-up (R1 fixes)**: Acted on `docs/reviews/followup_review_2026-05-02.md` round 1. **R1-F1** (Medium, real bug): `validate_wire_path` accepted single-leading-backslash forms like `\Windows\System32` on Unix because `Path::components` treats backslash as a normal character there. Extended `looks_like_windows_absolute` to flag any input starting with `\`. Tests added for `\foo`, `\foo\bar`, bare `\`. **R1-F3** (Low): wire path `"."` previously normalized to empty, getting the same single-file treatment as the legitimate empty-path case. `validate_wire_path` now rejects non-empty inputs that produce zero normal components (`"."`, `"./"`, `"./."`) — those are directory hints, not file paths. Fallout: the daemon's `resolve_relative_path` (used for list/find/du request paths where `.` legitimately means "module root") needed to disambiguate between request-path and file-path contexts. Added a request-path-context fold that maps `""`, `"."`, `"./"` etc. to `PathBuf::from(".")` before invoking strict validation; file-path callers (`resolve_manifest_relative_path`, sink writes) keep using the strict validator directly. **R1-F2** (Medium): `safe_join` is lexical only — `dest_root/symlink/file` lexically passes even when `symlink` escapes the root. Reviewer correctly identified this as F2 (chroot/canonical-containment) territory, not an F1 fix. Documented the lexical-only contract prominently in `path_safety.rs` module doc with an explicit pointer to F2 for canonical-parent containment. Updated `TODO.md` F2 entry to call out the symlink-traversal concern. **Tests**: 214 pass (was 213) — net +1 from R1-F1 leading-backslash test minus zero from R1-F3 (the existing `just_dot_normalizes_to_empty` test was rewritten as `just_dot_rejected` covering three inputs in one test).

**2026-05-02 02:30:00Z** - **STEP 3a (DiffPlanner extraction, orchestrator migration)**: New `blit-core::remote::transfer::diff_planner` module consolidates the diff + payload-planning stage. Public API: `plan_local_mirror(headers, LocalDiffInputs) -> PlannedPayloads` is the single entry point the orchestrator's local-mirror path now calls; under the hood it composes `filter_unchanged` (the comparison-filter stage previously named `filter_headers_for_copy` and inlined in `orchestrator.rs`) with the existing `plan_transfer_payloads`. Translates the proto `ComparisonMode` enum onto the `Option<ChecksumType>` that the existing comparison primitives consume — `Checksum` enables Blake3, all other variants (`Unspecified`, `SizeMtime`, `SizeOnly`, `IgnoreTimes`, `IgnoreExisting`, `Force`) currently fall back to size+mtime. The non-`SizeMtime` variants are accepted at the API surface so callers can pass them through; their proper behavior arrives with step 4 (pull_sync.rs migration brings the comparison strategies that today live as `PullSyncHeader` bools). Orchestrator's local-mirror path migrated: the previous inline scan → spawn_blocking(filter_headers_for_copy) → plan_transfer_payloads sequence is now scan → spawn_blocking(plan_local_mirror) — same effect, single chokepoint, smaller call site. Dead `filter_headers_for_copy` removed. `ChecksumType` gained `PartialEq + Eq` so the diff_planner unit tests can assert mappings. **Push and pull origin paths NOT yet migrated** — those land in step 3b and step 4 respectively. **Tests**: 213 pass (+4 new diff_planner unit tests covering the ComparisonMode→ChecksumType mapping for every variant, and the filter_unchanged identity-vs-modified behavior), 0 failures. Behavior preservation verified by the unchanged orchestrator integration tests (admin verbs, remote_remote, remote_pull_mirror, remote_resume, remote_tcp_fallback all still pass).

**2026-05-02 01:45:00Z** - **STEP 2 (operation spec contract)**: Added the canonical `TransferOperationSpec` message to `proto/blit.proto` along with `FilterSpec`, `ComparisonMode` enum, `MirrorMode` enum, `ResumeSettings`, `PeerCapabilities`. Per `docs/plan/PIPELINE_UNIFICATION.md` step 2: contract only, no behavior change yet. PushHeader and PullSyncHeader stay as the today-active control messages until the DiffPlanner extraction (step 3) and pull_sync.rs refactor (step 4) port to the new spec. Notable design choices: (1) `ComparisonMode` is an enum rather than the bool soup currently in PullSyncHeader (`size_only`, `ignore_times`, `ignore_existing`, `force`, `checksum` — five booleans that admit nonsensical combinations), (2) `MirrorMode` enum makes F4's filtered-mirror-delete-scope question explicit at the wire boundary (`OFF`, `FILTERED_SUBSET`, `ALL`), (3) `optional` proto3 scalars on `FilterSpec` size/age fields so callers can distinguish "not set" from "set to 0", (4) `PeerCapabilities` is additive — new capabilities default to `false` so a missing field on an old peer reads as "not supported." Re-export module `blit-core::remote::transfer::operation_spec` so callers don't reach into the generated `blit.v2` namespace directly. **Verification**: `cargo check --workspace` clean; `cargo test --workspace` 209 pass (unchanged); generated bindings include all six new types in the build artifact.

**2026-05-02 01:15:00Z** - **F1 (path safety)**: Centralized receive-side path validation in `blit-core/src/path_safety.rs`. Two helpers: `validate_wire_path(s) -> PathBuf` (pure validation, normalized form) and `safe_join(root, s) -> PathBuf` (validate + join, with empty input returning root unchanged for the single-file destination case). Rejects: `..` components (not just substrings — the previous tar-shard `rel.contains("..")` check was too weak and also rejected legitimate filenames containing literal `..`), absolute paths, Windows drive prefixes (`C:\…`), UNC and DOS device paths (`\\?\…`, `\\server\share\…`), root components, embedded NUL bytes. Migrated `pull.rs::sanitize_relative_path` and daemon `service/util.rs::resolve_relative_path` / `resolve_manifest_relative_path` to thin wrappers that delegate to the shared module — single chokepoint, no drift between sanitizers. Applied at all five receive-side sites the review identified: `sink.rs:218` (streamed file write), `sink.rs:404` + `:426` (tar shard entry validation + per-entry join), `sink.rs:462` (resume block write), `sink.rs:498` (block completion), and the gRPC fallback block-hash path in `pull.rs:532`. Wire-boundary defense in depth: `pipeline.rs::read_file_header` and per-entry path read in `read_tar_shard` validate at the moment paths arrive off the socket, so unsafe headers never enter the FileHeader stream. **Tests**: 209 passed (was 185 baseline) — 16 new `path_safety::tests` covering the validator surface (`..`, absolute Unix, Windows drive, UNC, NUL, valid `..`-containing filenames, deep paths, Unicode, trailing slash, just-dot), 8 new sink-level integration tests confirming `FsTransferSink::write_file_stream` rejects malicious wire paths before any filesystem write and that the legitimate single-file empty-path case still works. **Verification**: `cargo check --workspace` clean; `cargo test --workspace` 0 failures.

**2026-05-02 00:30:00Z** - **PLAN**: Architecture clarification in response to GPT-5.5's review of the universal-filter / blit check / daemon-metrics work and follow-up discussion. The review's findings (esp. F1 path safety, F10 filter parity overstatement, F11 pull checksum capability) revealed the deeper architectural shape: a transfer has three roles (initiator / origin / target) and `pull_sync.rs` is the only origin path that doesn't run the unified pipeline. The corrected model is documented in `docs/plan/PIPELINE_UNIFICATION.md`: the same pipeline (`TransferSource → DiffPlanner → execute_sink_pipeline_streaming → TransferSink`) runs on whichever side holds the data, regardless of where the CLI was invoked. The unified stage is `DiffPlanner`, not `FilteredSource` — `FilteredSource` is one input among filter rules, destination manifest, comparison mode, and capabilities. The wire contract is a normalized `TransferOperationSpec`, not a flag bag. Updated `TODO.md` with the 5-step priority sequence: F1 path safety first (independent prereq), then `TransferOperationSpec` data shape, then `DiffPlanner` extraction from `pull_sync.rs`, then `pull_sync.rs` refactor onto the unified pipeline, then re-evaluate remote→remote (currently CLI-mediated relay; should become daemon-A → daemon-B direct).

**2026-05-01 23:43:57Z** - **REVIEW**: Completed a repo-level codebase review and saved it to `docs/reviews/codebase_review_2026-05-01.md`. Highest-priority findings: receive-side path sanitization is incomplete across streamed files, tar shards, and resume block records; daemon `use_chroot` is documented/configured but not enforced; filtered mirror delete semantics need an explicit product decision; metrics lifecycle and endpoint behavior need hardening. Added a short `TODO.md` follow-up section and marked Phase 4 as in progress with the review artifact linked. **Verification**: `cargo check --workspace` passed; `cargo test --workspace` passed with 185 tests after rerunning outside the sandbox to allow local test port binding.

**2026-04-24 03:15:00Z** - **FIX**: Fixed single-file remote push crash — `blit copy FILE server:/mod/` previously died with "opening FILE/ during payload planning: Not a directory". Root cause was two-sided: on the client, `filter_readable_headers` and `build_tar_shard` computed `source_root.join(&rel)` where `rel=""` for single-file sources, which on Unix preserves a trailing `/` and makes `File::open` reject with ENOTDIR. On the daemon, `resolve_relative_path` folded empty paths to `"."` (which broke `module.path.join("")` = wanted single file, but got `module.path/.`), and the subsequent `module.path.join(empty_rel)` added another trailing `/`. Fix applies the `rel.is_empty() → use root directly` pattern in three places on the client side (matching `FsTransferSource::open_file`'s existing handling), and adds two new helpers on the daemon side: `resolve_manifest_relative_path` preserves empty as empty for per-file manifest entries, and `resolve_dest_path(base, rel)` handles the empty-rel join case. Swapped all four push-side `resolve_relative_path` callers and three `module.path.join` sites. Regression tests in `crates/blit-cli/tests/remote_push_single_file.rs` pin both the container-dir (`blit copy FILE server:/mod/`) and rename (`blit copy FILE server:/mod/new.txt`) cases. Annotated all four `docs/bugs/*.md` files with their resolution status. **Tests**: 173 passed (+2 remote push single-file), clippy clean.

**2026-04-24 02:54:00Z** - **ACTION**: Completed the remaining UX feedback items #4, #5, #6, #7, #8, #9, #10 from `docs/ux-feedback-migrate-games-poc.md`. #4 TTY auto-progress: `TransferArgs::effective_progress()` enables the progress indicator when stdout is a TTY and `--json` is off, matching rsync/rclone; piped stdout stays silent for scripts. #8 `--help` rsync docs: `after_long_help` on `TransferArgs` appends a PATHS section summarizing the trailing-slash rules to `blit copy/mirror/move --help`. #6/#7/#5 banner cleanup: demoted the `starting copy` line to stderr and dropped the version prefix; added `collapse_slashes()` display-only normalization so script-appended `//` no longer shows in the banner (semantics unchanged); under `-v`, print a follow-up line when rsync resolution materially rewrote the destination. #9 flag grouping: split `TransferArgs` flags across clap `help_heading` buckets (Options / Comparison / Reliability / Performance/debug) so first-time users see common flags before niche debug knobs. #10 `blit diagnostics dump SRC DST [--json]`: new subcommand emits a pasteable snapshot (version, invocation argv, parsed endpoints, rsync resolution, local filesystem caps, free/total disk, same-device check) without performing a transfer — built for one-shot bug reports. Added `sysinfo = "0.31"` (disk-only) to blit-cli, exposed `source_is_contents`/`dest_is_container` as `pub(crate)`, wrote 3 smoke tests pinning the JSON shape. Updated `blit(1)` manpage SYNOPSIS and DIAGNOSTICS sections. **Tests**: 170 passed (added 3), clippy clean.

**2026-04-24 02:32:00Z** - **ACTION**: Addressed UX feedback items #1 and #3 from `docs/ux-feedback-migrate-games-poc.md`. Added `TransferOutcome` enum (`Transferred` / `JournalSkip` / `UpToDate` / `SourceEmpty`) to `LocalMirrorSummary` so the CLI can emit distinct, honest messages for the three legitimate zero-files cases instead of the ambiguous `Copy complete: 0 files, 0 B`. `FastPathDecision::NoWork` now carries an `examined` count to split empty-source from already-up-to-date; the single-file path also sets `UpToDate` when `skip_unchanged` short-circuits. CLI `print_summary` in `crates/blit-cli/src/transfers/local.rs` matches on outcome and suppresses the `• Throughput / Workers used` line unless verbose, ≥1 MiB, or `>1` file copied. JSON output now includes `outcome` and `files_examined`. Strengthened `single_file_copy_idempotent` to pin `Copy complete: 1 files` on first run and `Up to date` on second run — would have caught the regression where the single-file rerun emitted misleading `Copy complete: 0 files`. **Tests**: 167 passed, clippy clean.

**2026-04-14 20:00:00Z** - **ACTION**: Completed unified-pipeline remediation across all transfer paths. Previously only local→local and gRPC-fallback push routed through `execute_sink_pipeline`; multi-stream TCP push and both daemon pull paths had bespoke dispatch code. Added `execute_sink_pipeline_streaming(source, sinks, payload_rx, prefetch, progress)` in `crates/blit-core/src/remote/transfer/pipeline.rs` and made the one-shot form a thin wrapper. Rewrote `MultiStreamSender` in `crates/blit-core/src/remote/push/client/mod.rs` as a lightweight `payload_tx` + `JoinHandle` shell — deleted `data_plane_sink_worker`, batch-splitting helpers, and `StreamStats` (~130 lines). Converted both daemon pull paths in `crates/blit-daemon/src/service/pull.rs` to use the pipeline; deleted `handle_pull_stream` and `handle_pull_stream_streaming` (~125 lines). Deleted dead modules: `transfer_engine.rs` (WorkerFactory trait + task-channel infra), `transfer_facade/` (TaskAggregator, PlannerEvent, build_local_plan, stream_local_plan), and `RetryableTask` from `transfer_plan.rs` — 1139 lines total. Updated `docs/ARCHITECTURE.md` with the unified-pipeline diagram and source/sink matrix. Added `blit(1)` command-summary hints and expanded `TransferArgs` doc-comments for `--help`. **Tests**: 152 passed (87 core + 65 integration), clippy clean. **Remaining**: end-to-end 10 GbE benchmark runs.

**2026-04-07 18:00:00Z** - **ACTION**: Completed all Phase 4 production hardening items that don't require hardware. Created `CHANGELOG.md` with full 0.1.0 feature inventory. Added `docs/plan/AI_TELEMETRY_ANALYSIS.md` scoping statistical anomaly detection, tuning recommendations, and diagnostics (all local, no ML, opt-in). Created `.github/workflows/ci.yml` with cargo fmt/clippy, tri-platform tests, and release artifact builds. Added `scripts/build-release.sh` and `scripts/windows/build-release.ps1`. Updated TODO to mark packaging, CI, telemetry, logging review, install docs, and release notes complete. **Remaining**: benchmark runs (need hardware), RDMA (post-release), ReFS privilege investigation (Windows).

**2026-04-07 17:00:00Z** - **ACTION**: Rewrote `docs/DAEMON_CONFIG.md` fixing multiple inaccuracies: wrong port (50051→9031), wrong TOML format (`[modules.backup]`→`[[module]]`), wrong URL syntax. Added client config directory paths, mDNS section, and expanded service installation templates for all platforms. Performed logging/error audit: removed duplicate println/eprintln debug output from block clone paths (`copy/windows.rs`, `copy/file_copy/mod.rs`). Added expect() messages to bare unwrap() calls in `remote/endpoint.rs` and `remote/push/client/mod.rs`. No dbg!()/todo!() found in production code. Full structured logging migration deferred to post-release. **Tests**: `cargo build` (clean).

**2026-04-07 16:00:00Z** - **ACTION**: Completed blit-utils hardening (P1). Added 21 integration tests in `crates/blit-cli/tests/blit_utils.rs` covering all 9 blit-utils commands: scan (mDNS exit), list-modules (text+JSON), ls (remote/local/JSON), find (pattern/JSON/dirs-only/limit), du (text+JSON), df (text with human-readable check, JSON), rm (file/directory/module-root refusal), completions (prefix/dirs-only), profile (text+JSON). Added human-readable byte formatting to `df` output using `format_bytes()` alongside raw byte counts. **Tests**: `cargo test -p blit-cli --test blit_utils` (21 passed); `cargo test -p blit-cli --test admin_verbs` (10 passed).

**2026-04-07 15:00:00Z** - **ACTION**: Updated README, ARCHITECTURE, and documentation. Added macOS to supported platforms. Expanded blit-utils feature bullet and usage examples. Replaced `cargo run` examples with direct binary names. Expanded ARCHITECTURE.md with blit-utils file structure, filesystem capability probing section, copy operations primary/fallback table, and updated test categories. Created `docs/cli/blit-utils.1.md` man page with full synopsis, options, examples, and exit codes. Cross-referenced blit-utils(1) from blit(1) and blit-daemon(1) SEE ALSO. Updated BLIT_UTILS_PLAN.md implementation checklist. Fixed `edition = "2024"` typo to `"2021"` in `crates/blit-utils/Cargo.toml`. **Tests**: `cargo build` (clean).

**2026-03-06** - **ACTION**: Added filesystem type detection and capability probing. Created `crates/blit-core/src/fs_capability/probe.rs` with statfs-based FS type detection on macOS (`f_fstypename`) and Linux (`f_type` magic numbers). Maps 12+ filesystem types (APFS, HFS+, btrfs, XFS, ext4, ZFS, tmpfs, NFS, CIFS, NTFS, ReFS) to accurate capability flags (reflink, sparse, xattrs, sendfile, copy_file_range, block clone). Results cached by device ID via `cached_probe()`. 7 tests. Added `filesystem_type: Option<String>` and `reflink: bool` fields to `Capabilities` struct. Updated platform defaults in macos/unix/windows modules.

**2026-03-06** - **ACTION**: Added schema versioning to performance history. `CURRENT_SCHEMA_VERSION = 1` constant with version history docs. Added `schema_version: u32` field to `PerformanceRecord` with `#[serde(default)]`. `migrate_record()` stamps current version on load. `migrate_history_file()` rewrites JSONL in-place. 7 unit tests covering v0→v1 deserialization, migration, invalid line skipping, limit behavior.

**2026-03-06** - **ACTION**: Added adaptive predictor regression test suite. 9 tests: `default_coefficients_produce_positive_prediction`, `coefficients_never_go_negative` (10K observations), `predictions_converge_toward_observations` (500 iterations, <15% error), `prediction_improves_with_observations`, `profiles_are_isolated`, `save_load_round_trip`, `predict_planner_ms_returns_none_for_unseen_profile`, `predict_planner_ms_returns_value_after_observation`, `prediction_scales_with_file_count`. Used small feature values to avoid gradient explosion with lr=0.0005.

**2026-03-06** - **ACTION**: Added 16 integration tests. `crates/blit-cli/tests/admin_verbs.rs`: ListModules via discovery, CompletePath via blit-utils, subdirectory listing, find with pattern, recursive rm. `crates/blit-cli/tests/remote_transfer_edges.rs` (new): nested push/pull, copy-vs-mirror semantics, tar shard batching (50 files), empty directory, empty module pull.

**2026-03-06** - **FIX**: Fixed remote-to-remote transfers delivering zero-content files. `RemoteFileStream::poll_read` returned EOF on non-data messages (ManifestBatch) instead of skipping them. One-line fix: changed `_` catch-all from `Poll::Ready(Ok(()))` to recursive `self.poll_read(cx, buf)` in `crates/blit-core/src/remote/pull.rs`.

**2025-01-29 12:00:00Z** - **ACTION**: Added resume integration tests (`remote_resume.rs`) covering partial file resume, identical file optimization, and gRPC fallback path. Security hardening: MAX_BLOCK_SIZE (64 MiB) enforced on both client and server to prevent OOM attacks from malicious block_size values. Export added to `copy/mod.rs`. Updated TODO.md to mark P0 parity tests and refactoring items as complete. **Tests**: `cargo test --workspace` (64 passed).

**2025-01-29 10:00:00Z** - **FIX**: Addressed Gemini code review findings for remote block-level resume. (1) Fixed critical memory vulnerability: `compute_block_hashes` in `pull.rs` and both `stream_via_data_plane_resume`/`stream_via_block_resume_grpc` in `pull_sync.rs` now stream files in block-sized chunks instead of loading entire files into memory. (2) Fixed stop-and-wait protocol in data plane path: `stream_via_data_plane_resume` now pipelines all block hash requests upfront (Phase 1), collects all responses (Phase 2), then processes files (Phase 3) - eliminates per-file RTT penalty. gRPC fallback keeps simpler stop-and-wait since it's for diagnostic use only. **Tests**: `cargo build --package blit-daemon --package blit-cli` (passed).

**2025-01-28 16:00:00Z** - **ACTION**: Implemented remote block-level resume for TCP data plane (primary path). Added `DATA_PLANE_RECORD_BLOCK` and `DATA_PLANE_RECORD_BLOCK_COMPLETE` record types to data plane protocol. Server-side: `stream_via_data_plane_resume` uses gRPC for block hash exchange then TCP data plane for block transfer - combines best of both worlds. Client-side: `handle_block_record` and `handle_block_complete_record` in data plane receiver handle seek+write for in-place block updates. Remote resume now works with default `--resume` flag (no `--force-grpc` needed). **Tests**: `cargo test -p blit-core` (63 passed).

**2025-01-28 15:00:00Z** - **ACTION**: Implemented remote block-level resume (gRPC fallback path). Extended protocol with `BlockHashRequest`, `BlockHashList`, `BlockTransfer`, and `BlockTransferComplete` messages. Server-side: `stream_via_block_resume_grpc` in `pull_sync.rs` identifies Modified files (resume-eligible), requests block hashes from client, compares Blake3 hashes, sends only differing blocks via gRPC. Client-side: `compute_block_hashes` helper, handlers for all block messages with seek+write for in-place block updates. **Tests**: `cargo test -p blit-core` (63 passed).

**2025-01-28 14:00:00Z** - **ACTION**: Changed `resume_copy_file` to use Blake3 hash comparison instead of raw byte comparison. This unifies the approach for local and remote transfers - same logic, just different transport for hash exchange. For remote (TODO), hashes can be sent over the wire (32 bytes) instead of full blocks (1 MiB). Slight CPU overhead for local (Blake3 ~3 GB/s) but eliminates code bloat from having two separate comparison mechanisms. **Tests**: `cargo test -p blit-core resume` (5 passed).

**2025-01-28 13:00:00Z** - **ACTION**: Integrated resumable transfers into the transfer flow. Added `resume: bool` field to `CopyConfig` and `LocalMirrorOptions`. Modified `local_worker.rs` to use `resume_copy_file` when resume mode is enabled - this calls the block-level comparison function that skips matching blocks and only transfers differences. Added `--resume` CLI flag to `TransferArgs`. Full integration: CLI flag → orchestrator options → worker config → resume_copy_file. **Tests**: `cargo test -p blit-core resume` (5 passed); `cargo test -p blit-cli` (all passed).

**2025-01-28 12:00:00Z** - **ACTION**: Implemented resumable file copy with block-level comparison. Added `resume_copy_file` function in `blit-core/src/copy/file_copy/resume.rs` that compares source and destination block-by-block (default 1 MiB blocks), skips matching blocks, overwrites mismatches, appends remaining bytes, and truncates if dest is longer. This enables resuming interrupted transfers and efficiently updating files with localized changes. 5 unit tests covering: new file, partial resume, identical file (all blocks skipped), truncation, and corrupted block repair. Integration with CLI `--resume` flag pending. **Tests**: `cargo test -p blit-core resume` (5 passed).

**2025-01-28 11:00:00Z** - **ACTION**: Reverted temp file approach for atomic copies after design review. Temp files create orphan cleanup complexity - if process crashes, orphans need manual or automatic cleanup. Instead, implementing resumable transfers: partial files are left with real filename, and next run with `--resume` can verify/resume from where it stopped using block-level checksums. This aligns with RELIABLE principle (correctness > speed) while keeping SIMPLE (no user intervention for cleanup).

**2025-01-28 10:00:00Z** - **ACTION**: Added buffer pool for daemon tar shard reception. Extended `BufferPool` with `return_vec()` method for direct buffer return. Modified `TarShardExecutor` in `blit-daemon/src/service/push/data_plane.rs` to use pooled buffers (4 MiB size, pool of 8). Buffers acquired before shard processing, returned after extraction. Reduces allocation pressure for streaming tar transfers. **Tests**: `cargo test -p blit-daemon apply_tar_shard_sync` (passed).

**2025-01-26 14:00:00Z** - **CLEANUP**: Eliminated all compiler warnings across workspace. Removed 17 unused imports (`Context`, `bail`, `eyre`, `tokio::fs`, `Path`, `PathBuf`, `File`, `BufReader`, `BufWriter`, `Write`, `ErrorKind`, `Component`, `RemotePullProgress`, `RemotePushProgress`, `filter_readable_headers`, `record_unreadable_entry`, `spawn_manifest_task`). Fixed one unused `mut` binding. Added `#[allow(non_snake_case)]` to Windows FFI structs that intentionally use Windows API naming conventions. Build is now warning-free. **Tests**: `cargo check --workspace` (0 warnings); `cargo test --workspace` (all pass).

**2025-01-26 13:30:00Z** - **FIX**: Fixed `blit df` failing on Windows. `filesystem_stats_for_path` used `fs::canonicalize` which returns extended-length paths with `\\?\` prefix on Windows (e.g., `\\?\D:\temp\...`), but `sysinfo::Disks` returns mount points without this prefix (e.g., `D:\`). The `Path::starts_with` comparison failed because the prefixes don't match. Fix: strip `\\?\` prefix on Windows before comparing. **Tests**: `cargo test -p blit-cli --test admin_verbs` (all 5 pass).

**2025-01-26 13:00:00Z** - **ACTION**: Removed all backwards-compatibility code from `DataPlaneSession` per "no tech debt" directive (project hasn't shipped yet). Eliminated `SessionBuffer` enum, legacy `from_stream()` signature, `connect_with_pool` wrapper, and single-buffer fallback path. Now `DataPlaneSession::from_stream` and `::connect` both require `Arc<BufferPool>` and always use double-buffered I/O. Updated callers: `MultiStreamSender::connect` passes pool directly (removed `Some()` wrapper), `pull.rs::handle_pull_stream` creates a pool sized for single-stream operation. Clean compile, all buffer pool tests pass. **Tests**: `cargo check --workspace`; `cargo test -p blit-core buffer`.

**2025-01-26 12:30:00Z** - **ACTION**: Verified parallel payload dispatch already exists via `MultiStreamSender` architecture. Multiple workers spawned as concurrent tokio tasks, each with its own `DataPlaneSession`. Round-robin batch distribution (32-512 MiB per batch) ensures all streams receive work. Combined with buffer pool and double-buffering, the data plane now has: reusable allocations, overlapped I/O, and concurrent multi-stream transfers. Work-stealing queue noted as potential future optimization over round-robin. **Tests**: `cargo check --workspace`.

**2025-01-26 12:00:00Z** - **ACTION**: Implemented async read-ahead pipeline with double-buffering in `DataPlaneSession::send_file`. When a buffer pool is available, acquires two buffers and uses `tokio::join!` to overlap disk reads with network writes. While buffer A is being written to network, buffer B is filled from disk, then roles are swapped. This hides disk latency behind network latency for improved throughput. Falls back to single-buffer path when no pool is available. **Tests**: `cargo test -p blit-core`.

**2025-01-26 11:00:00Z** - **ACTION**: Integrated `BufferPool` with `DataPlaneSession` and `MultiStreamSender`. Added `SessionBuffer` enum to support both owned and pooled buffers transparently. `DataPlaneSession::connect_with_pool` accepts optional pool parameter. `MultiStreamSender::connect` now creates a shared pool sized from orchestrator parameters (`chunk_bytes`, `stream_count`) and passes it to all stream workers. Pool memory budget calculated as `buffer_size * pool_size * 2` to allow double-buffering headroom. Buffers automatically returned to pool on session drop. **Tests**: `cargo test -p blit-core`; `cargo check --workspace`.

**2025-01-26 10:00:00Z** - **ACTION**: Implemented buffer pool for reusable allocations (25GbE performance work). Added `BufferPool` struct to `blit-core/src/buffer.rs` with rclone-inspired design: semaphore-based memory budget control, RAII `PoolBuffer` guards for automatic return, statistics tracking. Critically, the pool does NOT define hardcoded defaults—it accepts parameters from the orchestrator/auto_tune system so buffer sizes remain dynamically tuned based on runtime conditions. **Tests**: `cargo test -p blit-core pool_tests`.

**2025-01-21 13:00:00Z** - **ACTION**: Verified and documented remote↔remote transfer support. Implementation was already complete via `RemoteTransferSource` abstraction in `blit-core/src/remote/transfer/source.rs`. Removed debug print statement from `RemoteTransferSource::open_file`. Existing integration test `remote_remote.rs` covers dual-daemon server-to-server copy. Updated all integration tests (`remote_parity.rs`, `remote_pull_mirror.rs`, `remote_tcp_fallback.rs`, `remote_move.rs`, `remote_remote.rs`) to include `--yes` flag for mirror/move commands following the destructive operation prompts implementation. **Tests**: All remote integration tests now use `--yes` to skip confirmation prompts.

**2025-01-21 12:30:00Z** - **ACTION**: Documented security requirements for remote transfers. Added SECURITY sections to both `docs/cli/blit.1.md` and `docs/cli/blit-daemon.1.md` explaining that blit does not include built-in TLS encryption. Documented recommended secure deployment patterns: SSH tunnel (with example command), VPN, trusted private network, and TLS-terminating reverse proxy. Also noted that daemon binds to 0.0.0.0 by default and that there is no built-in authentication—operators must use network-level access controls.

**2025-01-21 12:00:00Z** - **ACTION**: Implemented destructive operation confirmation prompts. `blit mirror` now prompts before executing because it can delete extraneous files at the destination. `blit move` now prompts before executing because it deletes the source after transfer. Both commands accept `--yes`/`-y` to bypass the prompt (useful for scripting). `blit rm` already had this behavior. Added `confirm_destructive_operation` helper in `transfers/mod.rs`. Updated CLI `TransferArgs` with new `yes` field. Tests updated to use `yes: true` to skip prompts. **Tests**: `cargo check -p blit-cli`.

**2025-11-19 16:55:00Z** - **ACTION**: Added integration tests for remote transfer parity. `crates/blit-cli/tests/remote_parity.rs` verifies that both push and pull operations correctly negotiate the TCP data plane (checking for absence of fallback tag) and respect `--force-grpc` (checking for fallback tag). Fixed a bug in `RemotePullClient` where leading `./` components in file paths caused the mirror purge logic to delete valid files. **Tests**: `cargo test -p blit-cli --test remote_parity`; `cargo test -p blit-cli --test remote_tcp_fallback`.

**2025-11-15 01:25:00Z** - **FIX**: Hardened the push client so it only streams files the daemon explicitly requested. We now track the requested set coming from `FilesToUpload`, prune unrequested files out of payload batches, and log/skipping anything the daemon didn’t ask for. This prevents the upload queue from desyncing (which caused the recent “unexpected file entry … connection closed” failures) while we continue investigating the underlying need-list mismatch. **Tests**: `cargo fmt`; `cargo build -p blit-core`.

**2025-11-15 01:05:00Z** - **ACTION**: Cranked up the auto-tuned stream heuristics so big pushes/pulls negotiate more TCP workers. `determine_remote_tuning` now scales `initial_streams`/`max_streams` up to 12/16 based on byte estimates, and both push + pull daemons request up to 16 streams for multi-GiB manifests. This should raise aggregate throughput immediately (each stream was ~0.4 Gbps in the latest logs) while we work on per-file sharding next. **Tests**: `cargo fmt`; `cargo build -p blit-core`; `cargo build -p blit-daemon`.

**2025-11-15 00:20:00Z** - **ACTION**: Reworked the remote push data-plane scheduler so all negotiated TCP streams stay busy. `MultiStreamSender` now estimates payload sizes, slices plans into 32–512 MiB batches, and round-robins them to each worker instead of dumping an entire manifest into stream 0. This eliminates the ~450 Mbps ceiling we were seeing on multi-terabyte mirrors and keeps per-stream throughput near the auto-tuned target. TODO updated (Phase 3 parity) with the completed batching work plus the follow-on remote↔remote requirement. **Tests**: `cargo fmt`; `cargo build -p blit-core`; `cargo build -p blit-daemon`.

**2025-11-10 10:05:00Z** - **ACTION**: Hardened remote push against unreadable files. Manifest enumeration now preflights file readability, logs inline warnings for permission/not-found cases, and defers failure until after the transfer so throughput/progress still materialize. After completion we surface a summarized error listing skipped paths to keep context compact. **Tests**: `cargo build -p blit-core`; `cargo build -p blit-daemon`.

**2025-11-10 11:10:00Z** - **FIX**: Added an async readability check before each manifest header is sent so unreadable files never reach the daemon need list. Pushes now start streaming even when protected files exist because they’re excluded up front, and the CLI reports them immediately. **Tests**: `cargo build -p blit-core`.

**2025-11-10 12:20:00Z** - **ACTION**: Shortened manifest→payload latency and hardened permission handling for remote pushes. The daemon now flushes `FilesToUpload` batches immediately during enumeration, so first payloads launch within milliseconds even on 300 k+ file manifests. On the client, unreadable files are logged in red and filtered out before any payload planning, keeping the negotiated TCP path active instead of falling back to slow gRPC streams. Remaining gap: data-plane concurrency still single-threaded, so next step is implementing multiplexed streams driven by auto-tuned worker counts to hit 10 GbE. **Tests**: `cargo build -p blit-core`; `cargo build -p blit-daemon`.

**2025-11-10 09:05:00Z** - **PROGRESS**: Wired the remote auto-tuning path through both data-plane and fallback streaming. `RemotePushClient` now feeds the tuned chunk size into every transfer mode and passes the tuned stream counts to the TCP sender + control-plane payload builder so tar-shard construction scales with observed bandwidth. The daemon pull data-plane adopts the same parameters, ensuring both push and pull honor the shared heuristics. Updated TODO/plan docs accordingly. **Tests**: `cargo build -p blit-core`; `cargo build -p blit-daemon`.

**2025-11-10 05:05:00Z** - **PROGRESS**: Started the remote-transfer parity refactor by extracting the shared transport pieces. Added `remote::transfer::{payload,progress,data_plane}` (tar-shard planner, progress channel, TCP sender) and rewired the push client/server to import them, so both future push + pull changes share one code path. TODO/P0 updated; next up is extending `PullChunk`/daemon pull to negotiate the TCP data plane and plugging the shared modules plus auto-tune into `RemotePullClient`. (Build still pending—`cargo build -p blit-core` was interrupted early; rerun after the next edits.)  

**2025-11-10 04:20:00Z** - **ACTION**: Implemented the Windows clone-only metadata fast path. `copy_file` now returns a `FileCopyOutcome` so callers know when `FSCTL_DUPLICATE_EXTENTS_TO_FILE` succeeded, skips `preserve_metadata`/mtime sync when the block clone already handled attributes, and has the local worker avoid redundant timestamp IOCTLs in that case. This removes the `BLIT_SKIP_METADATA_ON_CLONE` prototype flag, reduces the `NtSetInformationFile` overhead highlighted in `logs/windows/refs_clone_profile_20251026T022401Z.etl`, and checks off the Phase 3 TODO. Operators can reuse the existing PowerShell bench + ETW trace to compare against robocopy. **Tests**: `cargo fmt`; `cargo check -p blit-core` (full `cargo test -p blit-core` build timed out in the sandbox twice before `cargo check` completed).

**2025-11-07 22:10:00Z** - **PLAN**: Captured the remote transfer parity workflow (`docs/plan/REMOTE_TRANSFER_PARITY.md`) and promoted it to a P0 in TODO. The plan folds in missing v1 capabilities: shared `remote::transfer` modules, auto-tuned chunk/stream sizing, larger data-plane buffers with zero-copy, and hybrid transport for pull. Next up is implementing the shared modules + scheduler wiring, extending `PullChunk` with negotiation/summary, then rewriting the daemon/client pull paths on top of the shared transport before adding parity integration tests.  

**2025-11-07 21:45:00Z** - **NOTE**: Remote pull parity gap. Field run (`./blit-cli mirror -p -v 10.1.10.180://Steam/ /mnt/...`) showed no progress output and sustained ~28 Mbps because pulls are still pure gRPC streams (64 KiB chunks) with no hybrid transport. Running the reverse direction surfaces output but tops out around 400 Mbps. Added a P0 plan/TODO item to mandate absolute throughput/feature parity across all src/dst combinations; remote pull must reuse the streaming/data-plane stack and emit `-p`/`-v` progress before GA.  

**2025-11-07 18:30:00Z** - **ACTION**: Hardened remote push memory usage and scheduling. `drain_pending_headers` now removes dispatched headers (so the manifest lookup stays bounded), the push loop no longer uses a biased `tokio::select!` and drains the manifest channel after every daemon response, and `RemotePushReport` only tracks the requested-file count to avoid cloning every path. Added a regression + ignored stress test (`drain_pending_headers_handles_one_million_entries`) so we can capture RSS/timing data for 1 M-entry manifests on demand, and documented that operators are expected to secure the data plane via trusted networks/SSH tunnels instead of in-proc TLS. **Tests**: `cargo fmt`; `cargo check`; `cargo test -p blit-core drain_pending_headers_removes_matched_entries`.

**2025-10-27 23:10:00Z** - **ACTION**: Split the daemon binary into manageable modules. Created `runtime.rs` for CLI parsing + config loader and `service.rs` for gRPC/data-plane handlers (including purge helpers and tests), leaving `main.rs` to bootstrap config, mDNS, and the tonic server. Updated TODO/phase docs to mark the refactor complete. **Tests**: `cargo fmt`; `cargo check -p blit-daemon`.

**2025-10-27 23:35:00Z** - **ACTION**: Refactored `blit-cli` entrypoint into modular components. Moved Clap definitions to `cli.rs`, context handling to `context.rs`, diagnostics reporting to `diagnostics.rs`, discovery/list verbs into `scan.rs`/`list.rs`, and transfer logic/tests into `transfers.rs`; `main.rs` now delegates to these modules. Updated TODO + workflow/state docs. **Tests**: `cargo fmt`; `cargo check -p blit-cli`.

**2025-10-27 23:52:00Z** - **ACTION**: Broke up `blit-utils/src/main.rs` (~840 LOC) into dedicated modules. Added `cli.rs` for Clap definitions, `util.rs` for shared helpers, and verb-specific modules (`scan`, `list_modules`, `ls`, `find`, `du`, `df`, `completions`, `rm`, `profile`). `main.rs` is now a thin dispatcher that wires command enums to the async handlers. Updated TODO/workflow/state docs accordingly. **Tests**: `cargo fmt`; `cargo check -p blit-utils`.

**2025-10-27 23:59:00Z** - **ACTION**: Refactored `blit-core::copy` to keep files under 500 LOC. Moved comparison helpers to `compare.rs`, copy execution to `file_copy.rs`, parallel orchestration to `parallel.rs`, and stats into `stats.rs`; `mod.rs` now just re-exports and the existing `windows.rs` remains for platform hooks. Ensured high-level functions (`copy_file`, `chunked_copy_file`, `mmap_copy_file`, `parallel_copy_files`, `file_needs_copy*`) retain their public API. Updated TODO/state docs. **Tests**: `cargo fmt`; `cargo check -p blit-core`.

**2025-10-28 22:05:00Z** - **ACTION**: Parallelised daemon tar-shard handling so streaming mirrors no longer block while unpacking. Introduced `TarShardExecutor` (bounded `JoinSet` with semaphore) that decodes shards on `spawn_blocking`, accumulates stats as workers finish, and reuses the same path for gRPC fallback. Throughput validation on `skippy`/`mycroft` still pending once new binaries are deployed. **Tests**: `cargo fmt`; `cargo check -p blit-daemon`.

**2025-10-28 23:20:00Z** - **ACTION**: Split `blit-daemon` service logic into modules (`core`, `push`, `pull`, `admin`, `util`, plus a `push::data_plane` helper). `main.rs` still instantiates `BlitService`, but each file now stays well under 400 LOC and the TCP/GPRC handling is easier to navigate. **Tests**: `cargo fmt`; `cargo check -p blit-daemon`.

**2025-10-28 23:55:00Z** - **ACTION**: Began throughput push tuning. CLI now pipelines tar-shard preparation (data plane shares the same buffered stream as control-plane fallback) and the daemon batches need-list flushes instead of emitting single-file requests. Added verbose `[data-plane] …` logging around the daemon’s TCP handler to catch the “Connection reset by peer” root cause (currently seeing `upload_tx send failed` when the channel closes). **Tests**: `cargo fmt`; `cargo check -p blit-core`; `cargo check -p blit-daemon`.

**2025-10-28 00:10:00Z** - **ACTION**: Modularised the change journal. Split the single file into `change_journal/{mod,types,snapshot,tracker,util}.rs`, isolating platform capture logic per OS and keeping `ChangeTracker` implementation focused on persistence + probing. Orchestrator now consumes the same re-exported API. **Tests**: `cargo fmt`; `cargo check -p blit-core`.

**2025-10-28 00:18:00Z** - **ACTION**: Refactored `transfer_facade` into directory modules. Added `types.rs` for plan structures/streams, `aggregator.rs` for task batching + tests, and `planner.rs` housing the `TransferFacade` impl; `mod.rs` now re-exports the public surface. **Tests**: `cargo fmt`; `cargo check -p blit-core`.

**2025-10-28 00:28:00Z** - **ACTION**: Broke `remote::push` client into smaller modules. `client/mod.rs` now focuses on the async push loop, with `types.rs` defining public structs and `helpers.rs` handling manifest/response tasks and payload utilities. Control-plane helpers were deduplicated; behaviour unchanged. **Tests**: `cargo fmt`; `cargo check -p blit-core`.

**2025-10-26 21:56:50Z** - **ACTION**: Reworked remote push streaming to honour the 1 s heartbeat rule. The CLI prints an immediate startup banner, logs manifest progress every second, and the daemon now flushes need-list batches as soon as they’re discovered—starting the TCP listener (or gRPC fallback) on the very first batch so transfers begin before manifest enumeration completes. **Tests**: `cargo test -p blit-core`; `cargo test -p blit-daemon`; `cargo test -p blit-cli --test remote_tcp_fallback`.

**2025-10-27 21:44:00Z** - **HOTFIX**: Bumped need-list batching limits (12 MiB / 16 k entries) to keep the daemon’s upload queue from saturating during home-directory pushes. This is a stopgap until the fully-streaming manifest work lands. **Tests**: `cargo fmt`.

**2025-10-26 23:40:00Z** - **ACTION**: Implemented streaming manifest negotiation for remote pushes. CLI now enumerates files via a bounded channel (64 entries) feeding gRPC stream, while the daemon batches need-list responses on the fly and applies back-pressure. Control-plane fallback streams file data in 1 MiB chunks, eliminating whole-file buffering. Updated TODO/workflow docs to mark the blocker resolved and added a follow-up stress-test task. **Tests**: `cargo fmt`; `cargo check`; `cargo test`.

**2025-10-26 02:33:00Z** - **ACTION**: Coordinated with WingPT on ReFS clone validation. Added info-level logging when `FSCTL_DUPLICATE_EXTENTS_TO_FILE` succeeds and captured cloned vs fallback runs (`logs/windows/bench_local_windows_4gb_clone_{20251026T015438Z,20251026T020337Z}.log`). ETW trace `logs/windows/refs_clone_profile_20251026T022401Z.etl` shows the clone IOCTL completes in ~15 ms while metadata/ACL updates dominate remaining runtime (~0.28 s vs robocopy 0.17 s). Benchmark harnesses now honour `BLIT_BENCH_ROOT` on all platforms. Added TODO to prototype a metadata-lite clone mode for further tuning. **Tests**: `cargo fmt`; `cargo check`; `cargo test -p blit-core`.

**2025-10-28 01:15:00Z** - **ACTION**: Reworked remote push streaming so need-list batches flush immediately. `RemotePushClient` now biases response handling, streams gRPC fallback payloads as soon as headers arrive, and only emits `UploadComplete` when queues drain, guaranteeing data movement within one second even when the daemon negotiates fallback. Added helper to finish lingering data-plane sessions after the control loop exits. **Tests**: `cargo fmt -p blit-core`; `cargo check -p blit-core`; `timeout 120s cargo test -p blit-cli remote_tcp_fallback -- --nocapture`.

**2025-10-26 20:23:35Z** - **ACTION**: Manifest/need-list streaming flagged as blocker. Paused development until remote push can negotiate arbitrarily large payloads without exhausting memory. Added TODO (`manifest_streaming_plan`) and updated workflows to prioritise CLI/daemon streaming refactor plus back-pressure handling.

**2025-10-25 03:15:00Z** - **ACTION**: Added Linux metadata snapshot backend for the change journal. `LinuxSnapshot` records device/inode/ctime (+mtime) and enables the planner to skip no-op runs on ext-based filesystems. Added `scripts/linux/run-journal-fastpath.sh` and captured validation run (`logs/linux/journal-fastpath-20251025T031500Z.log`, zero-change mirror in ~3 ms). **Tests**: `cargo fmt`; `cargo check -p blit-core --target x86_64-unknown-linux-gnu`; `bash scripts/linux/run-journal-fastpath.sh`.

**2025-10-25 02:20:00Z** - **ACTION**: Implemented macOS FSEvents snapshot backend for the change journal. `MacSnapshot` now records device FSID, current FSEvents ID, and root mtime; orchestrator refreshes checkpoints so zero-change runs can skip planning on macOS once verified. Added `scripts/macos/run-journal-fastpath.sh` for validation and updated TODO/workflows. **Tests**: `cargo fmt`; `cargo check -p blit-core --target x86_64-unknown-linux-gnu`.

**2025-10-25 02:16:11Z** - **ACTION**: Finalised the Windows USN fast-path integration. Relaxed snapshot comparison to trust matching volume/journal/USN, added checkpoint re-probe before persisting, and confirmed zero-change NTFS mirrors skip planning (28 ms second run via `scripts/windows/run-journal-fastpath.ps1`). Updated TODO/workflows accordingly. **Tests**: `cargo fmt`; `cargo check -p blit-core --target x86_64-unknown-linux-gnu`; `pwsh scripts/windows/run-journal-fastpath.ps1 -Volume NTFS`.

**2025-10-24 03:05:00Z** - **ACTION**: Added filesystem journal fast-path for local mirrors on Windows (USN checkpoints) with persistent cache + CLI integration. The orchestrator now short-circuits planning when the USN journal reports no changes, updating the cache after each run; macOS FSEvents and Linux fanotify remain TODO. Introduced `change_journal.rs` with serde-backed storage and unit tests. **Tests**: `cargo check -p blit-core`; `cargo test -p blit-core change_journal`.

**2025-10-24 01:20:00Z** - **ACTION**: Landed Phase 3 admin RPCs and utilities. Daemon now serves `Find`, `DiskUsage`, `FilesystemStats`, and an expanded `CompletePath` RPC; `blit-utils find/du/df/completions` stream results (with `--json`/filters) off those endpoints. Proto regenerated, CLI/plan/TODO docs updated. **Tests**: `cargo check`; `cargo test -p blit-daemon`; `cargo test -p blit-cli`; `cargo test -p blit-utils`.

**2025-10-23 23:45:00Z** - **ACTION**: Wired up mDNS advertising + discovery. `blit-daemon` now advertises `_blit._tcp.local.` unless `--no-mdns`, and both `blit scan` and `blit-utils scan` list discovered daemons (shared helper in `blit_core::mdns`). Docs/manpages updated accordingly. **Tests**: `cargo check`; `cargo test -p blit-daemon`; `cargo test -p blit-cli`; `cargo test -p blit-utils`.

**2025-10-23 23:05:00Z** - **ACTION**: Remote mirror pushes now purge stale remote files by reusing the daemon's Purge helper; `PushSummary` reports `entries_deleted` and the CLI surfaces the deletion count. Added server-side purge helpers, mirrored fallback handling, and coverage for extraneous deletions. **Tests**: `cargo build -p blit-core`; `cargo check`; `cargo test -p blit-daemon`; `cargo test -p blit-cli`; `cargo test -p blit-utils`.

**2025-10-23 22:05:00Z** - **ACTION**: Implemented daemon Purge RPC and `blit-utils rm` (interactive confirmation plus `--yes` bypass). Added purge unit tests, refreshed Phase 3 workflow notes, and updated TODOs. **Tests**: `cargo check`; `cargo test -p blit-daemon`; `cargo test -p blit-utils`.

**2025-10-21 01:45:00Z** - **ACTION**: Reworked small-file heuristics. `TransferFacade` now samples workloads and flushes tar shards once batches hit ~8 MiB or ≈1 k entries (auto-detecting ≤64 KiB averages), with leftover bundles forced through tar in small-file mode. `transfer_plan::build_plan` mirrors the new thresholds, and regression tests cover both the shard path and the raw-bundle fallback. `cargo test -p blit-core transfer_facade` passes.
**2025-10-20 22:30:00Z** - **RESULT**: Phase 2.5 benchmarks refreshed with isolated target dirs. Linux (`logs/linux/bench_local_size{000,512,2048}_20251020T22****Z/`) shows blit ahead of rsync on 512 MiB (3.85 s vs 6.61 s) and 2 GiB (9.37 s vs 10.19 s); macOS (`logs/macos/bench-local-mirror-size{0,512,2048}-20251020T22****Z/`) has blit ≈3.1× faster than rsync for 512 MiB/2 GiB while rsync still wins the tiny-tree case; Windows (`logs/wingpt/bench-{0mb,512mb,2048mb}-20251020.log`) confirms blit outperforming robocopy by ~7 % (512 MiB) and ~2 % (2 GiB). Phase 2.5 gate criteria satisfied.
**2025-10-20 22:55:00Z** - **ACTION**: Implemented daemon config loader + CLI overrides. `blit-daemon` now reads `/etc/blit/config.toml` (or `--config`), merges module definitions and default root, respects `--bind`, `--port`, `--root`, `--no-mdns`, `--mdns-name`, and warns when falling back to the working directory. Tests updated to use the new constructor. `cargo test --all-targets` remains green.
**2025-10-21 00:45:00Z** - **RESULT**: Captured remaining Phase 2.5 workloads on Linux using the enhanced bench harness (datasets staged under `/tmp` via `SOURCE_DIR`/`DEST_DIR`). 100 k × 4 KiB small-file run (`logs/linux/bench_smallfiles_20251021T001245Z/bench.log`) shows blit 14.29 s vs rsync 9.97 s (70 % of comparator). Mixed workload (512 MiB payload + 50 k × 2 KiB files, `logs/linux/bench_mixed_20251021T002511Z/bench.log`) yields blit 7.66 s vs rsync 6.77 s (88 %). Incremental scenario (`logs/linux/bench_incremental_base_20251021T003800Z/bench.log`, `..._update_20251021T003950Z/bench.log`) has blit 1.98 s vs rsync 1.17 s on the mutation pass. Phase 2.5 remains NO-GO until small/mixed/incremental gaps are solved.
**2025-10-21 01:10:00Z** - **ACTION**: Updated benchmark harnesses to use fair comparator settings. Linux script now defaults to `rsync -a --delete --whole-file --inplace --no-compress --human-readable --stats (--timeout configurable)` and logs the exact args; Windows PowerShell wrapper honours `ROBOCOPY_FLAGS` with `/MIR /COPYALL /FFT /R:1 /W:1 /NDL /NFL /NJH /NJS /NP` by default. All artefacts generated under `/tmp` via `SOURCE_DIR`/`DEST_DIR`; previous `blit_*` directories cleaned up before re-running the suites. Re-ran Linux suites: small-file (`logs/linux/bench_smallfiles_20251021T012247Z/bench.log`) now blit 4.43 s vs tuned rsync 7.72 s; mixed workload (`logs/linux/bench_mixed_20251021T012509Z/bench.log`) blit 2.59 s vs rsync 5.80 s. Incremental sample reduced to 128 MiB + 10 k files (`logs/linux/bench_incremental_base_20251021T012748Z/.log`, `..._update_20251021T012818Z/.log`) with blit 1.15 s vs rsync 0.68 s on the mutation pass; further tuning needed.
**2025-10-21 02:45:00Z** - **RESULT**: Cross-platform small-file reruns validate the tar-shard heuristics. Linux (`logs/linux/bench_smallfiles_tar_20251021T024313Z/bench.log`) averages 2.90 s (≈194 MiB/s) vs tuned rsync 8.56 s; macOS (`logs/macos/bench_smallfiles_tar_20251021T021418Z/bench.log`) averages 10.53 s vs rsync 11.62 s (~9 % lead). Windows (`logs/wingpt/bench-100k-smallfiles-20251021.log`) now averages 60.63 s vs robocopy 218.48 s (3.6× faster) after the planner fix.
**2025-10-21 03:00:00Z** - **ACTION**: Added planner task instrumentation. `TransferFacade` now tracks tar/raw/large task counts and bytes, exposing them via `LocalPlanFinal` → `LocalMirrorSummary`. Performance history records these stats and the orchestrator seeds small/medium shard thresholds from the most recent runs before planning. Updated PowerShell harness earlier still applies; `cargo test -p blit-core` remains clean.
**2025-10-22 01:55:00Z** - **RESULT**: Phase 2.5 mixed/incremental workloads now clear the GO bar. Linux mixed suite (`logs/linux/bench_mixed_tar_20251022T015203Z/bench.log`) averages 2.24 s vs tuned rsync 6.95 s; incremental baseline (`logs/linux/bench_incremental_base_tar_20251022T015347Z/bench.log`) clocks 0.86 s vs 1.32 s, and the mutation pass (`..._update_20251022T015347Z/bench.log`) averages 0.61 s vs 1.23 s. macOS mirrors these gains (`logs/macos/bench_mixed_tar_20251022T014611Z/bench.log`, `bench_incremental_{base,update}_tar_20251022T0148*.log`). Windows harness confirmation from WingPT captured in `logs/wingpt/bench_mixed_incremental_20251021T230000Z/bench.log` (mixed 31.26 s vs robocopy 110.51 s; incremental baseline 7.10 s vs 20.72 s; mutation 6.45 s vs 6.94 s) with small-file comparator still pending.
**2025-10-22 02:28:00Z** - **ACTION**: CLI verbose summary now reports planner mix (tar shards, bundles, large tasks) using the new instrumentation in `LocalMirrorSummary`, helping debug Phase 2 heuristics when `--verbose` is set. `cargo check -p blit-cli` passes.
**2025-10-23 04:15:10Z** - **ACTION**: CLI remote surface aligned with plan v6. `blit copy|mirror|move` now accept `host:/module/path` endpoints (module sub-paths supported), reusing the existing push/pull transports; redundant push/pull mentions removed from docs. Daemon push handling honours per-transfer destination paths, and `blit list` now interacts with the daemon (`ListModules` / `List`) or falls back to local listings. `cargo clippy` and `cargo test -p blit-core` run clean.
**2025-10-23 22:29:26Z** - **ACTION**: Seeded `blit-utils` with initial admin tooling. `blit-utils list-modules` and `blit-utils ls/list` call the new daemon RPCs (with `--json` support), `blit-utils profile` surfaces local perf history, and other verbs return clear placeholders pending RPC work. Added detailed `docs/plan/BLIT_UTILS_PLAN.md`, expanded Phase 3/4 workflows (predictor tests, schema migrations, hardening).
**2025-10-25 20:01:22Z** - **ACTION**: Added integration coverage for the hybrid transport fallback. New test (`crates/blit-cli/tests/remote_tcp_fallback.rs`) spins up `blit-daemon --force-grpc-data`, runs `blit mirror --force-grpc` against `server:/module/`, and asserts the run completes with gRPC fallback messaging and remote files present. Helper scripts now use `--config-dir` exclusively. **Tests**: `cargo build --bin blit-daemon --target x86_64-unknown-linux-gnu`; `cargo test -p blit-cli --test remote_tcp_fallback --target x86_64-unknown-linux-gnu`.

**2025-10-25 22:54:02Z** - **RESULT**: Verified remote fallback on skippy/mycroft. Normal runs negotiated the TCP data plane; forcing `--force-grpc` logged `[gRPC fallback]` and mirrored successfully (`logs/remote/skippy.internal_force_20251025T225340Z.log`, `logs/remote/mycroft.internal_force_20251025T225340Z.log`).
**2025-10-26 00:09:53Z** - **RESULT**: Captured refreshed large-file benchmarks across all platforms to close out Phase 2.5 evidence. Linux 1 GiB run (`logs/linux/bench_local_linux_20251025T230101Z.log`) shows blit 1.427 s vs rsync 3.206 s (~225 %). macOS 1 GiB/4 GiB runs (`logs/macos/bench_local_20251025T231137Z/bench.log`, `logs/macos/bench_local_20251025T235415Z/bench.log`) landed at 0.712 s vs 2.427 s and 2.823 s vs 9.721 s respectively. Windows NTFS 1 GiB (`logs/windows/bench_local_windows_20251025T233442Z.log`) is within ~7 % of robocopy (1.619 s vs 1.516 s), while the ReFS 4 GiB case (`logs/windows/bench_local_windows_4gb_20251025T235715Z.log`) still trails (0.374 s vs 0.155 s, ~41 %). Added a TrueNAS/ZFS datapoint (`logs/truenas/bench_local_zfs_20251026T004021Z.log`): blit averaged 0.879 s vs rsync 18.309 s on the same 4 GiB workload. TODO updated to track the remaining ReFS delta; plan/workflow docs reference the new numbers. **Tests/Commands**: `SOURCE_DIR=/tmp/blit_linux_bench/src DEST_DIR=/tmp/blit_linux_bench/dest SIZE_MB=1024 RUNS=5 WARMUP=1 KEEP_BENCH_DIR=1 scripts/bench_local_mirror.sh`; `SIZE_MB=1024 RUNS=5 WARMUP=1 KEEP_BENCH_DIR=1 ./scripts/bench_local_mirror_macos.sh`; `SIZE_MB=4096 RUNS=5 WARMUP=1 KEEP_BENCH_DIR=1 ./scripts/bench_local_mirror_macos.sh` (macGPT); `pwsh scripts/windows/bench-local-mirror.ps1 -SizeMB 1024 -Runs 5 -Warmup 1`; `pwsh scripts/windows/bench-local-mirror.ps1 -SizeMB 4096 -Runs 5 -Warmup 1` (WingPT); `BLIT_BIN=./target/release/blit-cli BENCH_ROOT=/mnt/specific-pool/home/stuff/backup/temp SOURCE_DIR=/mnt/specific-pool/home/stuff/backup/temp/src DEST_DIR=/mnt/specific-pool/home/stuff/backup/temp/dest SIZE_MB=4096 RUNS=5 WARMUP=1 KEEP_BENCH_DIR=1 scripts/bench_local_mirror.sh` (TrueNAS).
**2025-10-26 02:06:31Z** - **RESULT**: Re-ran 4 GiB ReFS mirror with the new block-clone path on a ReFS-backed temp root (`TEMP/TMP=D:\blit_bench`). `logs/windows/bench_local_windows_4gb_clone_20251026T020337Z.log` shows blit averaging 0.590 s (best 0.385 s) vs robocopy 0.165 s (best 0.149 s)—clone is active but robocopy still leads by ~3×. Planner integration confirmed via per-file raw tasks. TODO/workflow updated; future harness work needed to avoid silently running on NTFS when env isn’t set. **Commands**: `pwsh scripts/windows/bench-local-mirror.ps1 -SizeMB 4096 -Runs 5 -Warmup 1` with `TEMP/TMP` pointed at ReFS.

**2025-10-26 18:45:00Z** - **ACTION**: Windows bench harness now logs tool stderr, blit-cli emits a console line on successful `FSCTL_DUPLICATE_EXTENTS_TO_FILE`, and a prototype `BLIT_SKIP_METADATA_ON_CLONE` flag makes metadata preservation optional. Without `SeManageVolumePrivilege` the clone ioctl still falls back (bench averages ~0.68 s vs robocopy 0.19 s), but once elevated we can compare the clone-only numbers. Logged runs: `logs/windows/bench_local_windows_4gb_clone_20251026T183749Z.log` (baseline) and `bench_local_windows_4gb_clone_nometa_20251026T184100Z.log`.
**2025-10-27 04:15:00Z** - **ACTION**: Completed remote tar-shard batching across TCP and gRPC paths. Extended `blit.proto` with `TarShard{Header,Chunk,Complete}` messages, taught the CLI to reuse the transfer planner for shard payloads, and upgraded both the TCP data plane and the daemon fallback path to stream shards instead of per-file payloads. Added a timeout-aware `remote_tcp_fallback` integration test (with a guard to reap the daemon) and a `tar_shard_preserves_relative_paths` unit test. Updated TODO / workflow docs to mark the milestone and captured follow-up stress-test work. **Tests**: `cargo fmt`; `cargo test -p blit-core`; `cargo test -p blit-cli`; `cargo check`.

**2025-10-27 05:10:00Z** - **ACTION**: Catalogued source files exceeding the 700-line AI maintainability limit (daemon/CLI/utils mains, copy/orchestrator helpers, change journal, transfer facade, push client). Added TODO items to split `blit-daemon`, `blit-cli`, `blit-utils`, and core helpers into submodules. Next step: refactor `crates/blit-daemon/src/main.rs` into service/data-plane/admin modules before addressing the others. Docs/TODO updated accordingly; code refactor pending.
**2025-10-28 01:30:00Z** - **ACTION**: Remote push streaming now flushes need-list batches immediately. `RemotePushClient` biases control-plane responses, streams gRPC fallback data as soon as headers land, and only emits `UploadComplete` once queues drain so the first bytes move in under one second even when the daemon negotiates fallback. Added control-plane helper for incremental sends and ensured data-plane sessions finish after the loop. **Tests**: `cargo fmt -p blit-core`; `cargo check -p blit-core`; `timeout 120s cargo test -p blit-cli remote_tcp_fallback -- --nocapture`.

**2025-10-28 01:55:00Z** - **ACTION**: Instrumented remote push to capture first-payload latency. `RemotePushReport` now records the elapsed time to the first transferred payload (data-plane or fallback), and `blit-cli` prints the metric when `--progress` or `--verbose` is supplied. This supports the Phase 2 requirement to document <1 s first-byte delivery across platforms. **Tests**: `cargo fmt -p blit-core -p blit-cli`; `cargo check -p blit-core`; `cargo check -p blit-cli`.

**2025-10-28 02:20:00Z** - **ACTION**: Added live throughput output for remote transfers. When CLI runs with `-p/--progress` (or `-v`), the new progress channel streams manifest batches and payload byte counts, letting the CLI print rolling MiB totals plus average/current MiB/s while data moves. Works for both TCP data-plane and gRPC fallback paths. **Tests**: `cargo fmt -p blit-core -p blit-cli`; `cargo check -p blit-core`; `cargo check -p blit-cli`; `timeout 120s cargo test -p blit-cli remote_tcp_fallback -- --nocapture`.
**2025-10-28 23:35:00Z** - **ACTION**: Broke the CLI transfer handler into modules. `crates/blit-cli/src/transfers/` now holds `mod.rs` plus focused `endpoints`, `remote`, and `local` helpers, keeping each file <250 LOC and preserving the existing tests. **Tests**: `cargo fmt`; `cargo check -p blit-cli`.

**2025-10-28 23:40:00Z** - **ACTION**: Split the orchestrator into `options.rs`, `summary.rs`, and `orchestrator.rs`. The public API stays the same (`TransferOrchestrator`, `LocalMirrorOptions`, `LocalMirrorSummary`) while fast-path/history modules remain untouched. **Tests**: `cargo fmt`; `cargo check -p blit-core`.

**2025-10-28 23:45:00Z** - **ACTION**: Refactored `copy/file_copy.rs` into submodules (`clone`, `metadata`, `mmap`, `chunked`) so the streaming copy logic, metadata helpers, and platform-specific code are easier to reason about. `copy_file`, `mmap_copy_file`, and `chunked_copy_file` re-export unchanged. **Tests**: `cargo fmt`; `cargo check -p blit-core`.
**2025-11-06 22:15:00Z** - **ACTION**: Stabilized remote push TCP data plane. Added `--trace-data-plane` CLI flag, hardened `DataPlaneSession` logging, and ensured daemon-streamed files restore permissions + mtimes so re-runs stop re-uploading identical content. Blit-cli/daemon paired traces now show 256 k files (~9.8 GiB) transferring without resets (`logs/blit-cli.log`, `logs/blitd.log`). **Tests**: `cargo fmt`; `cargo check -p blit-cli`; `cargo test -p blit-daemon service::push::data_plane::tests::apply_tar_shard_handles_long_paths -- --nocapture`.

**2025-11-06 22:55:00Z** - **ACTION**: Implemented remote→local mirror support. `blit mirror skippy://module dest/` now runs through the pull path, tracks downloaded files, and purges extraneous local entries (using `walkdir` + async removals). CLI rejects remote mirrors no longer; workflows updated. **Tests**: `cargo fmt`; `cargo test -p blit-cli`; `cargo test -p blit-cli --test remote_tcp_fallback`.

**2025-11-09 19:05:00Z** - **ACTION**: Fixed the Windows change-journal snapshot path and sparse copy helpers so the workspace builds again. Replaced the lingering winapi/widestring usage with windows-rs conversions, taught capture_snapshot to unwrap Win32 Results properly, and updated the sparse copy helper to mutate the destination handle and reissue FSCTL_SET_SPARSE via DeviceIoControl. **Tests**: cargo fmt; cargo build.
**2025-11-10 06:20:00Z** - **PROGRESS**: Wired the remote pull path into the shared hybrid-transport stack. `proto/blit.proto` now includes pull negotiation/summary messages, the daemon’s pull service enumerates manifests, plans payloads via `remote::transfer`, and negotiates a TCP data plane (with `--force-grpc` fallback) before sending tar shards/files. `RemotePullClient` connects to the negotiated port, streams file records and tar shards to the destination, tracks downloaded paths, and captures the server summary just like push. **Tests**: `cargo build -p blit-core`; `cargo build -p blit-daemon`.
**2025-11-10 07:10:00Z** - **PROGRESS**: Finished wiring the remote pull stack into the shared transport/UX. `RemotePullClient` now feeds `RemoteTransferProgress`, downloads file + tar-shard payloads over the negotiated TCP data plane (with forced gRPC fallback still supported), and surfaces the server’s summary in `describe_pull_result`. The CLI uses a single progress monitor for push/pull so `-p/-v` show manifest + throughput in both directions. TODO/plan docs updated; auto-tune scheduling for remote transfers remains the last parity gap. **Tests**: `cargo build -p blit-core`; `cargo build -p blit-daemon`.
