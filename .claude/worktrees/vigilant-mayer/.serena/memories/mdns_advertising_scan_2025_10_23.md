Implemented mDNS support: `blit-daemon` advertises `_blit._tcp.local.` by default (disable via `--no-mdns`, override name with `--mdns-name`). Shared helper in `blit_core::mdns` handles advertising and discovery; `blit scan` and `blit-utils scan` now list discovered daemons. Docs/TODO/DEVLOG updated. Tests: cargo check; cargo test -p blit-daemon; cargo test -p blit-cli; cargo test -p blit-utils (2025-10-23).